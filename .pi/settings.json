{
  "packages": [
    {
      "source": "/Users/robson/Project/pi-agent-dashboard",
      "extensions": [
        "+packages/kb-extension/src/index.ts"
      ]
    }
  ],
  "worktreeInit": {
    "gate": "test ! -e node_modules/.package-lock.json || test ! -d .pi/skills/openspec-explore || test ! -f .pi/dashboard/kb/index.db",
    "run": {
      "type": "script",
      "command": "(npm ci || npm install) && npx @fission-ai/openspec init --tools pi --force && npm run build --workspace=@blackbelt-technology/pi-dashboard-kb && NODE_OPTIONS=--experimental-sqlite npx kb index"
    }
  }
}
