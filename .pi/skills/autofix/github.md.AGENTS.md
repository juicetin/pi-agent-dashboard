# autofix/github.md — index

Reusable GitHub primitives companion to autofix SKILL. Resolve PR number (`gh pr list --head`), owner/repo coords, fetch thread-aware CodeRabbit feedback (GraphQL `reviewThreads` + pagination), detect "Come back in a few minutes" status, post summary comment, optional 👍 reaction.
