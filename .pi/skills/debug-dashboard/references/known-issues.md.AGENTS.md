# debug-dashboard/references/known-issues.md — index

Known-issue catalogue distilled from `docs/faq.md`: server won't start (Electron Node bin fallback `pickNodeForServer`, Fastify crash on Node 22.0–22.17.x / 24.1–24.2.x, `EADDRINUSE`), bridge disconnect (`server_restarting` quiesce window), blank page (Vite missing, prod fallback), restart loops, tunnel 502 (zrok watchdog), OAuth callback registration.
