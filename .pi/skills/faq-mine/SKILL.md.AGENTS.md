# faq-mine/SKILL.md — index

Skill (v2.0): mine `docs/faq.md` from TWO source classes — docs (README.md + evergreen `docs/*.md`) AND pi-hermes memory stores (project `~/.pi/agent/projects-memory/<proj>/MEMORY.md` + global `~/.pi/agent/pi-hermes-memory/failures.md`, `MEMORY.md`). Memory source surfaces runtime problems (tool-quirks/failures/insights) that never reach docs. Inputs `--memory <off|project|failures|all>` (default `failures`), `--docs <list|skip>` (`skip` = memory-only). Passing both `--docs` + `--memory` skips Phase 1 prompt (headless `--docs skip --memory failures` = the ship-change step-10.5 harvest). Phase 0 dedupe existing `## ` headings + enumerate stores; Phase 1 `ask_user` multiselect (docs + stores); Phase 2A doc-mining subagents, Phase 2B memory-mining subagents (symptom-first Q&A, global stores get repo-relevance filter), each writes own `docs/.faq-draft-*.md`; Phase 3 cross-draft + cross-store + doc-vs-memory dedupe; Phase 4 `cat >> faq.md` + rm drafts + refresh condensed `docs/faq.agent.md` via subagent. Caveman style, `Cross-refs:` blocks (memory entries cross-ref store path). READ-ONLY on `~/.pi/agent/**`. Never rename existing faq.md headings (install-hints.test.ts asserts `docsAnchor`→heading). Excludes `AGENTS.md`, `docs/faq.agent.md`, `session-knowledge-*.md`, `spec-gap-analysis.md`.
