# implement/references/code-discipline.md — index

Reference: expands AGENTS.md "Code Instructions" 5 rules with worked examples. Think-before-coding (state assumptions, `ask_user`, read before claiming). Simplicity (YAGNI, senior-engineer test, DRY-when-duplicated). Surgical (no adjacent edits, orphan rule, every line traces to ask). TDD (failing test first, multi-step plan format `1. [Step] → verify: [check]`). Communication (high-level summary, `ask_user` tool over plain text).
