# nano-banana-imagegen/references/photography-and-editing.md — index

Prompt library: photorealistic generation (product, food, portrait, landscape, architecture) + `npx @the-focus-ai/nano-banana "..." --file` editing recipes (background change, style transfer, object add/remove/modify, enhancement, compositing) + scene generation (interior, outdoor). Each editing entry = bash command with prompt string + `--file`/`--output` flags.
