# pi-dashboard/commands/ — index

`/dashboard:*` slash-command templates (33 `dashboard-*.md`). 13 LLM-free (`executable: bash` → run as bash, no LLM, "ran locally" footer); 20 LLM-bound (expand to user message). Naming `/dashboard:<resource>-<verb>[-<modifier>]`. README.md documents frontmatter + convention. Reference: `references/slash-commands.md`. See change: add-dashboard-slash-commands.
