# pi-dashboard/commands/dashboard-session-abort-all.md — index

LLM-bound command. GET /api/sessions lists streaming/active; confirms scope with user (all / current cwd / named subset) before acting; POST /api/session/<id>/abort per chosen session. Usage `/dashboard:session-abort-all [filter-hint]`.
