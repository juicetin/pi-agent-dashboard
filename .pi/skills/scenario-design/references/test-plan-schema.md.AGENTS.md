# scenario-design/references/test-plan-schema.md — index

Exact `test-plan.md` layout. Header: stage + generated date. Soft-gate clarification banner. Scenario tables per class (edge-case / performance / frontend-quirk / error-handling) with columns id · requirement · technique · level · input · trigger · expected observable. Coverage summary + "New infra needed" section. `level` ∈ L1/L2/L3. Triple slots concrete or carry `[NEEDS CLARIFICATION: <slot> — <q>]`. HARD gate: no file written until answered. Stable ids E#/P#/F#/X#.
