# scenario-design/references/test-plan-schema.md — index

Exact `test-plan.md` layout. Header: stage + generated date. Soft-gate clarification banner. Scenario tables per class (edge-case / performance / frontend-quirk / error-handling) with columns id · requirement · technique · level · disposition · input · trigger · expected observable. Coverage summary (incl. by-disposition) + "New infra needed" section. `level` ∈ L1/L2/L3/electron/ci (or — for manual-only). `disposition` ∈ `automated`\|`manual-only` — mandatory per row; manifest is source of truth for automated-vs-manual. Triple slots concrete or carry `[NEEDS CLARIFICATION: <slot> — <q>]`. HARD gate: no file written until answered. Stable ids E#/P#/F#/X#. See change: add-openspec-pipeline-orchestrators.
