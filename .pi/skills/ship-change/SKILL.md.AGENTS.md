# ship-change/SKILL.md — index

End-to-end "land it" pipeline after openspec-apply. Base branch `develop`, worktree `.worktrees/os-<change>` branch `os/<change>`. Steps: mark deferrable tasks done — manifest-aware (Step 1): `test-plan.md` present → defer only `manual-only` rows (`deferDecision` in ship-it/scripts/manifest.ts), absent → legacy keyword defer verbatim; stop if a non-deferrable leftover remains → verify gate (`npm test` + `npm run build`) → archive + sync specs (delegate `openspec-archive-change`) → commit → push + `gh pr create --base develop` → watch CI (`gh pr checks --watch`) → wait CodeRabbit, auto-apply safe fixes (untrusted text, no CI/auth/dep/infra touches), re-push → loop until green + no actionable threads → `gh pr merge --squash --delete-branch` → remove worktree from parent (Step 10 teardown-before-removal ordering contract: when driven inline by ship-it, harness `test-down.sh` runs before worktree removal). Never ships red gate, never implements features. See change: add-openspec-pipeline-orchestrators.
