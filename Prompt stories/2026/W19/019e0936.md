---
session: 019e0936
week: 2026/W19
type: development
model: "@fast"
premium: false
premium_reason: ""
upgrade_status: n/a
---

# How we did it: Fix macOS first-run unblocking docs — an AI collaboration guideline

> A reusable playbook reconstructed from a real pi session. It explains **what was
> asked, how it was built with the AI, what had to be steered, and how to reproduce
> the result faster next time.** Write for a future operator who has the same goal.

---

## 1. Goal (the ask)

The session opened as a support question, not a task:

> `xattr: /Applications/PI-Dashboard.app: No such xattr: com.apple.quarantine?`

The user pasted a confusing terminal message from following the app's own
first-run instructions. The AI correctly diagnosed it as *harmless* (nothing to
remove, not an error). But the real objective surfaced one prompt later: **the
install docs were giving users a command that produces a scary-looking message,
so fix the docs everywhere** — README, the release changelog, and the Astro
marketing site — to use an idempotent command and pre-empt the confusion.

## 2. TL;DR playbook

1. **Answer the literal question first.** Explain that `No such xattr:
   com.apple.quarantine` means the quarantine flag was already absent — safe to
   ignore.
2. **Recommend the better command:** `sudo xattr -cr /Applications/PI-Dashboard.app`
   (idempotent + recursive) over `xattr -d com.apple.quarantine …`.
3. **Grep for every copy of the stale instruction** across README, CHANGELOG,
   `docs/`, and `site/src/` — install text is duplicated in multiple surfaces.
4. **Fix the app-name typo too:** `PI Dashboard.app` → `PI-Dashboard.app` (verify
   against `forge.config.ts` `productName`).
5. **Edit all three surfaces** with matching wording: README first-run section,
   `InstallTabs.tsx`, and a `CHANGELOG.md` Unreleased → Changed entry.
6. **Verify with a final grep** that no `xattr -d`/`quarantine` remnants survive.
7. **Commit and push** with a `docs:` message that names both fixes and the
   rationale.

## 3. How the collaboration unfolded

Three short phases over ~4 minutes, medium thinking.

- **Diagnose (prompt 1).** No tools — the AI read the error text and explained it
  in plain language, then proactively offered the stronger command. *Why it
  worked:* it separated "is this broken?" (no) from "should the docs change?"
  (yes), setting up the follow-up.
- **Locate & fix (prompt 2, "Update README, release note, astro site").** A burst
  of ~14 `grep`/`sed`/`od`/`python3` commands to find every occurrence and to
  decode a tricky README line (the `` glyph and truncated text needed `od -c`
  and a Python read to edit precisely). Three files edited: README, InstallTabs,
  CHANGELOG. *Why it worked:* the user named the exact three surfaces, so the AI
  didn't have to guess scope.
- **Ship (prompt 3, "commit and push").** `git status`/`diff --stat`, staged only
  the three touched files, wrote a descriptive `docs:` commit, pushed to
  `develop` as `f6b30a11`.

## 4. Prompts that worked

- **Goal prompt** (the pasted error): effective *because it was concrete* — a
  verbatim terminal message. The AI could diagnose without back-and-forth. A
  future operator could sharpen it to: *"This first-run command prints `No such
  xattr…` — is it an error, and should we fix the docs?"*
- **"Update README, release note, astro site"** — the high-leverage follow-up. Five
  words that named all three doc surfaces, so the AI fixed every copy in one pass
  instead of one file at a time. This is the move to imitate: **enumerate the
  surfaces that duplicate the content.**
- **"commit and push"** — terse but sufficient once scope was settled; the AI chose
  a sensible `docs:` message and target branch.

## 5. Steering & corrections (what to watch for)

| The AI tended to… | The human had to steer by… | Bake this in next time by… |
|-------------------|----------------------------|----------------------------|
| Stop at diagnosing the error | "Update README, release note, astro site" | State up front that fixes must land in *all* doc surfaces, not just an answer |
| Treat docs as a single file | Explicitly listing three targets | Grep for the instruction across README + `docs/` + `site/src/` before editing |

No hard corrections — the session was clean (0 failed commands). The 3 `edit`
errors were retries against a tricky README line (special glyph + truncation),
not steering.

## 6. Skills, tools & memory created — and why they're effective

None created — this was a small, self-contained doc fix. The reusable lesson worth
capturing if it recurs: **"install instructions are duplicated across README,
CHANGELOG, and the Astro site — fix all three together."** If macOS/Windows
first-run docs get edited again, that would justify a short project convention
memory so the multi-surface scope is stated automatically.

## 7. Pitfalls & dead ends

- **`xattr -d com.apple.quarantine` is a bad instruction to ship** — it errors
  (`No such xattr`) whenever the flag is already absent, confusing users. Ship
  `sudo xattr -cr <app>` instead: idempotent (never errors on a missing attr) and
  recursive (clears every file in the bundle).
- **A special glyph broke naive edits.** The README line held the Apple `` menu
  character and was truncated in normal reads; three `edit` attempts failed until
  the exact bytes were recovered with `sed -n '63p' | od -c` and a `python3` read.
  If an edit won't match, dump the raw bytes with `od -c` first.
- **App-name typo:** `PI Dashboard.app` (space) vs `PI-Dashboard.app` (hyphen) —
  verify against `forge.config.ts` `productName` before writing paths.

## 8. Reproduce it faster — checklist

- [ ] Answer the literal error first: `No such xattr` = already clean, harmless.
- [ ] `grep -rn "xattr\|quarantine" README.md CHANGELOG.md docs/ site/src/` to find
      every copy.
- [ ] Replace with `sudo xattr -cr /Applications/PI-Dashboard.app` everywhere.
- [ ] Fix the `PI Dashboard.app` → `PI-Dashboard.app` name (check `productName`).
- [ ] Add a note that `No such xattr` is expected and harmless.
- [ ] Add a `CHANGELOG.md` Unreleased → Changed entry.
- [ ] Final `grep` for `xattr -d`/`quarantine` remnants → expect none.
- [ ] Stage only the touched files, commit `docs:`, push to `develop`.

**Artifacts produced:** `README.md`, `CHANGELOG.md`,
`site/src/components/InstallTabs.tsx` — committed as `f6b30a11`.

---

_Generated from session `019e0936-0fec-720d-aef0-05250700b65f` · `/Users/robson/Project/pi-agent-dashboard` · 2026-05-08._
