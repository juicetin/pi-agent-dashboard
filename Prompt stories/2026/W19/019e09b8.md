---
session: 019e09b8
week: 2026/W19
type: development
model: "@fast"
premium: true
premium_reason: "heavy steering (8 user prompts)"
upgrade_status: pending
openspec_changes: [linked-session-pill-lifecycle-icons, add-dashboard-slash-commands, fix-fork-empty-session-silent-timeout]
proposal_excerpt: "In the sidebar's `FolderOpenSpecSection`, every active proposal lists its attached sessions as compact blue pills under the change row (e.g. `honcho-dashboard-plugin-impl2` under `honcho-dashboard-plugin`). Today the…"
---

# How we did it: Lifecycle icons on linked-session pills — an AI collaboration guideline

> A reusable playbook reconstructed from a real pi session. It explains **what was
> asked, how it was built with the AI, what had to be steered, and how to reproduce
> the result faster next time.** Write for a future operator who has the same goal.

---

## 1. Goal (the ask)

The user opened in **explore mode** (`openspec-explore`) — a thinking stance, not an
implementation one — to reason about a small UI gap in the dashboard sidebar. In the
`FolderOpenSpecSection`, each OpenSpec proposal lists its attached sessions as compact
blue pills. Those pills were "jump only": clicking navigated to the session, but there
was **no way to hide/unhide, resume, or fork a linked session without first jumping
into it** and acting on its `SessionCard`.

The real objective, once the exploration converged: **surface the SessionCard
lifecycle actions (hide/unhide, resume, fork) as trailing icons directly on each
linked-session pill**, keeping the name region's click = jump behavior. Then run it
through the full OpenSpec loop — proposal → apply → build/deploy → verify → archive →
commit — end to end in one sitting.

## 2. TL;DR playbook

1. **Explore first, don't code.** Launch `openspec-explore`; ask the AI to map the
   *current* pill shape and lay out the design space as a small variant table (A/B).
2. **Answer the design questions tersely.** The AI surfaced 3 decisions; the user
   replied `1. A / 2. yes / 3. yes` — Variant A (name=jump, icons to the right).
3. **`create proposal`** — capture the decision as an OpenSpec change
   (`proposal.md` + `tasks.md` + a MODIFIED spec delta with new scenarios). Validate.
4. **Apply the change** (`openspec-apply-change`): thread 3 optional props into
   `FolderOpenSpecSection`, forward the 3 handlers already in scope at `SessionList`,
   add `e.stopPropagation()` on each icon, extend the test file.
5. **Run the scoped test file** with `HOME=$(mktemp -d) npx vitest run …` — isolate
   from the real `$HOME` so no user config bleeds in.
6. **`build and deploy`**: `npm run build`, then `pi-dashboard restart` (NOT raw curl —
   the hook blocks direct HTTP to `/api/restart`).
7. **Verify** (`openspec-verify-change`): map every spec scenario → code + test evidence.
8. **Archive + sync** the MODIFIED requirement into the main spec, then **commit only
   this change's files**, leaving unrelated pre-staged WIP untouched.

## 3. How the collaboration unfolded

**Phase 1 — Explore & frame the design (00:34–00:41).**
The AI read `FolderOpenSpecSection.tsx` (~L249–262), rendered the *current* pill as an
ASCII sketch, then proposed the target shape with trailing icons and a **decision
table** (Variant A: body=jump + hide/resume icons; Variant B: body inert + 3 icons).
It also pre-checked that `SessionList` already had all four handlers (`handleHide`,
`handleUnhide`, `onResume`, `onSelect`) in scope — proving the change was "pure
threading, no new state, no server change." *Why it worked:* the AI de-risked the
whole change during exploration, so the human's decision was a one-liner.

**Phase 2 — Capture as an OpenSpec proposal (00:41–00:43).**
On `create proposal`, the AI wrote `proposal.md`, `tasks.md` (~14 tasks in 5 sections),
and a `specs/openspec-folder-section/spec.md` delta MODIFYING the "Change list
displays linked sessions" requirement with 7 new scenarios (icon visibility,
stopPropagation, hidden-session presence). `openspec validate` passed clean.

**Phase 3 — Implement (00:49–00:53).**
`openspec-apply-change` against the change. The AI grounded edits by re-reading the
code (found the field is `session.hidden`, prop `isHidden`; mirrored the SessionCard
conditionals: hide↔unhide always, resume when `(!isAlive || isHidden) && sessionFile`,
fork when `sessionFile`). Edited `FolderOpenSpecSection.tsx`, `SessionList.tsx`, and
the test file. **8 new tests + 31 existing = 39 passing.** A whole-client `tsc` showed
one error at line 413 — the AI **stashed/re-applied its edits to prove the error was
pre-existing** and unrelated, then marked 10/12 tasks done (2 were manual visual checks).

**Phase 4 — Build & deploy (01:06).**
`build and deploy`: `npm run build` (49 files, 8.90 MB → 2.58 MB gzipped), then
`pi-dashboard restart`. Direct `curl http://localhost:8000/api/health` and `/api/restart`
attempts **failed by design** (a hook blocks raw HTTP) — the AI routed through the CLI.

**Phase 5 — Verify, archive, commit (01:16–01:20).**
`openspec-verify-change` produced a scenario→evidence map (Completeness 12/12,
Correctness 9/9, Coherence OK). `openspec-archive-change` moved the change to
`archive/2026-05-09-…` and synced the MODIFIED requirement into the main spec (with a
user confirmation via `ask_user`). Final commit **7d4a8fc8** staged *only* the 7 files
of this change, explicitly leaving two unrelated pre-staged entries in the tree.

## 4. Prompts that worked

- **The goal prompt (explore mode).** Starting in `openspec-explore` was the highest-
  leverage move: it forced a design-space pass *before* any code, yielding the A/B
  variant table and the "handlers already exist" discovery. Reusable kickoff:
  *"Enter explore mode. Look at how linked-session pills render in FolderOpenSpecSection.
  I want lifecycle actions (hide/resume/fork) on the pill itself — lay out the options
  before we commit."*
- **`1. A / 2. yes / 3. yes`** — a terse batch answer to the AI's 3 design questions.
  High leverage because the AI had already framed the choices as a table; the human
  just picked a row.
- **`create proposal`** — one verb that converted the settled exploration into a
  validated OpenSpec artifact set.
- **`build and deploy`** — trusted the AI to pick the right deploy path (it correctly
  avoided raw HTTP and used `pi-dashboard restart`).
- The `/opsx:apply`, `/opsx:verify`, `/opsx:archive` skill invocations chained the
  standard OpenSpec loop without re-explaining intent each time.

## 5. Steering & corrections (what to watch for)

| The AI tended to… | The human had to steer by… | Bake this in next time by… |
|-------------------|----------------------------|----------------------------|
| Offer to implement while still in explore mode | Answering design Qs, then explicitly `create proposal` before any code | Keep explore/implement separate; explore only produces artifacts, never edits |
| Present multiple design variants and wait | Picking Variant A tersely (`1. A`) | Ask the AI to recommend a default so the pick is `yes` |
| Treat a `tsc` error as possibly its own | Letting it stash/re-apply to prove pre-existing | Trust the stash-diff technique; don't chase unrelated errors |
| Reach for `curl /api/restart` | (Hook blocked it) → AI fell back to `pi-dashboard restart` | Deploy via the CLI, never raw HTTP — the restart hook blocks it |
| Stage everything at commit time | Restrict `git add` to the change's 7 files | Explicitly enumerate files; leave unrelated pre-staged WIP alone |

The dominant quality bar the user imposed: **run the full OpenSpec loop, not just the
edit** — proposal, apply, verify, archive, and a clean scoped commit.

## 6. Skills, tools & memory created — and why they're effective

No new skill or memory was created this session. The workflow leaned on existing
skills, which is itself the lesson: **the `openspec-explore → new-change → apply →
verify → archive` chain is the reusable asset.** For a small, purely-UI surfacing
change like this (no server/protocol/persistence), that chain is the fast path.

Worth capturing as a project skill if not already: *"surface existing SessionCard
lifecycle actions onto a new UI surface"* — the pattern of (1) confirm handlers exist
at the call site, (2) thread optional props, (3) mirror the conditional-visibility
logic, (4) add `stopPropagation`, (5) extend the co-located test file.

## 7. Pitfalls & dead ends

- **Raw HTTP to the dashboard is blocked.** `curl`/`wget` to `http://localhost:8000/api/health`
  and `/api/restart` all failed (4 failed attempts). Use `pi-dashboard restart` /
  `pi-dashboard status` instead.
- **`tsc` on the whole client shows a pre-existing error (line 413).** Don't panic —
  `git stash` your edits, re-run, confirm the error persists, then `stash pop`. It's
  unrelated to a linked-session-pill change.
- **vitest picks up real `$HOME` config.** Run with `HOME=$(mktemp -d) npx vitest run
  <file>` to isolate.
- **Unrelated pre-staged WIP in the index.** Before committing, inspect `git status
  --short` and `git restore --staged` anything not part of your change (here:
  `fix-fork-empty-session-silent-timeout` archive + `session-resume/spec.md`).

## 8. Reproduce it faster — checklist

- [ ] Launch `openspec-explore`; have the AI sketch current vs. target pill and a
      variant table. Pick a variant tersely.
- [ ] `create proposal` → `proposal.md` + `tasks.md` + MODIFIED spec delta; `openspec
      validate`.
- [ ] `openspec-apply-change`: add optional props to `FolderOpenSpecSection.tsx`,
      forward `handleHide`/`handleUnhide`/`onResume` from `SessionList.tsx`, mirror
      SessionCard conditionals, add `e.stopPropagation()`, extend the test file.
- [ ] `HOME=$(mktemp -d) npx vitest run packages/client/src/components/__tests__/FolderOpenSpecSection.test.tsx`
- [ ] `npm run build` → `pi-dashboard restart` (never raw curl).
- [ ] `openspec-verify-change` → scenario→evidence map.
- [ ] `openspec-archive-change` → sync MODIFIED requirement into main spec.
- [ ] `git add` **only** the change's files → commit.

**Inputs to have ready:** a running dashboard (for `pi-dashboard restart`/`status`),
the OpenSpec CLI on PATH, and the `openspec-*` skills.

**Final artifacts produced:**
- `openspec/changes/archive/2026-05-09-linked-session-pill-lifecycle-icons/` (proposal, tasks, specs)
- `packages/client/src/components/FolderOpenSpecSection.tsx` (3 props + icon group)
- `packages/client/src/components/SessionList.tsx` (handler forwarding)
- `packages/client/src/components/__tests__/FolderOpenSpecSection.test.tsx` (+8 tests)
- `openspec/specs/openspec-folder-section/spec.md` (+7 scenarios)
- Commit **7d4a8fc8** — `feat(folder-openspec): lifecycle icons on linked-session pills` (7 files, +392 / -17)

---

_Generated from session `019e09b8-6f08-7786-bc07-bb3ae56e0147` · `/Users/robson/Project/pi-agent-dashboard` · 2026-05-09. Source extract: `/tmp/facts-1784861492-85567.md`._
