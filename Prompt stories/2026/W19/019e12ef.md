---
session: 019e12ef
week: 2026/W19
type: development
model: "@fast"
premium: false
premium_reason: ""
upgrade_status: n/a
---

# How we did it: Fix a flaky jiti cold-start test timeout — an AI collaboration guideline

> A reusable playbook reconstructed from a real pi session. It explains **what was
> asked, how it was built with the AI, what had to be steered, and how to reproduce
> the result faster next time.** Write for a future operator who has the same goal.

---

## 1. Goal (the ask)

The user pasted a raw vitest failure — a single red test in a 5,365-test suite:

```
Error: Test timed out in 5000ms.
 ❯ src/__tests__/pi-dashboard-bin-wrapper.test.ts:61:3
   it("resolves jiti from process.argv[1] anchor and re-execs cli.ts", …
```

No instruction beyond the paste. The **real objective**: make that one
`pi-dashboard-bin-wrapper` test stop timing out and go green, without weakening what
it verifies — then land the fix. The failure is not a logic bug; the test spawns the
wrapper (which cold-starts jiti to load `cli.ts`) under the vitest default 5 s budget,
and a cold jiti transform of `cli.ts` blows past 5 s on a loaded machine.

## 2. TL;DR playbook

1. Locate the file: `find . -name 'pi-dashboard-bin-wrapper.test.ts' -not -path '*/node_modules/*'`.
2. Read the failing `it()` around line 61 — confirm it does a real `spawnSync` with its **own** 30 s timeout, so the bottleneck is the *outer* vitest 5 s limit, not the spawn.
3. Fix minimally: append a per-test timeout to that single `it()` → `it("…", async () => { … }, 60_000)`. Do **not** touch `testTimeout` globally.
4. Verify the file alone: `npx vitest run packages/server/src/__tests__/pi-dashboard-bin-wrapper.test.ts 2>&1 | tail -30`.
5. Verify hermetic: re-run with `HOME=$(mktemp -d)` prefixed, to prove it passes with a clean home (no cached jiti/warm state masking the fix).
6. Commit only that file: `git add <the test> && git commit -m "test: bump pi-dashboard wrapper test timeout to 60s"`; leave unrelated unstaged work (openspec artifacts) alone.
7. `git push` to `develop`.

## 3. How the collaboration unfolded

**Phase A — Locate (1 command).** From the pasted stack the AI ran a scoped `find`
to resolve the file's real path (`packages/server/src/__tests__/…`), rather than
trusting the relative `src/__tests__/…` shown in the vitest output.

**Phase B — Read & diagnose (3 reads).** The AI opened the `it()` at line 61 and saw
it spawns the wrapper with a 30 s `spawnSync` timeout. That reframed the problem: the
inner operation is fine; the *outer* vitest 5 s default is the ceiling being hit
during jiti cold-start of `cli.ts`. Decision point: raise the per-test budget, not the
spawn budget, and not the global config.

**Phase C — Surgical edit (revert + redo).** The AI's first edit was overcomplicated;
it explicitly reverted ("Let me revert that overcomplicated edit and do it simply")
and instead appended `, 60_000)` to the single `it()`. One-line, one-test blast radius.

**Phase D — Double verify.** Ran the file twice: once normally, once with
`HOME=$(mktemp -d)` to guarantee the pass isn't an artifact of a warm/cached home.
Both green.

**Phase E — Land it.** On the second user prompt ("commit and push") the AI staged
*only* the test file, wrote a descriptive `test:` commit explaining the jiti
cold-start rationale, committed (`4eebe6c4`), and pushed to `develop` — deliberately
leaving unrelated unstaged openspec proposal artifacts untouched.

## 4. Prompts that worked

- **The goal prompt (a raw paste).** Pasting the full vitest failure — the timeout
  message, the file:line, and the suite summary — was enough for the AI to locate,
  diagnose, and fix without a single clarifying question. A stronger version would add
  one line of intent: *"This wrapper test times out on jiti cold-start; raise just its
  budget and land it on develop."*
- **High-leverage follow-up: "commit and push".** Three words closed the loop. It
  worked because the diff was already minimal and verified, so "commit and push" had
  an unambiguous target.

## 5. Steering & corrections (what to watch for)

| The AI tended to… | The human had to steer by… | Bake this in next time by… |
|-------------------|----------------------------|----------------------------|
| Over-engineer the first edit | (self-corrected) — "revert that overcomplicated edit and do it simply" | State "smallest possible change" up front; prefer a per-test arg over config surgery |
| Stop after the fix | "commit and push" | Say "fix, verify, then commit+push to develop" in the goal so it's one shot |
| Risk staging unrelated work | (self-limited) staged only the test file | Name the exact file to stage; call out "leave openspec artifacts unstaged" |

Quality bars the user (implicitly) imposed: the fix must keep the test's real
assertion intact (don't skip/loosen it), and unrelated working-tree changes must not
be swept into the commit.

## 6. Skills, tools & memory created — and why they're effective

No skill or memory was created this session — appropriately, since it was a
one-line, well-scoped fix. The *pattern* is reusable, though:

- **When a spawned/cold-start subprocess test times out at vitest's 5 s default**, the
  fix is a per-test timeout arg (`it(name, fn, ms)`), sized above the inner
  `spawnSync`/subprocess timeout — **not** a global `testTimeout` bump (which hides
  slow tests everywhere) and **not** a `spawnSync` timeout change (that's not the
  ceiling).

If this recurs across several wrapper/PTY/jiti tests, consider a tiny project skill:
"jiti-coldstart-test-timeout" capturing the diagnosis (inner timeout vs. outer vitest
budget) and the `HOME=$(mktemp -d)` hermetic re-verify step.

## 7. Pitfalls & dead ends

- **First edit was overcomplicated → reverted.** If your first instinct is to
  restructure the test or touch config, stop: the correct change here is a single
  `, 60_000)` argument. Do the simple thing first.
- **Relative path in vitest output is misleading.** The failure printed
  `src/__tests__/…` but the file lives under `packages/server/src/__tests__/…`. Run a
  scoped `find` to resolve the real path before reading/editing.
- **A single normal pass can be a false positive.** Cold-start timing depends on cache
  warmth. Re-run with `HOME=$(mktemp -d)` to prove the fix holds from a clean state.

## 8. Reproduce it faster — checklist

**Inputs to have ready:** the failing test's file:line, repo at `/Users/robson/Project/pi-agent-dashboard`, clean `develop` checkout.

- [ ] `find . -name '<test>.test.ts' -not -path '*/node_modules/*'` — resolve real path.
- [ ] Read the failing `it()`; confirm inner subprocess timeout > vitest 5 s default (that's the ceiling being hit).
- [ ] Append per-test timeout: `it("…", async () => { … }, 60_000)` — no global/config change.
- [ ] `npx vitest run <path> 2>&1 | tail -30` — green.
- [ ] `HOME=$(mktemp -d) npx vitest run <path> 2>&1 | tail -30` — green from clean home.
- [ ] `git add <only the test file> && git commit -m "test: bump … timeout to 60s"` — descriptive body naming jiti cold-start.
- [ ] `git push` to `develop`; leave unrelated unstaged work untouched.

**Final artifact:** `packages/server/src/__tests__/pi-dashboard-bin-wrapper.test.ts`
(one line changed) — committed as `4eebe6c4` on `develop`.

---

_Generated from session `019e12ef-8ead-751f-b9d4-5165156e3770` · `/Users/robson/Project/pi-agent-dashboard` · 2026-05-10. Source extract: deterministic facts sheet._
