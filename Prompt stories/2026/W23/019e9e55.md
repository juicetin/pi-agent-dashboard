---
session: 019e9e56
week: 2026/W23
type: development
model: "@fast"
premium: false
premium_reason: ""
upgrade_status: n/a
---

# How we did it: Reorder the folder spawn buttons below OpenSpec — an AI collaboration guideline

> A reusable playbook reconstructed from a real pi session. It explains **what was
> asked, how it was built with the AI, what had to be steered, and how to reproduce
> the result faster next time.** Write for a future operator who has the same goal.

---

## 1. Goal (the ask)

The user opened with `"Almost OK, but +New session and New Worktree button place before openspec."`
The literal ask reads like "these buttons already sit *before* OpenSpec" — but the AI
found the code already rendered `FolderSpawnButtons` before `FolderOpenSpecSection`, so the
UI already matched the literal reading. The **real objective**, clarified once the AI
inspected the live UI and asked, was the opposite: move the `+ New Session` / `New Worktree`
stack to render **below** the OpenSpec section in every folder group of the dashboard's
`SessionList`. Then persist that: update the `folder-action-bar` spec to match, commit, push.

## 2. TL;DR playbook

1. Locate the components: `grep -rn "FolderSpawnButtons\|FolderOpenSpecSection" packages/client/src/components/SessionList.tsx`.
2. **Don't trust the prompt's wording** — open the *live* UI (`browser` on `http://localhost:8000`) and confirm the current visual order before touching code.
3. When the ask and the code disagree, `ask_user` one crisp question to resolve intent (before/after OpenSpec) instead of guessing.
4. Move the `FolderSpawnButtons` JSX block from before the plugin/OpenSpec slot to **after** `FolderOpenSpecSection` in `SessionList.tsx`.
5. Rebuild + restart production: `npm run build` then `curl -X POST http://localhost:8000/api/restart`; poll `/api/health` until back up.
6. Re-open the live UI and confirm the new order (`OPENSPEC → + New Session → New Worktree`) across multiple folders.
7. Sync the spec: edit `openspec/specs/folder-action-bar/spec.md` so the "Elevated folder spawn buttons" requirement says the stack renders *below* the plugin/OpenSpec sections.
8. `git add -A && git commit` with a descriptive `feat(client):` message, then `git push`.

## 3. How the collaboration unfolded

**Phase 1 — Discovery (the ask contradicts the code).** The AI grepped `packages/client/src/`
(after a false start against `src/client/` and `packages/web/src/` — wrong paths for this
repo) and found `SessionList.tsx` already rendered the spawn buttons *before* the OpenSpec
section. Rather than edit blindly, it noted the contradiction. *Why it worked:* it treated
the prompt as intent-to-clarify, not literal spec.

**Phase 2 — Verify against reality.** The AI drove the `browser` tool against the running
dashboard (`localhost:8000`) and read the actual on-screen order: Terminals → +New Session →
New Worktree → OPENSPEC. It reported this back and used `ask_user` to confirm the desired
target order before changing anything. *Decision point:* the human's real intent was "put
them *below* OpenSpec" — the opposite of the literal words.

**Phase 3 — Surgical edit.** The AI moved a single `<div className="mt-1">` JSX block from
before the plugin/OpenSpec slot to after `FolderOpenSpecSection`. One `edit`, no adjacent
refactoring.

**Phase 4 — Build, restart, re-verify.** `npm run build`, then `POST /api/restart`, polling
`/api/health` until the server returned. Re-opened the browser and confirmed the new order
across two folder groups (including the `pi-judo` folder).

**Phase 5 — Persist & ship.** On `"update spec and commit"`, the AI edited the
`folder-action-bar/spec.md` requirement to match the new position, committed both files with
a clear message (`60a875a1`), and on `"push"` pushed to `develop`.

## 4. Prompts that worked

- **Goal prompt** — `"Almost OK, but +New session and New Worktree button place before openspec."`
  Short but *ambiguous*; its power was that the AI treated the discrepancy as a signal to
  verify rather than obey. A stronger version: *"Move the +New Session / New Worktree stack
  to render **below** the OpenSpec section in every folder group; update the spec and ship."*
- **High-leverage follow-up** — `"its okay, update spec and commit"` unlocked the whole
  persist-and-ship phase in one line (spec edit + staged commit).
- **`"push"`** — the minimal ship trigger; effective because the commit was already clean.

## 5. Steering & corrections (what to watch for)

| The AI tended to… | The human had to steer by… | Bake this in next time by… |
|-------------------|----------------------------|----------------------------|
| Take the prompt's "before openspec" literally when the code already matched | (AI self-corrected: verified live UI + `ask_user`) | State the *target* order explicitly ("below OpenSpec"), not relative to current state |
| Grep the wrong source roots first (`src/client/`, `packages/web/src/`) | — | Remember this repo's client lives in `packages/client/src/` |
| Stop after the code edit | `"update spec and commit"` | State up front: "also update the `folder-action-bar` spec and commit + push" |

The key guardrail: **when the prompt's wording contradicts what the code/UI already does,
verify against the running system and confirm intent — don't silently pick an interpretation.**

## 6. Skills, tools & memory created — and why they're effective

No skill or memory was created this session — it was a small, well-scoped UI reorder. The
repeatable pattern worth internalizing (not worth a skill on its own): **verify-in-browser →
edit → rebuild+restart → re-verify → sync spec → commit+push**, which is already codified in
the project's `implement` skill (client change ⇒ `npm run build` + `/api/restart`). If this
kind of "prompt contradicts current UI" situation recurs, the reusable move is the
`ask_user`-to-disambiguate step before editing.

## 7. Pitfalls & dead ends

- **Wrong source roots:** `grep` against `src/client/` and `packages/web/src/` returned
  nothing / errored. *If you hit this:* the dashboard client lives in `packages/client/src/`.
- **Health check before restart raced:** an early `curl /api/health` failed. *Do:* after
  `POST /api/restart`, `sleep` a few seconds and poll `/api/health` until it returns before
  re-checking the UI.
- **Trusting the literal prompt:** the ask's wording matched the *existing* state, which would
  have produced a no-op edit. *Do:* verify the live UI and confirm intent first.

## 8. Reproduce it faster — checklist

- [ ] `grep -rn "FolderSpawnButtons\|FolderOpenSpecSection" packages/client/src/components/SessionList.tsx`
- [ ] Open live UI (`browser` → `http://localhost:8000`); note current button order.
- [ ] If prompt contradicts reality, `ask_user` one question to fix intent.
- [ ] Move the `FolderSpawnButtons` block relative to `FolderOpenSpecSection` (one `edit`).
- [ ] `npm run build` → `curl -X POST http://localhost:8000/api/restart` → poll `/api/health`.
- [ ] Re-open UI; confirm new order across ≥2 folders.
- [ ] Edit `openspec/specs/folder-action-bar/spec.md` to match.
- [ ] `git add -A && git commit -m "feat(client): …"` → `git push`.

**Inputs to have ready:** a running dashboard on `localhost:8000` (for live verification),
write access to `develop`. **Artifacts produced:** `packages/client/src/components/SessionList.tsx`,
`openspec/specs/folder-action-bar/spec.md`, commit `60a875a1` pushed to `develop`.

---

_Generated from session `019e9e56` · `/Users/robson/Project/pi-agent-dashboard` · 2026-06-06. Source extract: deterministic facts sheet._
