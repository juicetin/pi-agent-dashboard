---
session: 019ec58e
week: 2026/W24
type: planning
model: "@fast"
premium: false
premium_reason: ""
upgrade_status: n/a
openspec_changes: [fix-popover-viewport-flip, relocate-view-menu-to-status-bar, fix-settings-panel-and-reset]
proposal_excerpt: "The `⚙ View` popover (`ChatViewMenu`) opens **off the bottom of the screen**: its lower rows (\"Use global settings\", the tool-call toggles) render past the viewport edge and are unreachable. The menu lives in the bott…"
---

# How we did it: fix-popover-viewport-flip — an AI collaboration guideline

> A reusable playbook reconstructed from a real pi session. It explains **what was
> asked, how it was built with the AI, what had to be steered, and how to reproduce
> the result faster next time.** Write for a future operator who has the same goal.

---

## 1. Goal (the ask)

The session opened in **explore mode** (`openspec-explore`) — a "think, don't implement"
stance. The real objective, once the steering clarified it: **the `⚙ View` popover
(`ChatViewMenu`) opens off the bottom of the viewport** because it lives in the
bottom-pinned `StatusBar` but is hard-coded to grow downward (`absolute right-0 mt-1`).
The lower rows ("Use global settings", the tool-call toggles) render past the window edge
and are unreachable. The ask evolved from "fix this one popover" into "**audit every
popover for off-screen risk, then draft an OpenSpec proposal** that fixes the bug the
right way." No code was written — the deliverable is a validated change proposal.

## 2. TL;DR playbook

1. **Enter explore mode** for a bug you don't fully understand yet — it forces
   investigation-before-implementation and keeps you from patching the wrong layer.
2. **Locate the offending component** and read its anchoring CSS
   (`grep -n "absolute\|fixed\|bottom\|top-full\|mt-1"`), then find *where it mounts*
   (`StatusBar` leading slot → bottom of viewport = the real cause).
3. **Decide portal-vs-flip early:** check for a clipping `overflow-hidden` ancestor. None
   here → the clip is the viewport itself → a simple **flip-up** solves it, no portal or
   collision library needed.
4. **Sweep the whole surface:** grep every popover/dropdown in the client, then extract
   each one's *actual anchor strategy* and classify 🔴 buggy / ✅ safe / 🟡 latent.
5. **Ground the proposal in history:** search OpenSpec archives — you may find the spec
   *already requires* the behavior. `grep` the archived change + `git log` the file to
   prove drift.
6. **Scaffold the change** (`openspec change new` or `mkdir -p` the dirs) and write
   proposal / design / tasks / spec deltas.
7. **`openspec validate <change>`** until clean.
8. **Commit surgically:** `git reset -q` first, add only your artifacts, leave unrelated
   pre-staged files untouched.

## 3. How the collaboration unfolded

**Phase 1 — Locate & diagnose (prompt 1).** The AI found `ChatViewMenu.tsx`, read its
anchoring (`absolute right-0 mt-1` = always opens downward), and traced its mount point to
`StatusBar`'s `leading` slot — pinned near the viewport bottom. It then checked for a
clipping ancestor and confirmed there was none, which settled the key architectural
question: **the clip is the viewport bottom, not an ancestor**, so an in-flow flip-up (not
a portal) is the correct, self-contained fix. *Why it worked:* it separated the symptom
(clipped rows) from the cause (downward anchor + bottom mount) before proposing anything.

**Phase 2 — Full popover audit (steering #1: "check all PopOver").** Rather than eyeball
it, the AI grepped every dropdown in the client, then extracted each component's real
anchor strategy and classified them into three tiers: 🔴 the bug (`ChatViewMenu`),
✅ already-correct (`ModelSelector`, `ThinkingLevelSelector`, `CommandInput` — all use
`bottom-full mb-1` + `max-h-* overflow-y-auto`), 🟡 latent risk. *Why it worked:* it
derived the correct pattern from the components that already got it right.

**Phase 3 — Ground in history (steering #2: "draft proposal").** Before writing, the AI
searched OpenSpec archives and found the smoking gun: an archived change
(`2026-06-11-fix-settings-panel-and-reset`) had **marked auto-flip tasks `[x]` done** with
a `flipUp` + `IntersectionObserver` plan — but `git log` on `ChatViewMenu.tsx` proved the
file was only ever touched by its original commit. **The flip code was never committed.**
The spec requires auto-flip; the shipped UI violates its own accepted spec. This reframed
the work from "new feature" to "**fix spec/code drift**."

**Phase 4 — Draft & commit (steering #3: "commit changes").** Scaffolded the change dir,
wrote 5 artifacts (proposal, design, tasks, 2 spec deltas), `openspec validate` passed
clean, then committed *only* those files — leaving an unrelated pre-staged
`windows-authenticode-signing/tasks.md` alone.

## 4. Prompts that worked

- **Goal prompt — `openspec-explore` stance.** Starting in explore mode was the key move:
  it guaranteed investigation and an OpenSpec artifact instead of a hasty code patch. Good
  for any "something's broken and I'm not sure why" bug.
- **`check all PopOver that avoid the out of screen excperience`** — a tiny, high-leverage
  follow-up. It expanded scope from one component to the whole surface, which is what
  surfaced the correct shared pattern and the drift. Stronger phrasing:
  *"audit every popover/dropdown in the client for off-screen clipping; classify each as
  buggy / safe / latent and show its anchor strategy."*
- **`draft proposal`** — short, but the AI correctly grounded it in archive history first.
  Stronger: *"draft the OpenSpec proposal; first check archives/specs for related prior
  work and prove the root cause with git history."*

## 5. Steering & corrections (what to watch for)

| The AI tended to… | The human had to steer by… | Bake this in next time by… |
|-------------------|----------------------------|----------------------------|
| Fix the single reported popover | "check all PopOver…" | State up front: "audit *every* popover, classify by off-screen risk." |
| Treat it as a new feature | (implicit — AI self-corrected via archive search) | Ask "does a spec/archive already cover this?" before scoping a proposal. |
| — (commit hygiene handled well) | "commit changes" | Keep the surgical-commit habit: `git reset -q`, add only your files. |

The session was low-friction (only 3 steering turns, all additive). The one big judgment
call — "is this a feature or a regression?" — the AI resolved itself by checking `git log`,
which is the reusable discipline worth copying.

## 6. Skills, tools & memory created — and why they're effective

No skills or memories were created this session. But two repeatable disciplines emerged
and are worth capturing:

- **"Prove drift with git history"** — when a spec claims a behavior exists, `git log
  <file>` the implementation before trusting the tasks.md `[x]`. This turned a vague bug
  into a precise root cause. Worth a memory: *tasks marked done ≠ code committed.*
- **"Anchor-strategy audit"** — for any UI-positioning bug, grep the whole surface and
  classify by the *actual* anchor CSS, deriving the correct pattern from components that
  already work. A candidate skill: `audit-popover-viewport-safety`.

## 7. Pitfalls & dead ends

- **`openspec change new fix-popover-viewport-flip` failed** (CLI scaffold unavailable in
  this context). Fallback that worked: `openspec --help` to confirm the command shape, then
  `mkdir -p openspec/changes/<name>/specs/...` and write artifacts manually.
- **A `git log` command failed** during investigation (2 of 22 commands errored) — recover
  by simplifying the pipeline; the file's history was still readable.
- **Don't trust `[x]` tasks.** The archived change marked auto-flip done; the code never
  landed. Always verify implementation against `git log`, not the checklist.
- **Pre-staged foreign files:** an unrelated `windows-authenticode-signing/tasks.md` was
  already staged. `git reset -q` before adding your own artifacts keeps the commit clean.

## 8. Reproduce it faster — checklist

- [ ] Enter `openspec-explore` mode for the bug.
- [ ] Find the component + its anchor CSS + its *mount location* (viewport region).
- [ ] Check for a clipping `overflow-hidden` ancestor → decides portal vs. flip.
- [ ] Grep + classify every popover (🔴/✅/🟡); derive the correct pattern from the ✅ ones.
- [ ] Search OpenSpec archives + `git log` the file to prove root cause / drift.
- [ ] `mkdir -p` the change dir; write proposal, design, tasks, spec deltas.
- [ ] `openspec validate <change>` until clean.
- [ ] `git reset -q`; add only your artifacts; commit.

**Artifacts produced** (`openspec/changes/fix-popover-viewport-flip/`):
`proposal.md`, `design.md`, `tasks.md`,
`specs/popover-viewport-positioning/spec.md` (ADDED),
`specs/chat-display-preferences/spec.md` (MODIFIED). Commit `6e4f88b2`.

---

_Generated from session `019ec58e` · `/Users/robson/Project/pi-agent-dashboard` · 2026-06-14. Source extract: deterministic facts sheet (session-to-guideline)._
