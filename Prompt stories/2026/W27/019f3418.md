---
session: 019f3418
week: 2026/W27
type: planning
model: "@fast"
premium: true
premium_reason: "heavy steering (8 user prompts)"
upgrade_status: pending
openspec_changes: [open-loopback-links-in-split-viewer, unify-file-link-openability]
proposal_excerpt: "Loopback dev-server URLs surface on two chat surfaces: (1) assistant/user prose via `MarkdownContent`, and (2) the `serve_mockup` MCP result card, which renders through `GenericToolRenderer` → `LinkifiedText` → `UrlLi…"
---

# How we did it: Open loopback links in the split viewer — an AI collaboration guideline

> A reusable playbook reconstructed from a real pi session. It explains **what was
> asked, how it was built with the AI, what had to be steered, and how to reproduce
> the result faster next time.** Write for a future operator who has the same goal.

---

## 1. Goal (the ask)

The session opened in **explore mode** (`openspec-explore`) — a thinking-partner stance,
not an implementation task. The first prompt was the skill's boilerplate: *"Enter explore
mode. Think deeply. Visualize freely. Follow the conversation wherever it goes… you must
NEVER write code or implement features… You MAY create OpenSpec artifacts."*

The **real objective** emerged over the first two steering turns: when a pi chat message
prints a **loopback dev-server URL** (e.g. `http://localhost:50452/…`), a plain click should
**open it inside the dashboard's split-pane live viewer** instead of spawning a new browser
tab — with a **modifier/middle-click escape hatch** to the system browser. The deliverable
was a **spec-only OpenSpec proposal** (`open-loopback-links-in-split-viewer`), never code.

## 2. TL;DR playbook

1. **Enter explore mode** with `openspec-explore` — stance is "capture thinking as OpenSpec
   artifacts," not implement.
2. **Ground before sketching.** `rg` the actual anchor renderers (`MarkdownContent.tsx`,
   `UrlLink.tsx`), the live-server plumbing (`live-server.ts`, `LiveServerViewer`), and the
   split-pane seam (`SplitWorkspaceContext.tsx`, `viewer-registry.tsx`) *before* proposing anything.
3. **Find the "already 90% built" insight.** State it plainly: the pieces exist; only the
   chat-link→viewer wire is missing. This collapses scope to ~3 real edits.
4. **Coherence-check against archive/active** (`openspec/changes/archive/…`) so you extend
   `unify-file-link-openability` rather than duplicate it.
5. **Scaffold with the CLI:** `openspec new change <name>` (not `openspec change new`).
6. **Write proposal → design → specs → tasks** matching the house format; `openspec validate`.
7. **Fold in the second surface on steering** ("serve mockup") — the `serve_mockup` card is
   `GenericToolRenderer → LinkifiedText → UrlLink`, a *different* renderer. Apply DRY: one
   shared hook, not duplicated logic.
8. **Run a real doubt review** against the code, not memory. Verify the routing API, the
   hook-throws-outside-provider claim, and tab de-dup with `rg`/`sed`.
9. **Bake corrections in**, re-validate, then **commit only this change's artifacts.**

## 3. How the collaboration unfolded

**Phase A — Discovery / grounding (23:05).** Before any prose, the AI `rg`-ed for anchor
renderers and live-server plumbing, then read `UrlLink.tsx`, `MarkdownContent.tsx`,
`live-server.ts`, `EditorPane.tsx`, `viewer-registry.tsx`. *Why it worked:* it surfaced the
key insight — *"you've basically already built this"* — turning a vague feature into three
concrete edits. The decision point: the human confirmed the loopback-only scope and the
"auto-open on click, browser escape hatch from the internal window" behavior.

**Phase B — Crystallize the shape (23:10–23:17).** The AI confirmed the one risky seam
(*can a target URL ride through the viewer's `path` prop?* — yes) and drew the data-flow
diagram. The human's *"auto-open with a modifier click browser escape hatch"* locked the
interaction model. *"go on"* / green light → move to capture.

**Phase C — Scaffold + write artifacts (23:18–23:21).** Coherence check against archive
(`unify-file-link-openability`), then `openspec new change`, then proposal/design/specs/tasks
in house format, validated.

**Phase D — Scope expansion (23:22).** The human said **"serve mockup"** — fold the
`serve_mockup` result card into scope. The AI discovered it renders through a *different*
anchor renderer and, per the project DRY rule, introduced **one shared hook** rather than
duplicating the click logic across `MarkdownContent` and `UrlLink`.

**Phase E — Doubt review (23:53).** The human said **"doubt review"**. Run adversarially
against the real code, it caught **three defects**, two of which were correctness-critical
(see §5/§7). Corrections were threaded through all four artifacts and re-validated.

**Phase F — Commit (00:00).** The human said **"commit"**. The tree had unrelated changes;
the AI staged **only** this change's 7 files and committed `1a771baac`.

## 4. Prompts that worked

- **The goal prompt** — the `openspec-explore` boilerplate set the *stance* (think, don't
  implement; capture as OpenSpec). A stronger kickoff would front-load the actual ask in one
  line: *"Explore how a loopback URL in a chat message could open in the split live viewer
  instead of a new tab, and capture it as a proposal."*
- **`"auto-open with a modifier click browser escape hatch"`** — one line that fully specified
  the interaction contract (default routes to viewer; ⌘/ctrl/middle → system browser).
- **`"serve mockup"`** (2 words) — high-leverage scope expansion that forced the second-surface
  discovery and the DRY shared-hook decision.
- **`"doubt review"`** (2 words) — the single most valuable prompt: it triggered an adversarial
  re-verification that caught a *critical wrong-API* defect before the spec stood.
- **`"commit"`** — trusted the AI to stage surgically amid an unrelated dirty tree.

## 5. Steering & corrections (what to watch for)

| The AI tended to… | The human had to steer by… | Bake this in next time by… |
|-------------------|----------------------------|----------------------------|
| Explore broadly without pinning the interaction model | *"auto-open with a modifier click browser escape hatch"* | State the click/escape contract in the first prompt |
| Scope the feature to chat markdown only | *"serve mockup"* — the tool-result card is also in scope | Enumerate every surface (chat prose **and** MCP result cards) up front |
| Trust its own recollection of the routing API (`openInSplit(…, "live-server")`) | *"doubt review"* — verify against code | Always verify a proposal's *core mechanism* against the real signature before it stands |
| Assume the shared hook could no-op via `useSplitWorkspace()` | doubt review found it **throws** outside its provider | Name the non-throwing variant (`useOptionalSplitWorkspace()`) explicitly |
| Leave a stale phrase ("through the `openInSplit` funnel") after a mechanism change | manual correctness sweep | After any API change, grep the artifacts for now-stale references |

## 6. Skills, tools & memory created — and why they're effective

No new skill or memory was created — this session *consumed* existing skills well:

- **`openspec-explore`** — kept the AI in a capture-thinking stance; prevented premature code.
- **`doubt-driven-review`** (invoked as *"doubt review"*) — the star of the session. It caught
  three defects (one critical wrong-API, one would-crash) that reading alone had missed.
  **Invoke it before any spec's core mechanism is committed.**
- **Recommended skill to create:** a small **"verify-the-routing-API"** checklist for
  UI-plumbing proposals — *before claiming `fn(x, y)` routes to Z, grep the signature and the
  reducer that actually produces Z.* This session shows that pattern paying off twice.

## 7. Pitfalls & dead ends

- **`openspec change new` fails** — the correct form is **`openspec new change <name>`**. The
  session burned one failed command discovering this (checked `openspec new --help`).
- **Wrong routing API in the first draft (critical).** The spec claimed
  `openInSplit("live:"+href, "live-server")`, but `openInSplit(relPath, line?)` has **no viewer
  arg** — it derives the viewer from `fileKind(absOf(cwd, relPath))`, which can *never* yield
  `live-server`. Fix: a new context method `openLiveTarget(url)` doing
  `dispatch({type:"openFile", path:"live:"+url, viewer:"live-server"}) + updateSplit({open:true})`.
- **Wrong hook for graceful degradation (would-crash).** `useSplitWorkspace()` **throws**
  outside its provider (line 170); use `useOptionalSplitWorkspace()` (returns `null`).
- **Repeated stray-field edits.** Two `edit` calls failed / had to be reissued because of a
  stray `oldText_note` / extra field — re-issue the edit cleanly rather than patching the patch.
- **Dirty tree at commit.** Unrelated files (`manage-flows/SKILL.md`, `groups.json`, scratch
  `b05_*.txt`) sat in the tree; stage the change dir explicitly (`git add openspec/changes/<name>/`),
  never `git add -A`.

## 8. Reproduce it faster — checklist

**Inputs to have ready:** the running dashboard client sources (`packages/client/src`), the
`live-server.ts` shared plumbing, and explore mode via `openspec-explore`.

- [ ] Enter explore mode; state the one-line ask (loopback link → split viewer + modifier escape).
- [ ] `rg` the anchor renderers (`MarkdownContent.tsx`, `UrlLink.tsx`) and live-server seam
      (`live-server.ts`, `LiveServerViewer`, `SplitWorkspaceContext.tsx`, `viewer-registry.tsx`).
- [ ] Name the "already-built" insight → collapse scope to ~3 edits.
- [ ] Enumerate **both** surfaces (chat markdown + `serve_mockup`/tool-output card) now, not later.
- [ ] Coherence-check the archive; `openspec new change open-loopback-links-in-split-viewer`.
- [ ] Write proposal → design (shared hook `useLoopbackLinkOpen` via `useOptionalSplitWorkspace`)
      → specs → tasks; `openspec validate`.
- [ ] **Doubt review against code:** verify `openLiveTarget`/`openInSplit` signature, hook-throws,
      and reducer tab de-dup with `rg`/`sed`. Bake corrections in; re-validate.
- [ ] `git add openspec/changes/<name>/` only → commit spec-only artifacts.

**Final artifacts produced:**
`openspec/changes/open-loopback-links-in-split-viewer/{proposal.md, design.md, tasks.md, .openspec.yaml}`
and `specs/{chat-markdown-rendering, tool-output-linkification, live-server-preview}/spec.md`
— committed as `1a771baac`, spec-only, doubt-review corrections baked in.

---

_Generated from session `019f3418-7524-744e-817f-fc2b86d4bfed` · `/Users/robson/Project/pi-agent-dashboard` · 2026-07-06. Source extract: `/tmp/facts-1784854098N.md`._
