---
session: 019f62c7
week: 2026/W29
type: planning
model: "@fast"
premium: true
premium_reason: "yes — created 0 skill(s) / 1 memory(ies)"
upgrade_status: pending
openspec_changes: [add-session-scoped-init-trust]
proposal_excerpt: "Worktree-init TOFU trust is single-scoped and permanent: the first confirmation writes `repoRoot + hash` to `~/.pi/dashboard/worktree-init-trust.json` **forever**, even for a directory the user opened once. Running a…"
---

# How we did it: Planning session-scoped TOFU trust for worktree-init — an AI collaboration guideline

> A reusable playbook reconstructed from a real pi session. It explains **what was
> asked, how it was built with the AI, what had to be steered, and how to reproduce
> the result faster next time.** Write for a future operator who has the same goal.

---

## 1. Goal (the ask)

The session opened in **explore mode** (the `openspec-explore` skill prompt), with a
terse follow-on intent: *"enable TOFU `-s` for external directory support, as in
other agent tools, for session / project bases."*

Decoded, the real objective: today the worktree-init hook uses **Trust-On-First-Use**
that is *permanent and global* — the first "Run" confirmation writes `repoRoot + hash`
to `~/.pi/dashboard/worktree-init-trust.json` **forever**. The user wanted to add a
**scope dimension** — a *session-scoped* (ephemeral) trust option alongside the
existing *project* (persistent) one, matching what Claude Code / Cursor / pi offer,
and covering external non-git directories. The deliverable was **not code** — it was a
fully planned, doubt-reviewed OpenSpec change committed to `develop`, stopping at the
worktree boundary.

## 2. TL;DR playbook

1. **Load `plan-proposal`** and search the codebase for the domain term ("TOFU") before asking anything — the ask is terse, so ground it first.
2. **Read the real implementation** (`worktree-init-trust.ts`: `isTrusted`/`recordTrust`, keyed by `trustKey = path.resolve(configRoot) \0 hookDefHash`) so you argue from facts, not the spec's description.
3. **Confirm scope decisions with `ask_user`** (5 questions here) — but *verify each answer against the code* before accepting it.
4. **Catch the referent bug early:** the user said "session that requested it," but the manual trust path is a **browser click with no `sessionId`**. Surface the contradiction, walk A/B/C bindings, let the user pick **B (server-process ephemeral)**.
5. **Run the mandatory pre-scaffold coherence sweep** against `openspec/changes/archive/` + active changes (project rule) before writing artifacts.
6. **Scaffold via `openspec new change`**, then write proposal.md → design.md → specs delta → validate.
7. **Doubt-review proposal + design together** (`doubt-driven-review`): spawn one adversarial `Explore` reviewer with ARTIFACT + CONTRACT only (no CLAIM), then a **cross-model** reviewer via `@propose-review-1` (skip Claude-family roles — author is Claude).
8. **Reconcile only real findings** into the artifacts (here: upward scope coercion, two-button dialog, exact `trustKey` reuse), document trade-offs, then `scenario-design` → fold into tasks.md.
9. **Confirm before committing to `develop`**, commit, **stop at the worktree boundary.**

## 3. How the collaboration unfolded

**Discovery (ground the terse ask).** The AI loaded `plan-proposal`, `kb_search`'d
"TOFU," then grep'd `packages/server/src` for `isTrusted`/`recordTrust`/`trustStore`
and read the trust store itself. This is the effective bit: instead of trusting the
one-line ask, it established that trust today is *permanent + global, keyed by
`repoRoot + hash`, no scope choice at all.* It rendered the current flow as an ASCII
diagram before proposing anything.

**Clarify (5 `ask_user` questions, verified).** The AI asked scope-shaping questions
(lifetime, where the choice surfaces, non-git inclusion). Crucially it *did not take
the answers at face value* — answer #3 ("session that requested it") triggered a code
check of the init route.

**The pivotal decision point.** The AI verified `POST /api/git/worktree/init` carries
`{ cwd, requestId, confirmHash }` — **no `sessionId`** — and is called by
`WorktreeInitButton` (a human browser click). So "the pi session that requested it"
had *no referent* on the manual path. It then checked the browser gateway and found
**no stable client identity** (raw `ws` sockets, constant reconnects). It laid out
three bindings — **A** browser client (churns on reconnect), **B** server process
(ephemeral, survives reconnect), **C** pi session id (breaks for external dirs). The
human chose **B**, with honest UI labels.

**Generate.** Coherence sweep ran clean (delta on existing `worktree-init-hook` spec;
prior art `generalize-worktree-init-hook` + `support-non-git-init-hook`). The AI
scaffolded the change and wrote proposal.md, design.md, the MODIFIED spec delta, then
validated (3/4).

**Verify (doubt-driven, cross-model).** It ran the doubt cycle: a first adversarial
`Explore` reviewer produced 10 findings; a second **cross-model** reviewer
(`@propose-review-1` = glm-5.2) *converged* on the top ones. The load-bearing flaw
both caught: a malformed `scope` value silently coercing **upward** to permanent
project trust — an RCE-class escalation against ephemeral intent. Fix: omitted→project
(back-compat), present-but-unrecognized→**reject** `bad_request`.

**Fold + commit.** `scenario-design` mapped scenarios to existing test exemplars (L1
`worktree-init-trust.test.ts`, `routes-git-worktree-init.test.ts`; L3
`worktree-init-feedback.spec.ts`), folded into 27 vanilla-checkbox tasks, validated,
then committed to `develop` (`f4b1f27d6`) after confirming — stopping at the worktree
boundary.

## 4. Prompts that worked

- **The goal prompt** (`openspec-explore` + the one-liner). It worked *because the AI
  refused to treat it as complete* — it grounded "TOFU" in code first. A stronger
  kickoff would name the concrete files up front: *"Plan a session-scoped trust option
  for the worktree-init hook (`worktree-init-trust.ts`), matching Cursor/Claude Code
  scope choices; external non-git dirs included."*
- **The high-leverage follow-up: `"B"`** — a single character. It resolved the entire
  design because the AI had already done the work of laying out A/B/C bindings with
  their exact trade-offs. Lesson: *make the AI enumerate grounded options so a
  one-token answer can lock the architecture.*

## 5. Steering & corrections (what to watch for)

| The AI tended to… | The human had to steer by… | Bake this in next time by… |
|-------------------|----------------------------|----------------------------|
| Accept the ask's framing ("session that requested it") as coherent | The AI self-corrected by verifying the init route had **no `sessionId`** — but a terser session could have shipped the bug | State up front: *the manual trust path is a browser action; there is no owning pi session* |
| Reach for the obvious binding (pi session id) | Choosing **B (server-process)** once shown external dirs break under C | Ask for A/B/C bindings *with a persistence/reconnect/external-dir matrix* before deciding |
| Risk coercing an unknown `scope` value upward to permanent | Cross-model review caught it; reconciled to **reject unrecognized, never escalate** | Make "never escalate trust durability on malformed input" an explicit design invariant |
| Reuse the shared `Confirm` (one `onConfirm`) for a two-button choice | Review flagged it; design specifies a **purpose-built dialog** | Note when a UI needs two *affirmative* actions — the shared component can't express it |

## 6. Skills, tools & memory created — and why they're effective

- **Memory (attempted, project · insight):** the TOFU trust architecture — trust lives
  server-side in `worktree-init-trust.ts`, keyed by
  `trustKey = path.resolve(configRoot) \0 hookDefHash`, persisted forever. **The write
  was skipped** because the project memory store was at capacity (4949/5000) and the
  same facts are durably captured in the committed `design.md` Context section. Lesson:
  *when memory is full, don't evict — lean on the artifact that already records it.*
- **No skill created.** The workflow *is* already a skill (`plan-proposal` chaining
  `openspec-*` + `doubt-driven-review` + `scenario-design`). The reusable nugget worth
  keeping is narrower: **"verify the request's referent against the route signature
  before designing around it"** — the single move that turned a latent bug into a design
  decision.

## 7. Pitfalls & dead ends

- **"Session-scoped" is ambiguous — resolve the referent first.** Don't assume it means
  a pi session id. Check whether the triggering path even *carries* a session id
  (`POST /worktree/init` did not).
- **Browser client identity is not stable** here — raw `ws` sockets churn on every
  reconnect. Binding trust lifetime to a browser client (option A) would silently drop
  grants; only paired-device ids survive, and that's heavy.
- **Upward scope coercion = RCE-class hole.** A malformed `scope` defaulting to
  permanent project trust escalates durability against user intent. Reject unrecognized
  values; only *omitted* may fall back to project for back-compat.
- **Memory at capacity (4949/5000).** Don't disrupt existing entries for a fact already
  in a committed artifact — skip the write.

## 8. Reproduce it faster — checklist

- [ ] Load `plan-proposal`; `kb_search` the domain term; read the real store (`worktree-init-trust.ts`).
- [ ] `ask_user` for scope decisions — then **verify each answer against the route/code**.
- [ ] Trace the triggering request signature (`POST /api/git/worktree/init` = `{ cwd, requestId, confirmHash }`, no `sessionId`).
- [ ] Enumerate binding options (A browser / B server-process / C pi-session) with a persistence × reconnect × external-dir matrix; let a one-token answer decide.
- [ ] Run the pre-scaffold coherence sweep over `archive/` + active changes.
- [ ] `openspec new change add-session-scoped-init-trust`; write proposal → design → spec delta; `openspec validate`.
- [ ] `doubt-driven-review`: adversarial `Explore` + cross-model `@propose-review-1` (skip Claude-family); reconcile real findings only.
- [ ] `scenario-design` → point tasks at existing exemplars → fold into vanilla-checkbox tasks.md → validate.
- [ ] Confirm, commit to `develop`, **stop at the worktree boundary.**

**Key inputs to have ready:** the trust store + init-route files; configured
`@propose-review-N` roles (non-Claude); the OpenSpec CLI.
**Artifacts produced:** `openspec/changes/add-session-scoped-init-trust/{proposal,design,test-plan,tasks}.md` + `specs/worktree-init-hook/spec.md`; commit `f4b1f27d6` on `develop`.

---

_Generated from session `019f62c7-6ab4-7dd9-93e1-26aad04d8ea7` · `/Users/robson/Project/pi-agent-dashboard` · 2026-07-15. Source extract: deterministic facts sheet._
