# Delta

Delta document inside the worktree (kb-parent/worktrees/kb-wt). The worktree
ships NO knowledge_base.json, so its KB origin is defaults until Copy-from-parent
seeds sources[]. After the copy, a reindex over ref "." picks up this file.

## Cogs

Cogs turn. Cogs index cleanly once the worktree has a config.
