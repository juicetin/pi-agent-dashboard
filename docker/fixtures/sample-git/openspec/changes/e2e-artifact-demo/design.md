# Design — e2e artifact demo

## Context

Fixture design artifact. Its only job is to exist so the `D` badge renders and
the artifact dialog's Design tab has distinct content to show. See change:
openspec-artifact-dialog-desktop.

## Decision

Ship a single demo capability with one requirement + scenario.
