# Proposal — e2e artifact demo

## Why

E2E fixture change. Gives the sample-git session a real OpenSpec change with
all four artifacts so the non-mobile artifact-dialog specs can click the
P/D/S/T badges. See change: openspec-artifact-dialog-desktop.

## What Changes

- Add a demo capability spec so the `S` badge renders.
- Provide proposal/design/tasks so `P`/`D`/`T` badges render.

## Non-goals

- No production behavior. Fixture-only.
