# e2e-demo-cap

## ADDED Requirements

### Requirement: Demo capability

The fixture SHALL expose a demo capability so the artifact dialog's Specs tab
renders distinct content.

#### Scenario: demo renders

- **WHEN** the artifact dialog opens the Specs tab
- **THEN** the demo capability content is shown
