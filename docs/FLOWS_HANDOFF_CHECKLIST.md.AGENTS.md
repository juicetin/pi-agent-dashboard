# FLOWS_HANDOFF_CHECKLIST.md — index

(repo root) LLM handoff checklist. Verifies pi-flows + Anthropic-messages bridge + model resolution end-to-end. Reports ACTUAL resolved path+version per tier vs reference baseline. Tier 1 `createRequire` cwd-anchored; Tier 2 `resolvePiPackageEntry` walks `packages[]`. Peer present if Tier 1 OR Tier 2 resolves.
