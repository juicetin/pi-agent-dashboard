# architecture.md — index

Full architecture reference. 3 components: bridge extension, Node server, React client. WS ports 9999 (bridge) + 8000 (client). In-memory + JSON persistence. `/site` marketing app product-adjacent, separate build. "Server-Keypair Device Pairing" section: Topology 3 static PWA shell, Ed25519 TOFU identity, QR pairing, compare-code approval, bearer + WS-ticket auth, genuine-local trust. Global Escape dismissal: `escape-stack.ts` LIFO stack arbitrates portaled dismissible surfaces. `useEscapeDismiss` hook replaces self-managed `keydown` listeners; stacked overlays peel one layer per Escape. Adopted by Dialog, ImageLightbox, FilePreviewOverlay. Overlay layering: `--z-*` token scale in `packages/client/src/index.css`; shared `Dialog` at `z-dialog` (50), toast 60 above; nested dialogs tie at 50, later portal/DOM mount wins; `z-layer-lint` `SCAN_DIR` skips `packages/client-utils` (raw-z gate blind spot). Cold-start recovery section: exit-intent boot record (`~/.pi/dashboard/boot-state.json`), recovery gate rule, recording points (restart/shutdown/user-quit/idle/signal), deferred offer broadcast, keeper reattach probe. "Plugin settings pages" section: settings contribution = `settings-section` claim OR intent; `contributesSettings()` predicate shared by nav rail, cog, route guard; contribution renders on `/settings/plugins/<id>` under host-owned chrome; `claim.tab` inert; enable-state desired-state (overlay) vs `loaded` runtime truth. "Project-scope disable of global resources" section: origin classification by longest-prefix path match (never `metadata.scope`/`source`/`baseDir`). 4 write forms: `.pi` loose `-rel`; `.agents` base dir; package delta `{source, autoload:false}` mandatory at project scope; global-loose = own-file `~` entry + anchored glob. Normalised package identity. Ownership `~/.pi/dashboard/resource-entry-ownership.json`. `POST /api/resources/trust` + `defaultProjectTrust` gate. Settings write NOT JSONC-preserving. Session-start resolve. User image attachments: two-phase display fit. Bounded `attachmentState:"pending"` placeholder row first. Fitted 768px derivative = own `attachment_fitted` event, sha256-addressed. Originals via `GET /api/sessions/:sessionId/attachments/:attachmentId`, transcript-backed, not load-bearing. OAuth `auth.redirectBaseUrl` subsection: base precedence (`auth.redirectBaseUrl` → `getTunnelUrl()` → `http://localhost:<port>`); 3 auth-plugin call sites; hot-reload chain; boot-time empty-registry trap (`_reloadAuth` never installed); `warnOnInvalidRedirectBase` warn-not-discard; scope = OAuth redirect URIs only; Settings ▸ Security input `redirect-base-url-input`. Gateway URL Management section: `publicBaseUrls` top-level promotion (legacy `pairing.publicBaseUrls` fallback, not OAuth tier D7); gateway action one statement → one `PUT /api/config` + `gateways[]` provenance (D12/D13); mtime-gated config snapshot `config-snapshot.ts` (D15); `trustProxy` stays off (D14, `forwarded-ip-trust.test.ts`). See changes: add-server-keypair-pairing, fix-stacked-escape-closes-layers, add-embed-session-lifecycle, fix-recovery-exit-intent, plugin-settings-pages, fit-attachments-for-display, project-scope-disable-global-resources, config-override-oauth-redirect-base, add-folder-actions-menu. Plugin requirement probes: `paths` 4th category + `${configKey}` interpolation, `shouldReconcilePath` reconciliation, client warning pill. Health `/api/health` observability: `storeTrim` (trimmedEvents/evictedSessions/collapsedUpdates) from `eventStore.getTrimStats()`; collapse keeps ≤2 `tool_execution_update` per `toolCallId`; superseded ticks dropped at retention only, never suppresses live broadcast. See changes: add-apple-tools-imcp-plugin, collapse-superseded-tool-execution-updates. "Knowledge Base (KB)" section: single FTS5 table `chunks` over `node:sqlite`; zero network/LLM, mechanical ranking. Backend `SqliteFtsStore` (`packages/kb/src/sqlite-store.ts`); config layered project → global → defaults (`packages/kb/src/config.ts`). "KB retrieval pipeline" `store.search()`: one BM25 pass + staged pipeline. Stage order: body-hash collapse (byte-exact dedup, `akaPaths`, runs first) → source dedup (one hit per `(root,path)`, best BM25 score, `suppressedSections`) → MMR (`diversity`, config-gated default on) → coverage rerank (default off) → lane interleave → limit slice → parent expand (`expandParent` default on, attaches parent `headingPath`). `limit` bounds distinct sources not chunks (BREAKING); fetch depth `limit × 6` source-dedup / `limit × 4` body-hash-only, cap 4000. Lane quota `ranking.laneQuota` (default 0.5): `agents` 3.2% of index ~3× longer, BM25 length normalisation buries ~30:1; second FTS pass `doc_type='agents'` shallow pool interleaved; explicit `docType` bypasses quota. Coverage rerank + RM3-style PRF (`queryExpansion.mode: "prf"`) default off; measured regression (markdown-intent R@10 0.630→0.491, latency ~4×). Condensed render emits leaf heading not breadcrumb + `(+N more sections)`; full `headingPath` retained. `kb_get` path-only fetch returns first chunk + `suppressedSections`, never silent 1-of-N. Measured 31,121-chunk index K=10: combined R@10 0.363→0.566, duplicate-slot share 0.48→0.00, distinct sources/page 5.2→10.0, render tokens −32.8%. Latency budget met at median (lane-quota 53.2 ms), p95 fails (84.8 ms; ~22k chunks ≈60 ms). Eval + sweep table: `packages/kb/eval/` (`run-fixtures.ts`, `measure-render.ts`), `measurements.md`. See change: fix-kb-search-retrieval-quality. Bridge Connection Contention section: one-live-bridge-per-session-id invariant enforced at gateway; claim point = first-message identity block (`pi-gateway.ts`) not `session_register` dispatch; two-factor liveness rule (pong → keep, TCP writable → busy → keep, neither → displace) in `bridge-contention.ts`; probe window 5 s; terminal `register_rejected` before close, watchdog stays armed; `stop()` terminates `wss.clients`; close identity-scoped on `connections.get(id)===ws`; resume session-file guard `findLiveSessionBySessionFile` at both sites (D1 liveness, fork exempt); health `bridgeContentionCount` + `contendedSessionIds` + `piGatewayPort`; refusal rate-limited 1/id/5 s; prompt annotates + `delivered:true`. "Bind-vs-trust reachability" section: `bindHost` vs `trustedNetworks`/`auth.bypassHosts` coupling; predicate home `packages/shared/src/bind-reachability.ts` (one impl, both packages import); 6-step evaluation order; `resolvedBindHost` vs `pendingBindHost` (`--host` → `PI_DASHBOARD_HOST` → `config.bindHost` → `127.0.0.1`, draft outranks); `reachability` on the GUARDED `/api/config`, never `/api/health`, stripped on write; `reachability_updated` broadcast + connect replay; advisory independent of `BlockEventTrustBanner`; `/api/network-interfaces` `label`/`pointToPoint`/`suggestions`, one entry per address, dropdown dedupes on suggestion `value`; interface offer table. See changes: fix-duplicate-bridge-registration, warn-unreachable-trusted-networks. Pi Gateway Transport & Identity section (shipped, add-pi-gateway-transport-identity): transport default = unix socket `<dashboardConfigDir>/gateway-<piPort>.sock` (`~/.pi/dashboard/gateway-<piPort>.sock`), mode 0600, dir 0700; TCP opt-in only, `PI_GATEWAY_TCP` truthy (`packages/server/src/pi/gateway-transport-policy.ts`); Windows always loopback + `X-Pi-Local-Token`; stale-socket fail-closed via `gateway-<piPort>.sock.lock` probe/unlink/bind. Endpoint resolution D3 precedence ladder (`packages/extension/src/endpoint-resolution.ts`): explicit PINNED 1-3 > HOME rendezvous record > paired remote > mDNS suggest-only, never overrides; stickiness `pinned` + `decideRetarget`. Per-transport auth (`packages/server/src/pi/bridge-upgrade-auth.ts`): none unix / `X-Pi-Local-Token` loopback / single-use bridge-scoped ws ticket remote; server identity = Ed25519 fingerprint; instance id names, never entitles (D14). Move (`/dashboard-connect`, `/dashboard-list`, `/dashboard-where`): provisional registration → verify instance id → `session_move_commit`; origin serves till commit; `session_moved` → `movedTo` + `status:"ended"`. Remote transcripts read-only boundary (D12/D13): `decideTranscriptRequest` refuses path-bearing fields; remote-origin refuses local file reads + resume; retention `~/.pi/dashboard/remote-transcripts/<sessionId>.jsonl`. See change: add-pi-gateway-transport-identity. "OpenSpec main-spec integrity" section: main specs `openspec/specs/<cap>/spec.md`; h2 `## Purpose` + `## Requirements` parse contract (`MarkdownParser.parseSpec` throws otherwise); `findSection` exact title match, case-insensitive (`## ADDED Requirements` != `## Requirements`); delta headers (`## ADDED|MODIFIED|REMOVED|RENAMED Requirements`) changes-tree only; archive once copied delta verbatim — 80/546 unparseable, 384 blocks invisible to validate/list/show/archive; fix `node scripts/repair-main-specs.mjs` (`--dry-run`, `--specs-dir <path>`), idempotent, refuses `## REMOVED Requirements`; gate `npm run spec:validate` = `openspec validate --specs --no-interactive`, step "Validate OpenSpec main specs" in `ci` job; corrupt archive blocks `develop`; retired zero-requirement cap: tombstone if authored `**DEPRECATED**` pointer, else delete. See change: repair-corrupted-main-specs. Model selector pairing rule section: RUN-CONFIGURING selectors MUST pair thinking-level control; REFERENCE-LISTING MUST NOT. `:<level>` suffix on model ref, parsed by `splitThinkingSuffix` (`packages/extension/src/provider-register.ts`). Levels `off` `minimal` `low` `medium` `high` `xhigh` `max`. `off` writes bare ref. No server change. See change: add-default-thinking-level. Plugin Architecture runtime bullets: `manifest-validator.ts` rejects unknown `shell-overlay-route` `presentation` FATAL; `slot-consumers.tsx` `ShellOverlayRouteSlot` renders claim body only (flex height wrapper); `presentation` consumed by host via `useShellOverlayRoutePresentation` (default `"dialog"`, `"page"` opts out) — host lifts dialog claim into `RouteBackedOverlay` (underlay must cover viewport, slot cannot); `dialogContainer` prop + `OverlayContainerProps`/`OverlayContainerComponent` removed; hook returns string, no `client-utils` → `dashboard-plugin-runtime` cycle; D2a SUPERSEDED; bundled gate `__tests__/bundled-overlay-claims.test.ts`. See change: add-route-backed-overlay-dialogs.
