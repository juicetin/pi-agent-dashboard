# electron-qa-smoke-test-inventory.md — index

(repo root) QA/smoke test inventory. All 574 archived OpenSpec proposals. 11864 test/verification items extracted by parallel gemma + deepseek workers, one per proposal. Grouped by month/change. Each bullet `[type]` what-verifies — path — status. Reference to find + build missing tests.
