# examples/c4-example.md — index

C4-model diagram example. Mermaid fenced blocks: `C4Context`, `C4Container`. Renders native GitHub/GitLab/VS Code, no toolchain. Mermaid C4 support experimental; nudge via `UpdateLayoutConfig`/`UpdateElementStyle`.
