# File Index — Web client

> Part of [pi-agent-dashboard file index](./file-index.md). Loaded on demand.
>
> **Change-history annotations** (e.g. *"See change: foo-bar"*) → OpenSpec changes archived under `openspec/changes/archive/`.
>
> **Update protocol**: see `AGENTS.md` → "Documentation Update Protocol".

| File | Purpose |
|------|---------|
| `src/client/components/ProcessList.tsx` | Session card process list with elapsed time + red ✕ kill button |
| `src/client/components/BranchPicker.tsx` | Typeahead branch picker with keyboard navigation |
| `src/client/components/BranchSwitchDialog.tsx` | Checkout orchestration: dirty-state stash, pop prompt |
| `src/client/lib/git-api.ts` | Client-side fetch helpers for git API endpoints |
| `src/client/hooks/useImagePaste.ts` | Reusable clipboard-image-paste hook with controlled/uncontrolled modes. Uncontrolled (`useImagePaste()` with no args): hook owns `pendingImages` in local `useState` — used by `ExploreDialog` whose lifetime IS the dialog. Controlled (`useImagePaste({ images, onImagesChange })`): caller owns the array — used by `<CommandInput>` so App can key pending images by `sessionId`. `imageError` stays local in BOTH modes (auto-clears after 3 s, no value in lifting). Supports image/png, image/jpeg, image/gif, image/webp; 10 MB base64 cap. See change: lift-pending-images-to-app |
| `src/client/components/extension-ui/GenericExtensionDialog.tsx` | Phase-1 modal renderer for `ExtensionUiModule`. Supports `view.kind ∈ {table, grid, form}`, dispatches `ui_management { action: "list", event: dataEvent }` on mount for table/grid, gates `UiAction.confirm` through `ConfirmDialog`. MDI icons resolved via `mdi-icon-lookup.ts` (unknown keys render no icon). |
| `src/client/components/extension-ui/decorator-utils.ts` | Phase-2 helper: `decoratorsOfKind(decorators, kind)` — typed filter over `Session.uiDecorators` returning only descriptors of requested `DecoratorKind`. Used by every Phase-2 slot component. See change: add-extension-ui-decorations. |
| `src/client/components/extension-ui/FooterSegmentSlot.tsx` | Phase-2 slot. Renders all `kind: "footer-segment"` descriptors as inline pills mounted inside `SessionHeader.tsx` to right of model/thinking-level info. Supports MDI `payload.icon` (resolved via `mdi-icon-lookup`) + `payload.tooltip` (HTML title attribute). |
| `src/client/components/extension-ui/AgentMetricSlot.tsx` | Phase-2 slot. Renders `agent-metric` descriptors whose `payload.agentId` matches `FlowAgentCard.agent.agentName` of parent. Mounted inside `FlowAgentCard.tsx`. Orphan descriptors (no matching agent) render nothing. |
| `src/client/components/extension-ui/BreadcrumbSlot.tsx` | Phase-2 slot. Renders most-recently-cached `breadcrumb` descriptor as horizontal step indicator at top of `FlowDashboard.tsx`. Steps with `status: "done"` show check; `"error"` show red alert; active step matched against `payload.current` (or first `status: "active"`). |
| `src/client/components/extension-ui/GateSlot.tsx` | Phase-2 slot. Aggregates `gate` descriptors targeting same `flowId` via pure helper `aggregateGateState(decorators, flowId)`: most-restrictive-wins (any `available: false` blocks; reasons concatenated). Mounted inline in `FlowLaunchDialog.tsx`; dialog also reads `aggregateGateState` directly to disable Run button. |
| `src/client/components/extension-ui/ToastSlot.tsx` | Phase-2 slot. Top-right fixed tray mounted in `App.tsx`. Reads toast descriptors across every session in `sessions` Map, stacks without deduplication, auto-dismisses each toast after `payload.durationMs` (default 5000ms; `0` = sticky), caps simultaneous-display at 5 (FIFO eviction — cache unaffected). Manual dismiss via close button latches descriptor key into local `dismissed` set so cache replay does not resurrect dismissed toasts. |
| `src/client/lib/mdi-icon-lookup.ts` | `resolveMdiIcon(key) → string \| null` — looks up MDI key string against `@mdi/js` exports; allowlists by `mdi`-prefix, returns `null` for unknown keys (no error). XSS-safe icon vocabulary for extension descriptors. |
| `src/client/lib/prompt-component-registry.ts` | Client-side component registry mapping prompt type strings to render metadata (placement, component) |
| `src/client/components/ImageLightbox.tsx` | Full-size image lightbox with zoom/pan (useZoomPan), Esc/backdrop close |
| `src/client/components/CollapsedToolGroup.tsx` | Collapsed group of repeated tool calls with expand toggle |
| `src/client/lib/group-tool-calls.ts` | Groups consecutive identical tool calls for chat display |
| `src/client/lib/collapse-retried-errors.ts` | Two pure helpers used by `ChatView` to remove duplicate tool cards: `findRetriedErrorIds(messages)` returns ids of error `toolResult`s immediately superseded by successful retry of same tool (skipping `assistant`/`thinking`/`turnSeparator`/`rawEvent`/`commandFeedback`); `findActiveInteractiveToolResultIds(messages)` returns ids of `running` `toolResult`s paired with *pending* `interactiveUi` message that follows them, so running tool card can be hidden while `InteractiveUiCard` is sole interactive surface. See change: collapse-duplicate-tool-cards |
| `src/client/components/RetriedErrorBadge.tsx` | One-line `⚠ <toolName> failed — retried ›` pill rendered by `ChatView` in place of full `ToolCallStep` for ids returned by `findRetriedErrorIds`. Click toggles expanded view that re-uses `<ToolCallStep status="error">` with "Hide failed attempt" toggle so original validation message + `Received arguments:` JSON still recoverable. See change: collapse-duplicate-tool-cards |
| `src/client/components/SkillInvocationCard.tsx` | Renders user messages whose `ChatMessage.skill` is set as a collapsible purple-tinted card (wrench icon + full `/skill:name args` slash form in header, `MarkdownContent` body, args section, three CopyButtons — Copy as Markdown / plain text / **`/skill:` command** — + fork). Default collapsed; per-instance React state. Pi's wrapper format aligned at the bridge so this card renders identically for both pi-TUI- and dashboard-typed skill turns. See change: render-skill-invocations-collapsibly. |
| `src/client/components/FolderOpenSpecSection.tsx` | Folder-level OpenSpec UI: collapsible change list, refresh, bulk archive, archive button. Each change row's `N/M tasks` indicator is button that opens existing `TasksPopover` (one popover at a time; opening another row swaps popover); `mdiPlay` icon button rendered after artifact letters invokes `onSpawnAttached(cwd, changeName)` to spawn pi session with change pre-attached. Both buttons stop propagation so surrounding folder collapse unaffected. See change: add-folder-task-checker-and-spawn-attach. |
| `src/client/components/ArchiveBrowserView.tsx` | Searchable archive browser: date-grouped list, two-level nav to artifact reader |
| `src/client/hooks/useArchiveListing.ts` | Fetch hook + pure helpers (groupByDate, filterEntries) for archive endpoint |
| `src/client/components/SessionOpenSpecActions.tsx` | Session-level OpenSpec: searchable attach dialog, action buttons, detach |
| `src/client/components/DialogPortal.tsx` | Portal wrapper rendering dialogs at document.body with scroll lock |
| `src/client/components/PinDirectoryDialog.tsx` | Dialog to pin directory (wraps PathPicker) |
| `src/client/components/PathPicker.tsx` | Reusable keyboard-first path picker with typeahead directory list. Change `fix-pathpicker-windows-trailing-sep`: `tryConfirm` Rule 2 accepts `\` trailing separator (Windows + UNC), not just `/`. |
| `src/client/lib/browse-api.ts` | Client-side browse API helper for PathPicker |
| `packages/client/src/hooks/useBootstrapStatus.ts` | Client hook for bootstrap state. Fetches `/api/bootstrap/status` on mount, subscribes to `bootstrap-status` `CustomEvent` dispatched by `useMessageHandler` on `bootstrap_status_update` WS broadcasts. Exposes `{ state, isLoading, error, refresh, retry, upgradePi }`. |
| `packages/client/src/components/BootstrapBanner.tsx` | Banner mounted in `App.tsx` above `<MobileShell>`. Hidden at status="ready" with no compatibility hints; blue "Installing pi…" when installing; red "pi install failed — [Retry]" when failed; amber upgrade hints when `compatibility.upgradeRecommended` or `upgradeDashboard` is true. |
| `src/client/lib/tools-api.ts` | Client-side fetch helpers for `/api/tools*` (`fetchTools`, `rescanAll`, `rescanOne`, `setOverride`, `clearOverride`, `downloadDiagnostics`) |
| `src/client/components/ToolsSection.tsx` | Settings → General → **Tools** section. One row per registered tool: status badge, source, truncated path, expand-to-trail, override input, per-row rescan. Top-level: Rescan all / Reset overrides / Export diagnostics |
| `src/client/components/SortablePinnedGroup.tsx` | Drag-to-reorder wrapper for pinned directory groups |
| `src/client/lib/providers-api.ts` | Client-side fetch helper: `testProvider({ name?, baseUrl, apiKey, api })` → structured `{ ok, status?, modelCount?, sample?, error? }`. Used by **Test** button on LLM Providers card |
| `src/client/components/ProviderAuthSection.tsx` | Settings section: OAuth login buttons, device-code modal, API key inputs |
| `src/client/components/SettingsPanel.tsx` | Settings UI: all dashboard config fields, grouped form, save to server. Includes `spawnRegisterTimeoutMs` field. See change: spawn-failure-diagnostics. |
| `packages/client/src/components/DiagnosticsSection.tsx` | Settings → Diagnostics. Fetches `/api/doctor`. Groups by section in fixed order, omits empty sections (no n/a placeholders for Electron-only rows). Status pill + message + truncated path + `<MarkdownContent>` suggestion. Toolbar `[Re-run]` (disabled while in flight, "Running…" label) + `[Copy as Markdown]` / `[Copy as Plain]` (textarea-modal fallback on `navigator.clipboard.writeText` rejection). Inline error block on non-200 / shape-mismatch with HTTP status + 500-char body excerpt. |
| `packages/client/src/lib/doctor-api.ts` | Typed `fetchDoctorReport(): Promise<DoctorReport>` against `/api/doctor` via auth-aware fetch wrapper. |
| `src/client/hooks/useAuthStatus.ts` | Client auth status hook + login redirect helper |
| `src/client/components/ServerSelector.tsx` | Server selector dropdown showing persisted known servers. Availability probing runs **only on dropdown open** (once per open, not on mount, not on timer, not while closed). Accepts `inFlightSwitchKey` to render spinner on entry being switched to. Unreachable entries `disabled`, rendered with `opacity-50` + `cursor-not-allowed`, clicks no-ops. Reachable clicks delegate to transactional staging-socket switch (see `server-switch.ts`). See change: safe-server-switch |
| `packages/client/src/lib/staging-socket.ts` | `openStagingSocket(url, {timeoutMs}): Promise<WebSocket>` — single-settle helper that resolves on first `OPEN`, rejects on error/close/timeout, closes socket on timeout to avoid leaks. Used by transactional server-switch. |
| `packages/client/src/lib/server-switch.ts` | `performServerSwitch(target, deps)` — extracted two-phase transaction (stage → commit) from `App.tsx`'s `handleServerSwitch`. Guarantees ordering `clearInMemoryState` → `setWsUrl` → `persistLastServer`, never persists localStorage or clears state on staging failure. Fully unit-tested. |
| `packages/client/src/components/RetryBanner.tsx` | Amber transient banner above ChatView input while `SessionState.retryState` set. Renders countdown when `delayMs > 0` AND `maxAttempts > 0`; else renders indeterminate "retrying…" (sentinel `-1` from bridge synthesis). "Stop retrying" button calls `onAbort`. See change: fix-provider-retry-infinite-loop. |
| `packages/client/src/components/SpawnErrorBanner.tsx` | Renders structured spawn error: code→hint, preflight reasons, stderr details, timeout banner. See change: spawn-failure-diagnostics. |
| `packages/client/src/components/ConnectionStatusBanner.tsx` | Disconnection banner: appears only after active WebSocket has been non-`OPEN` for &gt;3s continuously; hidden immediately on reconnect; suppressed during in-flight staging switch. Mounted above `<MobileShell>` in `App.tsx`. |
| `src/client/components/KnownServersSection.tsx` | Settings section: list/add/remove persisted known remote servers |
| `src/client/components/NetworkDiscoverySection.tsx` | Settings section: mDNS network scan with "Add" action + label prompt. When scan finds zero servers (common — Wi-Fi AP isolation, mesh routers, VLANs, VPN, firewall), renders diagnostic block listing failure causes plus inline manual-add form that accepts free-form host input parsed via `parseHostInput()`. Surfaces scan errors instead of swallowing them. See change: diagnose-empty-mdns-scan |
| `src/client/lib/parse-host-input.ts` | Pure helper `parseHostInput(input, defaultPort = 8000) → {host, port} \| null` accepting full URLs (`http://192.168.16.202:8000`), `host:port`, bare hostnames, bracketed IPv6 (`[::1]:8000`). Rejects empty input, bare IPv6 (ambiguous with `host:port`), invalid ports, malformed URLs. 12 unit tests in `parse-host-input.test.ts`. See change: diagnose-empty-mdns-scan |
| `src/client/lib/known-servers-api.ts` | Client-side fetch helpers for known servers CRUD + discovery endpoints |
| `src/client/components/TunnelButton.tsx` | Unified tunnel/QR button — tunnel icon when not set up, QR icon when inactive, green QR icon when connected; opens QR dialog with disconnect/setup |
| `src/client/components/QrCodeDialog.tsx` | QR code dialog showing tunnel URL as scannable QR code with copy, disconnect, setup buttons |
| `src/client/components/ZrokInstallGuide.tsx` | OS-aware zrok installation guide view (macOS/Linux/Windows) |
| `src/client/lib/session-grouping.ts` | `inferPlatform(samples)` heuristic (backslash/drive-letter = Windows, leading `/` = POSIX) + `groupSessionsByDirectory` using `normalizePath`-keyed Maps so sessions group under pinned folder across separator/case/trailing drift. **Per-session group-key precedence** (change: add-jj-workspace-plugin, Decision 15): pure helper `resolveSessionGroupPath(session, pinnedKeys, platform)` resolves key as **explicit pin > `jjState.workspaceRoot` > `cwd`** — session inside `.shadow/<name>/` jj workspace collapses under parent repo's group, but explicit pin on workspace path still wins. Within group, sessions pre-sorted by `clusterByWorkspaceName` so rows sharing `(jjState?.workspaceName ?? "")` cluster adjacently (empty / main-tree first, then ws-A, ws-B, …); `sortSessionsByOrder` still applies inside each cluster. Tests: `packages/client/src/lib/__tests__/session-grouping.test.ts` (5 tests) — 4 spec scenarios + default-workspace regression guard. |
| `src/client/components/PiResourcesView.tsx` | Content area view for browsing pi extensions, skills, prompts. Two tabs: **Resources** (browse-only — loose `.pi/{skills,extensions,prompts}` files plus per-package nested resource trees that contribute to session; renamed from "Installed" but internal route id stays `"installed"`) + **Packages** (workspace-scope manage surface, hosts `PackageBrowser`). `MergedScopeSection` filters out installed packages with zero contributed resources — those visible only in Packages tab. No standalone manage rows in Resources tab. See change: unify-workspace-package-management. |
| `src/client/components/InstalledPackagesList.tsx` | Shared rich-row list of installed packages (Settings → Packages + Pi Resources → Installed). Composes `<PackageRow>` per entry with `useInstalledPackages` + `usePackageOperations` (extended: `move`, `moveStateFor`, `clearMove`). Per-row expand-chevron reveals inline tree of contained skills/extensions/prompts from `containedResources: Map<source, PiPackageInfo>` prop (caller projects from `usePiResources`). Move → button derived from `currentScope`, gated by `otherScopePackages` prop (compared via `computeDestIdentity` from `lib/installed-list-helpers.ts` — client-side mirror of server identity rules for npm/git/https; path sources fall back to literal). Partial-success banner inline when move's `package_operation_complete` carries `partialSuccess`; Cleanup button re-POSTs `/api/packages/remove` against `fromScope`. See change: unify-package-management-ui. |
| `src/client/lib/move-tracker.ts` | In-flight move tracker (singleton). Decoupled from `package-queue` because moves are moveId-keyed (not source-keyed) + have partial-success semantics. Listens on `pi-package-event` → `package_operation_complete` events that carry `moveId`; updates state per-moveId; auto-clears successful moves after 3 s; keeps partial-success states sticky until user clicks Cleanup or Dismiss. Exposed through `usePackageOperations.moveStateFor(source) / clearMove(moveId)`. See change: unify-package-management-ui. |
| `src/client/lib/packages-api.ts` | Client-side fetch helpers for package endpoints that don't fit `package-queue` source-keyed model. Currently hosts `movePackage(args): Promise<MoveResponse>` (POSTs `/api/packages/move`, returns discriminated union `{ok:true, moveId, phases}` / `{ok:false, status, code, message}` so callers can branch on stable error codes without try/catch). See change: unify-package-management-ui. |
| `src/client/components/PackageBrowser.tsx` | Reusable inline package browser: npm search, type filters, manual URL input. **Workspace-scope manage surface** — renders "Installed Packages" section above search using `PackageRow` + `classifySource(pkg.source)`, mirroring `UnifiedPackagesSection` (Settings → Pi Ecosystem). Every source shape (`npm:`, absolute path, relative path, `file://`, `git@`, `https://...git`) gets working `Update` / `Uninstall` actions; `operations.remove(pkg.source)` flows raw source verbatim, no regex reshape. Cross-scope `installedInfo` map keyed by `pkg.source` (search-result rows synthesize `npm:${pkg.name}` at lookup time). Legacy "Installed" filter pill removed — dedicated section above search replaces it. See changes: unify-workspace-package-management, fix-local-path-install-spinner. |
| `src/client/components/PackageCard.tsx` | Package card with type badges, downloads, install/uninstall actions |
| `src/client/components/PackageReadmeDialog.tsx` | Dialog overlay showing package README with install/uninstall action |
| `src/client/components/PackageInstallConfirmDialog.tsx` | Confirmation dialog before package install (name, source, scope) |
| `src/client/components/PackageRow.tsx` | Generic row component shared by `UnifiedPackagesSection` (Settings → Packages) + `InstalledPackagesList` (Pi Resources). Props: `displayName`, `source`, `sourceType` (`npm`/`git`/`local`/`global`), `isBundled`, `currentVersion`, `latestVersion`, `updateAvailable`, `busy`, `progress`, `error`, `canUpdate`, `canUninstall`, `onUpdate`, `onUninstall`, `onViewReadme`, `onMove`, `currentScope`, `moveDisabledReason`, `testId`. Renders color-coded source-type badge, amber `[bundled]` badge when `isBundled`, version pill, `current → latest` with Update affordance when `updateAvailable`, kebab `[⋯]` menu for Uninstall / View README / Move. See change: consolidate-packages-settings-ui. |
| `src/client/components/UnifiedPackagesSection.tsx` | Settings → Pi Ecosystem unified packages section. Three sub-groups in priority order: **Core** (whitelist from `pi-core-checker.ts#CORE_PACKAGE_NAMES`, Update only), **Recommended Extensions** (rows where `isRecommended` true on `/api/packages/installed`), **Other Packages** (everything else). Each row in exactly one group; Core wins over Recommended wins over Other. Header: "Pi Ecosystem" title + `Last checked` timestamp + `Check Now` button. Core sub-group keeps update flow via `/api/pi-core/update` (see also openspec change `unify-pi-core-into-package-queue` proposing route through `packageQueue`). Composes `PackageRow` for every row — single visual language across all three groups. See change: consolidate-packages-settings-ui. |
| `src/client/hooks/usePackageSearch.ts` | Debounced fetch hook for `/api/packages/search` |
| `src/client/hooks/useInstalledPackages.ts` | Fetch hook for `/api/packages/installed` |
| `src/client/hooks/useRecommendedExtensions.ts` | Fetch hook for `/api/packages/recommended`; auto-refreshes on `package_operation_complete` |
| `src/client/components/RecommendedExtensions.tsx` | Curated-recommended-extensions card grid, rendered above search in Packages tab |
| `src/client/components/MissingRequiredBanner.tsx` | Top-of-page banner when any `required` recommended extension missing from `~/.pi/agent/settings.json` `packages[]` |
| `src/client/hooks/usePackageOperations.ts` | Thin React subscriber over singleton `packageQueue`. Public API (`operation`, `install/remove/update`, `clearOperation`) preserved for backwards-compat with `PackageBrowser`, `RecommendedExtensions`, `MissingRequiredBanner`, `PiResourcesView`, `SettingsPanel`. Adds `statusFor(source)` (`idle\|queued\|running\|success\|error`), `messageFor(source)`, `queueDepth` for richer per-row state. Single `pi-package-event` window listener now lives inside `package-queue`, not the hook. See change: package-install-queue. |
| `src/client/lib/package-queue.ts` | Module-level singleton FIFO queue scheduling pi package install/remove/update across entire client. Owns running op (≤ 1) + queue keyed by `source`, per-source status map, *one* `pi-package-event` window listener advancing queue on `package_operation_complete`. Dedup on duplicate enqueue, retry-once on HTTP 409 `PackageOperationBusyError` (500 ms backoff), success auto-clear after 3 s, sticky error until next enqueue. **WS-vs-HTTP race fix**: `matchesRunning(opId, source)` falls back to `source` match when `running.operationId === null` (HTTP response not yet parsed) so completions/progress arriving before fetch resolves — common for fast local-path installs — aren't silently dropped; once `operationId` set it takes priority. Exports `__resetForTests()`. See changes: package-install-queue, fix-local-path-install-spinner. |
| `src/client/hooks/usePiResources.ts` | Fetch + 30s polling hook for pi resources API |
| `src/client/components/MarkdownPreviewView.tsx` | Generic reusable markdown preview with back button, tabs, loading/error states |
| `src/client/components/MarkdownContent.tsx` | Shared markdown renderer for chat/previews; supports GFM, raw HTML, syntax highlighting, Mermaid, table copy buttons; strips raw HTML `ref` attributes so pasted/generated JSX-like markup cannot trip React ref validation. |
| `src/client/hooks/useOpenSpecReader.ts` | Maps OpenSpec artifacts → file paths, fetches content, concatenates specs |
| `src/client/components/interactive-renderers/` | Registry + renderers for interactive UI dialogs (confirm, select, multiselect, input, editor, notify) |
| `src/client/components/TerminalView.tsx` | xterm.js terminal emulator wrapper with keep-alive |
| `src/client/components/TerminalsView.tsx` | Tabbed terminal container per folder (tab bar, keep-alive, rename) |
| `src/client/components/EditorView.tsx` | code-server iframe embedding with lazy start + heartbeat |
| `src/client/components/EditorInstallGuide.tsx` | Platform-specific code-server installation guide |
| `src/client/components/FolderActionBar.tsx` | Unified action bar per folder: +Session, +Terminal, Terminals(N), Editor, Zed, Pi Resources |
| `src/client/lib/folder-encoding.ts` | Base64url encode/decode for folder paths in URL routes |
| `src/client/components/TerminalCard.tsx` | Sidebar card for terminal sessions (cyan accent) |
| `src/client/App.tsx` | React app with WebSocket. Owns per-session ephemeral input state alongside `sessionStates`: `drafts: Map<sid, string>` (persisted to `localStorage`, see `chat-input-draft-and-history`) + `pendingImagesMap: Map<sid, ImageContent[]>` (in-memory only, not persisted — change: lift-pending-images-to-app). Both passed to `<CommandInput>` as controlled props (`draft`/`onDraftChange`, `images`/`onImagesChange`) so they survive content-area route changes that unmount chat panel + don't leak across session switches. `wrappedHandleSend` clears both via `clearDraftForSession` + `clearImagesForSession` after send. Module-level `EMPTY_IMAGES = Object.freeze([])` gives stable empty-array ref; sessions with no pending images don't trigger child re-renders. **Desktop back-arrow** (change: fix-desktop-back-navigation): session-header back on desktop dispatches via `useDesktopBack({...})` wrapping pure `selectDesktopBackTarget` — pops content-area overlays in priority order before falling back to `navigate("/")`; cold-load `/session/:id` back no longer hits silent-no-op `window.history.back()`. `useContentViews` + `useOpenSpecActions` accept `navigate`/`settingsMatch`/`tunnelSetupMatch` so sidebar-triggered overlays auto-close `/settings`/`/tunnel-setup` before opening (fixes hidden-overlay-masks-Settings bug). **Reconnect effect** (change: fix-stale-sessions-on-reconnect): on transition to `connected`, no longer calls `setSessionOrderMap(new Map())` — server `sessions_snapshot` replaces it atomically. `subscribedRef.current.clear()` + `setTerminals(new Map())` preserved. Populates SlotRegistry from generated PLUGIN_REGISTRY at module load (see change: wire-plugin-registry-into-shell). |
| `packages/client/src/generated/plugin-registry.tsx` | Committed stub. Overwritten by viteDashboardPluginsPlugin on dev start + build. Exports PLUGIN_REGISTRY: RegistryEntry[]. Empty array on fresh checkout. |
| `src/client/components/MobileShell.tsx` | Two-panel mobile shell with slide transitions + swipe-back |
| `src/client/components/MobileActionMenu.tsx` | Kebab menu for session actions on mobile (includes OpenSpec commands) |
| `src/client/components/MobileOverlay.tsx` | Hamburger button + sidebar overlay for mobile |
| `src/client/components/SessionHeader.tsx` | Session header: OpenSpec attach/detach, flow launcher, MobileAttachButton. **`onResume?: (mode: "continue" | "fork") => void`** prop (change: resume-button-in-session-header): when viewed session has `status === "ended"` AND non-empty `sessionFile` AND non-null `onResume`, desktop toolbar replaces dimmed elapsed-duration span with green Resume pill (`mdiPlayCircleOutline`) + blue Fork pill (`mdiSourceFork`) mirroring sidebar `SessionCard.tsx:544-563`. Both carry `data-testid="header-{resume,fork}-button"`, `disabled={!!session.resuming}`, invoke `onResume("continue")` / `onResume("fork")`. `App.tsx` wires prop to `(mode) => handleResumeSession(selectedId, mode)` — reuses existing `resume_session` WS protocol, no server-side changes. Mobile unchanged: `MobileActionMenu` kebab owns Resume on mobile; desktop pills gated `isMobile === false`. **Mobile** also renders read-only `mobile-header-attached-chip` (paperclip + `session.attachedProposal`) between title + `MobileAttachButton` so attach state visible without opening popover. See change: fix-mobile-attach-proposal-display. **Two-row mobile layout** (change: fix-mobile-header-and-orientation): when `session.attachedProposal` non-empty, `MobileHeader` becomes `flex-col`: row 1 = back+title+attach+kebab, row 2 = chip span (no `max-w-[55%]` since not competing with title). When null/empty: single-row container as before, no empty row 2. **Attached-proposal artifact summary** (change: add-attached-proposal-header-summary): both desktop + `MobileHeader` look up `session.attachedProposal` in `openspecChanges`; when found, render `ArtifactLettersButton` (P/D/T/S, single button → opens proposal via `onReadArtifact` from `App.tsx → useContentViews`) + `(completedTasks/totalTasks)` counter (`data-testid="attached-proposal-task-counter"`, only when `totalTasks > 0`). Gated on explicit `attachedProposal` only — auto-detected `openspecChange` does not trigger. |
| `src/client/components/SessionList.tsx` | Sidebar session list. Auto-scrolls selected card into view via `data-session-id` querySelector + `scrollIntoView({block:"nearest", behavior:"auto"})`. Triggers: background re-sort of unchanged selection (status/hidden/cwd/order-index change in `sessionOrderMap`) AND one-shot first-mount deep-link. Suppresses scroll on subsequent `selectedId` changes (user click). Uses `prevSelectedRef` + `firstMountRef` + memoized `scrollFingerprint` from `lib/session-list-scroll.ts`. See change: auto-scroll-selected-session-card. |
| `src/client/hooks/useMobile.tsx` | `MobileProvider` + `useMobile()` hook. Predicate is **width OR height** — wrapped `useMediaQuery("(max-width: 767px), (max-height: 599px)")` flips to mobile whenever viewport is < 768px wide OR < 600px tall. Height arm catches landscape phones (iPhone 14 in landscape is 844×390, Pixel 8 landscape 915×412) so they get single-panel mobile layout instead of cramped desktop two-panel one. Documented side effect: shrinking desktop window to <600px tall also enters mobile mode (pinned by regression test in `useMobile.test.tsx`). See change: fix-mobile-header-and-orientation. |
| `src/client/hooks/useSwipeBack.ts` | iOS-style left-edge swipe-back gesture (40px edge zone, document-level listeners) |
| `src/client/components/ChatView.tsx` | Chat message view with scroll-lock: pauses auto-scroll when user scrolls up, floating scroll-to-bottom button, per-session scroll position persistence. Race-safe across multi-batch `event_replay`: `markProgrammatic()` helper raises `programmaticScroll` for ~150 ms around every self-initiated `scrollTo`, `handleScroll` early-returns while flag set so spurious onScroll that lags `scrollTo` (and would otherwise misread now-grown `scrollHeight` as "user scrolled away") cannot flip `isNearBottom` during replay. See change: fix-chat-scroll-race-during-replay. **User-message branch routes to `<SkillInvocationCard>` when `msg.skill` is set** (raw `<MessageBubble>` for plain users); container preserves `mt-4 mb-4 flex justify-end` + `bubbleMax`. See change: render-skill-invocations-collapsibly. |
| `src/client/components/CommandInput.tsx` | Chat textarea + autocomplete + controlled draft / history / images. Props: `sessionId`, `draft`, `onDraftChange` (draft lifted to App.tsx so it survives navigation), `history` (newest-first user prompts for this session), `images`, `onImagesChange` (pending pasted images lifted to App.tsx as `pendingImagesMap` so they survive route takeover + don't leak across sessions). `ArrowUp`/`ArrowDown` walk history bash-style (only when no dropdown open AND caret on first/last line); `Escape` during history mode restores in-progress draft. See changes: chat-input-draft-and-history, lift-pending-images-to-app |
| `src/client/lib/draft-storage.ts` | Per-session chat input draft persistence helpers: `readAllDrafts()`, `writeDraft(sid, text)`, `deleteDraft(sid)`, key prefix `chat-draft:`. Wraps `localStorage` in try/catch (private-mode / quota safe). |
| `src/client/lib/message-history.ts` | `extractUserPromptHistory(messages)` — pure filter+dedup over `ChatMessage[]` returning newest-first user prompts with consecutive duplicates collapsed. Drives `ArrowUp`/`ArrowDown` history recall in `CommandInput`. **For user msgs whose content matches the `<skill>` envelope (via `msg.skill?.condensed` first, then ad-hoc `parseSkillBlock` fallback for older state), substitutes the slash form `/skill:name args` so ↑ recalls the user-typed command, not the multi-thousand-character expanded body.** See change: render-skill-invocations-collapsibly. |
| `src/client/lib/mobile-depth.ts` | Pure function computing MobileShell depth from route state |
| `src/client/lib/desktop-back.ts` | Pure helper `selectDesktopBackTarget(state) → BackTarget` for desktop session-header back button. Mirrors priority chain mobile's inline `onBack` switch uses (archiveBrowserCwd → specsBrowserCwd → flowYamlPreview → diffViewSessionId → piResourceFilePreview → readmePreview → piResourcesState → previewState → navigate("/")). Returns discriminated union `{kind:"clear", target}` or `{kind:"navigate", to:"/"}`. Pinned by 256-combination parity test against mobile inline switch. See change: fix-desktop-back-navigation. |
| `src/client/hooks/useDesktopBack.ts` | Hook wrapping `selectDesktopBackTarget` with live overlay setters + `navigate`. Returns memoised `goBack()` callback used by `App.tsx`'s desktop session-header. Replaces pre-fix `window.history.back()` which was silent no-op on cold loads. See change: fix-desktop-back-navigation. |
| `src/client/hooks/useZoomPan.ts` | Reusable zoom/pan hook (wheel, drag, pinch, buttons) |
| `src/client/hooks/useMessageHandler.ts` | WebSocket message dispatch hook (extracted from App.tsx). Handles `spawn_register_timeout` + `spawn_register_recovered`. `spawnErrors` state now `Map<string, SpawnErrorDetail>`. See change: spawn-failure-diagnostics. **Replay reset trigger** (change: fix-replay-duplicates-tool-and-flushed-rows): `event_replay` arm resets `SessionState` to `createInitialState()` whenever `firstSeq === 1` OR `firstSeq <= maxSeqMapRef.current.get(sessionId)` (i.e. server replaying events client has already accounted for, regardless of whether batch starts at seq=1 or somewhere later). Second clause catches paginated / lazy / multi-batch reconnect re-replay where first batch may not start at `seq=1`; without it, every reconnect cycle appended fresh full replay onto existing state + produced 14–16× duplicate tool/assistant rows over 5-day session. On reset, `maxSeqMapRef` for that session also cleared to 0 so next batch isn't misclassified, then re-stamped from just-applied events. **`sessions_snapshot` arm** (change: fix-stale-sessions-on-reconnect): REPLACES `sessions` Map + `sessionOrderMap` Map with payload contents (no merge). Drops stale ids from previous server lifetime so running session never lingers below "Show N ended" sidebar divider after reconnect. **`session_state_reset` arm + `event_replay` `shouldReset` branch** (change: preserve-pending-prompt-across-replay): both carry `pendingPrompt` across `createInitialState()` swap so optimistic UI bubble survives auto-resume bridge re-register; only reducer `message_start` / `agent_start`, 30s safety timeout, or explicit cancel clear it. See change: simplify-model-selection-channels — `case 'models_refreshed'` reduced to no-op; previous global `modelsMap` wipe removed. Kept for protocol-compat with older bridges. |
| `src/client/hooks/useSessionActions.ts` | Session action callbacks hook (send, abort, resume, spawn, etc.) |
| `src/client/hooks/useOpenSpecActions.ts` | OpenSpec action callbacks hook (refresh, archive, attach, detach); calls `clearAllContentViews` before opening preview |
| `src/client/hooks/useContentViews.ts` | Content view state + fetch (pi resources, readme, file preview); `clearAll()` resets all hook-owned states; `onBeforeOpen` callback for cross-component clearing |
| `src/client/lib/event-reducer.ts` | Event-sourced state reducer (delegates flow events to flow-reducer); extracts LLM errors from `agent_end` into `lastError`. **`tool_execution_start` idempotent on `toolCallId`** (change: fix-replay-duplicates-tool-and-flushed-rows): before pushing `tool-${toolCallId}`, runs `findLastIndex((m) => m.role === "toolResult" && m.toolCallId === toolCallId)` + updates row in place (refreshes `toolName`/`args`; preserves `toolStatus`/`result`/`images`/`duration`/`toolDetails`/`startedAt`/`timestamp`). `running`-only gate dropped — `id: tool-${toolCallId}` is React key, fall-through-push always collides. Pairs with idempotency in `flushStreamingTextAsAssistantRow` (id now `flush-${toolCallId}`, not `msg-${state.messages.length}`; skip-guard returns `state` unchanged if row exists, still flips `streamingTextFlushed = true` so later `message_update` events stop repopulating `streamingText`). Signature gained 3rd `toolCallId: string` param; single caller in `tool_execution_start` extracts before flush. **Streaming-text flush at tool start** (change: fix-streaming-text-vs-interactive-ui-order): on `tool_execution_start`, if `streamingText` non-empty AND `streamingTextFlushed === false`, helper `flushStreamingTextAsAssistantRow(state, timestamp)` pushes permanent `role:"assistant"` row with current `streamingText` BEFORE new `toolResult`, clears `streamingText`, sets `streamingTextFlushed = true`. Collapses bad-render window (long-running tools — `npm test`, `ask_user`, subagent — otherwise show card above their describing prose). Flushed row: `entryId/nonce: undefined`; on `message_end` reducer locates via `findFlushedAssistantRowIndex(messages)` (tail-backwards scan; **hard upper bound `TURN_BOUNDARY_ROLES`** prevents R3 cross-message pollution from orphan rows whose `message_end` dropped) + stamps `data.entryId`/`data.nonce` in place — no duplicate row. Flag reset on every assistant `message_start` AND `message_end` (R7 defense-in-depth: lifecycle = between `message_start` and `message_end`). `message_update` skips `streamingText = text` when flag set, so flushed prefix doesn't reappear in streaming bubble below `messages[]`. Tradeoff: `[text, toolCall, text]` messages — second text block doesn't stream live (lands at `message_end` only). **Assistant content-array reorder** (changes: fix-text-tool-render-order, fix-interactive-ui-reorder): on every assistant `message_end`, helper `reorderToolCardsForAssistantMessage(messages, content)` reorders trailing rows so text/thinking/toolCall blocks match model's `content[]` order. **Turn-boundary anchored window**: walks `messages[]` backwards from tail collecting non-boundary rows (`TURN_BOUNDARY_ROLES = {user, turnSeparator, commandFeedback, rawEvent}`). Window = `[boundaryIdx + 1 ..]` — prior-turn rows can't leak in. **Pairing rule** (fix-interactive-ui-reorder): for each `toolCall`, claims matching `toolResult` + any `interactiveUi` with same `toolCallId`, emits as `[toolResult, interactiveUi]` so `ask_user` + PromptBus tools render below their assistant intro text. `findLastUnclaimed` (most-recent first) handles back-to-back assistants without user response between them. **Hybrid unclaimed-row handling**: reorderable roles (`assistant`, `toolResult`, `thinking`) keep **original suffix index** (protects bled-in prior-message rows when no boundary between back-to-back assistants); trailing roles (`interactiveUi`, `bashOutput`) emitted AFTER claimed rows so free-floating ui dialogs (no `toolCallId`) sit below their tool card. Preserves React keys (`tool-${id}` / `ui-${id}`). Replay path inherits via same reducer. `addInteractiveRequest(state, requestId, method, params, toolCallId?)` accepts optional 5th param so `interactiveUi` ChatMessage carries `toolCallId`. **`ChatMessage.skill?: SkillBlock` stamp** (change: render-skill-invocations-collapsibly): on user `message_start`, runs `parseSkillBlock(text)` on the extracted content; on match, attaches `{name, location, body, args, condensed}` for downstream renderer + history-recall use. Raw `content` preserved unchanged so existing copy semantics still work. |
| `src/client/hooks/usePendingPromptTimeout.ts` | 30-second safety timeout for stuck `pendingPrompt` spinners |
| `src/client/lib/flow-reducer.ts` | **Moved to `packages/flows-plugin/src/flow-reducer.ts`** by change `extract-flows-as-plugin`. Flow state machine: all flow_* event handling. `event-reducer.ts` now imports `isFlowEvent`/`reduceFlowEvent` from `@blackbelt-technology/pi-dashboard-flows-plugin/reducer`. Symbol-name + contract unchanged. |
| `src/client/lib/session-grouping.ts` | Pure functions: group, sort, filter sessions by directory |
| `src/client/lib/truncate-path.ts` | Middle-truncation utility for filesystem paths |
| `src/client/lib/session-list-scroll.ts` | Pure helper `selectedCardScrollFingerprint(selectedId, sessions, sessionOrderMap): string | null`. Returns stable string fingerprint of position-affecting state (`status`/`hidden`/`cwd`/`sessionOrderMap` index) of currently selected session card. Returns null when selection missing or not in sessions. Test seam for `SessionList.tsx` auto-scroll effect. See change: auto-scroll-selected-session-card. |
| `src/client/lib/session-card-time.ts` | Pure helper `selectBadgeTimestamp(session)` for session-card relative-time badge. Precedence: `status === "ended"` → `endedAt ?? lastActivityAt ?? startedAt`; otherwise `lastActivityAt ?? startedAt`. Consumed by `SessionCard.tsx` at two badge sites; badge `<span>` also carries `title="Started <localized timestamp>"` tooltip so original spawn time stays discoverable. See change: session-card-last-activity-badge. |
| `src/client/lib/selectViewedSessionId.ts` | Pure helper `selectViewedSessionId(match, params)` returning `:id` segment of `/session/:id` route or `null`. Decoupled from `wouter` so rule unit-testable. Used by `App.tsx` to drive `useViewDispatcher`. See change: session-card-unread-stripes. |
| `src/client/hooks/useViewDispatcher.ts` | React hook wired into `App.tsx` that watches result of `selectViewedSessionId(...)` plus `useWebSocket` connection status, dispatches `session_view` / `session_unview` messages to server: (1) on every change of viewed session id (unview previous, view current); (2) on every transition INTO `connected` (re-send `session_view` for current id so server-side state re-syncs after reconnect). Network-layer drop-on-not-OPEN handled by `useWebSocket.send`; reconnect re-send rule guarantees recovery. See change: session-card-unread-stripes. |
| `src/client/components/ElapsedBadge.tsx` | Reusable elapsed time badge: static duration or live ticking counter |
| `src/client/components/SearchableSelectDialog.tsx` | Shared searchable select dialog (keyboard nav, filtering, badges) |
| `src/client/components/FileDiffView.tsx` | Split-pane container: file tree + diff panel, content-area view |
| `src/client/components/DiffFileTree.tsx` | Two-level file tree with change events, timestamps, context messages |
| `src/client/components/tool-renderers/EditToolRenderer.tsx` | Renders Edit tool cards in chat. **Desktop/mobile branch** (change: rich-diff-in-chat): calls `useMobile()` — on desktop (`!isMobile`) renders `<RichDiff oldText newText filePath maxHeight="20rem" />` for both `oldText`/`newText` case + each entry in `edits[]`; on mobile renders existing homegrown `DiffView` (CSS-class-colored unified patch via `createTwoFilesPatch`). Raw-JSON `<pre>` fallback + `OpenFileButton`/file-path header/result text unchanged + viewport-independent. Local `DiffView` component kept as mobile fallback — not deleted. Lazy-mount inherited from `ToolCallStep`'s `{expanded && <Renderer />}` gate (Edit cards default collapsed). |
| `src/client/components/RichDiff.tsx` | Shared rich-diff rendering primitive consumed by `EditToolRenderer` (desktop branch) + `DiffPanel` (Path A). Accepts `{ oldText, newText, filePath, mode? = "unified", maxHeight? }`. Encapsulates `EXT_LANG_MAP`, exports `getLang(filePath)` (also used by `DiffPanel`'s Path B), `generateDiffFile` + init/build calls, `<DiffView>` from `@git-diff-view/react` with internal defaults (`diffViewTheme` from `useThemeContext().resolved`, `diffViewHighlight`, `diffViewWrap`, `registerHighlighter`). Pure content primitive — no toolbar, no toggle, no header. `maxHeight` prop optionally caps height with `overflow-auto`. `data-testid="rich-diff"` on outermost element. See change: rich-diff-in-chat. |
| `src/client/components/DiffPanel.tsx` | Rich diff via @git-diff-view/react with syntax highlighting. **File view**: routes through `getSyntaxTheme(theme, themeName)` (via `useThemeContext`) instead of raw `oneDark` import, so file viewer respects active theme + inherits token-background strip. **Diff Path A (change-derived)**: delegates to `<RichDiff>` via `buildChangeDiffTexts` returning `{ oldText, newText }` — single-file rendering shared with chat surface; passes `mode` from split↔unified toggle but not `maxHeight` (parent flex sizing). **Diff Path B (git aggregate)**: inline `<DiffView data={...}>` kept because `<RichDiff>` only accepts `(oldText, newText, filePath)`. `EXT_LANG_MAP` moved to `RichDiff.tsx`; `getLang` imported from there for Path B. `EXT_PRISM_MAP` / `getPrismLang` remain for `viewMode === "file"`. Pinned by `packages/client/src/components/__tests__/DiffPanelTheme.test.tsx`. See changes: strip-token-backgrounds-in-code-blocks, rich-diff-in-chat. |
| `packages/client/src/lib/syntax-theme.ts` | Single source of truth for prism syntax styles in client. `getSyntaxTheme(resolved, themeName)` resolves prism palette for active app theme + runs every result through pure helper `stripTokenBackgrounds(style)` which removes `background` / `backgroundColor` from every selector containing `.token` (including `.token.deleted` / `.token.inserted` diff washes inside fenced ```diff blocks). Wrapper selectors (`pre[class*="language-"]`, `code[class*="language-"]`) left untouched so panel background stays overridable to `var(--bg-code)`. Fallback path (unknown theme) also runs through strip. Pinned by `packages/client/src/lib/__tests__/syntax-theme.test.ts` (13 assertions across base/dracula/nord/github/catppuccin × dark/light). See change: strip-token-backgrounds-in-code-blocks. |
| `src/client/hooks/useSessionDiff.ts` | Fetch hook for `/api/session-diff` endpoint |
| `src/client/lib/diff-tree.ts` | Directory tree builder from flat file paths |
| `packages/client/src/components/MarkdownContent.tsx` | ReactMarkdown-based renderer for chat bodies, thinking blocks, flow agent detail, package READMEs, markdown previews. **External-link hardening (change: harden-external-link-handling, #13)**: exports pure `isExternalHref(href)` + overrides `a` component so external URLs render with `target="_blank" rel="noopener noreferrer"` while fragment-only + same-origin hrefs stay in-document. |
| `packages/client/src/__tests__/no-bare-external-anchor.test.ts` | Repo-level lint: scans client `.tsx` files for `<a href="http(s)://...">` tags missing `target="_blank"`. Per-line opt-out via `// ban:bare-anchor-ok`. See change: harden-external-link-handling. |
| `packages/client/src/hooks/__tests__/useMessageHandler.snapshot-replace.test.tsx` | Pins REPLACE-not-merge semantics for `sessions_snapshot` arm on both `sessions` Map + `sessionOrderMap`: stale id dropped, ended overwrites active, empty snapshot drops everything. See change: fix-stale-sessions-on-reconnect. |
