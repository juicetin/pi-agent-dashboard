# File Index — Bridge extension

> Part of [pi-agent-dashboard file index](./file-index.md). Loaded on demand.
>
> **Change-history annotations** (e.g. *"See change: foo-bar"*) → OpenSpec changes archived under `openspec/changes/archive/`.
>
> **Update protocol**: see `AGENTS.md` → "Documentation Update Protocol".

| File | Purpose |
|------|---------|
| `packages/anti-slop/.pi/skills/anti-slop-frontend/SKILL.md` | Skill `anti-slop-frontend`. Mechanical/countable anti-slop checklist; every rule binary or counted, not taste. Three dials VARIANCE/MOTION/DENSITY set up front. Part A universal tells (fire every surface incl dashboards): AI-purple/glow ban, Inter-as-default discouraged, em-dash ban, fake-data ban (Jane Doe/Acme/99.99%), div-based fake screenshots, hand-rolled SVG icons, happy-path-only states, motion-must-be-motivated. Part B marketing-only (skip product UI: dashboards/tables/wizards/editors): hero discipline, eyebrow ≤ ceil(sections/3), equal-3-card ban, zigzag cap, bento rhythm, decoration/locale/scroll-cue strips, duplicate-CTA-intent. Advisory only; scores never blocks. Every rule has override path when brief asks for banned thing. Pairs with frontend-mockup-loop; loop a11y floor + cite-a-source win; feeds failing items into loop FIX step; never overrides WCAG/severity-4 gates or cited rules. Adapted from Leonxlnx/taste-skill (design-taste-frontend, MIT): countable rules distilled, stack-coupling removed, scoped universal vs marketing. |
| `packages/anti-slop/README.md` | Install via `pi install npm:@blackbelt-technology/anti-slop-frontend` (or `pi -e npm:...`). Registers skill `anti-slop-frontend`, no tools/commands. Part A/B scope table (universal vs marketing-only). Loop-relationship table vs frontend-mockup-loop: loop owns hard gates (WCAG-AA, severity-4), this skill advisory, feeds FIX step. Override path per rule. Attribution Leonxlnx/taste-skill MIT. |
| `packages/anti-slop/package.json` | Skill-only manifest. name `@blackbelt-technology/anti-slop-frontend`, v0.1.0. No extension, no tools, no deps. Declares `pi.skills: [".pi/skills/anti-slop-frontend"]`. `files: [".pi/skills/","README.md"]`. license MIT, publishConfig access public. |
| `packages/extension/.pi/skills/browser/LICENSE` | Apache-2.0 license text reproduced verbatim from upstream agent-browser repo (https://github.com/vercel-labs/agent-browser) for attribution. See change: ship-browser-skill-and-electron-cdp. |
| `packages/extension/.pi/skills/browser/references/electron.md` | Vendored agent-browser electron skill (same CLI/tag). Appended `## Worked example: Pi Dashboard` section: `open -a 'PI Dashboard' --args --debug-cdp`, `agent-browser connect 9222`, `tab` listing main/wizard/doctor windows, `screenshot pi-dashboard.png`. See change: ship-browser-skill-and-electron-cdp. |
| `packages/extension/.pi/skills/browser/references/web.md` | Vendored agent-browser core skill (CLI 0.27.0, upstream tag v0.27.0 SHA c830d1b67dc18b754e305859f0ae587f858a1447). Frontmatter stripped. Pi Dashboard addenda appended: detect-dashboard.sh invocation, dashboard recipes, responsive-testing presets. See change: ship-browser-skill-and-electron-cdp. |
| `packages/extension/.pi/skills/browser/scripts/detect-dashboard.sh` | Probes $HOME/.pi/dashboard/config.json for port, curls /api/health, prints DASHBOARD_URL/MODE/VITE_URL key=value. Used by web.md Pi Dashboard addenda. Migrated from `.pi/skills/browser-visual-debug/scripts/`. See change: ship-browser-skill-and-electron-cdp. |
| `packages/extension/.pi/skills/browser/SKILL.md` | Universal browser skill orchestrator. Step-0a preflight `command -v agent-browser`. Step-0b auto-detect: probes :9222 + `pgrep -f 'Pi Dashboard|pi-dashboard'`, routes to references/electron.md when Pi Dashboard running, else references/web.md. Intent overrides capability. See change: ship-browser-skill-and-electron-cdp. |
| `packages/extension/.pi/skills/browser/UPSTREAM.md` | Provenance record for vendored content: repo URL, upstream tag, commit SHA, CLI version, refresh date. Includes refresh procedure (run `agent-browser skills get core --full` + `... electron`, diff against vendored, replace bodies, preserve Pi Dashboard addenda). See change: ship-browser-skill-and-electron-cdp. |
| `packages/extension/.pi/skills/pi-dashboard/commands/` | Shipped `/dashboard:*` slash-command templates (33 `dashboard-*.md`: 13 LLM-free `executable: bash`, 20 LLM-bound) + README. Declared in package.json `pi.skills`+`files`; baked into npm tarball + Docker image. Resolved via `addSkillCommandsFromRegistry`. See change: add-dashboard-slash-commands. |
| `packages/extension/src/__tests__/bridge-abort-orderer.test.ts` | Verifies wrapper-abort does NOT clear `usageLimitOrderer.pending`; `retryTracker.noteAbort` still clears; orderer synths on subsequent `agent_end`. See change: unify-status-banner-and-terminal-limit-stop. |
| `packages/extension/src/__tests__/bridge-followup-idle-guard.test.ts` | Verifies `rewriteFollowupQueue` early-returns on `isAgentStreaming === false`; emits `command_feedback`; no `pi.sendUserMessage` refire. See change: unify-status-banner-and-terminal-limit-stop. |
| `packages/extension/src/__tests__/bridge-retry-ordering.test.ts` | Bridge wire-ordering invariant test. `BridgeSim` mirrors `message_end`/`agent_end` synthesizer pipeline; asserts `auto_retry_start` ships before `agent_end` on back-to-back retryable errors. See change: fix-retry-banner-stuck-on-limit-exceeded. |
| `packages/extension/src/__tests__/bridge-terminal-limit-abort.test.ts` | Verifies bridge auto-aborts on `message_end` `USAGE_LIMIT_PATTERN` match; first-attempt USAGE_LIMIT branch on `agent_end` outside retry chain; mutual-exclusion with `orderer.maybeSynthesize`. See change: unify-status-banner-and-terminal-limit-stop. |
| `packages/extension/src/__tests__/browser-skill-registered.test.ts` | Repo-lint: asserts SKILL.md/UPSTREAM.md/LICENSE/references/{web,electron}.md/scripts/detect-dashboard.sh exist, frontmatter declares `name: browser` + allowed-tools Bash patterns, pi.skills[] and files[] both list `.pi/skills/browser`, no agent-browser/pi-agent-browser runtime deps. See change: ship-browser-skill-and-electron-cdp. |
| `packages/extension/src/__tests__/command-handler.test.ts` | Covers `tryExecSlashTemplate`: exec template → `bash_output` `source:"slash-exec"`, no LLM; `excludeFromContext:false` → also sendUserMessage; positional args after `--`; `PI_DASHBOARD_PORT` injected; non-exec → false. See change: add-dashboard-slash-commands. |
| `packages/extension/src/__tests__/dashboard-commands-shipped.test.ts` | Asserts `/dashboard:*` templates ship in the extension-bundled skill (≥30 dashboard-*.md, ≥13 executable:bash, package.json declares + ships skill, README + slash-commands.md present). See change: add-dashboard-slash-commands. |
| `packages/extension/src/__tests__/dashboard-context-injector.test.ts` | NEW. Unit tests for `buildContextFragment`/`spliceContextFragment`/`sanitizeChangeName`/`registerDashboardContextInjector` + pi anchor probe. See change: inject-session-context-into-agent. |
| `packages/extension/src/__tests__/fixtures/usage-limit-error-strings.ts` | `USAGE_LIMIT_FIXTURES` + `NON_USAGE_LIMIT_FIXTURES` — real production-log error strings (Gemini monthly cap, Codex `usage_limit_reached`, OpenAI `insufficient_quota`, Anthropic credit balance, Copilot daily limit) for `USAGE_LIMIT_PATTERN` coverage tests. See change: fix-retry-banner-stuck-on-limit-exceeded. |
| `packages/extension/src/__tests__/git-poll.test.ts` | Unit tests for `runGitPollTick`. Regression guard: name/model polling fires after session change even when cwd absent. See change: fix-stale-ctx-cwd-crash. |
| `packages/extension/src/__tests__/prompt-expander.test.ts` | Covers `loadPromptTemplate` frontmatter parse, exec vs llm classification, unsupported executable degrade, malformed-frontmatter fallback, `.pi/skills/<skill>/commands/*.md` resolution. See change: add-dashboard-slash-commands. |
| `packages/extension/src/abort-latch.ts` | Pure class `AbortLatch`. Keeps user abort latched so provider backoff (5–60s) outliving 2s persistent-abort scheduler still stops pi retry. `request(sessionId)` latches on abort; `shouldAbort(sessionId)` true while latched (call on resumption); `clear(sessionId)` on settle or new prompt; `isActive(sessionId)` probe. Abort-on-sight scoped to aborted turn. Bridge re-calls `cachedCtx.abort()` on resumed agent_start / assistant message_start while latched. Clears on new user prompt (command-handler `noteUserPrompt` pre-send + bridge user message_start) OR terminal agent_end. "No intervening user prompt" = discriminator; needs no retry signal from pi. See change: unify-error-retry-lifecycle. |
| `packages/extension/src/artifact-roots.ts` | Artifact-root allowlist for Fix B bridge image inlining. `resolveArtifactRoots({homedir,env,realpathSync})` → realpath'd roots: `~/.agent-browser/tmp` + `AGENT_BROWSER_SCREENSHOT_DIR`. `isUnderArtifactRoot(absPath,roots,realpathSync)` → realpath containment check, rejects symlink escapes + missing files. Pure; fs/env injected. Gates inlining so tools cannot disclose arbitrary local images by echoing paths. See change: inline-agent-screenshot-artifacts. |
| `packages/extension/src/bridge-default-model-gate.ts` | Pure predicate `shouldApplyDefaultModel({reason, entryCount, hasModelRegistry, hasDefaultModel})`. Bridge applies `config.defaultModel` only when `reason==="startup"` AND `entryCount===0`. Resume/fork/reload keep existing model. Mirrors pi `!hasExistingSession` gate. See change: fix-resume-keeps-session-model. |
| `packages/extension/src/git-poll.ts` | Exports `runGitPollTick(deps)` + `GitPollDeps` interface. Pure git + name/model poll-tick body. Shared by bridge.ts session-start timer + session-change timer so they cannot drift. Inactive tick → no-op. cwd present → `sendGitInfoIfChanged` + `sendCwdMissingIfChanged`. cwd absent → skip git/cwd, still run `sendSessionNameIfChanged` + `sendModelUpdateIfChanged` every active tick. Dependency-injected `cachedCwd` thunk so each tick reads current cached cwd after session swap. See change: fix-stale-ctx-cwd-crash. `GitPollDeps.sendPiVersionIfChanged`; `runGitPollTick` invokes it each tick. See change: restore-pi-version-skew-surface. |
| `packages/extension/src/markdown-image-inliner.ts` | Bridge helper rewriting assistant `![alt](path)` → `![alt](pi-asset:<hash>)` (SHA-256/16, MIME allowlist, 5 MB/img + 20 MB/msg caps). Adds `inlineLocalImagePath(absPath, opts)` single-path inliner returning `AssetToEmit` \| `ReadFileError` (kinds extended with `NOT_IMAGE` / `TOO_LARGE`). See change: inline-agent-screenshot-artifacts. |
| `packages/extension/src/prompt-queue.ts` | Bridge-owned mid-turn prompt queue. `PromptQueue` class: `enqueue(text)`, `drain(sink, onAfterStep?)`, `clear()`, `snapshot()`. Id format `bq_<sessionId>_<counter>`. Drains via `pi.sendUserMessage` on `agent_end` in FIFO order. Snapshot pushed to server as `queue_state` after each mutation. Queue lost on bridge restart. See change: surface-mid-turn-prompt-queue. |
| `packages/extension/src/retry-tracker.ts` | Pure helper class `RetryTracker` synthesizes `auto_retry_start` / `auto_retry_end` events from observed `message_end` / `agent_end`. pi ExtensionAPI does NOT expose `auto_retry_*` to extensions (verified pi 0.70/0.73; tracked at https://github.com/badlogic/pi-mono/discussions/2073). Exports `RETRYABLE_PATTERN` regex copied verbatim from pi-coding-agent's `_isRetryableError`. See change: fix-provider-retry-infinite-loop. See change: fix-retry-banner-stuck-on-limit-exceeded — `observeMessageEnd` called sync from bridge before `agent_end`. |
| `packages/extension/src/role-manager.ts` | Manages session model roles. Exports `DEFAULT_ROLE_NAMES` (planning, coding, compact, fast, vision, research) + `overlayDefaultRoles(roles)`. `flow:role-get-all` overlays defaults at read time — table never empty, no auto-write to providers.json. `role:resolve-model` sets `probe.reason = "role '<name>' not configured yet"` when role unconfigured; resolved stays unset. No seeding, no auto-write. See change: roles-standalone-defaults-and-local-install-detection. |
| `packages/extension/src/tool-result-image-inliner.ts` | Bridge Fix B tool-result image inliner. On `tool_execution_end` scans result text for absolute image paths (png/jpg/jpeg/gif/webp/svg/avif/bmp) resolving to existing files. Inlines via `inlineLocalImagePath` as `type:"image"` content blocks. Caps: `MAX_IMAGES_PER_RESULT=4`, `MAX_PER_IMAGE_BYTES`, `MAX_PER_MESSAGE_BYTES`. Strips consumed path from text (no dead link, D5). Over-cap/missing/non-image left as text → falls back to artifact route. Pure; `readFile` injected. Exports `inlineToolResultImages`, `ImageContentBlock`, `MAX_IMAGES_PER_RESULT`. Adds `isAllowedPath` gate (artifact-root containment) before disk read; preserves pre-existing non-text content blocks; padding-aware base64 byte budget. See change: inline-agent-screenshot-artifacts. |
| `packages/extension/src/usage-limit-orderer.ts` | Pure helper class `UsageLimitOrderer` watches bridge `agent_end` flow + synthesizes `auto_retry_end { success: false }` BEFORE `agent_end` when terminal `errorMessage` matches `USAGE_LIMIT_PATTERN` (usage_limit_reached / usage_not_included / quota_exceeded / monthly limit / hourly limit / reset after Nh). UX ordering only — no behavior change. See change: fix-provider-retry-infinite-loop. See change: fix-retry-banner-stuck-on-limit-exceeded — `USAGE_LIMIT_PATTERN` broadened: Gemini `monthly_spending_cap`, `RESOURCE_EXHAUSTED`, OpenAI `insufficient_quota`, Anthropic `credit_balance`, `daily_limit`, `exceeded.{0,40}(quota|cap|spending)`. See change: unify-status-banner-and-terminal-limit-stop — `USAGE_LIMIT_PATTERN` moved to `packages/shared/src/error-patterns.ts`, re-exported from this module for source compat. |
| `packages/extension/src/vcs-info.ts` (was `git-info.ts`) | Hosts `gatherGitInfo(cwd)`. Bridge wires `sendGitInfoIfChanged` at all three sites (initial send + 30 s poll tick + session-change restart). Server `event-wiring.ts` broadcasts via `session_updated` channel. Adds `detectWorktree(cwd)` populating `GitInfo.gitWorktree = {name, mainPath} \| undefined` field; detection via `git rev-parse --git-common-dir` (worktree when value resolves to a different repo than `--git-dir`); `name` = basename of worktree cwd, `mainPath` = parent repo root. See change: add-worktree-spawn-dialog. |
| `packages/image-fit-extension/README.md` | Install + env vars (`PI_IMAGE_FIT_DISABLE`/`MAX_EDGE`/`MAX_BYTES`/`QUALITY`) + default thresholds (1568 px / 4 MiB / quality 85) + telemetry format + silent-quality-loss caveat. See change: pi-image-fit-extension. |
| `packages/image-fit-extension/package.json` | Workspace manifest. name `@blackbelt-technology/pi-image-fit-extension`. Declares `pi.extensions: ["src/extension.ts"]`. Dep `jimp@^1.6.1` (pure JS, no native binaries). Optional peer deps `@earendil-works/pi-coding-agent` + `@mariozechner/pi-coding-agent`. `files: ["src/", "README.md"]`. See change: pi-image-fit-extension. See change: rename-image-fit-package-suffix. |
| `packages/image-fit-extension/src/__tests__/cache.test.ts` | Tests `cacheKey` stability + mtime/maxEdge/maxBytes/quality invalidation, `scopeFor` sanitization (path traversal blocked, fallback to `default`), `cleanupSession` rm, `cleanupOrphans` 24 h sweep with injected clock. See change: pi-image-fit-extension. |
| `packages/image-fit-extension/src/__tests__/extension.test.ts` | Tests `imageFitExtension` registration via fake `ExtensionAPI` (mirrors `provider-register-reload.test.ts`). Covers non-read pass-through, non-image pass-through, oversize rewrite, already-small pass-through, cache hit on second read, JPEG-in→JPEG-out, ENOENT silent fall-through, undecodable warn-once fall-through, `PI_IMAGE_FIT_DISABLE=1` skips registration, `session_shutdown` cleanup, `PI_IMAGE_FIT_MAX_EDGE` override. See change: pi-image-fit-extension. |
| `packages/image-fit-extension/src/__tests__/policy.test.ts` | Tests `readConfigFromEnv` defaults, truthy/falsy disable parsing, numeric overrides, invalid-value single-warning fall-back. See change: pi-image-fit-extension. |
| `packages/image-fit-extension/src/__tests__/resize.test.ts` | Tests `isImagePath` allowlist, `needsResize` 4-corner policy matrix, `outputFormatFor` PNG-vs-not, real jimp `probeDims` + landscape/portrait round-trips with no-upscale guarantee, PNG/JPEG output format selection. See change: pi-image-fit-extension. |
| `packages/image-fit-extension/src/cache.ts` | SHA-256 temp-file cache under `os.tmpdir()/pi-image-fit/<session>/`. Exports `ROOT_DIR`, `cacheKey({absPath,mtimeMs,maxEdge,maxBytes,quality})`, `scopeFor(sessionScope)` (sanitizes `[^A-Za-z0-9_-]` → `_`, collapses runs, trims, falls back to `default`), `ensureDir`, `hasCached`, `cleanupSession`, `cleanupOrphans` (24 h sweep, injectable clock). See change: pi-image-fit-extension. |
| `packages/image-fit-extension/src/extension.ts` | Pi extension entry. Registers `pi.on("tool_call", ...)` gated on `toolName === "read"` + `isImagePath(input.path)`. Body wrapped in try/catch with fall-through (`event.input.path` reverts to original on any error; warns once via `console.warn`). `PI_IMAGE_FIT_DISABLE=1` skips registration entirely + logs one disabled-message line. Wires `session_shutdown` → `cleanupSession`. Fires `cleanupOrphans()` once on load. Emits one telemetry `console.log` per successful resize. See change: pi-image-fit-extension. |
| `packages/image-fit-extension/src/policy.ts` | Exports `readConfigFromEnv(): { disabled, maxEdge, maxBytes, quality }`. Env vars `PI_IMAGE_FIT_DISABLE` / `PI_IMAGE_FIT_MAX_EDGE` / `PI_IMAGE_FIT_MAX_BYTES` / `PI_IMAGE_FIT_QUALITY`. Defaults 1568 / 4194304 / 85. Invalid values fall back to default + emit one `[pi-image-fit] WARN ` line naming the variable. See change: pi-image-fit-extension. |
| `packages/image-fit-extension/src/resize.ts` | Exports `needsResize({bytes,maxBytes,dims,maxEdge})` predicate (long edge OR bytes), `outputFormatFor(srcPath)` (PNG-in → PNG-out, else JPEG), `isImagePath` regex `.png/.jpe?g/.webp/.gif`, `probeDims` (jimp lazy load), `resizeToFile` (long-edge `scaleToFit({w:maxEdge,h:maxEdge})` preserving aspect ratio, `getBuffer(JimpMime.png|jpeg, {quality})`). No `sharp` / `@napi-rs/image` / native deps. See change: pi-image-fit-extension. |
| `packages/image-fit-extension/tsconfig.json` | Package tsconfig. See change: pi-image-fit-extension. |
| `packages/image-fit-extension/vitest.config.ts` | Package vitest config (registered in root `vitest.config.ts` `test.projects`). See change: pi-image-fit-extension. |
| `packages/kb-extension/README.md` | Isolated pi extension docs. Registers pull tools `kb_search`/`kb_neighbors`/`kb_get`; nothing auto-injected. `tool_result` Job 1 (always on): edit `.md` → debounced hash-gated reindex; edit `AGENTS.md`/`CLAUDE.md` acknowledges DOX rows. Job 2 (opt-in `doxEnforcement` default OFF): edit non-md source → one nudge to update nearest `AGENTS.md`. `tool_call` push mode (opt-in `directoryLevelAgents.mode:push`) surfaces nearest `AGENTS.md`. NOT the dashboard bridge. Env `KB_DOX_ENFORCEMENT=1` force-enables Job 2. See change: add-markdown-knowledge-base. |
| `packages/kb-extension/package.json` | Workspace manifest. name `@blackbelt-technology/pi-dashboard-kb-extension`. Declares `pi.extensions: ["src/extension.ts"]`. Dep `@blackbelt-technology/pi-dashboard-kb`. Optional peer deps `@earendil-works/pi-coding-agent` + `@mariozechner/pi-coding-agent`. `exports."."` → `src/index.ts`. `files: ["src/", "!src/__tests__", "README.md"]`. test runs `vitest` under `NODE_OPTIONS=--experimental-sqlite`. See change: add-markdown-knowledge-base. |
| `packages/kb-extension/src/__tests__/reindex.test.ts` | Tests Job 1 reindex (`reindexNow` picks up edited `.md`, old content purged) over temp project + real `SqliteFtsStore`. Tests Job 2 `decideNudge` (missing/clean/treeless/stale), `nudgeText` content, `acknowledgeRows` clears stale flags after `AGENTS.md` edit, `nudged` Set dedup. See change: add-markdown-knowledge-base. |
| `packages/kb-extension/src/extension.ts` | Extension entry. `default kbExtension(pi)`. Registers `kb_search`/`kb_neighbors`/`kb_get` via `pi.registerTool` (typebox params; `kb_search` runs `reindexNow` for freshness). `tool_result` hook: `.md` → `scheduleReindex` + `acknowledgeRows` when `AGENTS.md`/`CLAUDE.md`; else `doxEnforcement` → `decideNudge` + deduped `sendMessage` steer nudge. `tool_call` push mode (opt-in) surfaces nearest `AGENTS.md`. `session_shutdown` → `closeKb`. `WRITE_TOOLS = write/edit/bash`. `KB_DOX_ENFORCEMENT=1` forces `doxEnforcement`. Re-exports reindex helpers. See change: add-markdown-knowledge-base. |
| `packages/kb-extension/src/index.ts` | Barrel. Re-exports `default` from `extension.js` + all of `reindex.js`. See change: add-markdown-knowledge-base. |
| `packages/kb-extension/src/reindex.ts` | Pure reindex + DOX-nudge logic, no pi imports (testable standalone). Exports `createReindexState`, `getKb` (lazy-opens `SqliteFtsStore`), `reindexNow` (hash-gated incremental via `indexSource`), `scheduleReindex` (`DEFAULT_DEBOUNCE_MS = 800`), `closeKb`, `acknowledgeRows`, `decideNudge`, `nudgeText`, types `ReindexState`/`NudgeDecision`. Staleness sidecar `.pi/dashboard/kb/dox-staleness.json` maps source-path → ack sha256. See change: add-markdown-knowledge-base. |
| `packages/kb-extension/vitest.config.ts` | Package vitest config. include `src/**/__tests__/**/*.test.ts`, node env, `pool: forks`, `maxWorkers: 1`. See change: add-markdown-knowledge-base. |
| `packages/kb/eval/golden.doc-example.json` | Golden retrieval set. 20 `{q, expect}` query → expected-path pairs over root `doc-example/` corpus. Drives `kb eval` P@1/Recall/MRR. See change: add-markdown-knowledge-base. |
| `packages/kb/eval/golden.doc-example.paraphrase.json` | Paraphrase golden set. 9 reworded `{q, expect}` pairs (query words differ from doc words). Measures lexical robustness vs synonyms. See change: add-markdown-knowledge-base. |
| `packages/kb/package.json` | Workspace manifest. name `@blackbelt-technology/pi-dashboard-kb`. `bin.kb` → `dist/cli.js`. `exports."."` → `src/index.ts`. `engines.node >=22.5.0`. Zero runtime deps (`node:sqlite`). scripts `verify` (`tsx verify.ts`) + `test` (`vitest`) under `NODE_OPTIONS=--experimental-sqlite`. See change: add-markdown-knowledge-base. |
| `packages/kb/skill/kb-search/SKILL.md` | Skill `kb-search`. Search local markdown KB before answering project-specific questions from memory. Procedure: `kb search "<entities>" --limit 8 --json` → `kb get` top hits → `kb neighbors`/`kb backlinks` → reformulate once with domain terms on paraphrase miss. Pull retrieval, sub-second, zero model tokens. See change: add-markdown-knowledge-base. |
| `packages/kb/skill/kb-setup/SKILL.md` | Skill `kb-setup`. One-time KB bring-up. Procedure: detect existing `.pi/dashboard/knowledge_base.json` → choose scope + `sources[]` → `kb init` (`--global`/`--source`/`--dry-run`/`--force`) → trust remote → `kb index` → smoke `kb search`. `dbPath` gitignored; no sources → `kb index` exits 2. See change: add-markdown-knowledge-base. |
| `packages/kb/src/__tests__/kb.test.ts` | Integration suite. Covers chunker (heading split, fence-safe, wikilinks/md links), indexer+store (index/search/dedup-akaPaths/incremental/delete-purge/graph/doc_type/eval), config layering + legacy `roots[]`, `kbInit`, retrieval Tier A/B/C (BM25F heading-over-body, parent expand, MMR, rerank no-op), `KbStore` double, `doc-example` corpus (skipped without it), source resolvers + TOFU trust (filesystem/npm/https), dox `agentsChain`/`doxInit`/`doxLint`. See change: add-markdown-knowledge-base. |
| `packages/kb/src/chunker.ts` | Structural heading chunker. `chunkMarkdown(input): ParseResult`. Fenced-code state machine (never treats `#` in fence as heading), breadcrumb `headingPath`, frontmatter parse, tiny-chunk merge (`MIN_CHUNK_CHARS = 100`), oversize paragraph split (`MAX_CHUNK_CHARS = 4000`), wikilink + md-link extraction. Stable `chunkId = <sha8>:<ordinal>`. See change: add-markdown-knowledge-base. |
| `packages/kb/src/cli.ts` | `kb` CLI. Commands `init`/`index`/`search`/`neighbors`/`backlinks`/`get`/`agents`/`dox init|lint`/`eval`/`config`. Flag parser (repeatable `--source`, `--json`, `--no-reindex`, `--db`, `--cwd`, `--config`). `search`/`eval` auto-run incremental reindex unless `--no-reindex`. No sources on `index` → exit 2. Run dev `NODE_OPTIONS=--experimental-sqlite tsx src/cli.ts`. See change: add-markdown-knowledge-base. |
| `packages/kb/src/config.ts` | Config layering: project `.pi/dashboard/knowledge_base.json` → global `~/.pi/dashboard/knowledge_base.json` → built-in `DEFAULTS` (field-level fill-in). Exports `loadConfig`, `validateConfig`, `DEFAULTS`, `projectConfigPath`, `globalConfigPath`, types `KbConfig`/`SourceConfig`/`ResolvedConfig`/`ResolvedSource`. Legacy `roots[]` → filesystem sources. No file-count cap default. `dbPath` `.pi/dashboard/kb/index.db`. See change: add-markdown-knowledge-base. |
| `packages/kb/src/dox.ts` | DOX directory-level AGENTS.md tooling. `agentsChain(cwd, path)` root→nearest AGENTS.md/CLAUDE.md chain; `fallbackManifest` KB routing map when none. `doxInit` scaffolds path-only rows (idempotent, never clobbers; `AREA_FILE_THRESHOLD = 8`, `ROW_CAP = 40`). `doxLint` detects stale/orphan/missing/broken-pointer/over-threshold/missing-companion, `--fix` prunes orphans + adds missing rows. `parseRowPaths`. Detect-don't-write: fills paths, LLM authors purposes. See change: add-markdown-knowledge-base. |
| `packages/kb/src/eval.ts` | Retrieval-quality eval harness. `evaluate(store, golden, opts): EvalMetrics`. Scores `kb search` against `{q, expect-path-substring}` golden set → `P@1`/`P@5`/`Recall@K`/`MRR`/`nDCG@K`/`avgLatencyMs`. Gate ranking changes on these. `GoldenItem`, `EvalMetrics`. See change: add-markdown-knowledge-base. |
| `packages/kb/src/index.ts` | Public API barrel. Re-exports `chunkMarkdown`, `SqliteFtsStore`, `indexSource`, `kbInit`, `loadConfig`/`validateConfig`/`DEFAULTS`, `resolveAll`/`classifyRef`/`sourceIdentity`/`resolverFor`, `isTrusted`/`recordTrust`/`canonicalSource`/`sourceHash`, `agentsChain`/`doxInit`/`doxLint`/`fallbackManifest`/`parseRowPaths` + core types. See change: add-markdown-knowledge-base. |
| `packages/kb/src/indexer.ts` | `indexSource(store, src, opts): IndexStats`. Walks filesystem source, mtime→sha256 incremental change detection, structural chunking, Tier-1 graph (`child_of`/`links_to`/`references`/`has_tag` edges), transactional upsert with rollback, deletion purge for missing paths. `docTypeOf` tags `doc`/`agents`/`source-md`. Minimal glob include/exclude. See change: add-markdown-knowledge-base. |
| `packages/kb/src/init.ts` | `kbInit(opts): InitResult` scaffolds `knowledge_base.json` (project default; `--global` → `~/.pi/dashboard/`). Seeds `DEFAULTS` + filesystem `sources[]`. Gitignores project-local `dbPath`. Refuses clobber without `--force`; `--dry-run` writes nothing. Exports `buildInitConfig`, `renderConfig`. See change: add-markdown-knowledge-base. |
| `packages/kb/src/sources.ts` | Pluggable source resolvers turn any spec → local dir; indexer only sees local dirs. `filesystemResolver`/`npmResolver`/`gitResolver`/`httpsResolver`, `classifyRef`, `sourceIdentity`, `resolveAll`, `resolverFor`. Remote (npm/git/https) require TOFU trust + cache; KB reads markdown only, never executes source code. Mirrors server `package-source-helpers.ts` to stay dependency-free. See change: add-markdown-knowledge-base. |
| `packages/kb/src/sqlite-store.ts` | Default `KbStore` over `node:sqlite` FTS5 (`SqliteFtsStore`). DDL: fts5 `chunks` (`porter unicode61`) + `files`/`nodes`/`edges`. `search` = weighted `bm25` BM25F + proximity boost + exact-content dedup (`akaPaths`, prefers higher-priority root) + lexical MMR diversity + parent small-to-big expand + rerank no-op + query expansion. `neighbors` recursive CTE, `backlinks`, `getChunk`/`getChunkById`, `counts`. WAL. Requires `--experimental-sqlite`. See change: add-markdown-knowledge-base. |
| `packages/kb/src/trust.ts` | TOFU trust store for remote KB sources at `~/.pi/dashboard/kb-source-trust.json` (override `KB_SOURCE_TRUST_PATH`). `isTrusted`/`recordTrust` keyed `sha256(canonicalSource(spec))` so editing a spec re-prompts. `canonicalSource`, `sourceHash`, `defaultPromptTrust` (y/N stdin). Filesystem sources skip trust. See change: add-markdown-knowledge-base. |
| `packages/kb/src/types.ts` | Core types + `KbStore` abstraction (storage accessed only through it → backend swappable). `DocType` = `doc`/`agents`/`source-md`. `Chunk`, `GraphNode`, `GraphEdge`, `KbHit` (score lower = better), `FileState`, `SearchOpts`, `Reranker`, `KbStore` interface. See change: add-markdown-knowledge-base. |
| `packages/kb/verify.ts` | Phase-1 verification script. Indexes `doc-example/` with the real package; reports index perf, incremental no-op (expect 0/0), golden P@1/Recall@10/MRR/avgLatency, dedup collapse, doc_type filter, graph neighbors/backlinks. Run `NODE_OPTIONS=--experimental-sqlite tsx packages/kb/verify.ts`. See change: add-markdown-knowledge-base. |
| `packages/kb/vitest.config.ts` | Package vitest config. include `src/**/__tests__/**/*.test.ts`. See change: add-markdown-knowledge-base. |
| `packages/mockup-loop/.pi/skills/frontend-mockup-loop/SKILL.md` | Generic skill `frontend-mockup-loop`. 7-step loop: GROUND→CONTRACT→MOCKUP→TEST→FIX→PROMOTE→LEARN. Defeats distributional convergence. References bundled tools `serve_mockup`/`score_mockup`/`init_ui_contract` + `ui-contract.md` control plane. Works any React/Tailwind/shadcn project. See change: add-frontend-mockup-loop. v0.2 adds expert-UX-designer stance: ground every decision in external documented public rules (references/ux-best-practices.md), never invent; GROUND step grounds in real UI + documented rules; TEST step runs the 5-step expert-UX evaluation protocol; accessibility floor = hard gate. See change: add-frontend-mockup-loop-ux. Adds design-system selection section: 5 presets, gate-vs-advisory model, validate_mockup, Apple HIG HTML-approximation + SwiftUI-on-PROMOTE guidance. See change: add-selectable-design-systems. Now references companion skill anti-slop-frontend (advisory AI-tell layer feeding FIX step; never overrides gates). |
| `packages/mockup-loop/README.md` | Install via `pi install npm:@blackbelt-technology/frontend-mockup-loop`. Lists tools table + `/mockup-loop` command. Notes optional playwright dep for `score_mockup`. See change: add-frontend-mockup-loop. Documents `--system` flags, 5 presets, dependency posture (bundled vs optional validators), snapshot `--refresh` upstream sources. See change: add-selectable-design-systems. |
| `packages/mockup-loop/package.json` | Workspace manifest. name `@blackbelt-technology/frontend-mockup-loop`. Declares `pi.extensions: ["src/extension.ts"]` + `pi.skills: [".pi/skills/frontend-mockup-loop"]`. peerDeps `@earendil-works/pi-coding-agent` (optional) + `typebox`. `files: ["src/","!src/__tests__",".pi/skills/","README.md"]`. See change: add-frontend-mockup-loop. Adds `dependencies` `@axe-core/playwright` + `eslint-plugin-tailwindcss`. Adds `presets-data/` to `files`. See change: add-selectable-design-systems. |
| `packages/mockup-loop/presets-data/apple-hig/contract.tokens.json` | Apple HIG hand-authored rule pack in DTCG envelope. iOS semantic colors (label, systemBackground, systemBlue), SF Pro type, 8pt spacing, 44px min-touch-target. Apple publishes no token JSON. See change: add-selectable-design-systems. |
| `packages/mockup-loop/presets-data/apple-hig/rubric.json` | apple-hig L4 boolean rubric. 7 checks: touch≥44pt, tab bar ≤5, Dynamic Type, safe areas, semantic colors, SF Pro, 8pt grid. See change: add-selectable-design-systems. |
| `packages/mockup-loop/presets-data/apple-hig/rules.md` | apple-hig rule pack prose. Semantic colors, Dynamic Type, 44pt targets, 8pt grid, safe-area insets, tab bar ≤5. Auditable by hig-doctor. See change: add-selectable-design-systems. |
| `packages/mockup-loop/presets-data/fluent-2/contract.tokens.json` | Fluent 2 webLightTheme DTCG snapshot. brand/neutral/status colors, Segoe UI, border-radius 2/4/6/8, spacing ramp. Source @fluentui/tokens. See change: add-selectable-design-systems. |
| `packages/mockup-loop/presets-data/fluent-2/rubric.json` | fluent-2 L4 boolean rubric. 7 checks. See change: add-selectable-design-systems. |
| `packages/mockup-loop/presets-data/material-3/contract.tokens.json` | Material 3 DTCG snapshot. --md-sys-* color roles, shape scale, 8dp spacing, 48dp min-touch-target. See change: add-selectable-design-systems. |
| `packages/mockup-loop/presets-data/material-3/rubric.json` | material-3 L4 boolean rubric. 7 checks (8dp grid, 48dp targets, tonal elevation). See change: add-selectable-design-systems. |
| `packages/mockup-loop/presets-data/mui/contract.tokens.json` | MUI default light theme DTCG snapshot. palette, spacing unit 8px, shape.borderRadius 4px, Roboto. Source @mui/material createTheme. See change: add-selectable-design-systems. |
| `packages/mockup-loop/presets-data/mui/rubric.json` | mui L4 boolean rubric. 7 checks (8px spacing, 48px targets, elevation). See change: add-selectable-design-systems. |
| `packages/mockup-loop/presets-data/shadcn/contract.tokens.json` | shadcn default theme DTCG snapshot. CSS-var colors (hsl), --radius 0.5rem, 4/8px spacing. See change: add-selectable-design-systems. |
| `packages/mockup-loop/presets-data/shadcn/rubric.json` | shadcn L4 boolean rubric. 7 checks (token fidelity, focus ring, neutral palette). See change: add-selectable-design-systems. |
| `packages/mockup-loop/references/ux-best-practices.md` | Citable UX rule corpus. Source hierarchy (USWDS CC0, GOV.UK OGL, NN/g, Laws of UX, Material/Carbon Apache-2.0). Nielsen 10 heuristics, Laws of UX, Gestalt, cognitive load. Per-component pattern rules (forms, nav, errors, empty states, loading, data, modals). 5-step expert-UX evaluation protocol (a11y floor gate → heuristic boolean rubric → PURE → severity 0-4 → prioritized fixes). 22-item checkable rubric seed. 38 public-source URLs. See change: add-frontend-mockup-loop-ux. |
| `packages/mockup-loop/src/__tests__/extension.test.ts` | Smoke tests. Fake ExtensionAPI captures tools. Asserts 5 tools registered, list_design_systems returns 5 presets, init_ui_contract back-compat + --system, score_mockup HIG rubric swap, validate_mockup shape. See change: add-selectable-design-systems. |
| `packages/mockup-loop/src/__tests__/presets.test.ts` | Unit tests. registry (5 presets, unknown rejected), contract DTCG load each snapshot, L1 off-token fail/pass, L2 low-contrast fail, L4 score=pass/N, validateMockup shape. See change: add-selectable-design-systems. |
| `packages/mockup-loop/src/extension.ts` | Pi extension entry `frontendMockupLoop(pi)`. Registers 3 tools: `serve_mockup{dir,port?,stop?}` (node:http static server on 0.0.0.0, returns local+LAN URL, `servers` Map keyed by port), `score_mockup{url,widths?,outDir?}` (dynamic-import playwright chromium, full-page screenshots at widths default [375,768,1440], returns paths + scoring rubric, falls back to install guidance if absent), `init_ui_contract{path?,force?}` (writes `CONTRACT_TEMPLATE` to `ui-contract.md`). Registers `/mockup-loop` command. `session_shutdown` closes all servers. Path-traversal guard in static handler. See change: add-frontend-mockup-loop. Now registers 5 tools: adds `list_design_systems{}` (enumerates registry) + `validate_mockup{system,url?,dir?}` (L1+L2 gates, L3+L4 advisory, returns {gates,advisory,pass}). `init_ui_contract` gains `system?`+`refresh?` (writes DTCG contract from preset snapshot; no system → blank template). `score_mockup` gains `system?` (swaps generic rubric for preset boolean rubric). Imports from src/presets/*. See change: add-selectable-design-systems. |
| `packages/mockup-loop/src/presets/contract.ts` | DTCG contract loader. `loadContract(id)` reads bundled presets-data/<id>/contract.tokens.json, asserts DTCG via `isDtcg`. `refreshContract(id)` re-fetches upstream (token systems only; rule-pack throws). `rubricPath`/`contractPath`/`PRESETS_DATA_DIR` resolvers. UPSTREAM_SOURCES map. See change: add-selectable-design-systems. |
| `packages/mockup-loop/src/presets/registry.ts` | Design-system preset registry. `DesignSystemPreset` type (id,label,platform,substrate,contractSource,minTouchTarget,spacingScale,validators[]). 5 v1 presets: shadcn,mui,material-3,fluent-2,apple-hig. `listPresets`/`getPreset`/`resolvePreset` (rejects unknown id without throw). See change: add-selectable-design-systems. |
| `packages/mockup-loop/src/presets/validators.ts` | Layered validators. `runL1` token-lint (built-in raw-hex scan, gate for bundled systems), `runL2` a11y floor (inline WCAG contrast scan + axe-if-present, hard gate), `runL3` named-system auditor (shell-out-if-present via `isToolAvailable`, advisory), `runL4` boolean rubric (score=pass/N in code, advisory), `validateMockup` orchestrator returns {gates,advisory,pass}; pass gate-only. Inline WCAG `contrastRatio`/`parseHex`. See change: add-selectable-design-systems. |
| `packages/mockup-loop/tsconfig.json` | Extends `../../tsconfig.base.json`. rootDir `src`, outDir `dist`. See change: add-frontend-mockup-loop. |
| `packages/mockup-loop/vitest.config.ts` | vitest config. include src/**/__tests__/**/*.test.ts, node env, forks pool, maxWorkers 1, testTimeout 30000. See change: add-selectable-design-systems. |
| `src/extension/ask-user-tool.ts` | `ask_user` tool registration (bundled in bridge, registered at session_start to avoid static tool-name conflicts). `multiselect` dispatches through `polyfillMultiselect` (pi-coding-agent's `ExtensionUIContext` no native `multiselect`). Tool description instructs agents: *"UI provides a Select all toggle; do not add one."* **Schema shape**: `parameters` is single flat `Type.Object` (root `type: "object"`) — NOT root `Type.Union` — because OpenAI's function-calling validator rejects root-level `anyOf` with *"schema must be a JSON Schema of 'type: \"object\"'"*. To restore Anthropic-friendly per-method strictness after commit a53933f, root object carries body-level `oneOf` discriminator over `method` (confirm/select/multiselect/input/batch) with per-arm `required` + `minItems`; sub-questions use same flat-object + `oneOf` with no `batch` arm (no nesting). `prepareArguments` provides runtime rescue (`params` unwrap, `question`→`title`, stringified `options`/`questions`, batch synthesis/title backfill, `{label,value}` → labels); `execute` retains empty-options guardrails. See changes: ask-user-multiselect-polyfill, refactor(schema)-restructure-ask-user-tool-schemas, fix-multiselect-auto-cancel-on-dashboard. Batch dispatches ONE ctx.ui.batch() request (not sequential per-question loop). See change: redesign-ask-user-question-cards. |
| `src/extension/bridge-context.ts` | Shared mutable state type + helpers for bridge modules. `BridgeContext` gains `attachedChange: string \| null`. See change: inject-session-context-into-agent. |
| `src/extension/bridge.ts` | Main extension entry (composes sync/tracker/flow modules, tracks `isAgentStreaming` in persistent BridgeState). **`hasRegisteredOnce: boolean`** local (default `false`, synced via `BridgeContext`) flips `true` after first `sendStateSync`; consumed by `session-sync.ts` to tag `session_register` with `registerReason: "spawn" | "reattach"` so server applies configured `reattachPlacement` on dashboard-restart reattaches. See change: reattach-move-to-front. **Invariant**: bridge code MUST NOT call `pi.newSession(...)` / `ctx.fork(...)` / `ctx.switchSession(...)` — enforced by `packages/extension/src/__tests__/no-session-replacement-calls.test.ts`. pi 0.69+ invalidates captured `pi`/`ctx` after these calls; bridge re-captures state in `session_start` keyed on `event.reason ∈ {"new","fork","resume"}`. **PromptBus patch site**: patches `ctx.ui.select/input/confirm/editor/multiselect` in `session_start`. TUI adapter handles `select/input/confirm/editor` only. `multiselect` bridge-attached (pi no native API) + routed exclusively through bus → `DashboardDefaultAdapter`'s browser dialog — decoded via `decodeMultiselectAnswer`. Intentionally NO TUI adapter arm for multiselect: pi 0.70 RPC mode's `ctx.ui.custom` is no-op, so any TUI arm awaiting it would auto-cancel dashboard render in <1s (changes: fix-multiselect-auto-cancel-on-dashboard, fix-multiselect-tui-arm-self-cancel; pinned by `no-tui-multiselect-arm-regression.test.ts`). **Per-message-fork entryId stamping** (change: fix-per-message-fork): `message_start` stamps `nonce` (no entryId — user entry not persisted yet on pi 0.69+); `message_end` enrichment deferred via `setTimeout(0)` (NOT `queueMicrotask`, which resolves inside pi's awaited extension dispatcher + misses `appendMessage`); wrapped `ctx.sessionManager.appendMessage` emits `entry_persisted { entryId, nonce }` so client reducer back-fills user-message bubble's `entryId`. See change: fix-retry-banner-stuck-on-limit-exceeded — `message_end` synthesizer state runs sync; `auto_retry_*` sends sync, `message_end` body stays in `setTimeout(0)` for entryId capture. User-role `message_start` also deferred via `setTimeout(0)` — preserves order vs deferred `message_end`; assistant `message_start` stays sync. See change: add-followup-edit-and-steer-cancel. Shutdown extension command resets bridgeSteering+bridgeFollowUp to [], calls pi.clearSteeringQueue/clearFollowUpQueue defensively, emits final queue_update before cachedCtx.shutdown(). See change: reset-shadow-queues-on-shutdown. See change: tighten-process-list-ux. Platform-aware process scan cadence: `PROCESS_SCAN_INTERVAL` + `PROCESS_MIN_ELAPSED_MS` = 5_000 ms on Unix, 10_000 / 30_000 ms on Windows. `selfSpawnedPgids: Set<number>` threaded through `BridgeContext`, passed to `scanChildProcesses` as `excludedPgids`. Populated synchronously after dashboard-server auto-start via `autoStartServer`'s new `onServerSpawned` callback. See change: classify-process-list-entries. Seeds `selfSpawnedPgids` with pi's own PGID via `getOwnPgid()` at bridge init so pi-self + same-group plugin/MCP sidecars (context-mode bun) never surface. See change: fix-bridge-hasui-for-headless-rpc. See change: unify-status-banner-and-terminal-limit-stop — bridge auto-aborts on `message_end` when `errorMessage` matches `USAGE_LIMIT_PATTERN`; first-attempt USAGE_LIMIT synth on `agent_end` outside retry chain; `rewriteFollowupQueue` early-returns on `!isAgentStreaming`; `rawAbort` wired separately from wrapper-abort; wrapper-abort no longer clears `usageLimitOrderer` nor calls `noteRetryEnd`. See change: honest-mid-turn-queue-surface. Six queue-mutation handlers (clear_steering_queue, clear_followup_slot, edit_followup_slot, edit_followup_entry, remove_followup_entry, promote_followup_entry) + rewriteFollowupQueue deleted. shutdown / abort arms no longer call pi.clearSteeringQueue / pi.clearFollowUpQueue (no-ops via ExtensionAPI through pi 0.76.0) nor reset shadow queues; pi's real queues persist across abort by design, shadows mirror. See change: rework-mid-turn-prompt-queue. bridgeFollowUp flipped from pi-owned-shadow to BRIDGE-OWNED BUFFER (authoritative). recordFollowupSent renamed → bufferFollowupSend (pre-send buffer, not post-send mirror). New drainFollowupQueue() runs on agent_end via queueMicrotask: re-entrancy lock + idle gate (ctx.isIdle) + TUI gate (pi.hasPendingMessages) + empty-buffer gate + pop-before-send + emit-before-send + catch+drop. Five mutation handlers (edit/remove/promote/clear/pull_followup_to_editor) mutate bridgeFollowUp only; never call pi.* methods. message_start matcher updated: now no-op for buffered entries (they reach pi as fresh-turn no-deliverAs sends, never via {deliverAs:followUp}). Image attachments not carried in buffer (Known Limitation, text-only). Patches ctx.ui.batch for single-request batch dispatch via PromptBus. See change: redesign-ask-user-question-cards. 3 models_list push sites use toModelInfo. See change: enrich-model-selector-capabilities-favorites. Adds `enqueueSystemFollowup(text)` — ungated push to `bridgeFollowUp` (bypasses `isAgentStreaming` gate), respects `FOLLOWUP_QUEUE_CAP`, schedules `drainFollowupQueue(0)`; safety-net so plugin-routed continuations share single drain. Listener `pi.events.on("dashboard:enqueue-followup")` → `enqueueSystemFollowup`. Listener `pi.events.on("dashboard:plugin-message")` → `connection.send({type:"plugin_pi_message", sessionId, pluginId, messageType, payload})` (generic plugin bridge→server channel). See change: add-goal-continuation-plugin. See change: inject-session-context-into-agent — declares closure `attachedChange`; syncBc/applyBc carry it; BridgeState persists `attachedChange` across reload; wires `registerDashboardContextInjector` + `onAttachProposalChanged` callback. See change: add-dashboard-slash-commands. `sessionPrompt` calls `tryExecSlashTemplate` after `tryDispatchExtensionCommand`, before `expandPromptTemplateFromDisk` fallback; `if (ranExec) return`. See change: unify-error-retry-lifecycle — wires `AbortLatch`; `abort:` callback calls `abortLatch.request(sessionId)`; agent_start + assistant message_start re-abort `cachedCtx.abort()` when `shouldAbort`; agent_end + user message_start clear latch; `noteUserPrompt` option clears latch pre-send. Calls `sendPiVersionIfChanged` at session_register + on `runGitPollTick` (30s). See change: restore-pi-version-skew-surface. |
| `src/extension/slash-dispatch.ts` | `tryDispatchExtensionCommand` helper. Routing-step 9 (three-way): pi.dispatchCommand 0.71+ → server-routed via RPC keeper UDS when headless → stopgap. Path-D hint string (`RPC_KEEPER_HINT`) explains tmux/Windows-Terminal sessions own pi's stdin (no `useRpcKeeper` flag mentioned post-`enable-rpc-keeper-by-default`). See change: fix-extension-slash-commands-in-dashboard, add-rpc-stdin-dispatch-with-keeper-sidecar, enable-rpc-keeper-by-default. |
| `packages/extension/src/command-handler.ts` | Command routing: `!`/`!!` bash, `/compact`, slash commands. Accepts `enqueueIfStreaming` option — when bridge `isAgentStreaming === true`, hands prompt to `PromptQueue.enqueue` instead of `pi.sendUserMessage`. See change: surface-mid-turn-prompt-queue. See change: unify-status-banner-and-terminal-limit-stop — drops `"Aborted by user"` placeholder `finalError`; persistent-abort scheduler uses `rawAbort` + streaming-transition break. See change: rework-mid-turn-prompt-queue. sessionPrompt followUp branch flipped: when wasStreaming, calls onFollowupSent ONLY (skips pi.sendUserMessage); steer + idle paths unchanged. sendUserMessageWithImages now used for steer + idle only. See change: fix-file-mention-search-ranking. Exports `searchFiles(cwd, query)`; ranks then caps. Decouples traversal budget `MAX_VISITS=4000` (entries scanned) from result cap `MAX_RESULTS=50` (was 20). BFS walk; visits shallow first; collects all in-budget candidates; ranks; caps. `splitQuery(q)` splits at last `/`; prefix scopes candidates; leaf ranks as basename. `scoreMatch(relPath, leaf)`: 4 exact basename; 3 basename prefix; 2 basename substring; 1 path substring; 0 absent/empty leaf. Sort: score desc; depth asc; pathLen asc; alpha. Empty leaf skips pathLen, gives depth+alpha; bare `@` lists top-level alphabetically. Fixes DFS-budget starvation; fixes bare-`@` deep-file noise. request_models push uses toModelInfo. See change: enrich-model-selector-capabilities-favorites. `handleBashCommand` resolves bash via `getDefaultRegistry().resolve("bash")`; spawns resolved absolute path (not literal `"sh"`); on miss emits `bash_output` event with `missingTool` payload, no spawn, no LLM send. See change: register-bash-and-tool-install-help. Adds `attach_proposal_changed` switch case + `onAttachProposalChanged` option. See change: inject-session-context-into-agent. See change: add-dashboard-slash-commands. Exports `tryExecSlashTemplate(pi, text, cwd, sessionId, eventSink)` — resolves via `loadPromptTemplate`; runs `handleBashCommand` when `kind:"exec"`; returns true (mirrors `tryDispatchExtensionCommand`). Exports `buildDashboardExecEnv()` reads port from `~/.pi/dashboard/config.json` (default 8000) → `{ PI_DASHBOARD_PORT, PI_DASHBOARD_BASE }`. `handleBashCommand` gains `execOpts {args,env,source}`: env prepended as `export` lines (ExecOptions no env field); positional args via `bash -c <script> -- a b` (`$1`,`$2`); sets `bash_output.data.source` only when execOpts.source set. Exec branch in handle() non-bridge slash arm after extension dispatch. `buildDashboardExecEnv()` port precedence: `PI_DASHBOARD_PORT`/`DASHBOARD_PORT` env → config.json `port` → 8000 (env wins; Docker harness config.json omits port). See change: add-dashboard-slash-commands. See change: unify-error-retry-lifecycle — adds `noteUserPrompt?: () => void` option; `send_prompt` case calls `options?.noteUserPrompt?.()` first (before parse/send) so new turn clears abort latch before agent_start. |
| `src/extension/connection.ts` | WebSocket with exponential backoff. **Auto-start suppression** (change: fix-restart-bridge-auto-start-race): exposes `pauseAutoStart(ms)` (idempotent extend-only) + `shouldSuppressAutoStart()`. Bridge calls `pauseAutoStart(quiesceMs)` on receipt of `server_restarting`; `server-auto-start.ts` consults `shouldSuppressAutoStart()` + skips spawn step while window active so bridges never race `restart-helper.ts` orchestrator. Discovery + reconnection NOT suppressed. |
| `packages/extension/src/dashboard-context-injector.ts` | Registers `before_agent_start` handler. Splice-replaces trailing `Current working directory:` line of system prompt with dashboard session-context fragment (delimiter + `You are pi session <sessionId> running in <cwd>.` + optional `Attached OpenSpec change: …`). Exports `buildContextFragment`, `spliceContextFragment` (pure), `registerDashboardContextInjector(pi, getBc, isActive)`. Reads live `bc.sessionId`/`bc.attachedChange` via getter; cwd from `event.systemPromptOptions.cwd ?? process.cwd()`. Coexists with pass-through before_agent_start forwarder (pi chains results). isActive guard prevents double-splice on /reload. See change: inject-session-context-into-agent. |
| `src/extension/dashboard-default-adapter.ts` | Built-in PromptBus adapter; renders prompts as generic interactive dialogs in dashboard chat |
| `src/extension/dev-build.ts` | Dev build-on-reload helper (client build + server shutdown) |
| `src/extension/flow-event-wiring.ts` | Flow event listener registration (flow:* → event_forward) |
| `src/extension/git-info.ts` | Git branch/remote/PR detection (polled every 30s) |
| `src/extension/git-link-builder.ts` | Git remote URL parsing + platform-specific links |
| `src/extension/hasui-flip.ts` | Pure helper `flipHasUI(ctx)`. Bridge `session_start` calls after ctx.ui.* PromptBus patch block. Sets `ctx.hasUI = true` so extensions branch into UI-present arm (context-mode `/ctx-stats`/`/ctx-doctor`, pi-agent-browser install prompt). try/catch swallows non-writable-ctx error, logs once. See change: fix-bridge-hasui-for-headless-rpc. |
| `src/extension/model-tracker.ts` | Model/thinking-level/git/name change detection. `sendPiVersionIfChanged(bc, readVersion?)` reads `@earendil-works/pi-coding-agent/package.json` via createRequire; module-scoped `lastPiVersion` debounces; sends `pi_version_update` on change. `_resetPiVersionCache` test helper. See change: restore-pi-version-skew-surface. |
| `src/extension/multiselect-list.ts` | `MultiSelectList` component implementing pi-tui's `Component` interface. Keyboard contract: `↑↓`/`k`/`j` navigate, `Space` toggles current, `Enter` confirms (selected values in original option order), `Escape` cancels. **No "select all" binding in TUI** — dashboard adapter provides that affordance. |
| `src/extension/multiselect-polyfill.ts` | `polyfillMultiselect(ctx, title, options, opts)` — primary path delegates to bridge-patched `ctx.ui.multiselect` (PromptBus → `DashboardDefaultAdapter` → client `MultiselectRenderer`). Legacy fallback uses `ctx.ui.custom<T>()` + `MultiSelectList` for older / non-bridge contexts; fallback **no-op in pi 0.70 RPC mode** (dashboard headless sessions) — pi-coding-agent defines `custom` as `async () => undefined` there — only effective in pure-TUI sessions if future pi version restores `ctx.ui.custom` in RPC mode. Resolves to `string[]` (confirmed, including empty `[]`) or `undefined` (cancelled). Used for both single-question + batch sub-question `multiselect` paths in `ask-user-tool.ts`. See changes: fix-multiselect-auto-cancel-on-dashboard, fix-multiselect-tui-arm-self-cancel. |
| `src/extension/process-metrics.ts` | Lightweight CPU/memory/event-loop metrics collector for heartbeats |
| `src/extension/process-scanner.ts` | Child process detection via ps + PGID tracking (leaf-only, grandchild recursion) + PGID-based kill. See change: tighten-process-list-ux. Adds `ScanOptions.excludedPgids?: Set<number>`. `captureChildPgids` refuses excluded PGIDs at capture time. `scanTrackedProcesses` skips excluded PGIDs at filter time (defense-in-depth). Reaps dead PGIDs from same set every scan tick. See change: classify-process-list-entries. Adds cached `getOwnPgid()` (`ps -o pgid= -p <pid>`, Unix-only, `undefined` on Windows/failure) + `__resetOwnPgidCacheForTests()`. Windows descendant lookup uses PowerShell Get-CimInstance only (`getWindowsDescendantsCim`); wmic fast-path + `parseWmicLine`/`wmicDateToElapsedMs` removed (wmic gone by default Win 11 22H2+). See change: replace-wmic-with-powershell. |
| `src/extension/prompt-bus.ts` | PromptBus — unified prompt routing to registered adapters (TUI, dashboard, custom). First-response-wins, cross-adapter dismissal. PromptRequest.type includes "batch". See change: redesign-ask-user-question-cards. |
| `src/extension/prompt-expander.ts` | Slash command → prompt template expansion (supports colon-to-hyphen aliasing: `/opsx:cmd` → `opsx-cmd.md`). **Skill expansions wrap output in pi's `<skill name=...>...</skill>\n\nargs` envelope** via `buildSkillBlock` from `pi-dashboard-shared/skill-block-parser.js`; detection via `isSkillResolution(templateName, filePath, pi)` (templateName starts with `skill:` OR pi.getCommands fallback returned `source: "skill"`). Plain prompt templates continue to emit unwrapped `body\n\nargs`. See change: render-skill-invocations-collapsibly. See change: add-dashboard-slash-commands. `readTemplate` returns `{frontmatter, body}`. `parseFrontmatterBlock` hand-rolled YAML-lite; `PromptFrontmatter {executable?:"bash"; excludeFromContext?; description?}`; unknown keys ignored; malformed/unclosed frontmatter → llm fallback, no throw. Exports `loadPromptTemplate(text,cwd,pi)` → `{kind:"llm";text} | {kind:"exec";body;excludeFromContext;argsString} | null`; `executable: bash` → exec (default excludeFromContext true); unsupported executable values degrade to llm. `expandPromptTemplateFromDisk` delegates to `loadPromptTemplate`. `scanCommandsSubdir` scans `.pi/skills/<skill>/commands/*.md` one level, keys by basename (pi.getCommands unreliable for nested skill commands). `addSkillCommandsFromRegistry(pi, templates)` harvests each registry skill's sibling `commands/*.md` via `sourceInfo.path` → `/dashboard:*` resolves from any cwd (not just extension install dir). See change: add-dashboard-slash-commands. |
| `src/extension/provider-register.ts` | Reads `~/.pi/agent/providers.json`, calls `pi.registerProvider()` with auto-discovered models, exports `reloadProviders(pi)` + `onProviderChanged(cb)`. `reloadProviders` diffs current file against module-level `lastRegistered` snapshot + applies add/remove/change via `registerEntry` / `pi.unregisterProvider`. Called from bridge's `credentials_updated` handler BEFORE `modelRegistry.refresh()` so new providers appear in `/model` without session restart. Every discovered model enriched via `enrichModelMetadata(id, api, probe)` where `probe` wraps pi's `modelRegistry.find()` (captured from `ctx.modelRegistry` at first `session_start`; `model_select` fallback capture point) — resolves `contextWindow`, `maxTokens`, `reasoning`, `cost`, `input` for catalog-known models (e.g. `proxy/cc/claude-opus-4-7` → 1M ctx / reasoning / Opus pricing); falls back to api-appropriate defaults (`anthropic-messages` → 200k/64k, `google-generative-ai` → 1M/65k, `openai-completions` → 128k/16k). Fallback keeps `input: ["text", "image"]` so pasted images reach vision-capable models without pi-ai's `downgradeUnsupportedImages` stripping client-side; text-only models ignore image silently or return user-visible 400. See changes: enrich-custom-provider-model-metadata, enable-image-input-custom-providers. Adds `_buildProviderCatalogue(modelRegistry, piAi)` (pure) + `buildProviderCatalogue()` (uses captured registry + lazy `import("@earendil-works/pi-ai")` (or legacy `@mariozechner/pi-ai`) for env var hints). Pushed alongside `models_list` from `bridge.ts` (3 sites) + `session-sync.ts` (2 sites) + `command-handler.ts` `request_providers` case + `credentials_updated` handler. Server caches in `provider-catalogue-cache.ts`; consumed by `getAuthStatus()` for full provider list. See change: replace-hardcoded-provider-lists. See change: fix-custom-provider-flag-race — `lastRegistered.set(name, ...)` runs synchronously at start of `registerEntry`, before `await discoverModels(...)`. First `providers_list` push (from `session_start` after `activate()` kicks off async `registerEntry()`) carries `custom: true` flags even when `/v1/models` slow/unreachable, so custom providers from `~/.pi/agent/providers.json` don't leak into Settings → Provider Authentication → API Keys. enrichModelMetadata returns metadataSource ("catalog" on probe hit else "fallback"). Exports toModelInfo(m) building ModelInfo from registry Model (vision=input.includes("image"); metadataSource from enrichmentSource map, default "catalog"). See change: enrich-model-selector-capabilities-favorites. |
| `src/extension/server-auto-start.ts` | mDNS-first discovery → health check fallback → auto-start with concurrent launch detection |
| `src/extension/server-launcher.ts` | Auto-start server as detached process; captures **both stdout AND stderr** to `~/.pi/dashboard/server.log` (append mode) by passing `stdoutFd: logFd` alongside `logFd` — parity with `pi-dashboard start`'s `stdio: ["ignore", logFd, logFd]`. Exports pure `buildSpawnDetachedOptions` + `buildReadyTimeoutMessage`; latter appends `nodejs/node#58515` upgrade hint when `isKnownBadNode(process.version)` true. |
| `src/extension/server-probe.ts` | TCP probe to detect running server |
| `src/extension/session-sync.ts` | Session register, replay, switch/fork handling. `sendStateSync` tags `session_register.registerReason` as `"spawn"` on first invocation per process + `"reattach"` thereafter, flipping `bc.hasRegisteredOnce`. `handleSessionChange` (new/fork/resume — fresh sessionId) ALWAYS tags `"spawn"` regardless of flag. See change: reattach-move-to-front. models_list push uses toModelInfo (2 sites). See change: enrich-model-selector-capabilities-favorites. |
| `src/extension/slash-dispatch.ts` | Shared `tryDispatchExtensionCommand` helper: routing-step 9 (three-way decision: pi.dispatchCommand 0.71+ → server-routed via RPC keeper UDS when headless → stopgap). See change: fix-extension-slash-commands-in-dashboard, add-rpc-stdin-dispatch-with-keeper-sidecar. |
| `src/extension/ui-modules.ts` | Extension UI System Phases 1+2: `refreshUiModules(ctx)` partitions `probe.modules` by `kind` — `management-modal` → `ui_modules_list` (Phase 1, last-write-wins on duplicate `id`); `footer-segment`/`agent-metric`/`breadcrumb`/`gate`/`toast` → one `ext_ui_decorator` per descriptor (Phase 2, last-write-wins on `(kind,namespace,id)`; `namespace` validated `/^[a-z0-9-]+$/`; `removed: true` forwarded verbatim). `subscribeUiInvalidate(ctx)` throttles `ui:invalidate` re-probes to 20/sec (50 ms leading + trailing) + one warning per offending burst (`INVALIDATE_RATE_CAP_PER_SEC = 20`). `handleUiManagement(ctx, msg)` re-emits browser-originated `ui_management` on `pi.events` with `_reply` injected + forwards sync `data.items` as `ui_data_list`. Wired into `bridge.ts` at `session_start` + `onReconnect`. See changes: add-extension-ui-modal, add-extension-ui-decorations. |
