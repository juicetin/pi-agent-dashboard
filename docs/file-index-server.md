# File Index — Dashboard server

> Part of [pi-agent-dashboard file index](./file-index.md). Loaded on demand.
>
> **Change-history annotations** (e.g. *"See change: foo-bar"*) → OpenSpec changes archived under `openspec/changes/archive/`.
>
> **Update protocol**: see `AGENTS.md` → "Documentation Update Protocol".

| File | Purpose |
|------|---------|
| `packages/server/src/git-operations.ts` | Server-side git commands: branch listing, checkout, init, stash pop. Adds `readHead(cwd)` (returns `{branch \| detached \| sha, hasSubmodules}`), `listWorktrees(cwd)` (porcelain parse), `addWorktree(cwd, {branch, base?, path?})` with stable error codes `branch_in_use` / `branch_exists` / `base_not_found` / `path_exists` / `git_failed`. See change: add-worktree-spawn-dialog. Adds lifecycle ops: `removeWorktree`, `mergeWorktree` (`--no-ff` into resolved base), `pushBranch`, `createPullRequest` (via `gh`), `worktreeDiffStat`, `resolveDefaultBase` (origin/HEAD → develop → main → master). See change: add-worktree-lifecycle-actions. Exports `resolveMainPath(cwd)` (parent repo root via `git rev-parse --git-common-dir`); previously private. See change: harden-worktree-spawn. Adds `listPullRequests(cwd, ghPath)` (shells `gh pr list`, parses JSON, collapses statusCheckRollup → `checkRollup`), `addWorktreeFromPr(cwd, prNumber, path?)` (fetches `refs/pull/N/head`, creates worktree via `git worktree add -b pr-N`). See change: add-worktree-from-pull-request. `AddWorktreeOptions.newBranch` now optional. Omit newBranch = checkout mode: `git worktree add <path> <commit-ish>` (no `-b`); fork mode keeps `-b <newBranch>`. Checkout commit-ish = `base` when `refs/heads/<base>` exists, else bare `localNameOf(base)` to DWIM-create tracking branch from `origin/<x>` (passing `origin/foo` verbatim yields detached HEAD). Path slug + success `branch` = `localNameOf(base)` in checkout. `branch_in_use` message enriched with holding-worktree path parsed from stderr `already used by worktree at '<path>'`. Imports `localNameOf` from `./git-worktree.js`. See change: worktree-checkout-existing-branch. |
| `packages/server/src/git-worktree-lifecycle.ts` | Pure stderr→code mappers `mapRemoveStderr` / `mapMergeStderr` / `mapPushStderr` / `mapPrStderr` + `parsePrUrl` + `parseShortstat`. Drives stable error codes (`dirty_worktree`, `branch_not_merged`, `merge_conflict`, `non_fast_forward`, `auth_failed`, `gh_not_authed`, `pr_exists`, ...) emitted by `/api/git/worktree/*`. See change: add-worktree-lifecycle-actions. |
| `packages/server/src/active-sessions-in-cwd.ts` | Pure helpers `isPathInside(parent, child)`, `activeSessionsUnder(path, sessions)` (excludes `status === "ended"`), `sessionsUnder(path, sessions)` (includes ended). Case-folds per OS via shared `normalizePath`. Drives pre-removal `active_sessions` guard + `cwd_missing` broadcast loop. See change: add-worktree-lifecycle-actions. |
| `packages/server/src/__tests__/git-pr-operations.test.ts` | Tests for `listPullRequests` + `addWorktreeFromPr`. Mock gh scripts, fixture repos with simulated PR refs. See change: add-worktree-from-pull-request. |
| `packages/server/src/git-worktree.ts` | Pure helpers for worktree handling: `slugifyBranch(branch)` (path-safe slug), `parsePorcelainWorktrees(out)` (`git worktree list --porcelain` → records), `resolveDefaultBase(cwd, head)` (origin/HEAD → main → master → HEAD fallback), `ensureWorktreeExcludeLine(cwd)` (idempotent append of `.worktrees/` to `.git/info/exclude`). See change: add-worktree-spawn-dialog. Re-exports `localNameOf` from shared helpers (`localNameOf("origin/foo")==="foo"`, `localNameOf("foo")==="foo"`). See change: worktree-checkout-existing-branch. |
| `packages/server/src/hydration-metrics.ts` | Ring-buffer recorder for session-hydration timings. `createHydrationMetrics(capacity)` → `{ record(sample), snapshot() }`. Sample `{ sessionId, wallMs, fileBytes, entryCount, eventCount, at }`. `snapshot()` newest-first, capped at capacity. Process-local, no persistence. Shared by `directory-service` + `/api/health`. Server boots one instance capacity 20. See change: instrument-session-hydration-timing. |
| `packages/server/src/worktree-init.ts` | Worktree-init hook engine. `readInitHook(repoRoot)` parses `.pi/settings.json#worktreeInit` → `WorktreeInitHook \| null`, fail-open. `evaluateGate(cwd,hook)` spawns `bash -c gate`; needsInit true iff exit 0; spawn-error/timeout fail closed. `runInitHook(cwd,hook,onProgress)`: script runs `bash -c command` via ring-buffer(4KB)/throttle(250ms)/timeout(10min); agent spawns DETACHED headless pi (tool-registry `resolve("pi")`), output → `<cwd>/.pi/worktree-init.log`, awaits exit, re-evaluates gate (still needsInit ⇒ failed). `hookDefHash(hook)` sha256 over canonical key-sorted serialization. See change: generalize-worktree-init-hook. |
| `packages/server/src/worktree-init-errors.ts` | Pure `mapInitStderrToHint(stderr)`. Ordered regex table. Codes: EACCES, EBADENGINE/Unsupported engine, ETARGET, ENOTFOUND/ECONNRESET/ETIMEDOUT/EAI_AGAIN, ERR_PNPM_NO_LOCKFILE, npm ci lockfile drift. Null on no match. See change: generalize-worktree-init-hook. |
| `packages/server/src/worktree-init-registry.ts` | `createWorktreeInitRegistry({ttlMs?,sendTo?})` maps requestId → WebSocket. Browser subscribes via `worktree_init_subscribe` (server.ts); init route streams `worktree_init_progress`/`_done`/`_failed` only to that ws. Drops on ws close. Default TTL 15min. See change: generalize-worktree-init-hook. |
| `packages/server/src/worktree-init-trust.ts` | TOFU trust store. `isTrusted(repoRoot,hash)`/`recordTrust(repoRoot,hash)` keyed by `repoRoot + sha256(canonical(worktreeInit))`. Persists JSON `~/.pi/dashboard/worktree-init-trust.json`. Untrusted until recorded; hash change re-prompts. See change: generalize-worktree-init-hook. |
| `src/server/routes/plugin-config-routes.ts` | `POST /api/config/plugins/:id` — validates `:id`, validates body against `configSchema`, merges into `plugins.<id>.*`, atomic-writes, broadcasts `plugin_config_update`. Auth-gated. |
| `src/server/session-stats-reader.ts` | Reads cumulative stats + context usage from session JSONL files at startup |
| `src/server/server.ts` | Wires new `getOpenSpecGroupAssignments(cwd)` option from `openspecGroupStore` alongside existing `enrichOpenSpecData` (force-path); both read from same store. See change: offload-openspec-poll-to-worker. Persists `session.displayPrefsOverride` to `.meta.json` via `metaPersistence.setDisplayPrefsOverride` in `sessionManager.onChange`. Registers `preferences-display-routes`. Wires `metaPersistence` into `browser-gateway` deps. See change: configurable-chat-display. HTTP + WebSocket server (composes route modules + wiring). No bootstrap-state subscribe; `bootstrapState`, `bootstrapQueue`, `runPostInstallRepair`, `makeBootstrapTransitionHandler` removed under R3. Passes `requirementDeps.listInstalled` into `loadServerEntries` so plugins with `manifest.requires` get probed at load. On every `package_operation_complete` calls `refreshRequirementProbesFor(pluginIds)` + broadcasts `plugin_config_update` for plugins whose `missingRequirements` changed. See change: add-plugin-activation-ui. Calls `startTunnelWatchdog(deps)` after `createTunnel`; `stopTunnelWatchdog()` in graceful shutdown. `ServerConfig.tunnelWatchdog` plumbed from CLI. See change: tunnel-watchdog. See change: eliminate-electron-runtime-install. Builds `pluginPiHandlers` Map + `pluginRawEventSubs` Set; wires real `registerPiHandler(type, handler)`, `onEvent(handler)`, `sendToSession(sessionId, text)` (=`piGateway.sendToSession` send_prompt) into `ServerPluginContext`; passes `dispatchPluginPiMessage`/`dispatchPluginRawEvent` to `wireEvents`. See change: add-goal-continuation-plugin. onChange alive→ended keeps id, gated moveToFront when completedFirst (no remove). Resume/reattach keyed by resolveOrderKey. Startup reconcile delegates to reconcileSessionOrder. See change: simplify-session-card-ordering. Registers `preferences-worktree-init-routes`. See change: auto-init-worktree-on-spawn. createContext spawnSession hook gated priority<=100, enqueues pendingAutomationRunRegistry, spawns headless. Creates pendingAutomationRunRegistry, threads to wireEvents. See change: add-automation-plugin. |
| `src/server/routes/session-routes.ts` | REST routes: sessions, events, session-diff |
| `packages/server/src/routes/file-routes.ts` | REST routes for file read/browse. `/api/file` decodes leading `file://` on `path` via `decodeFileUri`. Absolute paths accepted but still gated by known-session-cwd + traversal check. See change: unify-file-link-openability. Adds GET /api/favorite-models → {labels}. See change: enrich-model-selector-capabilities-favorites. |
| `packages/server/src/routes/git-routes.ts` | REST routes: git branches, checkout, init, stash-pop. Adds `GET /api/git/head`, `GET /api/git/worktrees`, `POST /api/git/worktree` (body `{cwd, branch, base?, path?}`) forwarding structured `{code, stderr}` errors from `addWorktree`. See change: add-worktree-spawn-dialog. Adds `POST /api/git/worktree/{remove,merge,push,pr}` + `GET /api/git/worktree/diff-stat`. Localhost-gated. Takes `sessionManager` + `browserGateway` deps. `/remove` pre-flight calls `activeSessionsUnder` → returns `{code:"active_sessions", sessionIds}` when non-empty; on success broadcasts `cwdMissing:true` per session under removed path. See change: add-worktree-lifecycle-actions. Adds `GET /api/git/worktree/bootstrap-status?cwd=<path>` (returns `{needsBootstrap, reason}` per decision tree: `not_required` / `ok` / `no_node_modules` / `stale_lockfile`) and `POST /api/git/worktree/bootstrap` (body `{cwd, requestId?}`; runs install in existing worktree). `POST /api/git/worktree` body extended with optional `requestId`; runs `detectBootstrapRequirement(repoRoot)` + `runBootstrap(...)` post-add; streams `worktree_bootstrap_progress` / `_done` / `_failed` to requesting browser via `worktreeBootstrapRegistry`. Response shape gains `bootstrap: {ran, durationMs?, skippedReason?}`. New error code `bootstrap_failed`. Takes `worktreeBootstrapRegistry` dep. See change: harden-worktree-spawn. worktree-init endpoints: `GET /api/git/worktree/init-status` (cached gate eval → {hasHook,needsInit,trusted}), `POST /api/git/worktree/init` (TOFU-gated; untrusted → init_untrusted carrying {hook,hash}; runs hook, streams progress). `POST /api/git/worktree` no longer auto-runs init. Replaced bootstrap + bootstrap-status endpoints. See change: generalize-worktree-init-hook. Adds `GET /api/git/pull-requests?cwd=` (lists open PRs via `listPullRequests`, `networkGuard` + gh resolution) + `POST /api/git/worktree/from-pr` (body `{cwd, prNumber, path?}`; runs `addWorktreeFromPr`). See change: add-worktree-from-pull-request. `POST /api/git/worktree` body `newBranch` now optional (string when present, else checkout mode); base still required. See change: worktree-checkout-existing-branch. |
| `src/server/routes/file-routes.ts` | REST routes: file read, browse (with `detect=0\|1` opt-in classifier), browse-flags (bulk classifier), browse-mkdir, pinned-dirs. See change: split-browse-flags. Adds `GET /api/file/raw?cwd=&path=` (binary-safe streaming via `createReadStream`, `Range` support for video seek, `Content-Type` via `extToContentType`, cwd-allowlist + traversal gate) + `GET /api/file/render?cwd=&path=` (`asciidoctor` lazy singleton in `safe:"secure"` mode, `.adoc`/`.asciidoc` only, HTTP 400 on other extensions). See change: render-file-previews. GET /api/readme removed. See change: remove-project-readme-button. |
| `packages/server/src/lib/decode-file-uri.ts` | NEW. `decodeFileUri(value)` strips `file://` scheme + percent-decodes payload. Returns non-`file://` input and decode failures unchanged. See change: unify-file-link-openability. |
| `packages/server/src/lib/mime-types.ts` | Exports `extToContentType(ext: string): string`. Maps `.pdf .png .jpg .jpeg .gif .svg .webp .mp4 .webm .mov .html .htm .txt .md .adoc .asciidoc`. Case-insensitive. Default `application/octet-stream`. See change: render-file-previews. |
| `packages/server/src/view-message-store.ts` | `ViewMessageStore` class. Per-session JSON store at `~/.pi/dashboard/view-messages/<sid>.json`. Methods `get(sid) / append(sid, target) / remove(sid)`. Sanitizes sid for filename (strips `..`, `/`). Separate from events.jsonl so agent never observes `/view` rows. Injected into `BrowserHandlerContext.viewMessageStore`. See change: render-file-previews. |
| `packages/server/src/__tests__/faux-session.integration.test.ts` | Spawns real `pi --mode rpc` subprocess driven by faux fixture + bridge extension. Connects in-process dashboard server. Drives prompts via REST `/api/session/:id/prompt`. Asserts events on browser `/ws`: assistant text stream, status busy→idle, usage `stats_update`, run lifecycle `agent_start`/`agent_end`, abort (slow-stream + `/abort` → run cleared), model error (`stopReason:"error"`), 2-session isolation, ask_user answer round-trip (`prompt_response` over `/ws` → faux factory echoes answer). Guards behind pi-on-PATH probe → `describe.skip` when pi absent. See change: add-faux-model-integration-tests. |
| `packages/server/src/__tests__/file-raw-render-endpoints.test.ts` | 18 tests: `extToContentType` mapping, `/api/file/raw` (cwd-allowlist, traversal, MIME, 404, Range 206/416), `/api/file/render` (extension rejection, AsciiDoc convert, secure-mode neutralizes `include::/etc/passwd[]`). See change: render-file-previews. |
| `packages/server/src/__tests__/view-message-store.test.ts` | 7 tests: append/get/persist/remove + sid sanitization + architectural-isolation grep proving `view-message-store` is not imported by `packages/extension/src/` or `pi-gateway.ts`. See change: render-file-previews. |
| `packages/server/src/routes/openspec-routes.ts` | REST routes: openspec-archive, pi-resources, pi-resource-file. Adds POST /api/openspec/config (core→CLI preset, expanded/custom→atomic JSON write, busts configCache), POST /api/openspec/update (per-cwd or all known cwds, records signature), GET /api/openspec/update-status (up-to-date|needs-update|unknown). knownCwds = union(session cwds, pinned dirs). GET /api/openspec/config reads via `configListOrAsync` (async, non-blocking). `currentGlobalSignature` async + computed ONCE per request (profile is global). GET /api/openspec/update-status + POST /api/openspec/update signature step no longer spawn `openspec config list` per-cwd — was ~11s blocking for 11 projects, starved concurrent config GET. See changes: add-openspec-profile-settings, fix-openspec-profile-load-race. |
| `packages/server/src/openspec-group-store.ts` | OpenSpec group store. Persists groups + assignments + `changeOrder: Record<groupId, changeName[]>` in groups.json (groupId or `__ungrouped__` sentinel via `OPENSPEC_UNGROUPED_KEY`). `createOpenSpecGroupStore(opts)` returns store with CRUD + `setChangeOrder(cwd, groupId, order[])` mutator. Subscriber payload + broadcast carry `changeOrder`. See change: add-openspec-change-grouping. See change: redesign-openspec-board. |
| `packages/server/src/routes/openspec-group-routes.ts` | REST routes for `/api/openspec/groups` CRUD + assignment. Adds PUT `/api/openspec/groups/change-order` (body `{groupId, order:string[]}`) → `store.setChangeOrder`. See change: add-openspec-change-grouping. See change: redesign-openspec-board. |
| `packages/server/src/routes/plugin-activation-routes.ts` | REST routes: `GET /api/plugins` (returns `PluginStatus[]` with `displayName`, `requirements`, `missingRequirements`) + `POST /api/plugins/:id/toggle` (writes `plugins.<id>.enabled` via config-api, broadcasts `plugin_config_update`). Auth-gated. Effective at next restart (compared client-side against `/api/health.startedAt`). See change: add-plugin-activation-ui. |
| `src/server/routes/system-routes.ts` | REST routes: config, health, shutdown, tunnel, editors. `/api/tunnel-connect` calls `startTunnelWatchdog` after `createTunnel`; `/api/tunnel-disconnect` calls `stopTunnelWatchdog` before `deleteTunnel`. See change: tunnel-watchdog. `PUT /api/config` live-reloads watchdog when `partial.tunnel` present + tunnel active: stop + start with new config. No restart for watchdog tweaks. Restart-required flag unchanged for tunnel section. See change: tunnel-watchdog-settings-ui. |
| `src/server/event-wiring.ts` | Pi gateway → browser gateway event forwarding (replay suppression via `skipReplayInsert` dedup, flows refresh dedup, context usage extraction). Phase-1 Extension UI: caches `ui_modules_list` on `Session.uiModules` + broadcasts; caches `ui_data_list` on `Session.uiDataMap[event]` with per-event item cap (default 1000, last-write-wins) + broadcasts. Phase-2: `ext_ui_decorator` arm caches descriptors under `` Session.uiDecorators[`${kind}:${namespace}:${id}`] `` (upsert, or delete when `removed: true`) + broadcasts verbatim; deleting absent key = no-op but still broadcasts. **Last-activity stamping** (change: session-card-last-activity-badge): every live (non-replay) `event_forward` whose `eventType` passes `isActivityEvent(...)` (`event-status-extraction.ts` allowlist: `prompt_send`, `message_*`, `turn_end`, `tool_execution_*`, `agent_*`, `bash_output`, `flow_*`, `architect_*`) updates `session.lastActivityAt = Date.now()` in memory + broadcasts ≤ 1×/30 s/session via `lastActivityBroadcastAt: Map<sessionId, ms>`; map entry dropped on `session_unregister` so re-register doesn't silently suppress its first broadcast. See changes: add-extension-ui-modal, add-extension-ui-decorations, session-card-last-activity-badge. **Unread-trigger evaluation** (change: session-card-unread-stripes): right after `extractSessionUpdates`, snapshots `{status, currentTool}` before/after + calls `isUnreadTrigger(eventType, before, after, payload)`; if true AND `viewedSessionTracker.isViewedByAnyone(sessionId) === false` AND `!replayingSessions.has(sessionId)`, stamps `session.unread = true` + broadcasts `session_updated`. `viewedSessionTracker` optional on `EventWiringDeps` for back-compat — wiring opt-in. **Rename-site defense-in-depth** (change: fix-uuid-rename-bug): auto-attach branch re-validates `detected.changeName` via `isValidOpenSpecChangeSlug` from shared detector module; predicate failure skips entire `activityUpdates` + auto-attach + `rename_session` block. Second regression of the same class as fix-openspec-flag-rename-bug justified the duplication. Manual paths (`session-meta-handler.ts`, REST `/attach-proposal`) intentionally unchanged. See change: fix-uuid-rename-bug. See change: fix-providers-list-spurious-models-refreshed — `providers_list` handler at line 628 reads `{changed}` from `setCatalogueForSession`, broadcasts `models_refreshed` only when `changed===true`. Identical re-pushes (browser subscribe → `request_providers` round-trip, `session_register`, reconnect, fork/resume) become silent. Eliminates global `modelsMap` wipe that left previously-visited sessions with disabled model selector. See change: simplify-model-selection-channels — supersedes fix-providers-list-spurious-models-refreshed: `models_refreshed` broadcast removed from `providers_list` handler entirely. Cache write via `setCatalogueForSession(sid, providers)` (void return). Handles `cwd_missing` extension message: `sessionManager.update({ cwdMissing: true })` + `broadcastSessionUpdated`. See change: add-worktree-lifecycle-actions. See change: fix-dashboard-spawn-correlation-by-token. Forwards followup_pulled from bridge to subscribers; caches no state (buffer mutation already broadcast via queue_update). See change: rework-mid-turn-prompt-queue. `process_list` handler builds pidIndex via `buildPidIndex(sessionManager.listActive())`, classifies entries via `classifyProcesses`, stores enriched entries on session (late-subscriber replay via sessions_snapshot) + forwards enriched `process_list_update`. See change: classify-process-list-entries. `piGateway.onEvent` dispatches `plugin_pi_message` to registered handlers by `messageType` and fans every `event_forward` out to `onEvent` subscribers via new `dispatchPluginPiMessage`/`dispatchPluginRawEvent` deps. See change: add-goal-continuation-plugin. `insert` keyed by `resolveOrderKey`. Gated agent_end/ask_user arms call moveToFront via `isCompletedFirst()`/`isQuestionFirst()` getters; broadcast only on real reorder. See change: simplify-session-card-ordering. Third auto-attach branch: MANUAL attachment with active OpenSpec change differing from `attachedProposal` sets `session.pendingReplaceProposal` (coalesce — latest wins, same name no-op, rejected name ignored) + broadcasts. Module helper `openSpecChangeExistsInCache(directoryService, cwd, changeName)` powers deleted-proposal bypass: attached proposal absent from poll cache → auto-attach directly, no dialog. `agent_end` handler clears `pendingReplaceProposal` + `rejectedReplaceProposals`. See change: replace-proposal-dialog-with-race-handling. onSessionRegistered automation-run arm consumes pendingAutomationRunRegistry → stamps kind="automation"+automationRun, persists .meta.json. See change: add-automation-plugin. |
| `src/server/idle-timer.ts` | Auto-shutdown idle timer with sleep-wake resilience |
| `src/server/session-bootstrap.ts` | Startup session discovery + OpenSpec polling init. OpenSpec broadcast callback now receives optional `serialized` from `startPolling`. Present → `browserGateway.broadcastOpenSpecUpdate(cwd, serialized)` (envelope built by concat, no re-stringify). Absent (pending/disabled emits) → falls back to `broadcastToAll({type:"openspec_update", cwd, data})`. See change: offload-openspec-poll-to-worker. |
| `src/server/pi-gateway.ts` | Extension WebSocket gateway (port 9999) |
| `src/server/browser-gateway.ts` | New `case "inject_view_message"`: appends to `ViewMessageStore` + broadcasts `view_messages_update` to every subscriber of session. Constructs `ViewMessageStore` (default instance) and threads into `BrowserHandlerContext.viewMessageStore`. See change: render-file-previews. Browser WebSocket gateway (dispatches to handler modules). **On-connect snapshot** (change: fix-stale-sessions-on-reconnect): single `sessions_snapshot` (built from `sessionManager.listAll()` + `sessionOrderManager.getAllOrders()` filtered to non-empty arrays) replaces prior per-session `session_added` loop + per-cwd `sessions_reordered` loop. Emitted before `pinned_dirs_updated` / `openspec_update` / `terminal_added`. Live updates after snapshot still use incremental messages. See change: honest-mid-turn-queue-surface. Six switch arms (clear_steering_queue, clear_followup_slot, edit_followup_slot, promote_followup_entry, remove_followup_entry, edit_followup_entry) deleted; matching handler imports removed. See change: rework-mid-turn-prompt-queue. Added 5 case arms routing follow-up mutation to bridge. Accepts `metaPersistence` in deps; threads into `BrowserHandlerContext`. Dispatches `setSessionDisplayPrefs` WS message to `handleSetSessionDisplayPrefs`. See change: configurable-chat-display. Wires `ctx.broadcastEvent`; dispatches `open_inline_terminal`/`close_inline_terminal`. See change: add-inline-terminal-card. Routes favorite_model/unfavorite_model; sends favorite_models_updated snapshot on browser connect. See change: enrich-model-selector-capabilities-favorites. Dispatcher wires `accept_replace_proposal` → handleAcceptReplaceProposal, `dismiss_replace_proposal` → handleDismissReplaceProposal. See change: replace-proposal-dialog-with-race-handling. `broadcast()` serializes payload once per fan-out (O(payload), not O(payload × subscribers)); preserves readyState + MAX_WS_BUFFER guards. See change: scope-openspec-poll-to-active-cwds. New `broadcastOpenSpecUpdate(cwd, dataSerialized)` builds envelope by string concat (`'{"type":"openspec_update","cwd":' + JSON.stringify(cwd) + ',"data":' + dataSerialized + '}'`); skips re-stringify of large `data`. Shared `fanout(serialized)` helper mirrors `broadcast()` back-pressure + `readyState === OPEN` + `MAX_WS_BUFFER` guards. See change: offload-openspec-poll-to-worker. |
| `packages/server/src/__tests__/browser-gateway-broadcast-serialize-once.test.ts` | Asserts `broadcast()` serializes once per fan-out, all open sockets receive identical frame, buffer-full + closed sockets skipped. See change: scope-openspec-poll-to-active-cwds. |
| `packages/server/src/__tests__/browser-gateway-load.test.ts` | Broadcast load harness scenario matrix A–E. Drives real gateway against DrainingFakeWs under virtual clock. Reproduces head-of-line blocking: focused `event` waits behind cross-cwd `openspec_update`. Asserts focused-event timeToFlush, wastedBytes>0 (cross-cwd leak), dropped frames via MAX_WS_BUFFER, peak buffer, periodic-vs-flat latency signature. Test-only. See change: add-ws-broadcast-load-harness. |
| `packages/server/src/__tests__/browser-gateway-snapshot-on-connect.test.ts` | Pins exactly-one `sessions_snapshot` per connect; alive+ended sessions both present; empty `orders` arrays filtered; snapshot ordered before `pinned_dirs_updated`. See change: fix-stale-sessions-on-reconnect. |
| `packages/server/src/__tests__/directory-service-known-dirs.test.ts` | Asserts `knownDirectories()` = pinned ∪ non-ended-session cwds; ended + hidden-ended cwds excluded; pinned-with-ended kept; idle/streaming kept. See change: scope-openspec-poll-to-active-cwds. |
| `packages/server/src/__tests__/draining-ws.test.ts` | Unit tests for DrainingFakeWs primitive. Byte accounting, clamp-at-0 drain, FIFO timeToFlush head-of-line, metadata parse, readyState/close surface, peak buffer. See change: add-ws-broadcast-load-harness. |
| `packages/server/src/__tests__/helpers/draining-ws.ts` | `createDrainingWs({ drainRateBytesPerMs, readyState? })`. Timing-aware fake WebSocket. `send` fills `bufferedAmount` by frame bytes + records frame. `advance(ms)` drains at rate, clamps 0. `drainFully()` clears buffer. `timeToFlush(pred)` = bytesAtEnqueue/rate (pure FIFO). Drop-in for makeFakeWs. See change: add-ws-broadcast-load-harness. |
| `packages/server/src/__tests__/helpers/load-fixtures.ts` | Load-harness builders. `seedSessions` populates MemorySessionManager across cwds. `makeOpenSpecPayload(sizeBytes)` pads valid OpenSpecData to target serialized size. `attachClients` emits connection for N draining sockets. `subscribeWs` connects+subscribes focused socket. `DRAIN_FAST`/`DRAIN_SLOW` presets (illustrative, not calibrated). See change: add-ws-broadcast-load-harness. |
| `packages/server/src/__tests__/openspec-poll-worker.test.ts` | Parity test: `deriveAndSerialize` output matches in-process derivation byte-for-byte; `serialized === JSON.stringify(data)`. Group join shape mirrors `joinGroupIdsToOpenSpecData`. Cache-hit reuses cached artifacts when `gateEnabled && cached.mtimeMs === preCallMtime`. Pool falls back in-process when `useWorker=false` and when worker URL unresolvable. With `useWorker=true` under vitest falls back; production spawns real worker. See change: offload-openspec-poll-to-worker. |
| `packages/server/src/openspec-poll-fs-helpers.ts` | Pure FS helpers extracted from `directory-service.ts` so worker imports without pulling SessionManager / PreferencesStore coupling. Exports `statMtimeOr`, `effectiveMtimeOr`, `perChangeArtifactPaths`. `directory-service.ts` re-exports `effectiveMtimeOr` for back-compat. See change: offload-openspec-poll-to-worker. |
| `packages/server/src/openspec-poll-worker-pool.ts` | `createOpenSpecPollWorkerPool({size?, timeoutMs=10_000, useWorker=true, workerUrlOverride?})`. Fixed slots = `max(1, min(maxConcurrentSpawns, os.cpus().length))`. Lazy spawn per slot via `new Worker(url, {execArgv: process.execArgv})` so jiti hook propagates. FIFO queue when slots busy. Per-request timeout terminates worker + falls back in-process. `error` / non-zero `exit` marks slot dead; lazy respawn. `useWorker=false` → always in-process. Spawn-fail flips `workersDisabled = true` permanently. `dispose()` drains queue in-process then terminates workers. `process()` always resolves — tick never drops broadcast. See change: offload-openspec-poll-to-worker. |
| `packages/server/src/openspec-poll-worker.ts` | Pure `deriveAndSerialize(req): {cwd, data, serialized, stampMtimes, racyNames}` + `parentPort` bootstrap. Worker computes pre + post call mtimes; reuses cached artifacts on `gateEnabled && cached.mtimeMs === preCallMtime`; runs `deriveArtifactStatus`; re-stats for TOCTOU and marks racy on mismatch; calls `buildOpenSpecData`; applies optional `groupId` join inline (`joinGroupIdsToOpenSpecData` shape); `JSON.stringify`s data in-worker so main loop never stringifies large payload. See change: offload-openspec-poll-to-worker. |
| `packages/server/src/session-load-worker-pool.ts` | Session-load worker pool. Fixed slots = `max(1, min(maxConcurrentSpawns, os.cpus().length))`; FIFO queue when slots busy; per-request timeout default `30000ms`; lazy spawn per slot via `new Worker(url, {execArgv: process.execArgv})` so jiti hook propagates. `load(req)` returns `{jobId, result}`. `cancel(jobId)`: queued job dropped, resolves `"cancelled"`; in-flight job abandoned, result discarded on arrival, worker NOT terminated (only timeout/crash terminates slot). Fallback: spawn fail / crash / non-zero exit / timeout → in-process `loadAndReplay`; `result` never rejects. `useLoadWorker=false` → always in-process, no spawn. Copies `openspec-poll-worker-pool` scaffold, not extracted. See change: offload-session-events-load-to-worker. |
| `packages/server/src/session-load-worker.ts` | Pure `loadAndReplay(req): {jobId, success, events, error, entryCount?}` + `parentPort` bootstrap. Runs `loadSessionEntries` (JSONL parse + tree-walk) and `replayEntriesAsEvents(...).map(m => m.event)` projection IN-WORKER; only final `events` array crosses thread boundary. Tests + fallback import function directly. `events` bytes identical to in-process projection; parity test enforces (`__tests__/session-load-worker.test.ts`). See change: offload-session-events-load-to-worker. |
| `src/server/browser-handlers/handler-context.ts` | Shared context type for browser message handlers. Adds optional `metaPersistence?: MetaPersistence` so `handleSetSessionDisplayPrefs` can write `.meta.json` overrides. See change: configurable-chat-display. Adds optional `broadcastEvent(sessionId, seq, event)` to BrowserHandlerContext. See change: add-inline-terminal-card. |
| `src/server/browser-handlers/subscription-handler.ts` | On subscribe, sends `view_messages_update` snapshot from `ViewMessageStore.get(sid)` BEFORE event replay so client merges view rows on cold-open. See change: render-file-previews. Subscribe/unsubscribe with async batched replay, backpressure, lazy loading. Exports `replayUiState(ws, sessionId, ctx)` for Extension UI System; called immediately after every `replayPendingUiRequests` site (4 sites). Replay sends cached `ui_modules_list` (Phase 1) → one `ui_data_list` per `(event, items)` entry (Phase 1) → one `ext_ui_decorator` per `Session.uiDecorators` cache entry (Phase 2; never with `removed: true` since deleted entries absent). Replay ordering: events → pending UI requests → ui_modules_list → ui_data_list → ext_ui_decorator. See changes: add-extension-ui-modal, add-extension-ui-decorations, fix-cold-subscribe-replay-interleave. Unsubscribe cancels in-flight hydration when last subscriber leaves (`getSubscribers(sessionId).length === 0` → `cancelLoad(sessionId)`); treats load error `"cancelled"` as no-op (no `dataUnavailable`, no replay). See change: offload-session-events-load-to-worker. |
| `src/server/browser-handlers/session-action-handler.ts` | Send prompt, abort, resume, spawn, shutdown, force kill, flow control. `handleSpawnSession` accepts optional `SpawnSessionBrowserMessage.attachProposal` + enqueues into `pendingAttachRegistry` BEFORE awaiting `spawnPiSession(...)` so fast `session_register` cannot lose intent (change: add-folder-task-checker-and-spawn-attach). `handleForceKill` delegates SIGTERM→wait→SIGKILL escalation to `killProcess` from `platform/process.ts` so Windows gets `taskkill /F /T /PID` (genuine tree kill). No direct `process.kill()` anywhere (enforced by `no-direct-process-kill.test.ts`). `handleSendPrompt` intercepts `/reload` on headless sessions (gated by `headlessPidRegistry.getPid`) + delegates to `handleHeadlessReload` — SIGTERMs pi + respawns with `--session <file> --mode continue`. Pi 0.68.0 has no extension-accessible reload in RPC mode, so kill-and-respawn is only way to reload settings/extensions/skills for headless. See changes: route-kill-paths-through-platform, headless-reload-via-respawn. See change: honest-mid-turn-queue-surface. handleClearSteeringQueue / handleClearFollowupSlot / handleEditFollowupSlot / handlePromoteFollowupEntry / handleRemoveFollowupEntry / handleEditFollowupEntry deleted; matching wire types removed. See change: rework-mid-turn-prompt-queue. Added 5 forwarders (handleClearFollowupEntries, handleEditFollowupEntry, handleRemoveFollowupEntry, handlePromoteFollowupEntry, handlePullFollowupToEditor). Each validates sessionManager.get + forwards via piGateway.sendToSession. |
| `src/server/browser-handlers/session-action-helpers.ts` | Pure helpers for session-action-handler. `shouldInterceptReload(msg, headlessPidRegistry)` gates headless-reload interception: exact `/reload` text, no images, PID tracked in registry. Extracted for testability. |
| `src/server/browser-handlers/session-meta-handler.ts` | Adds `handleSetSessionDisplayPrefs(msg, ctx)` — routes `setSessionDisplayPrefs` WS message: updates `session.displayPrefsOverride` via `sessionManager.update`, calls `ctx.metaPersistence?.setDisplayPrefsOverride(sessionFile, override)` (`null` clears), broadcasts `display_prefs_updated`. See change: configurable-chat-display. Rename, hide, unhide, attach/detach proposal, fetch, list. `handleAcceptReplaceProposal` validates changeName matches `pendingReplaceProposal` or `attachedProposal`, reuses `applyAttachProposal`, clears pending. `handleDismissReplaceProposal` appends changeName to `rejectedReplaceProposals` deduped, clears pending. `handleDetachProposal` clears `pendingReplaceProposal` + `rejectedReplaceProposals` in broadcast updates. See change: replace-proposal-dialog-with-race-handling. Attach/detach apply **idempotent auto-rename rule** via pure helpers in `packages/server/src/proposal-attach-naming.ts`: attach renames when name empty OR equals current `attachedProposal` (auto-set witness); detach reverts name only when that equality holds. Same helpers reused by REST endpoints in `session-api.ts` to keep WS + REST in lockstep. See change: fix-mobile-attach-proposal-display. hide → moveToFront(resolved key)+broadcast (top hidden tier); unhide → clear hidden + moveToFront (top ended tier). See change: simplify-session-card-ordering. |
| `src/server/proposal-attach-naming.ts` | Pure decision-matrix helpers `attachRenameTarget(session, changeName)` + `detachShouldClearName(session)` used by both WS handler + REST endpoint to compute idempotent auto-rename / auto-revert. Equality witness: `name === attachedProposal`. See change: fix-mobile-attach-proposal-display. |
| `src/server/browser-handlers/terminal-handler.ts` | Create, kill, rename terminals. `handleOpenInlineTerminal` spawns ephemeral PTY, broadcasts terminal_added, inserts+broadcasts inline_terminal_open. `handleCloseInlineTerminal` captures transcript, kills PTY, inserts+broadcasts inline_terminal_close. See change: add-inline-terminal-card. |
| `src/server/browser-handlers/directory-handler.ts` | Pin/unpin dirs, reorder, openspec refresh, pi-gateway forwards Adds handleFavoriteModel/handleUnfavoriteModel (persist via preferencesStore, broadcast favorite_models_updated). See change: enrich-model-selector-capabilities-favorites. |
| `src/server/memory-event-store.ts` | In-memory event buffer with LRU eviction, per-session cap, payload truncation |
| `src/server/memory-session-manager.ts` | Pure in-memory session registry |
| `packages/server/src/pending-automation-run-registry.ts` | FIFO-per-cwd registry of automation-run stamps {name,runId,visibility}. Enqueued by automation-plugin spawn hook; consumed in event-wiring onSessionRegistered to stamp kind="automation"+automationRun + persist .meta.json. TTL 60s, cap 8. See change: add-automation-plugin. |
| `src/server/pending-attach-registry.ts` | In-memory FIFO queue of pending `attachProposal` intents per cwd. `enqueue(cwd, changeName)` called from `handleSpawnSession` when browser sets `SpawnSessionBrowserMessage.attachProposal`; `consume(cwd)` called from `event-wiring.ts`'s `pi-gateway.onSessionRegistered` hook on every `session_register`, resolved name applied via shared `applyAttachProposal(sessionId, changeName, ctx)` helper in `session-meta-handler.ts` (same code as `handleAttachProposal`). Per-cwd cap = 8 (silent drop + warn), 60 s TTL on every read/write to avoid stranded intents from failed spawns. Cwd normalized via `safeRealpathSync` + trailing-sep strip so `/p`, `/p/`, symlink variants share queue. In-memory only — not persisted. See change: add-folder-task-checker-and-spawn-attach. |
| `src/server/openspec-archive.ts` | Scans `openspec/changes/archive/` + returns structured ArchiveEntry list |
| `src/server/openspec-change-watcher.ts` | Per-cwd `fs.watch(<cwd>/openspec/changes/, {recursive:true})`. Debounced 300 ms. Filter regex matches `tasks.md`, `proposal.md`, `design.md`, `specs/**/*.md`. Fires `onChange(cwd)` → DirectoryService `pollOne(cwd,false)`. Trigger only; no gate bypass. Fail-silent on ENOENT/EMFILE/EACCES — periodic poll covers correctness. `attach`/`detach`/`detachAll`/`size`. See change: fix-openspec-taskcheck-delay. |
| `src/server/browse.ts` | Directory listing + classification for browse API. Two responsibilities kept deliberately separate: `listDirectories(dir, q, { detect })` does enumeration (cheap, single `readdir`; per-entry `.git`/`.pi` probes only when `detect: true`); `classifyPaths(paths)` does bulk classification (≤ `MAX_FLAG_PATHS = 100`, `fs.access` fan-out bounded by `createSemaphore(32)`, any error → `{ isGit: false, isPi: false }`). `parseFlagsQuery(rawPaths)` validates `paths=<json-array>` query for `GET /api/browse/flags`. Worktree-safe: detection uses `fs.access` (never `readdir`) so `.git` regular-files in worktrees still classify correctly. See change: split-browse-flags |
| `src/server/pi-resource-scanner.ts` | Discovers pi extensions, skills, prompts from local, global, package sources |
| `src/server/package-manager-wrapper.ts` | Thin adapter around pi's `DefaultPackageManager` with operation serialization, progress forwarding, session reload; delegates module resolution to `ToolRegistry.resolveModule("pi-coding-agent")`. Includes `move(req)` for scope-to-scope package moves: hybrid execution — npm/git/https sources reinstall at dest then remove from origin (single `busy` lock, coalesced reload), filesystem-path sources settings-edit only (uses `settingsManager.getGlobalSettings/getProjectSettings/setPackages/setProjectPackages` to preserve filter objects). Identity preflight via `computeIdentity` before any side-effect; partial-success (install OK, remove failed) delivered through `package_operation_complete` WS event's `partialSuccess` field. See change: unify-package-management-ui. |
| `src/server/package-source-helpers.ts` | Pure helpers `parseSourceKind(source)` (npm/git/https/abs-path/rel-path) + `computeIdentity(source, settingsDir?)` (npm = bare name, git/https = url-no-ref, path = resolved absolute) mirroring pi's source-kind + dedup rules from `docs/packages.md`. Used by `package-manager-wrapper`'s `move()` arm-selection + identity preflight; safe for cross-platform tests (no `process.platform` branching). See change: unify-package-management-ui. |
| `packages/server/src/routes/tool-routes.ts` | REST routes: `GET /api/tools`, `GET /api/tools/:name`, `POST /api/tools/rescan`, `PUT/DELETE /api/tools/:name`, `POST /api/tools/diagnostics` (text/plain export). `GET /api/tools` rows carry `installHints` (`ToolListEntry[]`). See change: register-bash-and-tool-install-help. |
| `packages/server/src/routes/doctor-routes.ts` | Fastify plugin. `GET /api/doctor` returns `{checks, summary, generatedAt}`. Calls `runSharedChecks` with server deps. Auth-gated identically to `/api/config`. Top-level `try/catch` returns 200 fallback row on internal throw (never 500). Omits Electron-only rows. Tunnel section wired via `resolveZrokBinary` (delegates to `getDefaultRegistry().resolve("zrok")` so diagnostic matches Settings ▸ Tools) and `getTunnelWatchdogStatus`. See change: harvest-bootstrap-survivor-fixes, add-tunnel-diagnostic-checks. |
| `packages/server/src/changelog-fs.ts` | `findChangelogPath(pkg, opts)` resolves CHANGELOG.md (managed > bare-import > filesystem-walk). `readPackageJson(pkg)`. `deriveChangelogUrl(pkgJson)` from `repository` `github:` / `https` / object / monorepo `directory`. Strategy 3 filesystem-walk: walks up `node_modules` from `import.meta.url` (`opts.moduleUrl` overridable for tests) looking for `node_modules/<pkg>/CHANGELOG.md`. Needed because `@earendil-works/pi-coding-agent` exports only `"."` (no `"./package.json"`) — Strategy 2 `require.resolve` throws in CLI/global-npm/dev deployments. See change: pi-update-whats-new-panel. |
| `packages/server/src/changelog-parser.ts` | `parseChangelog(text)` Keep-a-Changelog regex parser. `readAndParseChangelog(path)` mtime-keyed 60s cache. `invalidateChangelogCache()` called from `PiCoreChecker.invalidate`. See change: pi-update-whats-new-panel. |
| `packages/server/src/pi-version-skew.ts` | Pi compatibility range reader. `readPiCompatibility` reads `piCompatibility` from `packages/server/package.json`. `readCurrentPiVersion` via `createRequire` + `fs.realpathSync` on registry-resolved bin path. Pure helpers: `parseVersion`, `compareVersions`, `isBelow`, `isAbove`, `computeCompatibility`. `BootstrapCompatibility` interface kept inline (return type for the pure helpers). `updateBootstrapCompatibility` + cache + CLI warning removed under R3. See change: eliminate-electron-runtime-install. |
| `src/server/npm-search-proxy.ts` | Cached proxy for npm registry search (`keywords:pi-package`) + README fetch |
| `src/server/routes/package-routes.ts` | REST routes: search, readme, installed, install, remove, update, check-updates |
| `src/server/preferences-store.ts` | Global UI preferences (pinned dirs, session order) in `preferences.json`. Extends `PreferencesData` with optional `displayPrefs?: DisplayPrefs`; exposes `getDisplayPrefs()` + `setDisplayPrefs(partial)` (deep-merges `toolCalls`). `undefined` until first PATCH — drives client first-launch modal. See change: configurable-chat-display. Adds favoriteModels:string[] + getFavoriteModels/setFavoriteModels/addFavoriteModel/removeFavoriteModel; persisted to preferences.json; mirrors pinnedDirectories. See change: enrich-model-selector-capabilities-favorites. Adds `openspecUpdateSignatures` map + `getOpenSpecUpdateSignature`/`setOpenSpecUpdateSignature` accessors. See change: add-openspec-profile-settings. Adds optional `autoInitWorktreeOnSpawn?:boolean` (absent → false) + `getAutoInitWorktreeOnSpawn()`/`setAutoInitWorktreeOnSpawn(value)`; persists to preferences.json. See change: auto-init-worktree-on-spawn. Seeds pinnedDirectories from PI_DASHBOARD_PIN_DIRS env first-run-only, gated by pinSeeded marker; UI edits win. See change: docker-packaging. |
| `packages/server/src/routes/preferences-display-routes.ts` | REST routes `GET /api/preferences/display` (returns `{ global: DisplayPrefs|undefined, sessionOverrides: Record<sessionId, Partial<DisplayPrefs>> }`) + `PATCH /api/preferences/display` (deep-merges `toolCalls` via `preferences-store.setDisplayPrefs`, broadcasts `display_prefs_updated`). Auth-gated. See change: configurable-chat-display. |
| `packages/server/src/routes/preferences-worktree-init-routes.ts` | REST routes `GET /api/preferences/worktree-auto-init` (returns `{autoInitWorktreeOnSpawn:boolean}`) + `PATCH /api/preferences/worktree-auto-init` (body `{value:boolean}` → `preferences-store.setAutoInitWorktreeOnSpawn`). Stores flag only; trusted auto-trigger lives client-side. See change: auto-init-worktree-on-spawn. |
| `src/server/meta-persistence.ts` | Per-session debounced `.meta.json` writer. Adds `setDisplayPrefsOverride(sessionFile, override | null)`; passing `null` deletes the field (revert to global). Triggers same debounce as other meta writes. See change: configurable-chat-display. |
| `src/server/session-scanner.ts` | Restores `displayPrefsOverride` from `SessionMeta` onto `DashboardSession` so per-session chrome overrides survive server restart. See change: configurable-chat-display. Startup session discovery by scanning `~/.pi/agent/sessions/`. Each restored `DashboardSession` has `lastActivityAt` cold-start-seeded from `events.jsonl` mtime (one `fs.statSync` per session, defensive try/catch — returns `undefined` on error so badge falls back to `startedAt`). See change: session-card-last-activity-badge. Restored session also carries `unread` from `meta.unread` so unread sessions stay flagged across server restart. `server.ts:273-279` cold-start "force status=ended" override only mutates `status` + `endedAt`, leaving `unread` intact — session unread when server stopped is still unread when it starts back up, even before its bridge reattaches. See change: session-card-unread-stripes. Probes `existsSync(cwd)` for every ended session at boot; stamps `cwdMissing:true` when gone. See change: add-worktree-lifecycle-actions. |
| `src/server/migrate-persistence.ts` | One-time migration from `sessions.json` + `state.json` → `.meta.json` |
| `src/server/session-order-manager.ts` | Per-cwd session ordering with persistence. **Move-to-front semantic** (change: top-of-tier-on-status-change): exports `moveToFront(cwd, sessionId)` — idempotent `remove + unshift` used by `server.ts onChange`'s ended→alive user-intent branch so just-resumed card always surfaces at top of alive tier, even on repeated end→resume cycles. Bridge auto-reattach short-circuits before any mutation via `pendingResumeIntents.consume()`. Order map now holds ALL-status ids (active+ended+hidden). Callers key by resolved group path (not raw cwd). alive→ended no longer calls remove(). See change: simplify-session-card-ordering. `DashboardServer` exposes `directoryService` (test-support, like `pendingDashboardSpawns`). See change: replace-proposal-dialog-with-race-handling. |
| `src/server/dashboard-source-decision.ts` | Pure `decideDashboardSource({dashboardSpawned, pendingCount, isNewSession, strictCorrelation})` → `{shouldStamp, consumeLegacyCounter, persistMeta}`. Strong signal (`msg.dashboardSpawned` from `PI_DASHBOARD_SPAWN_TOKEN` env) → stamp + `persistMeta=true` (sidecar written). Cwd-FIFO legacy fallback → stamp only, `persistMeta=false` (no sidecar). `STRICT_SPAWN_CORRELATION=1` suppresses fallback branch entirely. Consumed by `event-wiring.ts`. See changes: fix-dashboard-source-mislabelling, fix-dashboard-spawn-correlation-by-token. |
| `src/server/directory-service.ts` | Server-side session discovery, event loading, OpenSpec polling. Mtime-gated per-directory cache (`DirCache`), shared FIFO semaphore, deterministic per-cwd `phaseOffsetMs(cwd, jitterSeconds)` jitter (FNV-1a 32-bit hash). **Per-change watch set** (`perChangeArtifactPaths`): `<change>/`, `tasks.md`, `proposal.md`, `design.md` + `specs/`, every immediate `specs/<cap>/`, every `specs/<cap>/spec.md` — `readdirSync` of `specs/` try/catch-wrapped, missing dirs yield empty fan-out (change: fix-openspec-specs-mtime-gate-blind-spot — also wires `createFsSpecsProbeFactory(cwd)` into `buildOpenSpecData` as 2nd probe-factory arg so multi-spec authoring lights up green when gate misfires). **TOCTOU-safe stamping** (change: fix-openspec-mtime-gate-toctou): per-change status loop captures `preCallMtime` *before* awaiting `runOpenSpecStatus` + stamps that value; if post-call effective mtime differs, entry racy → cache untouched (next gated tick re-polls). DEBUG-gated `console.warn` cites change name on discard. **Refresh contract**: `refreshOpenSpec(cwd)` is user-clicked path, bypasses gate via `pollOne(cwd, true)` — manual escape hatch. `pollDirectoryGated`, `onDirectoryAdded`, post-archive refresh in `handleOpenSpecBulkArchive` all call `pollOne(cwd, false)`. Periodic/gated path derives artifact status via `deriveArtifactStatus` (no per-change `openspec status` spawn; ≤1 `openspec list` spawn per cwd per tick); force-refresh still spawns `openspec status` per change. See change: optimize-openspec-poll-derive-artifacts-locally. `reconfigurePolling(cfg)` applies live config changes without restart. Pi-resources scan on own 5×-interval timer so doesn't stack with openspec burst. See changes: fix-openspec-specs-mtime-gate-blind-spot, fix-openspec-mtime-gate-toctou, fix-openspec-mtime-gate-blind-spots, optimize-openspec-poll-burst. **Push trigger** (change: fix-openspec-taskcheck-delay): constructs `OpenSpecChangeWatcher` via `createOpenSpecChangeWatcher({onChange: onWatcherFired})`. `onWatcherFired(cwd)` runs gated poll + broadcasts when JSON differs. `reconcileWatchers()` runs at top of each `scheduleOpenSpecTick` — attaches newly-known cwds, detaches forgotten ones. `onDirectoryAdded` attaches eagerly. `stopPolling` calls `detachAll`. `DirectoryServiceOptions.changeWatcher` allows test injection of a stub. Poll work-set excludes ended sessions; pinned dirs unconditional. See change: scope-openspec-poll-to-active-cwds. **Worker offload** (change: offload-openspec-poll-to-worker): `DirCache` gains `serialized: string \| undefined`. New options `getOpenSpecGroupAssignments(cwd) => Promise<Record<string,string>>` (worker-path enrichment) + retained `enrichOpenSpecData` (force-path). Non-force `pollOne(cwd, false)` dispatches to `ensureWorkerPool().process(req)`; writes `cache.data` + `cache.serialized` from worker output; stamps `cache.changes` from `out.stampMtimes`, skips entries in `out.racyNames`. Force-path keeps existing per-change `runOpenSpecStatus` loop and explicitly clears `cache.serialized`. `onWatcherFired` + periodic-tick diff reuse `cache.serialized ?? JSON.stringify(...)` and pass `serialized` to `onChangeCallback?.(cwd, next, serialized)`. `stopPolling()` disposes pool (best-effort, async). `reconfigurePolling()` disposes pool so it respawns lazily at new `maxConcurrentSpawns`. Re-exports `effectiveMtimeOr` from `openspec-poll-fs-helpers.ts` for back-compat. **Hydration timing** (change: instrument-session-hydration-timing): `loadSessionEvents` times load via `performance.now()`, stats file size, counts entries+events, records sample into shared `HydrationMetrics` (option `DirectoryServiceOptions.hydrationMetrics`), warns `[hydration] slow load: <wallMs>ms (session=<id> bytes=<n>)` when wallMs > 5000. `LoadResult` unchanged. **Session-load worker offload** (change: offload-session-events-load-to-worker): `loadSessionEvents` dispatches parse+replay to session-load worker pool; main thread keeps `loadingSet` dedup, `eventStore.insertEvent`, `session_updated` + `event_replay` broadcast. `cancelLoad(sessionId)` cancels in-flight via `inFlightLoadJobs` map. `ensureLoadWorkerPool` lazy. Pool disposed in `stopPolling`. See change: offload-session-events-load-to-worker. |
| `src/server/pending-fork-registry.ts` | Tracks pending fork operations for session placement |
| `src/server/pending-resume-registry.ts` | Queues prompts for auto-resume of ended sessions |
| `src/server/pending-resume-intent-registry.ts` | In-memory `Map<sessionId, timestamp>` (60 s TTL, lazy expiry on read). `record(id)` called by `handleResumeSession` (WS) + REST `POST /api/session/:id/resume` handler immediately before `spawnPiSession`; `consume(id)` called by `server.ts`'s `sessionManager.onChange` in ended→alive branch. **4-way intent contract** (change: reattach-move-to-front): if `consume` returns `null` (no user intent), branch checks `OnChangeContext.registerReason`; if `"reattach"` applies configured `reattachPlacement` via `reattach-placement.ts::applyReattachPolicy`, otherwise (legacy bridge or genuine spawn) returns without mutation. Registry intents (`"front"` / `"keep"`) always win over `registerReason` per spec. `if (!order.includes(sessionId))` guard keeps drag-to-resume's dropped slot intact when intent present. In-memory only — not persisted across server restarts. See changes: preserve-session-order-on-reboot, reattach-move-to-front. |
| `src/server/reattach-placement.ts` | Pure `decideReattachAction(policy, status)` + I/O `applyReattachPolicy(sessionId, cwd, policy, deps, priorStatus?)` for bridge-reattach placement. Mapping: `"always"` → `moveToFront`; `"streaming-only"` → `moveToFront` iff `effectiveStatus === "streaming"` where `effectiveStatus = priorStatus ?? session.status`; `"preserve"` → no-op. **`priorStatus` required for `streaming-only`**: `memory-session-manager.ts::register` unconditionally sets `status: "active"`, so without prior value policy silently behaves as `"preserve"`. Captured by `register()` from `existing?.status` BEFORE assembling new session + forwarded via `OnChangeContext.priorStatus`. `applyReattachPolicy` wired from `server.ts onChange` at TWO sites: (a) ended→alive branch when consumed registry intent `null` + `ctx.registerReason === "reattach"`, (b) alive→alive branch (`!isEnded && !wasEnded && ctx?.registerReason === "reattach"`) handling common case where session persisted `"active"` so neither end-state transition fires. See change: reattach-move-to-front. `applyReattachPolicy` now receives resolved order key from server.ts. See change: simplify-session-card-ordering. |
| `src/server/json-store.ts` | Atomic JSON file read/write helpers |
| `src/server/process-classifier.ts` | Pure classifier. Enriches scanned `process_list` entries with `kind`/`label`/`sessionRef` before forward. Exports `classifyProcesses(processes, pidIndex)` + `buildPidIndex(sessions)` (`pid → {sessionId,name,model}`, connected sessions only — ended sessions skipped, pid-reuse guard). `command` basename `pi` + pid in index → `sub-session` (label = session name, falls back to model; `sessionRef` = sessionId). basename `pi` + pid not in index → `pi-worker` (label `"pi worker"`). command matches `.pi/agent/**/<name>/<file>` → `plugin` (label `<name>`). Else `task` (label = command). Non-destructive: `pid`/`pgid`/`command`/`elapsedMs` preserved. See change: classify-process-list-entries. |
| `src/server/process-manager.ts` | Session spawning: dispatches via `platform/spawn-mechanism.ts` `selectMechanism` → `tmux` / `wt` / `wsl-tmux` / `headless`. All mechanisms forward `sessionFile`/`mode` uniformly via `sessionFlagsToArgv`. Headless `--mode rpc` spawn routes exclusively through `spawnHeadlessViaKeeper` on Unix and Windows (legacy `tail -f /dev/null` wrapper + Windows direct-stdin pipe removed). `spawnHeadlessViaKeeper` resolves pi via ToolRegistry, forwards absolute argv as `PI_KEEPER_PI_CMD` env to keeper. `PI_NOT_FOUND` on resolver miss. See change: fix-rpc-keeper-pi-resolution, enable-rpc-keeper-by-default. `spawnHeadlessViaKeeper` sets env `PI_KEEPER_CAPTURE_PI_OUTPUT="1"` when `loadConfig().keeperLog.capturePiOutput`; read per spawn. See change: add-keeper-output-capture-toggle. SessionOptions.model → `--model` on spawn argv (via SessionFlags). See change: add-automation-plugin. |
| `packages/server/src/rpc-keeper/keeper-env.cjs` | Pure helper. Exports `buildPiEnv(baseEnv, isFirstLaunch)`. Returns fresh env for keeper pi launch. Sets `PI_DASHBOARD_SPAWNED=1` always. Strips `PI_KEEPER_PI_ARGS`/`PI_KEEPER_PI_CMD`. Injects single-use `PI_DASHBOARD_SPAWN_TOKEN` on first launch only; deletes it on respawn (`isFirstLaunch=false`). Does not mutate `baseEnv`. See change: fix-spawn-token-env-leak. |
| `packages/server/src/rpc-keeper/keeper-manager.ts` | `spawnKeeperFor(sessionId, cwd, env, sessionFile?, piCmd?)` spawns per-session keeper sidecar; JSON-encodes resolved `piCmd` into keeper env as `PI_KEEPER_PI_CMD` when non-empty. See change: fix-rpc-keeper-pi-resolution. |
| `packages/server/src/rpc-keeper/keeper.cjs` | CJS keeper sidecar. `readPiCmd()` parses `PI_KEEPER_PI_CMD` JSON; non-string-array → null + log `keeper: ignoring malformed PI_KEEPER_PI_CMD`. `spawnPi()` execs `[exe, ...prefix, ...piArgs]` when resolved argv present, else bare `spawn("pi", piArgs)` fallback. Strips `PI_KEEPER_PI_ARGS` + `PI_KEEPER_PI_CMD` from pi's inherited env. See change: fix-rpc-keeper-pi-resolution. `spawnPi()` branches pi child stdio by `PI_KEEPER_CAPTURE_PI_OUTPUT`: `"1"` → `["pipe", logFd, logFd]`, else `["pipe", "ignore", "ignore"]` (discard pi stdout/stderr). Strips var from pi env. Logs `pi output capture: enabled\|disabled`. Keeper lifecycle log() lines written regardless. See change: add-keeper-output-capture-toggle. `spawnPi()` tracks `piLaunchCount`; calls `buildPiEnv(process.env, piLaunchCount === 1)` so single-use `PI_DASHBOARD_SPAWN_TOKEN` injected on first launch only, scrubbed on respawn. See change: fix-spawn-token-env-leak. |
| `packages/server/bin/pi-dashboard.mjs` | `.bin` entry. Re-execs Node with `--import <jiti-url> cli.ts <args>` after resolving jiti from pi tree at runtime. `--version` / `-v` / `version` short-circuit: reads sibling `package.json`, prints version, exits 0 without resolving jiti. Other subcommands still require jiti; exit 1 with install-hint when unresolvable. See change: fix-electron-cold-launch-probe-cascade. `jiti` now direct dep of `@blackbelt-technology/pi-dashboard-server`; resolves from own `node_modules/jiti` via `argv[1]` walk-up — no pi pre-install required. Unresolvable-jiti error message updated to point at standalone install path. See change: enable-standalone-npm-install. |
| `packages/server/preload-fastify.cjs` | CommonJS preload file — loaded by Node's legacy synchronous CJS loader via `--require`. Populates `require.cache` with `fastify` + `@fastify/ajv-compiler` + `@fastify/ajv-compiler/standalone` so ESM→CJS translator short-circuits on later imports. MUST stay `.cjs`; MUST NOT use ESM syntax. |
| `src/server/editor-registry.ts` | Detects available native editors (running processes + CLI) |
| `packages/server/src/editor-keeper/keeper.cjs` | CJS-pure editor sidecar. Spawns code-server child. Owns per-editor UDS `<id>.sock` (POSIX) / Windows pipe `\\.\pipe\pi-editor-<id>`. Writes PID sidecar `{editorId, keeperPid, childPid, port, cwd, dataDir, binary, spawnedAt}`. JSON-line protocol: cmds `heartbeat` / `getStatus` / `stop`; emits `child_exit`. Outlives dashboard. See change: add-editor-keeper-sidecar. |
| `packages/server/src/editor-keeper/keeper-manager.ts` | Server-side helper. Exports `editorIdFromCwd(cwd)` = `sha256(cwd).slice(0,12)`. `createEditorKeeperManager` returns `spawnKeeperFor`, `probe` (sidecar + PID alive + socket present + TCP ready), `writeCommand`, `onChildExit`, `killKeeper`, `discoverExistingKeepers` (4-way adoption sweep). See change: add-editor-keeper-sidecar. |
| `src/server/editor-manager.ts` | Lifecycle manager for code-server child processes. `start(cwd)` 3-way: in-memory hit → `keeper.probe(editorId)` adopt → `keeper.spawnKeeperFor(cwd)`. `editorId = sha256(cwd).slice(0,12)` stable across restarts. `stop(id)` async; writes `{cmd:"stop"}` via `keeper.writeCommand`, removes entry on `child_exit` event or 6 s fallback. `stopAll()` gated by `EditorConfig.stopOnDashboardExit` (default false → local map cleanup only, keepers persist). `forceStopAll()` bypasses flag for tests. `adopt({editorId, cwd, port})` registers keeper-adopted instance from boot sweep. Direct `child_process.spawn(code-server, ...)` removed. See change: add-editor-keeper-sidecar. `writeVscodeThemeSettings` renamed `writeVscodeUserSettings`; seeds persistence keys (`window.restoreWindows: all`, `workbench.editor.restoreViewState`, `files.hotExit: onExitAndWindowClose`, `security.workspace.trust.enabled: false`, `update.mode: none`, `extensions.autoCheckUpdates: false`, `workbench.startupEditor: none`) merged so existing user values win, theme keys authoritative. `start(cwd)` wraps body as `startInner`; memoizes in-flight Promise per cwd in `inFlightStarts` map, clears on settle; prevents duplicate keeper/code-server spawn on one `--user-data-dir` when concurrent opens race. See change: fix-editor-settings-persistence. |
| `src/server/editor-pid-registry.ts` | Editor boot-time orphan reconciliation. `register()` removed — per-editor sidecars now owned by keeper. `adoptOrphans()` consumes `keeperManager.discoverExistingKeepers()` and calls `editorManager.adopt(...)` for each live keeper. `cleanupOrphans()` retained as defensive cmdline sweep for pre-keeper installs (matches `--user-data-dir` under `~/.pi/dashboard/editors/` with no sidecar present). Boot order in `server.start()`: `adoptOrphans()` → `cleanupOrphans()`. See change: add-editor-keeper-sidecar. |
| `src/server/editor-proxy.ts` | Reverse proxy for `/editor/:id/*` to code-server instances |
| `src/server/editor-detection.ts` | Auto-detect code-server/openvscode-server binary on PATH |
| `src/server/routes/editor-routes.ts` | REST routes: editor start, stop, heartbeat, status, detect |
| `src/server/event-status-extraction.ts` | Extracts session status/tool updates from events (incl. flow metadata). Hosts two pure classifiers consumed by `event-wiring.ts`: `isActivityEvent(eventType)` (allowlist driving `lastActivityAt` stamping; see change: session-card-last-activity-badge) + `isUnreadTrigger(eventType, before, after, payload)` (returns true on `streaming→idle\|active`, on `currentTool→"ask_user"`, + on `agent_end` with truthy `payload.error`; see change: session-card-unread-stripes). |
| `src/server/viewed-session-tracker.ts` | In-memory `Map<sessionId, Set<WebSocket>>` registry of which browser has which session displayed (`/session/:id`). Created by `browser-gateway.ts`, exposed on `BrowserGateway.viewedSessionTracker`, threaded into `wireEvents({ ..., viewedSessionTracker })`. `view`/`unview` called from `session_view`/`session_unview` switch arms; `unviewAll(ws)` called on every WS `close` so disconnected browsers cannot hold sessions in viewed state. `isViewedByAnyone(sessionId)` gates unread-trigger stamp in `event-wiring.ts`. Read state GLOBAL across browsers — mirrors mail/Slack. In-memory only. See change: session-card-unread-stripes. Adds `providers_list` forwarder updating `provider-catalogue-cache.ts` and broadcasting `models_refreshed` to browsers. See change: replace-hardcoded-provider-lists. |
| `src/server/headless-pid-registry.ts` | Maps headless child PIDs → session IDs. Phase 6 of add-rpc-stdin-dispatch-with-keeper-sidecar gained `keeperPid`/`piPid`/`keeperSockPath` fields, async `writeRpc(sessionId, line)` and `cleanupKeeperOrphans()`, and a keeper-aware `killBySessionId` (kills pi first, schedules 200 ms keeper-fallback). |
| `src/server/auth.ts` | OAuth2 authentication: provider registry, JWT helpers, user allowlist |
| `src/server/provider-auth-handlers.ts` | Pi provider OAuth handlers (Anthropic, Codex, GitHub Copilot, Gemini CLI, Antigravity). Each handler carries `displayName` consumed by `getOAuthProvidersMeta()` + `_buildAuthStatus()`. See change: replace-hardcoded-provider-lists. |
| `src/server/provider-auth-storage.ts` | Reads/writes `~/.pi/agent/auth.json` with mkdir-lockfile + atomic rename. Exports `_buildAuthStatus(catalogue, authData, oauthHandlers)` pure: emits OAuth handler rows + API-key rows derived from bridge-pushed catalogue. OAuth/api-key collision uses `<id>-api` suffix. Ambient catalogue rows force `authenticated:true` + `maskedKey:"(ambient)"`. `resolveAuthJsonKey(id)` strips `-api` suffix when bare id is OAuth handler. `OAUTH_PROVIDERS` + `API_KEY_PROVIDERS` arrays removed. See change: replace-hardcoded-provider-lists. |
| `src/server/provider-catalogue-cache.ts` | In-memory per-session `Map<sessionId, ProviderInfo[]>` + latest-snapshot tracker. Set by `event-wiring.ts` on `providers_list` from bridge. `getLatestCatalogue()` returns most recent push (empty before any bridge connects). Cleared by `clearForSession(id)`; `_resetForTests()` for unit tests. See change: replace-hardcoded-provider-lists. See change: fix-providers-list-spurious-models-refreshed — `setCatalogueForSession` returns `{changed:boolean}` from order-sensitive deep-equality vs prior cached payload (every `ProviderInfo` field incl. `custom`). `latestSnapshot` updates only on `changed`. Caller `event-wiring.ts` gates `models_refreshed` broadcast on `changed`; identical re-pushes (browser subscribe / `session_register` / reconnect / fork resume) become silent. See change: simplify-model-selection-channels — supersedes fix-providers-list-spurious-models-refreshed: catalogue cache collapsed to single `latest` var; broadcast removed entirely. `setCatalogueForSession` returns void; `getCatalogueForSession`/`clearForSession` exports removed. |
| `src/server/routes/provider-auth-routes.ts` | REST routes: provider OAuth authorize/exchange/callback, device-code, API key CRUD. `GET /api/provider-auth/status` cold-cache nudge: when catalogue empty, sends `request_providers` to every connected pi (best-effort, non-blocking). See change: replace-hardcoded-provider-lists. See change: simplify-model-selection-channels — `notifyBridges` helper no longer broadcasts `models_refreshed`; bridge `providers_list` push sole catalogue update channel. |
| `src/server/routes/provider-routes.ts` | REST routes: custom LLM provider CRUD (`GET/PUT /api/providers`) + **`POST /api/providers/test`** connection probe (reuses `provider-probe.ts`). See change: `hot-reload-custom-providers`. See change: simplify-model-selection-channels — `PUT /api/providers` no longer broadcasts `models_refreshed`; relies on bridge `providers_list` re-push via `credentials_updated`. |
| `src/server/provider-probe.ts` | Pure per-API-type probe builders (`buildProbeRequest` for `openai-completions`/`openai-responses`/`anthropic-messages`/`google-generative-ai`) + `resolveProbeApiKey` (handles literal, `$ENV_VAR`, `***` REDACTED sentinel via injected providers reader) + I/O-bearing `probeProvider` (8 s timeout, never echoes apiKey in error text, caps body excerpts at 500 chars). Used by `/api/providers/test` route + shared with bridge's discovery path |
| `src/server/auth-plugin.ts` | Fastify plugin: auth routes, onRequest hook, WS upgrade validation |
| `src/server/config-api.ts` | Config REST API: read (redacted), write (partial merge), secret preservation. `writeConfigPartial` auth-merge propagates `secret`, `providers`, `allowedUsers`, `bypassHosts`, `bypassUrls` (last two added by change `fix-trusted-networks-no-oauth` — silently dropped before, breaking Trusted Networks saves for users without OAuth). Deep-merges `partial.tunnel.watchdog` into existing `tunnel.watchdog`; previously shallow merge wiped unspecified watchdog fields. See change: tunnel-watchdog-settings-ui. |
| `src/server/localhost-guard.ts` | Network access guard: `createNetworkGuard` (loopback/trusted/authenticated), `isBypassedHost` (CIDR/wildcard/exact), netmask-to-CIDR helpers |
| `src/server/server-pid.ts` | PID file management for daemon mode |
| `src/server/routes/known-servers-routes.ts` | REST routes: known servers CRUD, on-demand mDNS discovery scan |
| `src/server/terminal-manager.ts` | PTY lifecycle, ring buffer, spawn/attach/kill terminals. `spawn(cwd, { ephemeral })` honors flag; `getTranscript(id)` returns ring-buffer contents as string. See change: add-inline-terminal-card. |
| `src/server/terminal-gateway.ts` | Binary WebSocket upgrade handler for `/ws/terminal/:id` |
| `src/server/tunnel.ts` | Zrok tunnel with reserved shares for persistent URLs, binary detection, PID tracking, stale cleanup. `getTunnelStatus()` merges `getTunnelWatchdogStatus()` snapshot into active variant under `watchdog` field. `loadZrokEnv()` delegates to shared `readZrokEnvironment` (see `packages/shared/src/zrok-env.ts`). See change: tunnel-watchdog, add-tunnel-diagnostic-checks. |
| `packages/server/src/recovery-server.ts` | Pure `node:http` recovery server. `startRecoveryServer({port, error})` spawned by `cli.ts` `runForeground` when top-level deps (fastify/toad-cache/etc) fail to resolve. Binds same port. Serves status HTML at `/`, `GET /api/health` returns `{mode:"recovery", error}`, `POST /api/recovery/retry` respawns dashboard via `process.execPath`, `POST /api/recovery/reinstall` runs `npm install` in detected layout. Zero third-party imports. `parseModuleNotFoundError(msg)` extracts missing specifier; `isModuleNotFoundError(err)` checks `ERR_MODULE_NOT_FOUND` / `MODULE_NOT_FOUND`; `detectInstallLayout(cwd)` returns `monorepo` / `npm-global` / `electron-bundled` / `unknown`; `suggestedReinstallCommand(layout)` maps layout → shell cmd; `buildRecoveryHtml({error, layout, cmd})` escapes XSS. Writes `~/.pi/dashboard/last-recovery.json` snapshot (timestamp, error, layout, cmd) on start. See change: add-startup-recovery-server. |
| `packages/server/src/__tests__/recovery-server.test.ts` | 19 cases: `parseModuleNotFoundError` happy + malformed, `isModuleNotFoundError` code+message variants, `detectInstallLayout` per layout, `suggestedReinstallCommand` per layout, `buildRecoveryHtml` XSS escape (`<script>` → `&lt;script&gt;`), live HTTP server `GET /`, `GET /api/health`, fallthrough 404 route. See change: add-startup-recovery-server. |
| `packages/server/src/tunnel-watchdog.ts` | Tunnel watchdog. Probes `${publicUrl}/api/health` on `intervalMs` (default 60000); 5xx/network/timeout count as failures. After `failureThreshold` (default 2) consecutive failures calls `deleteTunnel()` then `createTunnel()`; on recycle failure backs off ×2 up to ×8 cap. Exports `probeTunnel`, `startTunnelWatchdog`, `stopTunnelWatchdog`, `getTunnelWatchdogStatus`. Tracks `lastProbeAt`, `lastSuccessAt`, `lastFailureAt`, `lastFailureReason`, `consecutiveFailures`, `lastRecycleAt`, `recycleCount`. See change: tunnel-watchdog. |
| `packages/server/src/__tests__/tunnel-watchdog.test.ts` | 10 cases: probe success/5xx/network/timeout classification, threshold not-yet-met, recycle on threshold, counter reset on success, recycle-failure backoff ×2 up to ×8 cap, start/stop idempotency, status snapshot shape. See change: tunnel-watchdog. |
| `src/server/cli.ts` | CLI entry: start/stop/restart/status. `findPortHolders` cross-platform (netstat/taskkill on Windows, lsof on Unix); `server.log` opened append-mode with timestamped headers. **`runForeground`** under R3 inlines a single `ToolRegistry.resolve("pi")` call: success logs `[bootstrap] ready (pi resolved via <source>)`; failure throws hard citing corrupted `node_modules/`. `runDegradedModeBootstrap` + `maybeSeedDefaultInstallableList` + `upgrade-pi` subcommand deleted. Subcommands now `["start", "stop", "restart", "status"]`. Also dynamic-imports `detectLegacyManagedDir` after the ready line; logs `[legacy] legacy install directory detected at <path> (<pkgCount> packages, ~<sizeMb> MB). No longer used — safe to delete.` once on present (advisory only, swallows failures). **`cmdRestart(config, injected?)`** probes `isDashboardRunning(port)`; if up, POSTs `/api/restart` and exits; if down, falls back to local `cmdStop()` + `cmdStart()`. See changes: fix-restart-bridge-auto-start-race, tunnel-watchdog, eliminate-electron-runtime-install. `runForeground` dynamic-imports `./server.js` inside `try/catch`; on `ERR_MODULE_NOT_FOUND` delegates to `startRecoveryServer`. See change: add-startup-recovery-server. |
| `src/server/restart-helper.ts` | Cross-platform `/api/restart` orchestrator: spawns detached `node -e` child using only Node built-ins (net, http) — no sh/lsof/curl dependency; exports pure `buildOrchestratorScript(params)` for testing. **Explicit prior-daemon kill** (change: fix-restart-bridge-auto-start-race): embedded script reads `~/.pi/dashboard/dashboard.pid`, sends `SIGTERM` to recorded PID if alive, polls for exit (3 s deadline), then `SIGKILL`. Removes "wait for self-exit" ambiguity that let bridges race orchestrator before this change. `portFree` poll reduced from 10 s → 5 s since step 0 already guarantees previous server is dead. |
| `src/server/routes/recommended-routes.ts` | `GET /api/packages/recommended` — enrichment + 60s cache, invalidated on successful install/remove/update. `enrichEntry` propagates `dashboardPlugin` from manifest + computes `dashboardPluginInstalled` by lookup against installed plugin set; drives `+plugin: <id>` badge in `RecommendedExtensions.tsx`. See change: add-plugin-activation-ui. |
| `src/server/reconcile-session-order.ts` | pure `reconcileSessionOrder(orders, sessions, resolveKey)`. Prunes stale ids (absent from manager). Backfills absent ended ids by (endedAt ?? startedAt) desc. Idempotent. Replaces old strip-ended startup reconcile. See change: simplify-session-card-ordering. |
| `src/server/resolve-order-key.ts` | `resolveOrderKey(session, pinnedDirectories?, platform?)` wraps shared `resolveSessionGroupPath` with process.platform + preferencesStore.getPinnedDirectories(). All insert/moveToFront/remove route through it. See change: simplify-session-card-ordering. |
| `src/server/resolve-path.ts` | Safe realpath resolution (symlink handling) |
| `packages/server/src/routes/pi-changelog-routes.ts` | `GET /api/pi-core/changelog?pkg&from&to`. Whitelist-validates `pkg` against `CORE_PACKAGE_NAMES`. Bootstrap-gated (503 unless ready). 200 + empty release list when CHANGELOG missing. See change: pi-update-whats-new-panel. |
| `packages/server/src/routes/manifest-route.ts` | Dynamic `/manifest.json` route. Exports `stripPort`, `resolveManifestSource`, `buildManifestBody`, `loadStaticManifest`, `registerManifestRoute`. Resolves source: `config.dashboardName` → request `Host` header (port stripped, IPv6-safe) → `os.hostname()` → `"Pi-Dash"`. `buildManifestBody` sets `id="/"`, `name="Pi-Dash · <src>"`, `short_name=src.slice(0,12)`. Registered before `@fastify/static` so explicit route wins over on-disk asset. Re-reads config per request via `loadConfig()`; Settings change propagates without restart. Sets `Cache-Control: no-cache, must-revalidate` + `Content-Type: application/manifest+json`. See change: add-dynamic-pwa-manifest-naming. |
| `packages/server/src/routes/jj-routes.ts` | REST routes: `POST /api/jj/workspace/add` (creates workspace via `jj.workspaceAdd` with auto-resolved `baseRev` from source `@`'s bookmark, enqueues into `pendingAttachRegistry`, calls `spawnPiSession` — same lever as openspec attach-and-spawn); `POST /api/jj/workspace/forget` (refuses HTTP 409 `UNFOLDED_WORK` when workspace has commits between `fork_point(name@, trunk())` and `name@`; on `force:true` runs `jj workspace forget` AND `fs.rm({recursive,force})` on workspace dir); `POST /api/jj/init-colocated` (refuses 409 `DIRTY_INDEX` only on staged changes, allows working-tree dirt per Decision 6 — `jj git init --colocate` snapshots unstaged edits as `@` non-destructively); `GET /api/jj/workspace/list?cwd=` (returns `parseWorkspaceList` entries). All auth-gated via `networkGuard`. Pure helper `checkInitColocatedPreconditions(cwd)` extracted for testability. |
| `packages/server/src/spawn-failure-log.ts` | Appends/reads rolling NDJSON log of failed spawns (`~/.pi/dashboard/sessions/spawn-failures.log`). Single-shot rotation at 10 MB. See change: spawn-failure-diagnostics. |
| `packages/server/src/spawn-preflight.ts` | Pure sync preflight: checks cwd exists/is-dir/writable + pi+node resolvable. Returns `PreflightResult { ok, reasons[] }`. useLoginShell must be false. See change: spawn-failure-diagnostics. Emits BOTH legacy `DIR_MISSING` reason and new `cwd_missing` code when cwd absent (one-release overlap). See change: add-worktree-lifecycle-actions. |
| `packages/server/src/spawn-register-watchdog.ts` | Arms per-spawn timer; fires `spawn_register_timeout` if pi never registers. byPid + byCwd maps. recentlyFired (60s TTL) emits `spawn_register_recovered`. See change: spawn-failure-diagnostics. |
| `packages/server/src/session-diff.ts` (extended) | New `enrichWithVcsDiff(cwd, files, jjState?)` dispatcher routes through `enrichWithJjDiff` when `jjState.isJjRepo` is true, otherwise existing `enrichWithGitDiff` path. Pure helper `selectJjDiffBase(jjState)` returns `@-` for default workspace (equivalent to `git diff HEAD`) + `fork_point(@, trunk())` for non-default workspaces — killer-feature fix that makes Changed Files show every commit agent produced across multiple `jj new`s, not just working-copy delta. `SessionDiffResponse` additively extended with optional `vcsKind`, `diffBase`, `baseLabel`; older clients ignore. `FileDiffView` header renders `(vs <baseLabel>)` when `vcsKind === "jj"`. See change: add-jj-workspace-plugin Decision 9. |
| `src/server/session-diff.ts` | Server-side event scanning + git diff extraction for session file changes |
| `src/server/session-api.ts` | REST wrappers for WebSocket-only session operations (prompt, abort, spawn, resume, etc.) Detach endpoint clears `pendingReplaceProposal` + `rejectedReplaceProposals`. See change: replace-proposal-dialog-with-race-handling. |
| `packages/server/src/extension-register.ts` | Auto-registers bundled bridge extension in pi's global settings on startup |
| `packages/roles-plugin/package.json` | Bundled roles plugin. Manifest id=`roles`, displayName=`Roles`. Claims `settings-section` → `BuiltInRolesSettings`. `claim.tab` inert hint; render gated by Settings ▸ Plugins row. See change: fix-pi-flows-end-to-end, rename-builtins-to-roles-plugin. |
| `packages/roles-plugin/src/index.tsx` | Client entry barrel. Re-exports `BuiltInRolesSettings`. Export name preserved for Vite-plugin named-import generator. See change: fix-pi-flows-end-to-end, rename-builtins-to-roles-plugin. |
| `packages/roles-plugin/src/RolesSettingsSection.tsx` | Roles editing UI: preset CRUD, role grid, model picker. Reads roles + models via `useSessionData`. Dispatches existing `role_set` / `role_preset_*` WS messages. See change: fix-pi-flows-end-to-end. Consumes `ui:model-selector` via `useUiPrimitive(UI_PRIMITIVE_KEYS.modelSelector)` (replaces inline picker). Sends full `"provider/modelId"` string as `modelId` in `role_set`. Exports pure helper `inferProviderForBareId(stored, models)` for read-time migration of legacy bare-id role values. See change: add-ui-model-selector-primitive. |
| `packages/client/src/components/PluginStalenessBanner.tsx` | Banner on stale plugin bundle. Fetches `/api/health.bundleHash` on mount. Compares to imported `PLUGIN_REGISTRY_HASH`. Mismatch ⇒ banner with Refresh (`location.reload()`) + Dismiss buttons. Dismiss persists in `sessionStorage` key `pi-plugin-staleness-dismissed`. No new REST route, no new WS message. See change: fix-pi-flows-end-to-end. |
| `packages/dashboard-plugin-runtime/src/server/loader.ts` | `discoverPlugins` + `loadServerEntries` (failure-isolated) + `getPluginStatusStore`. Adds `deterministicSerializePlugins` (sorted manifest fields, stable JSON) + `pluginRegistryHash(plugins)` (sha256 over serialization). Shared by vite-plugin (build-time `PLUGIN_REGISTRY_HASH` emission) + server (`/api/health.bundleHash`). See change: fix-pi-flows-end-to-end. |
| `packages/server/src/routes/system-routes.ts` | REST routes: config, health, shutdown, tunnel, editors. **`/api/health`** adds `bundleHash` field via `pluginRegistryHash(discoverPlugins())` + enriches every `plugins[]` entry with `bridgeLoadedFrom` via `enrichWithBridgeSource(settings, id)` (`"packages[]"`/`"dashboardPluginBridges"`/`"both"`/`"none"`) + `lastProbe` from plugin-status-store. Consumed by `PluginStalenessBanner` + flows-anthropic-bridge diagnostics. See change: fix-pi-flows-end-to-end. Adds `startedAt` ISO timestamp; client `usePluginEnabledSet` + `PluginsSection` compare against snapshot to surface restart-required banner after toggle. See change: add-plugin-activation-ui. `POST /api/restart` handler passes `dev: useDev` into `spawnRestart({…})` so orchestrator picks mode-matched deadline (matches `--dev` argv already forwarded to child). See change: fix-mode-aware-server-ready-deadlines. `/api/open-editor` decodes leading `file://` on `file` via `decodeFileUri`. See change: unify-file-link-openability. `/api/health` adds `platform: process.platform` for host-OS install-hint filtering. See change: register-bash-and-tool-install-help. `/api/health` gains additive `eventLoopDelay {meanMs,p99Ms,maxMs}` (`perf_hooks` histogram, resets window each read) + `hydration` (`HydrationMetrics.snapshot()`, ≤20 newest-first). See change: instrument-session-hydration-timing. |
| `packages/shared/src/plugin-bridge-register.ts` | Dual-write contract. `registerPluginBridge` writes BOTH `dashboardPluginBridges["dashboard-<id>"]` AND `packages[]` + `_dashboardManagedPackages` ownership map. pi-coding-agent reads only `packages[]`; legacy key kept for forward compat. New exports: `ensurePackageEntry`, `removePackageEntry`, `reconcilePluginBridgePackages` (one-shot at server start, replays manifests + drops dangling managed entries, atomic write), `classifyBridgeSource(settings, id)` (returns load source for `/api/health`), `listManagedPackageOwnership(settings)`. Env `PI_DASHBOARD_DISABLE_PLUGIN_BRIDGE_PACKAGES_WRITE=1` skips `packages[]` write (legacy key still written). See change: fix-pi-flows-end-to-end. |
| `packages/dashboard-plugin-runtime/src/server/plugin-status-store.ts` | In-memory `PluginStatusStore` for `/api/health.plugins[]`. Adds `recordBridgeProbe(pluginId, snapshot)` + `getBridgeProbe(pluginId)`. `listAll()` enriches returned status rows with `lastProbe`. Plugins (e.g. flows-anthropic-bridge) push runtime telemetry via `recordBridgeProbe`; surfaces in health endpoint without dedicated REST route. See change: fix-pi-flows-end-to-end. |
| `packages/flows-anthropic-bridge-plugin/src/server/index.ts` | Server entry. Now subscribes to `flows-anthropic-bridge:status` events from bridge + publishes snapshots into shared plugin-status-store via `recordBridgeProbe`. Replaces prior plugin-local status cache. Read path stays `/api/flows-anthropic-bridge/status` but data sourced from shared store → surfaces in `/api/health.plugins[].lastProbe`. See change: fix-pi-flows-end-to-end. |
