# File Index — Shared types & protocols

> Part of [pi-agent-dashboard file index](./file-index.md). Loaded on demand.
>
> **Change-history annotations** (e.g. *"See change: foo-bar"*) → OpenSpec changes archived under `openspec/changes/archive/`.
>
> **Update protocol**: see `AGENTS.md` → "Documentation Update Protocol".

| File | Purpose |
|------|---------|
| `packages/shared/bin/pi-dashboard-resolve-tool.cjs` | Shell-callable resolver wrapper for tool registry. CommonJS, dependency-free, invokable as `node packages/shared/bin/pi-dashboard-resolve-tool.cjs <tool-name> [--json]`. Mirrors `override` + `bare-import` strategy semantics for build-time tools (electron, node-pty) so CI workflows / Dockerfiles / postinstall scripts can resolve hoisted-vs-nested layouts without depending on shared package's TS build. **Strategy chain MUST stay in sync with `tool-registry/definitions.ts`** — enforced socially via cross-reference comments + via `no-hardcoded-node-modules-paths.test.ts`. See change: register-build-time-tools |
| `packages/shared/src/__tests__/config-keeper-log.test.ts` | Unit tests `keeperLog` parse: default `false`, explicit true/false, non-boolean fallback, non-object fallback, unknown-key strip, round-trip. See change: add-keeper-output-capture-toggle. |
| `packages/shared/src/__tests__/error-patterns.test.ts` | Validates `USAGE_LIMIT_PATTERN` matches documented terminal categories; rejects generic retryable errors; cross-validates extension re-export. See change: unify-status-banner-and-terminal-limit-stop. |
| `packages/shared/src/__tests__/legacy-managed-dir.test.ts` | Unit tests for `detectLegacyManagedDir`: absent dir, file-not-dir, empty dir, pkgCount counting, sizeMb. See change: eliminate-electron-runtime-install. |
| `packages/shared/src/__tests__/no-bash-on-windows.test.ts` | Repo-level lint: parses every workflow YAML, computes per-step Windows reachability from `electron` matrix × each step's `if:` filter (small grammar: `matrix.platform == 'X'`, `matrix.platform != 'X'`, `&&`, `||`, `!(...)` , parens), fails when any `shell: bash` step reachable on Windows runner. Failure messages cite change `eliminate-bash-on-windows-runners` plus offending `file:line` + step name. Unrecognised `if:` expressions fail closed (force contributor to write recognisable form or extend evaluator). See change: eliminate-bash-on-windows-runners. |
| `packages/shared/src/__tests__/no-managed-dir-reference.test.ts` | Repo-lint. Walks `packages/electron/src/lib/`, `packages/server/src/`, `packages/shared/src/`. Asserts files referencing `.pi-dashboard` literal belong to explicit allowlist with one-line rationale per entry. Allowlist documents: detector, standalone-arm-only pi-core update writes, read-only fallback probes. See change: eliminate-electron-runtime-install. |
| `packages/shared/src/__tests__/no-remote-debugging-address.test.ts` | Repo-lint: forbids literal 'remote-debugging-address' in packages/electron/src/**. Encodes Decision 3 of ship-browser-skill-and-electron-cdp (CDP loopback-only, no promiscuous bind). See change: ship-browser-skill-and-electron-cdp. |
| `packages/shared/src/__tests__/publish-workflow-contract.test.ts` | Parses `.github/workflows/publish.yml`; pins electron `needs: [resolve, publish]` + `strategy.fail-fast: false`, lockfile-regen step in `tag-and-push`, and 6-clause gate contract: (1) `resolve.outputs.ref`, (2) `ci-checks` runs `npm ci` + lint + test + build on Node 22, (3) `smoke` `uses: ./.github/workflows/_smoke.yml` with `ref: ${{ needs.resolve.outputs.ref }}`, (4) `tag-and-push` `if: github.event_name == 'workflow_dispatch'`, (5) `publish.needs` includes ci-checks + smoke + tag-and-push, (5b) `publish.if:` accepts `tag-and-push.result` of `success` or `skipped`. See change: publish-fix-macos, gate-publish-on-smoke-and-tests. |
| `packages/shared/src/bridge-register.ts` | Shared bridge registration: `findBundledExtension(baseDir)` + `registerBridgeExtension(path)`; non-destructive cleanup, AppImage guard. Used by server startup and Electron wizard. |
| `packages/shared/src/changelog-types.ts` | `ChangelogBullet` / `Release` / `Response` types for parsed CHANGELOG.md responses. See change: pi-update-whats-new-panel. |
| `packages/shared/src/dashboard-paths.ts` | Single-source path helpers for dashboard runtime dirs. `getDashboardConfigDir` → `~/.pi/dashboard/`. `getDashboardServerLogPath` → `~/.pi/dashboard/server.log` (live server log). `getManagedDir` re-export → `~/.pi-dashboard/`. `getInstallerLogPath` → `~/.pi-dashboard/server.log` (legacy installer log, distinct from server log). All accept `DashboardPathsEnv = { homedir? }` override for test isolation. See change: harvest-bootstrap-survivor-fixes. |
| `packages/shared/src/display-prefs.ts` | `DisplayPrefs` interface gates chat-view chrome (thinking, tool calls per kind, turn separators, debug tools, token bar, context bar, etc.). `DISPLAY_PRESETS = { simple, standard, everything }`. `mergeDisplayPrefs(global, override)` deep-merges `toolCalls`. `toolCallPrefKey(toolName)` returns canonical prefs key; returns `null` for `ask_user` (non-hidable). See change: configurable-chat-display. |
| `packages/shared/src/doctor-core.ts` | Pure detection core. Types `DoctorCheck` / `DoctorReport` / `DoctorSection` (6: runtime/pi-tooling/server/tunnel/setup/diagnostics) / `ExecFailureKind` / `TunnelWatchdogStatusLike`. `SECTION_OF` + `SUGGESTIONS` maps. Helpers `safeExec` (bounded + classified spawn), `safeCheck` (per-check fault isolation), `assumedMandatory` (logs to `<managedDir>/doctor.log` with 1MB ring rotation, surfaces `diagnostics` row), `stripAnsi`, `formatDoctorReportMarkdown`, `defaultDnsLookup`, `runSharedChecks(deps)`. Tunnel section: `zrok binary`, `zrok environment`, `zrok API reachable`, `tunnel runtime`. Adds git source + bash source rows (Windows-only). See change: embed-git-bash-on-windows. Diagnostics section owns `Legacy install directory` advisory; emits only when `detectLegacyManagedDir().present`. `SharedChecksDeps.detectLegacyManagedDir` is injectable test seam. Obsolete `Managed install (~/.pi-dashboard)` row removed. See changes: add-tunnel-diagnostic-checks, fix-doctor-stale-managed-install-check. |
| `packages/shared/src/error-patterns.ts` | Single-source-of-truth for `USAGE_LIMIT_PATTERN` regex. Consumed by bridge auto-abort path + client banner selector. Extension `usage-limit-orderer.ts` re-exports for source compat. See change: unify-status-banner-and-terminal-limit-stop. |
| `packages/shared/src/legacy-managed-dir.ts` | `detectLegacyManagedDir({ homedir? })`. Returns `{present:false}` or `{present:true, path, pkgCount, sizeMb}`. Probes `~/.pi-dashboard/node_modules/`. Split-literal `".pi-" + "dashboard"` to pass `no-managed-dir-reference.test.ts` lint. Consumed by shared `runSharedChecks` to emit the sole `~/.pi-dashboard/` advisory row + server CLI startup log. See changes: eliminate-electron-runtime-install, fix-doctor-stale-managed-install-check. |
| `packages/shared/src/pi-package-resolver.ts` | Walks `~/.pi/agent/settings.json#packages[]` + per-workspace `<cwd>/.pi/settings.json`. Resolves `npm:` / `git:` / `https:` / abs / rel sources to absolute install dir. Returns entry path via `exports["."]` → `main` → `pi.extensions[0]` → `index.{js,ts}` chain. Read-on-call (no cache). Exports `resolvePiPackage` + `resolvePiPackageEntry`. Consumers import from `@blackbelt-technology/pi-dashboard-shared/pi-package-resolver.js`. Exports `listPiPackages(opts)` returning `ResolvedPiPackage[]` across user (`~/.pi/agent/settings.json`) + project (`<cwd>/.pi/settings.json`) scopes, deduped by `packageDir`, no name filter; consumed by Electron `probePiExtension` in `launch-source.ts`. See change: fix-electron-cold-launch-probe-cascade. See change: add-shared-pi-package-resolver. |
| `packages/shared/src/platform/ensure-bundled-git.ts` | `ensureBundledGitOnPath()` + `resolveBundledGitDir()`. Prepends bundled git/sh dirs to PATH (`cmd`, `usr/bin`, `<libdir>/bin`). Sets `GIT_EXEC_PATH` + `SSL_CERT_FILE`. `<libdir>` arch-specific: `mingw64` (x64) / `clangarm64` (arm64). Shell = `usr/bin/sh.exe` (dugite-native ships NO `bash.exe`). Idempotent. Only module allowed to hardcode `resources/git` paths (`no-hardcoded-bundled-git-paths.test.ts`). See change: embed-git-bash-on-windows. |
| `packages/shared/src/platform/ensure-windows-path.ts` | `ensureWindowsSystemPath(env, opts?)`: on win32 prepends to PATH (only when dir exists on disk + not already substring-present, case-insensitive): `C:\Windows\System32`, `C:\Windows`, `\System32\Wbem`, `\System32\WindowsPowerShell\v1.0`, `\System32\OpenSSH`, `%LOCALAPPDATA%\Microsoft\WindowsApps`. Idempotent. Non-win32 no-op. Shallow-clones `env`, never mutates `process.env`. Test seam via `{ platform, exists }` opts. Called from `ToolResolver.buildSpawnEnv` (binary-lookup.ts) so every spawned child inherits System32 even when parent PATH lacks it (Electron / minimal-env launchers). Re-exported by `packages/electron/src/lib/ensure-windows-path.ts` for back-compat. See change: fix-windows-path-system32-missing. Sibling pattern to `ensure-bundled-git.ts`. See change: embed-git-bash-on-windows. |
| `packages/shared/src/platform/git-source.ts` | Process-lifetime git-source cache. `setWindowsGitSourceSetting`, `getActiveGitSource(which)`, `augmentEnvWithGitSource(env, which)`, `getGitSourceReadout(which)` (Windows-only, cached), `invalidateGitSourceCache`. `which` injected to avoid binary-lookup cycle. See change: embed-git-bash-on-windows. |
| `packages/shared/src/platform/jj.ts` | Recipe-based jj tool module mirroring `platform/git.ts`. 15 recipes covering version, workspace add/list/forget/root, bookmark create/list, git init colocate / push, diff (with `--from`/`--to` for regime-aware session-diff), resolve list, op log head / restore (fold-back rollback escape hatch per Decision 12), rebase, log revset (used to derive `baseRev` from source's `@` bookmark + to enumerate unfolded commits in forget). Pure `parseWorkspaceList` + `findWorkspaceByName` parsers handle jj's standard `<name>: <change-id> <commit-id> [(empty)] [(no description set) \| <desc>]` output. No `child_process` import — enforced by `no-direct-child-process.test.ts`. |
| `packages/shared/src/platform/managed-node-path.ts` | `getManagedNodeBinDir(managedDir, platform?)` + `prependManagedNodeToPath(env, managedDir, platform?)`: shallow-clones `env`, prepends `<managedDir>/node/` (Win) or `<managedDir>/node/bin` (Unix) to `PATH`. No-op when runtime absent. Never mutates `process.env`. Consumed by every spawned-child env builder (pi-session, pi-core-updater, headless, server-launcher) so managed `node`/`npm`/`npx` resolves first. See change: embed-managed-node-runtime. |
| `packages/shared/src/platform/select-git-source.ts` | `selectGitSource()` tri-state `windowsGitSource` decision (`auto`/`host`/`bundled`) → `"host"`\|`"bundled"`. Pure, injectable `which`/`platform`. Non-win32 → host. `auto` needs git+bash on host PATH atomically else bundled. See change: embed-git-bash-on-windows. |
| `packages/shared/src/recommended-extensions.ts` → `BUNDLED_EXTENSION_IDS` | Single source of truth for which recommended ids ship bundled in Electron installer. **Currently `["pi-anthropic-messages"]` only** — `"pi-flows"` removed in commit b9b3d7e because upstream repo (BlackBeltTechnology/pi-flows) declares no SPDX license (no `LICENSE` file, no `package.json#license`), and `bundle-recommended-extensions.sh`'s allowlist (MIT/Apache-2.0/BSD-2-Clause/BSD-3-Clause/ISC) correctly rejects it — was blocking every electron build matrix variant. Re-add `"pi-flows"` once upstream declares license. npm publish path unaffected (only electron build was broken). See `openspec/changes/archive/2026-04-21-bundle-first-party-extensions/design.md` for original design + license-blocker discussion. |
| `packages/shared/src/server-launcher.ts` | `launchDashboardServer` — single shared spawn primitive (jiti loader, argv, env, log header, readiness) used by Bridge / Standalone / Electron starters |
| `packages/shared/src/session-group-path.ts` | Hoisted `inferPlatform`/`pathKey`/`resolveSessionGroupPath` from client session-grouping.ts. Single source of truth. Server keys order map by same resolved path client reads. Precedence pin > jjState.workspaceRoot > gitWorktree.mainPath > cwd. See change: simplify-session-card-ordering. |
| `packages/shared/src/test-support/setup-home-perfile.ts` | vitest `setupFiles` hook. Runs inside each worker fork before test file imports. Sets `process.env.HOME = mkdtempSync(os.tmpdir()/pi-test-)` per file. Pre-creates `.pi/agent/sessions` + `.pi/dashboard`. Isolates HOME across parallel forks (maxWorkers "50%"). Complements `setup-home.ts` globalSetup tripwire (kept as second-line guard). Wired in shared/server/extension/subagents-plugin vitest configs via config-relative path (worktree-local source wins over node_modules symlink). No localStorage handling — only client jsdom uses localStorage, isolated in-memory per fork. See change: parallelize-test-suite. |
| `packages/shared/src/types.ts::ViewTarget` | Discriminated union `{kind:"file",cwd,path} \| {kind:"url",url}`. Optional `ChatMessage.view` field renders message as `<PreviewCard>`. Stripped from pi-bound stream by structural isolation: view rows live in server-side `ViewMessageStore`, not events.jsonl. See change: render-file-previews. |
| `packages/shared/src/types.ts::MissingToolError` | `MissingToolError { kind:"missing-tool"; toolName }`. `BashOutputData` gains optional `missingTool` field. See change: register-bash-and-tool-install-help. |
| `packages/shared/src/types.ts::JjState` | Per-session jj probe state: `{ isJjRepo, isColocated, workspaceName?, workspaceRoot?, bookmarks?, lastError? }`. Surfaced as optional `DashboardSession.jjState` field. NOT persisted to `.meta.json` — live tool state, refreshed on every probe tick. Predicates (`isInJjRepo` / `isInJjWorkspace` / `isInGitRepoButNotJj`) read this field; absent = treated as not-jj. |
| `packages/shared/src/zrok-env.ts` | Pure `readZrokEnvironment({homedir,fs})` returning `{found, kind: v2\|v1\|null, path, env, reason}`. Prefers `~/.zrok2/environment.json` over `~/.zrok/environment.json`. Never throws. Consumed by `tunnel.ts#loadZrokEnv` and `doctor-core.ts` zrok-environment check so runtime and diagnostic agree. See change: add-tunnel-diagnostic-checks. |
| `src/shared/__tests__/no-direct-process-kill.test.ts` | Repo-level lint: scans `packages/*/src/` (excluding platform/ + `__tests__/`) for `process.kill(` calls + fails with file:line if any found. Mirrors `no-direct-child-process.test.ts`. |
| `src/shared/__tests__/no-hardcoded-node-modules-paths.test.ts` | Repo-level lint: scans `.github/workflows/publish.yml`, `.github/workflows/ci.yml`, `packages/electron/scripts/Dockerfile.build`, two `fix-pty-permissions.cjs` postinstalls for hardcoded `node_modules/electron` / `node_modules/node-pty` substrings outside explicit allowlist. Fails with `file:line:col` citation. Replaces hand-rolled inline `require.resolve(...)` patterns with registered tool registry. Mirrors `no-direct-process-kill.test.ts` + `no-raw-node-import.test.ts`. See change: register-build-time-tools |
| `src/shared/__tests__/no-raw-node-import.test.ts` | Repo-level lint: scans `packages/*/src/` (excluding `platform/node-spawn.ts` + `resolve-jiti.ts` + `__tests__/`) for `spawn(...)` argv with `"--import"` / `"--loader"` followed by raw identifiers not wrapped in `toFileUrl(...)` / `pathToFileURL(...)`. Mirrors `no-direct-process-kill.test.ts` pattern. |
| `src/shared/browser-protocol.ts` | Server↔Browser WebSocket messages. All types MUST be in `ServerToBrowserMessage` / `BrowserToServerMessage` unions — `as any` switch cases stripped by esbuild in production. PromptBus (`prompt_request`/`prompt_dismiss`/`prompt_cancel`), Extension UI Phase-1 (`ui_modules_list`/`ui_data_list`/`ui_management`), `SessionViewBrowserMessage`/`SessionUnviewBrowserMessage`, `SessionsSnapshotMessage`, `SpawnFailureCode`/`PreflightReason`/`SpawnRegisterTimeoutMessage`/`SpawnRegisterRecoveredMessage`, `RequestProvidersBrowserMessage`. `BootstrapStateSnapshot` + `bootstrap_status_update` + `bootstrap_ticket_complete` removed under R3. See changes: fix-interactive-ui-reorder, session-card-unread-stripes, fix-stale-sessions-on-reconnect, spawn-failure-diagnostics, replace-hardcoded-provider-lists, eliminate-electron-runtime-install. See change: honest-mid-turn-queue-surface. Six queue-mutation message types deleted: ClearSteeringQueueFromBrowserMessage, ClearFollowupSlotFromBrowserMessage, EditFollowupSlotFromBrowserMessage, EditFollowupEntryFromBrowserMessage, RemoveFollowupEntryFromBrowserMessage, PromoteFollowupEntryFromBrowserMessage. Removed from BrowserToServerMessage union. See change: rework-mid-turn-prompt-queue. Added 5 follow-up mutation types (ClearFollowupEntriesFromBrowserMessage, EditFollowupEntryFromBrowserMessage, RemoveFollowupEntryFromBrowserMessage, PromoteFollowupEntryFromBrowserMessage, PullFollowupToEditorFromBrowserMessage) + FollowupPulledMessage server→browser. New names; OLD deleted Phase 3 types (ClearSteeringQueue, ClearFollowupSlot, EditFollowupSlot) stay deleted forever. Adds three server→browser message types: `WorktreeBootstrapProgressMessage` (`worktree_bootstrap_progress { requestId, cwd, line }`), `WorktreeBootstrapDoneMessage` (`worktree_bootstrap_done { requestId, cwd, durationMs }`), `WorktreeBootstrapFailedMessage` (`worktree_bootstrap_failed { requestId, cwd, code, message, stderr }`). Adds two browser→server: `WorktreeBootstrapSubscribeMessage` / `WorktreeBootstrapUnsubscribeMessage` (both `{requestId}`). Distinct from existing pi-core `BootstrapStatusUpdateMessage` / `BootstrapTicketCompleteMessage`. See change: harden-worktree-spawn. Adds `display_prefs_updated` (server→browser, payload `{ global: DisplayPrefs|undefined, sessionOverrides: Record<sessionId, Partial<DisplayPrefs>> }`) + `setSessionDisplayPrefs` (browser→server, `{ sessionId, override: Partial<DisplayPrefs> | null }`; `null` clears). See change: configurable-chat-display. Adds `InjectViewMessageBrowserMessage` (browser→server `{sessionId, target: ViewTarget}`) + `ViewMessagesUpdateMessage` (server→browser snapshot of `/view` rows). See change: render-file-previews. See change: classify-process-list-entries. `ProcessListUpdateMessage.processes` entry gains optional `kind`/`label`/`sessionRef` (server populates kind+label always, sessionRef only for sub-session). Re-exports batch types from protocol.ts. See change: redesign-ask-user-question-cards. Adds `OpenInlineTerminalBrowserMessage { sessionId, cwd }`, `CloseInlineTerminalBrowserMessage { sessionId, terminalId }` to BrowserToServerMessage. See change: add-inline-terminal-card. Adds favorite_model/unfavorite_model (browser→server) + favorite_models_updated {labels} (server→browser); mirrors pin_directory family. See change: enrich-model-selector-capabilities-favorites. Adds `PluginEventBroadcast` to `ServerToBrowserMessage` union. See change: add-goal-continuation-plugin. Adds `BrowserOpenSpecGroupsUpdateMessage.changeOrder?: Record<string, string[]>` field; broadcast via `openspec_groups_update`. See change: redesign-openspec-board. Adds `AcceptReplaceProposalBrowserMessage` (`accept_replace_proposal {sessionId,changeName}`) + `DismissReplaceProposalBrowserMessage` (`dismiss_replace_proposal {sessionId,changeName}`) to `BrowserToServerMessage` union. See change: replace-proposal-dialog-with-race-handling. |
| `src/shared/config.ts` | Shared config loader (`~/.pi/dashboard/config.json`). Exports `ReattachPlacement = "preserve" | "streaming-only" | "always"` + `parseReattachPlacement(raw)` validator (default `"always"`); `reattachPlacement` config field controls how server places re-registering bridges in `sessionOrder` after dashboard restart. See change: reattach-move-to-front. Includes `openspec: OpenSpecPollConfig` block (`pollIntervalSeconds` 5–3600, `maxConcurrentSpawns` 1–16, `changeDetection` `mtime` or `always`, `jitterSeconds` 0–60) with clamping via `parseOpenSpecPollConfig`. See change: optimize-openspec-poll-burst. Adds `openspec.enabled` (boolean, default `true`); `false` disables polling + hides OPENSPEC subcards via cleared per-cwd broadcast. See change: auto-hide-empty-session-subcards. Adds `spawnRegisterTimeoutMs` (default 30000, clamped 5000-120000). See change: spawn-failure-diagnostics. Adds `tunnel.watchdog` block: `enabled` (default true), `intervalMs` (default 60000), `failureThreshold` (default 2), `probeTimeoutMs` (default 10000); parsed/clamped via `parseTunnelWatchdogConfig`. See change: tunnel-watchdog. Adds `dashboardName?: string` field; trim-aware (whitespace-only treated as unset). Drives `/manifest.json` name override. See change: add-dynamic-pwa-manifest-naming. Field `gitWorktreeEnabled: boolean` (default `true`). UI preference. Gates folder `+Worktree` + per-change `⑂+` buttons. Does not gate `/api/git/worktree*` REST endpoints. See change: openspec-worktree-spawn-button. Adds `keeperLog: KeeperLogConfig` block (`capturePiOutput` boolean, default `false`); `DEFAULT_KEEPER_LOG`; parsed via `parseKeeperLogConfig` (non-boolean/non-object → `false`). `ensureConfig` omits `keeperLog`. Gates keeper pi-stdout capture. See change: add-keeper-output-capture-toggle. Adds global booleans `completedFirst`/`questionFirst` (default `false`, parsed via `typeof === "boolean"`). Gate top-of-tier placement on agent_end(alive)/alive→ended and on ask_user. See change: simplify-session-card-ordering. `DEFAULT_OPENSPEC_POLL.pollIntervalSeconds` default now 60 (was 30). See change: optimize-openspec-poll-derive-artifacts-locally. Adds `OpenSpecPollConfig.useWorker: boolean` (default `true`). `parseOpenSpecPollConfig` reads `raw.useWorker` (typeof boolean) or falls back to default. `false` = permanent in-process path (no `worker_threads` spawn). See change: offload-openspec-poll-to-worker. Adds `windowsGitSource` key (`auto`\|`host`\|`bundled`, default `auto`). See change: embed-git-bash-on-windows. |
| `src/shared/dashboard-plugin/intent-types.ts` | Adds `PluginEventBroadcast` (`type:"plugin_event"`, `pluginId`/`sessionId`/`event{eventType,timestamp,data}`). Server→Browser generic plugin dashboard event. Shell routes `event` into plugin per-session store. Distinct from `plugin_intents`. See change: add-goal-continuation-plugin. |
| `src/shared/dashboard-plugin/manifest-types.ts` | `PluginManifest` + `PluginClaim` interfaces; includes `tab` field for `settings-section` claims + `fixture` flag for test-only plugins. Adds `PluginClaim.shouldRender?: string` (sync function name; wrapper-gate filter consulted by `forSessionRendered` + `useSlotHasClaimsForSession`). See change: auto-hide-empty-session-subcards. Adds optional `PluginManifest.requires: PluginRequirements` + new `PluginRequirements` type ({`piExtensions?: string[]`, `binaries?: string[]`, `services?: string[]`}); consumed by `requirement-probes.ts` to populate `PluginStatus.requirements`. See change: add-plugin-activation-ui. |
| `src/shared/dashboard-plugin/plugin-status.ts` | `PluginStatus` (for `/api/health`) + `PluginConfigUpdate` (WebSocket broadcast payload). Adds `BridgeLoadSource` union (`"packages[]"` \| `"dashboardPluginBridges"` \| `"both"` \| `"none"`) + `BridgeProbeSnapshot` interface (runtime telemetry from plugin bridge — timestamps, last status, counters). `PluginStatus` extended with optional `bridgeLoadedFrom?: BridgeLoadSource` + `lastProbe?: BridgeProbeSnapshot`. Both additive — older clients ignore. Surfaced by `/api/health.plugins[]`. See change: fix-pi-flows-end-to-end. Adds required `displayName: string` (server fills from manifest), optional `requirements?: PluginRequirementReport`, optional `missingRequirements?: string[]`. New `PluginRequirementReport` type carries per-category probe results (`piExtensions`, `binaries`, `services`); drives `PluginsSection` install affordances + `+plugin: <id>` badge in `RecommendedExtensions`. See change: add-plugin-activation-ui. |
| `src/shared/dashboard-plugin/slot-props.ts` | `SlotPropsMap` + `SlotProps<SlotId>` — typed prop contracts per slot id. Type-level test asserts all slot ids covered. Adds `SlotPropsMap` entry for `session-card-flows` ({session, pluginContext}). See change: add-flows-subcard. `tool-renderer` props expanded with optional `status`/`result`/`toolDetails`/`images`/`context`; required core `toolName`/`toolInput`/`sessionId` unchanged. Adds `ToolRendererImage`/`ToolRendererContext` structural types (mirror client `ChatImage`/`ToolContext`, no client dep). See change: wire-tool-renderer-slot. |
| `src/shared/dashboard-plugin/slot-types.ts` | Frozen slot taxonomy: `SlotId` union, `Multiplicity`, `PayloadTier` enums, `SLOT_DEFINITIONS` record. Adding slot = minor; removing = major. — Adds `session-card-memory` and `workspace-action-bar` slots (both multiplicity many, payloadTier react-only). See change: redesign-session-card-subcards. Exports `SlotPredicateInput<S>` mapping `SlotId` to claim-predicate input type. See change: slot-generic-claim-entry. Adds slot id `session-card-flows` (react-only, many, session-scoped) to `SlotId` union, `SLOT_DEFINITIONS`, `SessionScopedSlot`. See change: add-flows-subcard. |
| `src/shared/dashboard-plugin/ui-primitives.ts` | UI primitive registry contracts: `UI_PRIMITIVE_KEYS`, `UiPrimitiveMap`, per-primitive prop interfaces. See `docs/plugin-ui-primitives.md`. Adds `ui:dialog` key + `UiDialogProps`/`UiDialogComponent` contracts. See change: unify-dialog-system. |
| `src/shared/diff-types.ts` | Types for session file diff API (FileChangeEvent, FileDiffEntry, SessionDiffResponse) |
| `src/shared/editor-types.ts` | Editor instance types shared across components |
| `src/shared/mdns-discovery.ts` | mDNS advertise/discover/browse for `_pi-dashboard._tcp` services. Exports `pickBestHost(service)` which returns `service.host` only when matches DNS-safe pattern `/^[A-Za-z0-9.-]+$/` (no leading/trailing hyphen); otherwise falls back to first IPv4 address in `service.addresses`, then to any address, finally to original host. Bonjour on macOS advertises OS computer-name verbatim (e.g. `"MacBook 242"`) which contains spaces — without this fallback, saved known-server entries unresolvable in browser. `serviceToServer` calls `pickBestHost` so discovered servers always carry host browser can resolve. |
| `src/shared/openspec-activity-detector.ts` | Detects OpenSpec activity from tool events; auto-attach requires only changeName (phase optional). Bash arm rejects flag-shaped capture tokens (`name.startsWith("-")` → `null`) so `openspec archive --help` + similar discovery commands no longer rename unnamed sessions to `--help`. See change: fix-openspec-flag-rename-bug. Exports `isValidOpenSpecChangeSlug(name)` enforcing `^[a-z][a-z0-9-]{0,63}$` (mirrors `openspec new change` validation); all three branches (Read, Write, Bash) gate captured `changeName` through it, subsuming the prior `-`-prefix guard. Rejects UUID, mixed-case, underscored, >64-char tokens. See change: fix-uuid-rename-bug. |
| `src/shared/openspec-design-evidence.ts` | Pure rule evaluator + real-fs probe factory for OpenSpec design-artifact override. `evaluateLocalDesignSatisfaction(changeDir, probe)` short-circuits R1 (`^design.*\.md$` file present) → R2 (`design/` folder with `*.md`) → R3 (`tasks.md` contains `^\s*-\s+\[[ xX]\]\s` checkbox). `createFsDesignEvidenceProbe()` returns defensive sync probe (try/catch on every fs call). Probe-injection keeps `buildOpenSpecData` pure + unit-testable without filesystem mocks. See change: fix-openspec-design-detection. |
| `src/shared/openspec-poller.ts` | OpenSpec CLI polling (shared, used by server DirectoryService). `buildOpenSpecData(listResult, statusResults, designProbeFactory?, specsProbeFactory?)` accepts 2 optional probe factories: `DesignProbeFactory` promotes `design` artifact `"ready"` → `"done"` when local file evidence (R1/R2/R3) satisfies design (change: fix-openspec-design-detection); `SpecsProbeFactory` promotes `specs` artifact `"ready"` → `"done"` when ≥1 `specs/**/*.md` exists locally (change: fix-openspec-specs-mtime-gate-blind-spot). Both promote-only, single-artifact-only, never demote; post-override loop re-derives change-level `isComplete`. `pollOpenSpec` (sync) + `pollOpenSpecAsync` (async) wire real-fs probes via `createFsProbeFactory(cwd)` + `createFsSpecsProbeFactory(cwd)`. `directory-service.ts` does same on mtime-gated path. `deriveArtifactStatus(changeDir, listEntry, probes)` derives per-artifact status locally (proposal/design/specs/tasks + `isComplete`) without spawning `openspec status`; tasks rule = `totalTasks > 0 ? done : blocked`; used by directory-service periodic path. See change: optimize-openspec-poll-derive-artifacts-locally. |
| `src/shared/openspec-specs-evidence.ts` | Pure rule evaluator + real-fs probe factory for OpenSpec specs-artifact override. `evaluateLocalSpecsSatisfaction(changeDir, probe)` returns true iff at least one `*.md` file exists under `<changeDir>/specs/` (iterative DFS, short-circuits on first match). `createFsSpecsEvidenceProbe()` returns defensive sync probe — every `readdirSync` wrapped in try/catch so missing dirs / permission errors / symlink loops yield `false` rather than throwing. Mirror-shape of `openspec-design-evidence.ts`; consumed by `buildOpenSpecData` as defense-in-depth layer over `directory-service.ts` mtime gate (which now also watches `specs/**`). See change: fix-openspec-specs-mtime-gate-blind-spot. |
| `src/shared/platform/` | Unified cross-OS primitives (`index.ts` barrel). Sub-modules: `exec.ts` (**only module importing `node:child_process`** — wraps `execSync`/`exec`/`execFile`/`spawn`/`spawnSync` with `windowsHide: true` by default; enforced by `no-direct-child-process.test.ts`); `runner.ts` (Recipe engine — `run(recipe, input)` resolves binaries via `ToolResolver`, applies timeout/tolerate/error normalization, returns `Result<T>`); `git.ts` / `openspec.ts` / `npm.ts` (Recipe-based tool modules — typed functions like `git.diff(...)`, `openspec.list(...)`, `npm.rootGlobal()` never touch `child_process` or `process.platform`); `binary-lookup.ts` (`where`/`which`, `.cmd` ext, `ToolResolver` class; **`isAppImageSelfHit(path, opts?)`** flags candidate as Electron AppImage launcher when `realpath(path) === realpath(process.execPath)`, when `path` under `process.env.APPDIR` (squashfs mount), or when `realpath(path) === realpath(process.env.APPIMAGE)` — defensive try/catch around every `realpath`; consumed by `whereStrategy` (Layer 2 — every registry-resolved tool inherits guard) + `detectPiDashboardCli` / `detectPi` / `detectSystemNode` (Layer 1 — belt-and-braces). See change: fix-electron-appimage-cli-self-detection); `process.ts` (`findPortHolders`, `killProcess`, `isProcessAlive`, `killPidWithGroup`, `parseNetstatListeners`); `process-scan.ts` (`isProcessRunning` via pgrep/tasklist, `parseEtime`); `shell.ts` (`detectShell` for SHELL/COMSPEC, `getTerminalEnvHints`); `commands.ts` (`openBrowser`, `isVirtualMachine`). All OS-dependent exports take optional `platform: NodeJS.Platform` param so tests exercise both branches without mutating `process.platform`. `binary-lookup.ts` `buildSpawnEnv` now calls `augmentEnvWithGitSource(env, whichSync)` after `ensureWindowsSystemPath`; `whichSync` exported. See change: embed-git-bash-on-windows. See changes: consolidate-platform-handlers, platform-command-executor. See change: document-login-shell-non-interactive-fix. |
| `src/shared/platform/detached-spawn.ts` | Three primitives: `spawnDetached` (libuv-correct detached defaults on every OS), `waitForNoCrash` (negative liveness — did it survive window?), `waitForReady` (positive liveness — did probe turn true?). All spawn sites with long-lived detached children delegate here. `SpawnDetachedOptions` exposes optional `stdoutFd` (defaults to `"ignore"`) + `logFd` (stderr, defaults to `"ignore"`); bridge server-launcher sets both to same fd for parity with CLI. `logFd` routes to BOTH stdout + stderr (was stderr-only) so server's startup banner reaches `server.log`. See change: fix-electron-extracted-jiti-and-stdio-capture. |
| `src/shared/platform/node-spawn.ts` | **Sole source of `node --import <loader> <entry>` argv construction**: `toFileUrl(pathOrUrl)` (idempotent path → file:// URL, handles Windows drive letters on POSIX hosts); `spawnNodeScript(opts)` (wraps loader + entry as file:// URLs before delegating to `platform/exec.ts::spawn`). Every `spawn(process.execPath, ["--import", ...])` outside this file with raw path args banned by `no-raw-node-import.test.ts`. Fixes `ERR_UNSUPPORTED_ESM_URL_SCHEME` on non-C: Windows drives. See change: fix-windows-entry-script-url. |
| `src/shared/platform/node-version-check.ts` | Pure predicate `isKnownBadNode(version)` + message builder `buildNodeVersionWarning(version)` for Node ranges affected by [nodejs/node#58515](https://github.com/nodejs/node/issues/58515) (22.0.0–22.17.x + 24.1.0–24.2.x). Consumed by `packages/server/src/cli.ts` (preflight warning), `packages/extension/src/server-launcher.ts` (ready-timeout hint), `packages/electron/src/lib/doctor.ts` (Node runtime compatibility row). |
| `packages/shared/src/platform/openspec.ts` | Recipe-based OpenSpec tool module. Adds `OPENSPEC_CONFIG_PROFILE` + `OPENSPEC_UPDATE` recipes, `configProfile()`/`update()` runners, `writeOpenSpecConfigFile()` (atomic tmp+rename), `workflowSetSignature()`, `openSpecConfigFilePath()`. Adds `configListAsync` + `configListOrAsync` (async, non-blocking sibling of `configList`/`configListOr`, uses `runAsync`). Re-exports `CORE_WORKFLOWS`/`EXPANDED_WORKFLOWS` from types. See changes: add-openspec-profile-settings, fix-openspec-profile-load-race. |
| `src/shared/platform/paths.ts` | OS-aware path primitives: `normalizePath`, `samePath` (filesystem-level equality), `parsePathInput` (picker input), `withTrailingSep`, `isFilesystemRoot`. All accept optional trailing `platform: NodeJS.Platform` for testability. Windows multi-drive invariant: A:\, B:\, C:\ never merge; bare `B:` input treated as drive root, not cwd-relative. Exported from `platform/index.ts` as `paths.*` namespace alongside `git.*`, `openspec.*`, `npm.*` |
| `src/shared/platform/preload-fastify.ts` | Shared resolver `resolvePreloadFastifyPath(): string \| null` — returns absolute native path (never `file://`) to `packages/server/preload-fastify.cjs`. Used by all four server-spawn sites (cli daemon, bridge launcher, Electron lifecycle, restart-helper orchestrator) to inject `--require <preloadPath>` BEFORE `--import <jitiLoader>` so Fastify's CJS chain cached before any ESM hook runs — sidesteps nodejs/node#58515 race-independently. See change: `preload-fastify-cjs`. |
| `src/shared/platform/process-identify.ts` | `findPidByMarker` + `isProcessLikePi` + `isPiCommandLine` — consolidates three `process.platform === "win32"` branches that previously lived inside `session-action-handler.ts`. Windows stubs documented (command-line lookup goes via `headlessPidRegistry` instead). |
| `src/shared/platform/process.ts` | **Sole source of process termination + liveness primitives**: `isProcessAlive(pid)` (signal 0), `killProcess(pid, {timeoutMs})` (Windows `taskkill /F /T /PID`, POSIX `SIGTERM` → wait → `SIGKILL` tree kill), `killPidWithGroup(pid, sig)` (POSIX `kill(-pid, sig)`, Windows direct kill). Every `process.kill(...)` call outside this file banned by `no-direct-process-kill.test.ts`. See change: route-kill-paths-through-platform. |
| `src/shared/platform/spawn-mechanism.ts` | `SpawnMechanism` enum (`tmux`/`wt`/`wsl-tmux`/`headless`) + pure `selectMechanism({ platform, userStrategy, electronMode, available })` selector. `buildWtArgs` builds argv for Windows Terminal `new-tab`. `sessionFlagsToArgv` is uniform flag builder every mechanism MUST call. |
| `src/shared/plugin-bridge-register.ts` | Plugin bridge entry management: `registerPluginBridge`, `deregisterPluginBridge`, `registerAllPluginBridges`. Manages `dashboard-<plugin-id>` keys in `settings.json#dashboardPluginBridges`; never touches user-owned `packages[]` entries. Atomic write (tmp+rename). |
| `src/shared/protocol.ts` | Extension↔Server WebSocket messages. Phase-1 Extension UI System adds `ui_modules_list` / `ui_data_list` (extension → server) + `ui_management` (server → extension); these messages MUST stay in `ExtensionToServerMessage` / `ServerToExtensionMessage` unions or esbuild strips switch cases in production. See change: add-extension-ui-modal. Adds `ProvidersListMessage` (extension → server) + `RequestProvidersMessage` (server → extension). See change: replace-hardcoded-provider-lists. See change: classify-process-list-entries. `ProcessInfo` gains optional `kind`/`label`/`sessionRef`; exports `ProcessKind = "task"|"sub-session"|"pi-worker"|"plugin"`. See change: rework-mid-turn-prompt-queue. Added 5 *ToExtensionMessage types matching browser-protocol + FollowupPulledExtensionToServerMessage extension→server. Exports InteractiveMethod, BatchQuestion, BatchAnswer, BatchResult types. See change: redesign-ask-user-question-cards. Adds `PluginPiMessage` (`type:"plugin_pi_message"`, `pluginId`/`messageType`/`payload`) to `ExtensionToServerMessage` union. Generic plugin bridge→server envelope; opaque payload keeps plugin specifics out of core protocol. See change: add-goal-continuation-plugin. |
| `src/shared/recommended-extensions.ts` | Static `RECOMMENDED_EXTENSIONS` manifest (pi-anthropic-messages, pi-dashboard-subagents, pi-flows, pi-web-access, pi-agent-browser); enriched server-side with live description/version + installed/active cross-reference. Adds optional `RecommendedExtension.dashboardPlugin?: string` (plugin id this extension activates) + optional `EnrichedRecommendedExtension.dashboardPluginInstalled?: boolean`. `pi-memory-honcho` entry declares `dashboardPlugin: "honcho"`. Drives `+plugin: <id>` badge in `RecommendedExtensions.tsx` + cross-references plugin-activation UI. `pi-dashboard-subagents` added to `BUNDLED_EXTENSION_IDS` for first-run Electron availability; replaces `@tintinweb/pi-subagents` (removed — specialized `Agent` / `get_subagent_result` / `steer_subagent` renderers dropped, falls back to generic tool renderer). See change: add-plugin-activation-ui. See change: add-subagent-inspector. Adds seventh entry `@blackbelt-technology/pi-image-fit`, source `npm:@blackbelt-technology/pi-image-fit`, status optional, no `dashboardPlugin`, no `toolsRegistered`; not in `BUNDLED_EXTENSION_IDS`; surfaces image-fit in Recommended Extensions card; auto-downscales images on Read (1568px / 4 MiB / quality 85 defaults). See change: add-image-fit-recommended-extension. |
| `src/shared/resolve-jiti.ts` | Resolves pi's jiti register hook as `file://` URL (required for `node --import` on Windows); exports pure `buildJitiRegisterUrl(pkgJsonPath)` helper + `resolveJitiFromAnchor(anchorPath)` for managed-install/system-pi callers |
| `src/shared/rest-api.ts` | REST API type definitions. `ProviderAuthStatus` gains optional `envVar?: string` + `ambient?: boolean`. See change: replace-hardcoded-provider-lists. `TunnelStatus` active variant extended with optional `watchdog?: TunnelWatchdogPublicStatus` ({`lastProbeAt`, `lastSuccessAt`, `lastFailureAt`, `lastFailureReason`, `consecutiveFailures`, `lastRecycleAt`, `recycleCount`}); additive, older clients ignore. See change: tunnel-watchdog. Adds `PullRequestInfo` (number, title, headRefName, headRefOid, author, isDraft, isCrossRepository, checkRollup) + `PullRequestListResponse`. See change: add-worktree-from-pull-request. Adds `SetOpenSpecChangeOrderRequest` (`{groupId, order:string[]}`) + `SetOpenSpecChangeOrderResponse`. See change: redesign-openspec-board. |
| `src/shared/semaphore.ts` | Tiny FIFO semaphore (`createSemaphore(max)` → `{run, setMax, size}`). Used by `directory-service.ts` to cap concurrent `openspec` CLI spawns. Supports live resize via `setMax(n)` for runtime reconfig. |
| `src/shared/server-identity.ts` | Identity-verified health check (`isDashboardRunning(port, host?, opts?)`). Probes `GET /api/health`; verifies `ok===true && pid` shape; returns `DashboardStatus { running, pid?, version?, portConflict? }`. Optional `DashboardCheckOpts { timeoutMs?, retries?, retryDelayMs?, _sleep? }` for retry-aware bootstrap probes; defaults preserve single-shot 2 s legacy behaviour. See change: harvest-bootstrap-survivor-fixes (retry semantics). |
| `src/shared/session-meta.ts` | Session metadata sidecar (.meta.json) read/write helpers. Adds `SessionMeta.displayPrefsOverride?: Partial<DisplayPrefs>` field. Sparse; absent reads as `undefined` (= use global). Server writes via `meta-persistence.setDisplayPrefsOverride(sessionFile, override|null)`; `null` deletes field. See change: configurable-chat-display. **`SessionMeta.unread?: boolean`** (change: session-card-unread-stripes) mirrors `DashboardSession.unread`; persisted by `metaPersistence.save(...)` in `server.ts onChange` + restored by `session-scanner.ts::sessionFromMeta` so unread bit survives server reboot. Backwards compatible — absent field reads as `undefined` (treated as `false`). |
| `src/shared/skill-block-parser.ts` | Pure parser/builder for pi's `<skill name="..." location="...">body</skill>\n\nargs` envelope. `parseSkillBlock(text)` recovers `{name, location, body, args, condensed}`; strips `References are relative to <baseDir>.\n\n` preamble. `buildSkillBlock(args)` emits byte-identical wrapper to pi's `_expandSkillCommand`. `condenseForFirstMessage(text, maxLen)` returns slash form for wrapped content, raw slice otherwise; consumed by session-scanner / session-discovery to avoid 200-char truncation cutting wrapper in half. See change: render-skill-invocations-collapsibly. |
| `src/shared/state-replay.ts` | Synthesizes events from pi entries (shared, used by server + bridge) |
| `src/shared/stats-extractor.ts` | Extracts token/cost stats from turn_end events |
| `src/shared/terminal-types.ts` | TerminalSession type + control messages. Adds `ephemeral?: boolean` to TerminalSession. Ephemeral excluded from TerminalsView tabs. See change: add-inline-terminal-card. |
| `packages/shared/src/tool-registry/definitions.ts` | Registers standard tool set. Each definition declares ordered strategy chain (override → bare-import → managed → npm-global → where) + classifier (strategy → source). Includes build-time tools `electron` + `node-pty` (registered as `kind: "module"` returning package directory; resolved hoist-aware via `bareImportPackageDirStrategy` with optional `searchPaths`). See change: register-build-time-tools. Registers `binaryDef("bash")` on every platform. `INSTALL_HINTS` map (bash, git, node, gh, jj, zrok) attached via `binaryDef`. `npx` unchanged, no installHints. See change: register-bash-and-tool-install-help. |
| `src/shared/tool-registry/index.ts` | Barrel export + `getDefaultRegistry()` singleton accessor |
| `src/shared/tool-registry/overrides.ts` | Read/write `~/.pi/dashboard/tool-overrides.json` (machine-local, separate from `config.json`). Lazy-loaded, atomic write via tmp+rename, malformed-file tolerant |
| `packages/shared/src/tool-registry/registry.ts` | `ToolRegistry` service — single-source resolver for every external binary/module (pi, pi-coding-agent, openspec, npm, node, tsx, git, zrok, pi-dashboard). Ordered strategy chain per tool, per-resolution diagnostic trail, in-memory cache, override-aware. `list()` returns `ToolListEntry[]`: each row carries `def.installHints` opaquely (omitted when absent). `resolve()` unchanged. See change: register-bash-and-tool-install-help. |
| `src/shared/tool-registry/strategies.ts` | Reusable resolution strategies: `overrideStrategy`, `managedBinStrategy`, `managedModuleStrategy`, `npmGlobalStrategy`, `whereStrategy`, `bareImportStrategy` (uses `createRequire` for sync probe). All take injectable `StrategyDeps` for tests |
| `packages/shared/src/tool-registry/__tests__/install-hints.test.ts` | Asserts every user-installable binary tool (bash, jj, gh, zrok, git, node) ships installHints for darwin/win32/linux. Asserts platform-utility tools (wmic, powershell, ps, pgrep, wt) carry no hints. Lints every docsAnchor against docs/faq.md headings. See change: register-bash-and-tool-install-help. |
| `packages/shared/src/tool-registry/types.ts` | `ToolDefinition`, `Strategy`, `StrategyResult`, `Resolution`, `Source`, `UnknownToolError`, `ModuleResolutionError`. Adds `InstallHints` + `PlatformInstallHint` interfaces. Adds `ToolDefinition.installHints` optional field. Adds `ToolListEntry` (`Resolution` + `installHints`). See change: register-bash-and-tool-install-help. |
| `src/shared/types.ts` | Data models: Session, Workspace, Event. **`DashboardSession.lastActivityAt?: number`** (epoch ms): server-stamped on activity events via `isActivityEvent` in `event-status-extraction.ts`; cold-start seeded from `events.jsonl` mtime in `session-scanner.ts`; consumed by client `selectBadgeTimestamp` for session-card relative-time badge (time-since-last-activity, not time-since-spawn). Not persisted to `.meta.json`. See change: session-card-last-activity-badge. **`DashboardSession.unread?: boolean`** (change: session-card-unread-stripes): server-managed per-session unread bit. Set `true` by `event-wiring.ts` when `isUnreadTrigger(...)` fires AND no browser viewing session AND event not part of replay. Cleared when any browser sends `session_view`. Persisted to `.meta.json` so unread survives server restart. Bridges SHALL NOT send this field. Consumed by `SessionCard.tsx::getCardPulseClass` to render cyan-stripes (`card-unread-pulse`, Tailwind `cyan-400`) with lower priority than `card-input-pulse` (purple) + `card-working-pulse` (yellow). Adds `ProviderInfo` (id, displayName, hasOAuth, configured, source?, envVar?, ambient?, expires?) — bridge → server provider catalogue payload. See change: replace-hardcoded-provider-lists. Adds `DashboardSession.displayPrefsOverride?: Partial<DisplayPrefs>` — sparse per-session chrome override merged client-side via `mergeDisplayPrefs(global, override)`. Persisted to `.meta.json`. See change: configurable-chat-display. Adds `InlineTerminalOpenData { terminalId }`, `InlineTerminalCloseData { terminalId, transcript }`. See change: add-inline-terminal-card. ModelInfo widened: optional name, reasoning, vision, contextWindow, metadataSource ("catalog"|"fallback"); metadataSource gates confident vs ?-badge. See change: enrich-model-selector-capabilities-favorites. Adds `CORE_WORKFLOWS` + `EXPANDED_WORKFLOWS` constants. See change: add-openspec-profile-settings. Adds `OpenSpecGroupsFile.changeOrder?: Record<string, string[]>` field + `OPENSPEC_UNGROUPED_KEY` constant (`__ungrouped__` sentinel). See change: redesign-openspec-board. `DashboardSession` gains `pendingReplaceProposal?: string \| null` + `rejectedReplaceProposals?: string[]`. See change: replace-proposal-dialog-with-race-handling. |

