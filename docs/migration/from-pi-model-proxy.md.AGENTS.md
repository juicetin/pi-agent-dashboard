# migration/from-pi-model-proxy.md — index

Migration guide `@blackbelt-technology/pi-model-proxy` → dashboard built-in proxy. Extension binds `:9876` per-session vs dashboard `:8000/v1` always-on. Requires proxy API keys `pi-proxy-*`. URL + key migration steps. Coexistence supported (different ports/auth).
