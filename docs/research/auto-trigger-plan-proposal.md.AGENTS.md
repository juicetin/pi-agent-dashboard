# research/auto-trigger-plan-proposal.md — index

Research dossier. Auto-trigger `plan-proposal` on `proposal.md` draft. Explore-mode, no change / no impl. Verdict: detection easy + native, headless execution BLOCKED — `plan-proposal` frontmatter pins to main interactive session (invokes `doubt-driven-review` nested-spawn + `scenario-design` `ask_user` gate). Re-entrancy smell: step 1 self-drafts proposal → fire only on out-of-flow draft (`proposal.md` exists, `tasks.md` not folded). 3 mechanisms: A pi ext `pi.on("tool_result")` + `pi.sendUserMessage`; B server fs-watcher + `session-api.ts` send-prompt; C git post-commit nudge. Automation = launcher, not runner. Sources: `.pi/skills/plan-proposal/SKILL.md`, docs/extensions.md, `packages/server/src/session/session-api.ts`.
