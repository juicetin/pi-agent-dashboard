# research/browser-provider-registry.md — index

Research artifact. Explore-mode, no change / no impl. Asked: ship pi-dashboard as Chrome extension. Verdict: build named-browsers registry, NOT extension; extension tiers = provider rows. Notifications premise REFUTED — Web Push already designed (`openspec/changes/add-server-push-notifications/`, `public/sw.js`, wires `isUnreadTrigger` at `event-status-extraction.ts:201`, call `event-wiring.ts:647`); `chrome.notifications` Chrome-only, desktop-only, no phone. Only extension seam = air-gapped WS notifier (Web Push needs public push service); MV3 SW idle-kill 30 s reset by WS activity (Chrome 116+). Panerelay live-probed: NOT CDP, no `endpoint()` → cannot satisfy `CdpEndpointProvider` (§13), no `Page.startScreencast`, false-success (`upload` prints `✓ Done` attaches nothing), `--allowed-domains` rejects provider plugins; host BROKEN: `GLOBAL_DEFAULT=yes`, `AMBIGUOUS_BROWSER=yes`, 3 `BROWSER_ID`s, `READY=no`, 0 chrome procs → stale ghost registrations, no liveness reaping. Multi-profile = 3 real profiles (`AgentAutomation`, `Profile 21` blackbelt.hu, `Profile 23` semmi.se) ↔ 3 `BROWSER_ID`s 1:1; gap = identity not capability; breaks §15 ceiling (per-profile parallel, ~529 MB/browser); SECURITY: work + personal identities both carry `debugger`/`nativeMessaging`/`scripting`. Chrome API: `debugger` NOT allowed in `optional_permissions` (kills runtime T1→T2 escalation), `optional_host_permissions` allowed, extension-origin `fetch()` solves plain-http LAN escape (E). No-native-host insight: pi-dashboard long-lived → extension = plain WS to `ws://localhost:8000`, drops nativeMessaging + per-OS installers. Server fits: 3rd WS gateway = repeated pattern (`browser-gateway.ts` `noServer:true` line 268), seam `system-routes.ts:813` `capabilities`, `chat-embed/` full-fidelity embed, `useMobile` <768w/<600h, `isCorsOriginAllowed` needs `chrome-extension://<id>`, thin `agent-browser.plugin.v1` stdio→REST shim, no Panerelay fork. Server-side browser load-bearing: 114 Playwright specs, docker hermetic Chromium, §15 one-browser TOCTOU — extension can never replace. Registry grep `"browsers"|defaultBrowser|namedBrowser|browserProvider` in shared+server = EMPTY, never built. Capability matrix 4 providers × 8 columns: no dominator, gaps complementary, Panerelay trusted-input = reports success does nothing, ext T1 screencast = `captureVisibleTab` polling quota-limited. Hard rule §13: "detect capabilities to pick a default; pin the choice explicitly; NEVER silently fall back" — `GLOBAL_DEFAULT=yes` IS that failure. Sequencing: registry FIRST, validate vs bundled agent-browser + Panerelay, zero extension code; T1/T2 independent increments. Recommend TWO extensions (clean consent split), gateway merges registrations. Landscape: Claude for Chrome (validation), Atlas/Comet forked browsers, Browser MCP = prior art. Fork whole browser EVALUATED, REJECTED — fork buys distribution-as-browser, NOT capability; capability already present 3 ways. Flagship fork DIED: ChatGPT Atlas (OpenAI, Chromium/Blink, macOS-only) released `2025-10-21`, shut down `2026-08-09`, ~10-month lifespan; announced Windows/iOS/Android NEVER shipped; openai.com banner verbatim: "This post introduced ChatGPT Atlas. Atlas has since been deprecated."; unlimited budget could not sustain one-platform fork; Anil Dash: "anti-web browser", "actively fights against the web"; Axios: agent+memory widen prompt-injection exposure. Comet survives — but Perplexity product IS the browser. Repo ships Chromium and does NOT maintain it: `electron: 32.3.3` → Chromium 128 (`package.json:32`, published `2025-03-03`) vs latest stable `electron@43.4.1` → Chromium 150 (`2026-08-19`); 11 Electron majors / 22 Chromium majors / 533 days behind; support = latest 3 majors (43/42/41), Electron 32 EOL, no security backports; FILED as issue #529. Fork spectrum T0→T4: T0 bundled Chromium (Playwright `chromium-1228`) ALREADY SHIPPED, T1 Electron shell + `WebContentsView` ~90% ALREADY BUILT, T2 Chromium distribution + enterprise policies (no source change), T3 patch-set fork (Brave model) = permanent rebase treadmill, T4 hard fork = nobody sane does this. T3 = what "fork" actually means: `brave-core` = build tooling + patch set, NOT Chromium copy; fetches via `depot_tools`, pins exact rev (e.g. `65.0.3325.181`), mounts at `src/brave`; `pnpm run sync` = update chromium → apply patches → update gclient DEPS → run hooks, `--force` re-applies ALL patches; machinery exists because rebase permanent + staffed, Chromium ships every 4 weeks. Build cost (chromium build_instructions): ≥ 8 GB RAM ("more than 16GB highly recommended"), 16–32 GB swap, ≥ 100 GB disk, ~50–80 GB build output, clang + `libc++` only, Ubuntu 22.04 infra; benchmark HP Z600 16-core 12 GB: 12m20s tmpfs / 15m40s without. REFRAME: own browser already exists in embryo — `packages/electron` = Chromium under project control, CDP already wired: `resolve-cdp-activation.ts` (`--debug-cdp` / `--debug-cdp=<port>` / `PI_DEBUG_CDP=1|true|<port>`, default port `9222`, default OFF, CLI wins over env), `main.ts:31` `appendSwitch("remote-debugging-port", ...)`, switch must be set during browser-process init (`main.ts:26` comment), no retroactive CDP (`main.ts:527-530`, must fully quit + relaunch), `playwright.electron.config.ts` drives shell; shipped `See change: ship-browser-skill-and-electron-cdp` (archived `2026-05-28`). Window: `BrowserWindow` `main.ts:401`, `loadURL(serverUrl)` `:432`, `webPreferences` `nodeIntegration:false` + `contextIsolation:true` `:407-409`. Add `WebContentsView` agent-drivable tab, NO fork → full CDP already on `9222`, screencast local-window trivial, network intercept `session.webRequest`, plain-http LAN native (no mixed-content rule), native notifications built in; KEY: `session.fromPartition()` = NAMED isolated cookie jars first-class — exactly the identity layer multi-profile finding said missing (Chrome = opaque `BROWSER_ID`s across 18 profiles, no naming layer; Electron gives it free). What fork does NOT buy: user's EXISTING logged-in sessions (fork = fresh browser; Atlas needed import flow; imported cookies != imported passkeys); only Panerelay/extension reach real authenticated Chrome; fork = ONLY table option that does NOT solve what people actually want. Ranked: 1 fix Electron pin 32 → 41/42/43 (#529, security debt on shipped app, prerequisite), 2 `WebContentsView` = "own browser" outcome ~1% of fork cost, lands multi-profile via `session.fromPartition()`, 3 registry stays foundation, Electron tab = one more provider row. Upgrade surface MEASURED smaller than feared: `main.ts` Electron API narrow, all stable (`app.quit`(8), `BrowserWindow`(7), `dialog.showMessageBox`(5), `app.commandLine`(5), `app.on`(4), `ipcMain.removeHandler`(3), `ipcMain.handle`(3), `shell.openExternal`(2), `app.whenReady`(2), `app.requestSingleInstanceLock`(2), `app.isPackaged`(2), `app.name`, `app.disableHardwareAcceleration`); no `remote`, `contextIsolation:true` + `nodeIntegration:false` already; no native RUNTIME deps → no ABI rebuild (`electron-updater` pure JS, `sharp ^0.33.5` devDep icon-only); risk concentrates in toolchain/packaging (`@electron-forge/* ^7.6.0`, `electron-builder ^26.8.1`, `forge.config.ts`, `electron-builder.yml`, `electron-builder-nsis.json`, `entitlements.plist`); regression gate `playwright.electron.config.ts`; open changes `electron-platform-extraction` + `harden-electron-renderer-boundary` touch package. Security: relay MUST NOT enable `Runtime` domain, `Network.getAllCookies` exfiltrates SSO, real profile drivable via zrok = blast-radius escalation, per-origin auth in dashboard + kill switch + audit + `localhost-guard.ts` loopback gate, domain guard in our relay. 8 open spikes incl. `debugger.attach()` vs host permissions (UNVERIFIED, sets T2 ceiling) + Private Network Access; NEW: `WebContentsView` ergonomics (zero repo precedent — `WebContentsView` grep ZERO in source, only binary match in built framework; `BrowserWindow` confined to `main.ts` + `doctor-window.ts`/`remote-connect-window.ts`/`app-menu.ts`/`window-state.ts`/`tray.ts`), true Electron 32→43 size (toolchain unmeasured), CEF vs Electron (unevaluated). Sources fetched 2026-08-21: chrome.permissions, permissions-list, chrome.debugger, chrome.sidePanel, cross-origin network requests, Claude for Chrome, ChatGPT Atlas intro, ChatGPT Atlas Wikipedia, electron-timelines, chromium build_instructions, brave-core, Perplexity Comet intro, live registry.npmjs.org/electron query. Live-probed macOS host.
