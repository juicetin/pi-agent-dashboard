# research/hermes-memory-pressure-kb-archive.md — index

Research dossier. Decrease pi-hermes-memory pressure + keep most-relevant entries via a kb-indexed cold archive tier. Explore-mode, no change / no impl. Backend `npm:pi-hermes-memory` (src at `~/.pi/agent/npm/node_modules/pi-hermes-memory/src/`). KEY FINDINGS: (1) ceilings `constants.ts` memory=user=project=5000, failure=mem×2=10000 chars (counts `<!-- created=,last= -->` bytes); (2) `memoryMode="policy-only"` → full markdown NOT injected, only ~1KB policy + ≤5 failures ≤7d → context cost already minimal, felt pressure = STORAGE eviction not context; (3) THE GAP: eviction/consolidate `DELETE FROM memories` in BOTH .md AND SQLite (`sqlite-memory-store.ts` removeSyncedMemories/removeExactSyncedMemories) → evicted entries gone from memory_search, NO durable cold tier; (4) `memory-search-tool.ts:73` bumps `last=` = live LRU relevance signal, auto-consolidate drops >30d-unreferenced + preserves user prefs; (5) usage snapshot MEMORY 63% USER 76% failures.md 97% (pressure concentrated in failures + few of 89 project stores, not global); (6) kb indexes external dirs (`kb index --source`, `kb init --global`, BM25+trigram+RRF+rerank) → uncapped archive.md = kb_search cold tier; (7) two-corpora: memory_search=hermes SQLite ≠ kb_search=kb index; (8) POINTER goes in hermes `memoryPolicyCustomText` (always-injected, global) NOT AGENTS.md entries (anti-bloat doctrine) — one line teaching kb_search fallback. Write-path options: manual/periodic distill vs eviction hook vs point-kb-at-live-stores. Avoid fifo-evict (age≠relevance). Settable knobs: `*CharLimit`, `memoryOverflowStrategy`, `llmModelOverride/ThinkingOverride`, `consolidationTimeoutMs`, `failureInjection*`, `memoryPolicyCustomText`. Sources: pi-hermes-memory src, kb --help, live stores, `.pi/skills/consolidate-pi-memory-store`.
