# research/kb-search-retrieval-quality-investigation.md — index

Research dossier. Diagnose `kb_search` "sometimes similar, but unrelevant records" — feeds openspec `fix-kb-search-retrieval-quality`. Corpus 1929 JSONL, 312 calls / 154 sessions, avg 6.89 terms; index 21,945 chunks (doc 96.4%, agents 3.2% + 2.8× longer → buried ~30:1). Implicit-relevance golden sets (markdown n=101, source n=84), scored vs real-index copy `/tmp/kb-ab.db`, paired bootstrap 10k. OUTCOME (n=310): click 21.9% / refine 36.5% / fall-through 41.3% (= UPPER BOUND, warm-session grep often correct — ab-context calibration REFUTED "cued battery" p=1.0000, $20.47). R1 OR-dilution (`toMatch()` ORs tokens): term pruning B/C/D all LOST to A; E coverage .208/.416/.525/.298 vs A .178/.366/.436/.257, p=0.107 NS — tool advice "prefer 2–5 terms" wrong. R2: 64 reformulations = near-random synonym churn (`session` #1 add AND drop); PRF alone degrades P@5 (.297<.366), safe only after coverage rerank (Q p=0.0046). R3: fall-through targets markdown 25.9% vs source 74.1% (grep = exact code identifiers, 36% absent from KB); `doc_type=agents` quota 11%→51% Δ+0.405 34W/0L; intent router LOST (.273 vs .263); T vs A combined R@10 0.286→0.519. R4 chunker RETRACTED: row-level 2.3× worse on baseline; natural experiment sidecar-vs-dir-row = WASH; AGENTS_BYTE_CAP=30000 governs PUSH cost not retrieval. R5 dedup: 55.8% of result slots duplicate-file (100% of queries); bodyHash dedup wrong (by CONTENT, agent picks FILE); isolated dedup R@10 Δ+0.130, 24W/0L, ~4 LOC. R6 presentation: 10/10 hits 1 file 723 tok; breadcrumb 47% of page; render −29% tokens, 3.1× denser. D1–D9 → change: dedup (root,path), fetch limit×6, unconditional quota, PRF+coverage ship together, leaf heading + `(+N more sections)`, `limit`=sources (BREAKING), kb_get path-only fetch, redundancy eval metric + freeze golden sets, `dox lint` missing covers sources (today 47 missing = all `.md`, 0 `.ts`). Push cost 256k–615k tok per induced kb_search. Good/bad instincts + meta-lessons (render one page before optimising). Calibration artifacts `scripts/ab-context/tasks.calibration.jsonl`.
