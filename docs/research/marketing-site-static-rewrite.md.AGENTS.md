# research/marketing-site-static-rewrite.md — index

Implementation record. `site/` dropped Astro 5 + Tailwind 3 + Preact + MDX for ONE hand-written static page. Shipped 2026-08-27, commits `c52745af0` + `e305c361b`, live https://pi-dashboard.dev. User chose static over "port mockup INTO Astro", accepting loss of build-time release data; dropped sections (WhatIsPi, BigIdea, Why, HowItWorks) DELETED not kept. New tree: `site/index.html` (markup + all CSS + ~150 lines vanilla JS), `404.html`, `field.js` (three.js bg), `vendor/three.module.min.js`, `media/` (hero film dark+light + posters), `public/` (CNAME, favicon.png, og-card.png, app-icon.png), `build.mjs`, `design-scratch/` (design source, not deployed); `package.json` ZERO deps. Deleted: `site/src/` (components/layouts/lib/content + 2 tests), astro.config.mjs, tailwind.config.cjs, tsconfig.json, vitest.config.ts, package-lock.json, `site/scripts/` (orphaned product-screenshot pipeline → public/screenshots). Page PROMOTED not copied: iterated as mockup under design-scratch/mockup/, moved to site root so file under test == deployed file; `bg3d.html` lab → `design-scratch/field-lab.html` importing `../field.js`. Build = copy + gate: `node site/build.mjs` wipes dist/, copies ALLOWLIST (`index.html`, `404.html`, `field.js`, `media`, `vendor`) + `public/*` to dist root, FAILS if any local `src=`/`href=` unresolvable; allowlist not ignore-list (design-scratch/ 236 MB one level down); `public/` copy carries CNAME — losing it takes pi-dashboard.dev apex down; artifact contract `site/dist` unchanged so deploy-site.yml still uploads it, still copies packages/shell → site/dist/app; artifact 37.2 MB → 7.5 MB after pruning hero-web.*, promo-web.mp4, themes-web.*, hero-poster.jpg + orphaned public/screenshots (24 MB) + kraken-brain.png. RELEASE DATA = the one real loss: Astro fetched latest release at BUILD time (site/src/lib/github-release.ts + cache site/src/data/latest-release.json); static cannot. Replacement `site/design-scratch/scripts/sync-release.mjs` rewrites download block in index.html from api.github.com/repos/BlackBeltTechnology/pi-agent-dashboard/releases/latest; anchors carry `data-asset` keys (mac-arm64, mac-x64, linux-appimage-x64/arm64, linux-deb-x64/arm64, win-exe-x64/arm64, win-zip-x64/arm64), release line `data-rel-tag`/`data-rel-date`/`data-rel-notes`. KEY RULE: match by SHAPE (extension + arch suffix), never filename — electron-builder versions every name, literal match breaks on tracked release; `.blockmap` + `.yml` MUST be filtered (update metadata, share suffixes). formatBytes copied from Astro lib (binary, 1 decimal). `--check`: exit 1 stale, exit 2 network/API failure, distinct. Run `npm run sync-release` / `npm run check-release` from `site/`. Workflows: deploy-site.yml dropped npm ci / astro check / astro build / npm run size; runs `npm run build` + `npm run check-release` `continue-on-error: true` (stale page beats no page, drift lands in log); pnpm/action-setup KEPT (root workspace install builds packages/shell). sync-release-version.yml no longer writes latest-release.json (gone); runs `node site/design-scratch/scripts/sync-release.mjs` on `release: published`(+edited), commits site/index.html to develop. THEME inherited wholesale: storage key `pi-theme`, modes System/Light/Dark, aria `role="radiogroup"` + `aria-checked` from deleted ThemeScript.astro + ThemeToggle.tsx (Preact island → ~30 lines vanilla). ONE difference: Astro signalled dark with `class="dark"`; static signals light with `data-theme="light"`, dark = absence — what field.js MutationObserver already observes; inline `<head>` script runs FIRST (before stylesheets) or page flashes; second matchMedia listener in body script swaps hero film source. Measured: dark OS → dark, light OS → light, click Dark survives reload, System follows live OS flip both ways, Playwright `waitUntil:"commit"` attribute already correct (no flash). `site/` NOT a pnpm workspace member (`packages/*` only); `npm run build -w site` FAILS; run from site/, nothing to install. VERIFICATION (reusable): `site/design-scratch/scripts/serve.mjs` zero-dep static server (default root site/, port 8791) + `shoot.mjs --audit [--links]` exit non-zero on defect; ran vs built artifact + live domain: 21 audit runs (system/light/dark × 320/390/768/860/1024/1080/1440) zero doc/nav overflow, sticky header checked at page bottom, no dead anchors / broken images / page errors; 18/18 external links 200 incl. every download URL; live `/`, `/404.html`, `/field.js`, `/media/*`, `/vendor/*`, `/og-card.png` and `/app/` (neutral shell subpath — what a bad allowlist silently breaks) all 200. GIVEN UP: MDX collections, Preact islands, Tailwind class layer, `astro check`, 50 KB gzipped JS budget (scripts/check-js-size.mjs) — re-adding = re-introduction, components exist only in git history. STANDING RISK: page cannot discover new versions; sync-release-version.yml load-bearing — if it fails, site advertises previous release links, only non-blocking check-release line in deploy log says so. SUPERSEDES: neutral-shell-deploy-and-pairing-durability.md Finding 1 STALE (Astro + npm ci/npm run check/npm run size + stale site/package-lock.json blocking Deploy Site) — lockfile no longer exists; cross-reference only, that doc not edited.
