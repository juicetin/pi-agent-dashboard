# research/neutral-shell-deploy-and-pairing-durability.md — index

Research artifact. Explore-mode, no change / no impl. Question: release shell app on pi-dashboard.dev for testing? Answer: no release needed — site/shell deploy independent of npm/Electron pipeline; one stale lockfile blocks it. FINDING 1: packages/shell (neutral static PWA, @blackbelt-technology/pi-dashboard-shell v0.7.0, private) publishes to GitHub Pages SUBPATH pi-dashboard.dev/app/ via deploy-site.yml; site/ (Astro) owns apex. Triggers: push develop touching site/** or packages/shell/**, workflow_dispatch, release:published (redispatches on develop). Measured 2026-08-13: apex 200 (stale), /app/ 404, shell never live; last 5 Deploy Site runs all failure ~20s, last success 2026-05-30. ROOT CAUSE: site/package.json devDependencies gained vitest ^4.0.0, site/package-lock.json never regenerated (grep -c vitest = 0); npm ci aborts "Missing: @types/deep-eql@4.0.2" (also assertion-error, siginfo, stackback), dies at step 3, shell steps never run. FIX: regenerate lockfile → unblocks shell + stale marketing site. FINDING 2: secure-context rule BY DESIGN (archived add-server-keypair-pairing design D4): HTTPS shell cannot open ws://, only wss/https reachable, plain-http LAN OUT, http://localhost escape hatch. reachableUrls() (pairing.ts) enforces at read time, admits https/wss only except isTestLoopbackOrigin under PI_E2E_SEED=1; createPayload() null → route 200 {success:false,error:no_reachable_endpoint} (pairing-routes.ts:74); CORS rule 6 allows pi-dashboard.dev origin, subpath /app/ keeps it valid. FINDING 3 (real gap): urls[] written ONCE at pairing (PairView.tsx:93/96 addServer); connect.ts imports KeyringEntry type only, never writes back, no updateServerUrls; ephemeral zrok rotation (tunnel.zrok.persistent default false, architecture.md:2286) kills stored urls; resolveVerifiedUrl throws "no reachable url proved the pinned identity" — identity fine, URL dead, misleading UX. D3 multi-URL promise vs D4 https filter shrink; no refresh endpoint (createPayload mints fresh ~60s pairing code per call — wrong tool). Options: (a) read-only bearer-gated reachableUrls route (split nearly free), (b) shell writeback on connect (warm only, cannot rescue cold entry), (c) config-only reserved zrok name / second stable url. FINDING 4: bearer passes networkGuard — onRequest isAuthenticated runs before preHandler; branch order isGenuinelyLocal → verifyLocalToken → isBypassedHost → isAuthenticated → else 403 network_not_allowed. Auth not missing piece; endpoint is. FINDING 5 (measured, highest value): tailscale serve injects x-forwarded-for/x-forwarded-host + Tailscale-User-* headers (echo server 127.0.0.1:8399, serve --bg --http=8081): hasProxyForwardingHeaders true → isGenuinelyLocal false → no loopback bypass; forged Tailscale-User-Login/Name + XFF REPLACED not appended → not spoofable from tailnet client. Caveats: plain-http path only, not :443 (cert toggle off); tailnet identity = authentication not authorization (login allowlist for any tailnet-SSO); trust XFF presence, not value. FINDING 6: tailnet CertDomains null (HTTPS certs off — admin-console toggle, real blocker); deriveEndpoints (tailscale.ts:121) serveHasHttps from :443 serve keys else http tls false → dropped by reachableUrls; tunnel.provider single value, tailscale disconnect() runs serve reset; way around: tailscale serve outside provider lifecycle + add MagicDNS https url via UI "Add HTTPS URL" → appendPublicBaseUrl → auth-gated PUT /api/config → TWO urls under one pinned identity, restores D3 redundancy, zero code. BOTTOM LINE: regenerate site/package-lock.json, dispatch Deploy Site, durable https endpoint (tunnel.zrok.reservedName + persistent:true, or tailnet HTTPS + MagicDNS publicBaseUrls); no release, no code. Open items NOT actioned: read-only url-refresh route, shell writeback, dead-url identity-mismatch message. Candidate changes: fix-deploy-site-ship-shell, refresh-keyring-urls.
