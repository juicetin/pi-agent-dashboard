# research/omp-agent-parallel-support.md — index

Research dossier. Explore-mode, no change / no impl. Question: per-session selectable pi vs omp, both visible simultaneously. User constraint: adoption NOT decided — evaluation only. Flavor scope decided = PER-SESSION (spawn either pi or omp per card). VERDICT: CONDITIONAL GO — spikes 1+2 EXECUTED 2026-08-29; loader + core API GREEN live; risk profile IMPROVED spike 2: storage claim CORRECTED (sessions JSONL pi-identical layout; SQLite scope = settings/auth/models/history ONLY), version-skew REFUTED (display quirk), RPC RESOLVED (gap XS — keeper dispatches `prompt` only); runtime-undefined members CONFIRMED unconditional both modes; NEW gap #7 per-flavor env isolation (omp honors `PI_CONFIG_DIR`); session RECORD schema UNMEASURED = LAST RED. Fork NOT reusable: `oldschoola/omp-agent-dashboard` = 2 commits AHEAD / 1193 BEHIND develop, 256 files, +9866/−8570, blind sed rebrand `pi`→`omp` — renamed DASHBOARD-OWNED `PI_SETTINGS_PATH`→`OMP_SETTINGS_PATH`, `PI_PLUGINS_DIR`, state dirs `~/.pi-dashboard/`→`~/.omp-dashboard/`, left `package.json#name` upstream; DELETES pi support, not parallel — value = rename-axis checklist only. omp = `@oh-my-pi/pi-coding-agent` hard fork by `can1357` (repo pins `^0.84.1` `packages/server/package.json:45`, `>=0.80.10` `packages/extension/package.json:48`), v`18.0.11` (2026-08-29) vs pi `0.84.4`; `bin` = `omp` NOT `pi`; **requires Bun** (`engines.bun >=1.3.14`, shebang `#!/usr/bin/env bun`, marker `// @bun`); dep tree renamespaced `@oh-my-pi/*` (`pi-ai`, `pi-tui`, `pi-agent-core`, `pi-catalog`, `pi-utils`, `pi-wire`, `pi-natives` + omp-only `hashline`, `snapcompact`, `pi-mnemopi`, `omptype`, `omp-stats`); env vars MIXED — RETAINS `PI_*` (`PI_SESSION_FILE`, `PI_CODING_AGENT_DIR`, `PI_TOOL_BRIDGE_URL/TOKEN/SESSION`, `PI_ARTIFACTS_DIR`, `PI_NO_PTY`, `PI_COMPILED`, `PI_PROXY`) + ADDS `OMP_*` (`OMP_AUTH_BROKER_URL`, `OMP_PROFILE`, `OMP_MCP_TIMEOUT_MS`, `OMP_NO_WEBP`); home `.omp/` ×31, `".omp"` ×11, ZERO `.pi/` → `~/.omp/agent`, env largely compatible. RPC + extension subsystem preserved: `src/modes/rpc/{rpc-mode,rpc-client}.ts`, `src/extensibility/extensions/{types,loader,runner,wrapper,model-api,index}.ts`, CLI `--mode --extension --plugin-dir --print --continue --fork --no-extensions`. ENABLING: bridge runtime-agnostic ALREADY — all 9 pi imports type-only (`bridge.ts:21`, `provider-register.ts:36`, `role-manager.ts:31`, `canvas-tool.ts:22`, `command-handler.ts:17`, `dashboard-context-injector.ts:17`, `role-model-tools.ts:18`, `ask-user-tool.ts:8`, `bridge-context.ts:5`), `import type { ExtensionAPI }`, ZERO runtime coupling. ExtensionAPI: omp dropped only 0-usage members (`deliverAs`, `registerEntryRenderer`, `registerMarkdownTransformer`, `triggerTurn`); all used members present (`appendEntry` 11, `sendUserMessage` 210, `registerTool` 41, `registerProvider` 27, `registerCommand` 9, `setModel` 8, `exec` 13, `on` 50, `events` 249); omp-only additions `getServiceTiers`, `setServiceTier`, `logger`, `arktype`, `typebox`, `zod`, `registerAssistantThinkingRenderer`, `registerComposerShape`, `registerFileWriteFallback`, `registerFileDeleteFallback`, `getArgumentCompletions`. Event surface (pi union `…/dist/core/extensions/types.d.ts:773`, omp `types.ts:1068`): COMMON = `session`, `context`, `resources_discover`, `before/after_provider_request/response`, `agent_start/end`, `turn_start/end`, `message_start/update/end`, `tool_execution_*`, `tool_call/result`, `user_bash`, `input`; pi-ONLY absent = `project_trust` (already `as any` `bridge.ts:2584`), `model_select` (`provider-register.ts:1110`, `bridge.ts:2035/2204`), `thinking_level_select` (`bridge.ts:2036/2216`), `agent_settled` (`agent-settled.ts` + `bridge.ts:2025`), `BeforeProviderHeadersEvent`; omp-ONLY = `auto_compaction_start/end`, `auto_retry_start/end`, `retry_fallback_applied/succeeded`, `tool_approval_requested/resolved`, `todo_reminder`, `goal_updated`, `credential_disabled`, `mcp_notification`, `session_stop`, `user_python`, `ttsr_triggered`; `queue_update`+`session_info_changed` = bridge-internal emitters, NO gap. agent_settled SYNTHESIS exists (`agent-settled.ts`: `NATIVE_AGENT_SETTLED_FLOOR="0.80.4"`, `nativeAgentSettledSupported(v)`, `synthesizeAgentSettledEvent(ts)`, `settleFollowUp("agent_end", false, ts)`; pure, unit-tested; See change `adopt-pi-074-080-features`) BUT version-gate TRAP: raw semver major-first → `"0.84.1"`→true ✓, `"0.79.0"`→false ✓, **`"18.0.11"`→true WRONG** → omp assumed native, synthesis never fires, idle detection dies SILENTLY; latent for ANY major ≥1 incl. future pi 1.0; fix = FLAVOR-derived capability, not version. model/thinking: setters kept (`omp types.ts:1459/1462` `get/setThinkingLevel`, `setModel`, `registerProvider`, `unregisterProvider`) → dashboard→agent FULL, TUI→dashboard LAGGED (no `model_select`; `sendModelUpdateIfChanged()` dedup, re-drive at turn boundaries). Axis: FLAVOR-SCOPED = bin+runtime, agent home (`~/.pi/agent` vs `~/.omp/agent`), project dir (`.pi/` vs `.omp/`), manifest key+keyword (`pi`/`pi-package` vs `omp`/`omp-package`), npm scope+floor (`@earendil-works ^0.84` vs `@oh-my-pi ^18`), capability flags; DASHBOARD-OWNED SINGULAR = `~/.pi/dashboard/` (config, `server.log`), `~/.pi-dashboard/` (managed install), home lock, gateway socket, `sessions.json`. Gaps: (1) binary+runtime — `resolver.resolvePi()` `spawn-preflight.ts:76`/`process-manager.ts:444/445`, `PI_SPAWN_TOOL="pi"` `pi-runtime.ts:39`, `commandBasename(entry.command)==="pi"` `process-classifier.ts:80`, `registry.resolve("pi")` `cli.ts:233`/`pi-version-skew.ts:101`/`worktree-init.ts:304`/`doctor-routes.ts:61/88/94`, needs omp tool def + BUN PREFLIGHT, M; (2) **147 non-test source files** carry `.pi/agent|skills|settings` literals outside `managed-paths.ts`+`dashboard-paths.ts` (worst `provider-register.ts` 13, `pi-package-resolver.ts` 11, `git-operations.ts` 10, `scaffold.ts` 8, `git-routes.ts` 7, `pi-resource-scanner.ts` 6), L DOMINANT; (3) session discovery `resolvePiSessionsDir()` `dashboard-paths.ts:70` hardcodes `~/.pi/agent/sessions`, dual-root scan; JSONL layout pi-IDENTICAL (`<sessions>/<mangled-cwd>/<ISO>_<id>.jsonl`; id 16-hex vs UUID → scanner id-format tolerance, NOT new parser), M (downgraded from M–L HIGHEST RISK); RECORD schema unknown = LAST RED; (4) manifest keys ADDITIVE — one package declares BOTH `"pi"`+`"omp"` keys, same physical paths, `keywords: ["pi-package","omp-package"]`, each agent reads own key (`pi-package-resolver.ts:356`), NO dir rename needed, S; (5) version floor `piCompatibility.minimum` semver-compare + `pi-core-checker.ts`, `pi-core-updater.ts`, `pi-version-skew.ts` → per-flavor, S–M; (6) capability degradation — bridge tolerates absent `agent_settled`/`model_select`/`thinking_level_select`, client hides dead affordances, M. Options: A fork-and-rebrand REJECTED (not parallel, permanent divergence, effort L then 2× forever); **B `AgentFlavor` descriptor + capability flags RECOMMENDED** (single codebase, per-session flavor, dashboard state singular, L once then ~0 marginal); C pluggable agent-provider SPI DEFERRED (XL, over-engineered for n=2). Scorecard: spawn omp sessions YELLOW (bin+bun resolution, distribution-heavy), transcript GREEN (`message_*`+`tool_execution_*` present), provider auth GREEN, model/thinking UI GREEN (TUI→dashboard lagged), idle/`agent_settled` GREEN (MUST fix trap), discover external omp sessions RED (schema drift). Spike (~half day, throwaway, no repo changes): (1) `bun upgrade` ≥1.3.14 + `npm i -g @oh-my-pi/pi-coding-agent` + `omp --version`; (2) LOADER TEST — hand-register current `bridge.ts` as omp extension, everything moot if loader rejects entry shape; (3) EVENT FIDELITY — one real turn, log every event+payload, diff vs pi; (4) JSONL SCHEMA DIFF `~/.omp/agent/sessions/*.jsonl` vs `~/.pi/agent/sessions/` — THE EXPENSIVE UNKNOWN; (5) RPC HANDSHAKE `omp --mode rpc` vs rpc-keeper; (6) SETTLE PROBE — `agent_end` fires, `agent_settled` does NOT, validates synthesis + trap. Residual risks: JSONL drift 0.84→18.x (only cost balloon past B), Bun distribution dep (Electron bundle, docker image, `qa/` smoke, install docs), `agent_settled` trap silent (fix as capability-gating regardless of omp). Sequencing if GO: spike → extract `AgentFlavor` + fold 147 literals → per-flavor floors `server/src/pi/` → dual manifest keys → capability flags → bridge guards → client gating → dual-root scanner + JSONL verify. Reproduce: `gh api 'repos/BlackBeltTechnology/pi-agent-dashboard/compare/develop...oldschoola:develop'`; `npm pack @oh-my-pi/pi-coding-agent` + untar + inspect; type-only census `grep -rhE 'from "@earendil-works/pi-coding-agent' --include='*.ts' packages/extension/src | grep -v __tests__ | grep -c 'import type'`; stray census `grep -rlE '"\.pi"|\.pi/agent|\.pi/skills|\.pi/settings' --include='*.ts' --include='*.tsx' packages/*/src | grep -v __tests__ | grep -vE 'shared/src/(managed-paths|dashboard-paths)\.ts' | wc -l` → 147.

SPIKE 2026-08-29: `bun upgrade` 1.3.10→1.4.0; `npm i -g @oh-my-pi/pi-coding-agent` 156 pkg, bin `/Users/robson/.bun/bin/omp`. **VERSION SKEW REFUTED spike 2** — `dist/cli.js` `18.0.11` ×4, dist/src in sync at 18.0.11; `omp --version` `13.14.2` = display quirk (`config.yml` `lastChangelogVersion`) — PACKAGE version authoritative, `omp --version` output untrusted (gap #5 amended). LOADER GREEN live — `export default function activate(pi)` loaded + ACTIVATED via `omp -e <path.ts>` raw TS, no build; `loader.ts:58-61` `ExtensionFactory | {default?: ExtensionFactory}`, picks function else `.default`, matches `bridge.ts:170` exactly; omp example `api-demo.ts` same shape; `pi.registerTool` OK. RUNTIME CENSUS (`typeof`) PRESENT fn: `appendEntry, sendUserMessage, registerTool, registerCommand, registerProvider, setModel, setThinkingLevel, getThinkingLevel, getActiveTools, getAllTools, getCommands, exec, on, setLabel`; `events` object; **DECLARED in types, UNDEFINED at runtime**: `getSessionName` (`types.ts:1477`/`:1701`), `setSessionName` (`types.ts:1480`/`:1702`), `unregisterProvider` (`types.ts:1527` + `:1683` 2-arity overload) → **omp `.d.ts` NOT a compatibility oracle**; exposure = 22 call sites throw `TypeError: not a function` (`getSessionName` 12: `session-sync.ts:160/230`, `bridge.ts:2012/2569` +8; `setSessionName` 6: `bridge.ts:1972` ALREADY guarded, `command-handler.ts:910`, `auto-session-namer.ts` +3; `unregisterProvider` 4: `provider-register.ts:920/928` +2; UNGUARDED `session-sync.ts:160/230`, `bridge.ts:2012`) → capability SHIMS required; sig drift omp `setSessionName`→`Promise<void>` vs pi `void`, 2-arity `unregisterProvider(name, sourceId)` pi lacks. `pi.on` PERMISSIVE — 19/19 probed names `PROBE_SUB ok` incl `agent_settled`, `model_select`, `thinking_level_select`, `project_trust`; subscribe unconditionally, absent events never fire, no try/catch. LIFECYCLE measured (one turn): `session_start→before_agent_start→agent_start→turn_start→message_start→message_end→context→message_start→message_end→turn_end→agent_end`; LIVE absent confirms §3c: `agent_settled`, `model_select`, `thinking_level_select`, `project_trust`; `message_update`+`tool_execution_*` absent non-streaming `--print` run — re-probe under streaming/tool turn, NOT established gaps; `agent_end` FIRES → `settleFollowUp("agent_end", nativeSupported=false, ts)` synthesis VIABLE (trap CONFIRMED live). MCP HANG (NEW operational blocker): `omp --print "say ok"` rc=124, 0 bytes stdout/stderr, 90s; `omp models` hangs `Connecting to MCP servers: RepoPrompt…`; repro under PTY `script -q` → NOT TTY requirement; `OMP_MCP_TIMEOUT_MS=1500` did NOT bound; no `--no-mcp` flag (`--no-tools/--no-lsp/--no-skills/--no-rules/--no-extensions/--no-session/--no-pty/--no-title` only); live Bun runtime blocked on I/O not crashed (`sample <pid>`); MCP config source NOT found (`~/.mcp.json`, `~/.claude.json`, `~/.cursor/mcp.json`, `~/.config/mcp.json`, `~/.omp/agent/mcp.json`, `~/.pi/agent/mcp.json`; `agent.db` `settings` empty); WORKAROUND clean `HOME=/tmp/omp-home` → runs immediately; spawn preflight MUST bound omp startup + surface timeout (`spawn-preflight.ts`, `spawn_register_timeout`). STORAGE CORRECTED spike 2 (gap #3 DOWNGRADED): SQLite scope = settings/auth/models/history ONLY — omp `agent.db` (`settings` + `auth_credentials` tables: `id INTEGER PK AUTOINCREMENT, provider TEXT, credential_type TEXT, data TEXT, disabled_cause TEXT, identity_key TEXT, created_at INTEGER, updated_at INTEGER`), `models.db`, `history.db` FTS5 (`history(id,prompt,created_at,cwd)` + trigger `history_ai`; `history_fts_data/idx/docsize/config`) all WAL (`-shm`/`-wal`), + `~/.omp/agent/config.yml` YAML vs pi JSON files; SESSIONS JSONL pi-IDENTICAL — omp `~/.omp/agent/sessions/<mangled-cwd>/<ISO>_<id>.jsonl` (observed `--private-tmp-omp-spike--/2026-08-29T22-58-57-251Z_156b3d2c18c7c9fa.jsonl`) vs pi `~/.pi/agent/sessions/<project>/*.jsonl`; id token omp 16-hex vs pi UUID → scanner id-format tolerance, NOT new parser; RECORD-level schema UNKNOWN (4 omp files 0 bytes — no working credential); extras `terminal-sessions/`, `logs/`; `pi-package-resolver.ts` + `credential-detect.ts` need SQLite readers; credentials do NOT carry over — auth omp separately; `--session-dir=<value>` EXISTS overridable. RPC RESOLVED spike 2 — omp `rpc-mode.ts` case set 42 vs pi `RpcCommand` 36 (`dist/modes/rpc/rpc-types.d.ts:14`); true pi-only `clear_queue`, `clone`, `get_available_thinking_levels`, `get_entries`, `get_tree`; keeper dispatches EXACTLY `prompt` → gap XS; omp negotiates `{type:"negotiate_protocol", protocolVersion:2}` (`rpc-client.ts:449`), NDJSON stdout frames + backpressure (`rpc-mode.ts:713-721`). VERDICT REWEIGHT: keep CONDITIONAL GO — loader/entry + core API/events GREEN resolved the two sinkers; +2 costs (capability shims, MCP preflight); gap #3 deepened ENGINE-level. Scorecard: spawn YELLOW (MCP preflight), transcript GREEN (`message_*`/`context` live; `message_update`/`tool_execution_*` re-probe), provider auth GREEN→YELLOW (`unregisterProvider` undefined, SQLite auth, no credential carryover), model/thinking GREEN, idle/`agent_settled` GREEN (synthesis viable), discover external omp YELLOW (record schema unmeasured; layout JSONL pi-identical). REMAINING UNKNOWNS (post-spike-2): session RECORD schema ONLY = LAST RED (needs ONE authenticated turn); `message_update`/`tool_execution_*` unobserved under streaming/tool turn (note only); RPC RESOLVED; MCP config source MOOT (clean HOME + preflight bound). Reproduce: `npm i -g @oh-my-pi/pi-coding-agent`; `bun upgrade`; probe ext `/tmp/omp-spike/probe-ext.ts` (`export default function activate(pi){}` + `typeof` census + 19-name `pi.on` + `registerTool`); run `HOME=/tmp/omp-home omp --print --no-lsp --no-skills --no-rules -e /tmp/omp-spike/probe-ext.ts "reply with exactly: OK"` — **clean HOME MANDATORY**; hang repro `omp --print "hi" </dev/null` rc=124 — do NOT pipe to `head`, SIGPIPE masks rc=0.

SPIKE 2 2026-08-29: VERSION SKEW REFUTED — `dist/cli.js` `18.0.11` ×4, `"\.pi"` ×0, `.omp` ×47; `src/` `.omp` ×146; dist+src in sync at 18.0.11; `13.14.2` display quirk = `config.yml` `lastChangelogVersion`. RUNTIME-UNDEFINED CONFIRMED unconditional — `--mode rpc` probe IDENTICAL to `--print`: `getSessionName`/`setSessionName`/`unregisterProvider` undefined both; present in bundle (`getSessionName` ×20, `setSessionName` ×16, `unregisterProvider` ×3) — implemented, NOT exposed on ExtensionAPI → 22-call-site exposure REAL, NOT probe artifact; workaround = omp RPC `set_session_name` command. LIFECYCLE re-measured ×3 identical: `session_start→before_agent_start→agent_start→turn_start→message_start→message_end→context→message_start→message_end→turn_end→agent_end`; `agent_settled` absent every run. **FINDING H env leak**: omp HONORS `PI_CONFIG_DIR` (injected by OUR dashboard: `PI_DASHBOARD_SPAWNED=1`, `PI_DASHBOARD_URL=ws://localhost:9999`, `PI_DASHBOARD_SOCKET=/Users/robson/.pi/dashboard/gateway-9999.sock`, `PI_CODING_AGENT=true`) → dashboard-spawned omp writes into pi's tree = silent cross-flavor contamination; per-flavor env scrub/redirect REQUIRED = gap #7 S–M + residual risk 7. RPC RESOLVED — pi `RpcCommand` 36 vs omp 42; true pi-only `clear_queue`, `clone`, `get_available_thinking_levels`, `get_entries`, `get_tree` (rest = renames `fork`→`branch`, `get_fork_messages`→`get_branch_messages`, `get_commands`→`get_available_commands` + envelope types); keeper dispatches EXACTLY `prompt` → XS. Credentials spike 2: Copilot (ambient `GITHUB_TOKEN`, `https://api.individual.githubcopilot.com/v1/messages`) rejected `claude-haiku-4.5` → `400 model_not_supported`, OpenAI `no credits remaining`, Gemini `gemini-2.0-flash` → `404` → 0-byte sessions. omp 400 diagnostics → `<configdir>/logs/http-400-requests/*.json`. VERDICT REWEIGHT: CONDITIONAL GO — risk IMPROVED (session discovery L→M RED→YELLOW, RPC S→XS, skew fear gone); +env isolation (NEW gap #7); shims confirmed real; LAST RED = session RECORD schema.
