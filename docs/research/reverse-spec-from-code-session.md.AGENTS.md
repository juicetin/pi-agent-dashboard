# research/reverse-spec-from-code-session.md — index

Method playbook. HOW `reverse-spec-from-code` skill + 102-spec backfill built in one pi session. Companion to `reverse-spec-from-code.md` (data). Phased arc: investigate spec format -> build judge-JSON fitness fn on 6 ground-truth specs -> iterate prompt v1->v2 (66.8->97.2 req, cross-boundary exploration dominant lever) -> build skill (discover->generate->audit->validate->promote, scratch-first) -> live validate bus-client -> bulk 102 specs/~16 pkgs -> fix kb-pollution (scratch moved out of openspec/, promote=MOVE) -> model-loss test (judge constant) -> format hard-gate + validate gate -> land. Decisions: judge separate+constant, code=oracle not stale spec, scratch-first+confirm-promote, conservative discovery gate, deterministic validate over model judgment. Failure catalog + takeaways. Commits e9c582433, b999373c7. Skill: `packages/openspec-workflow/.pi/skills/reverse-spec-from-code/`.
