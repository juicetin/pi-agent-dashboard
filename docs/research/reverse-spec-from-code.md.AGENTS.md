# research/reverse-spec-from-code.md — index

Research artifact. `reverse-spec-from-code` skill prompt tuning + generator-model-loss experiment. 6 real openspec/specs = ground truth; generators run BLIND (code only), judge @research scores gen-vs-real semantically. v1->v2 lever = cross-boundary exploration (server-restart 40->95); avg req 66.8->97.2. Model-loss curve (judge constant): opus 97.2 req / 6/6 validate, deepseek-v4-flash 95.8 / 6/6*, haiku-4.5 88.0 / 3/6 (format collapse dominant). Fix: one-line `#### Scenario:` format directive recovers cheap model to 6/6 valid. Config: @fast generator + format directive + validate gate + @research auditor ~= opus quality cheap. Raw specs in gitignored `.reverse-spec-scratch/`. Large-scenario test: coverage degrades with size (97% small -> 50-85% large); cheap-model gap widens with complexity (53-req giant 22pt opus-flash, opus 72 vs flash 50); mitigation = decompose into bounded sub-capabilities + full-layer discovery scope. Skill: `packages/openspec-workflow/.pi/skills/reverse-spec-from-code/`.
