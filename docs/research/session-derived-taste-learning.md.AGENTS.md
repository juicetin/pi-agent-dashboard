# research/session-derived-taste-learning.md — index

Research dossier. Explore-mode, no change / no impl. Evaluate adapting CommandCode "taste" methodology to pi-dashboard: learn user taste automatically from own session history. User constraints: MY taste, not agent failure modes; MUST be automatic, no human ratification; repo-level sharing. VERDICT: DO NOT BUILD — corpus lacks the signal. CommandCode two layers: `taste-1` (hosted closed neuro-symbolic RL, `output = LLM(prompt | taste(user))`, edits/accepts/rejects reward, NOT adaptable) vs the ARTIFACT (plain markdown `.commandcode/taste/<pkg>/taste.md` + `~/.commandcode/taste/`, confidence ∈ [0,1], `npx taste lint/push/pull/list`, `cmd learn-taste`, FULLY adaptable); rules-vs-taste table (written-down vs learned, remember vs every session, broad vs micro, decays vs compounds); "10x faster" claims unverified; $5M seed. pi owns both halves unwired: `session-to-guideline` skill + `scripts/extract_session.ts` (parses JSONL via `parentId`, facts sheet, doctrine "Prompt 1 = goal; prompts 2..N = steering", human-request only, output prose in `Prompt stories/` never read at inference) + `~/.pi/agent/pi-hermes-memory/` (`MEMORY.md`/`USER.md`/`failures.md`/`sessions.db`/`skills/`, categories ALREADY taste vocab: failure/correction/insight/preference/convention/tool-quirk, append-only no score no decay); decay problem empirical via `consolidate-pi-memory-store` skill. Signal inventory: dashboard sees more than CLI (abort API, human file edits, git revert, CodeRabbit threads — `work-coderabbit-review-loop` triages fix/dispute/defer = hand-labelled pairs discarded every PR; silence = WEAK). Corpus: 2536 sessions, 8647 raw user prompts, 100% `parentId`; abort durable via JSONL `stopReason === "aborted"` 201 = 7.9% sessions (others: toolUse ~4872, stop ~229, rawStopReason tool_use/tool_calls/end_turn; `details.cancelled` = dialog cancels, separate); `AbortLatch` runtime-only NOT persistence. THREE FALSIFIED CLAIMS: (1) contamination 26% — 9475 role:user → 6985 real + 2490 injected machinery; "worktree…parent" probe 199 matches → 16 real (<400 ch), 183 injected skill bodies = 92% fake; median 9949 ch, p90 14544, max 92152; ouroboros AGENTS.md → injected → harvested → `.pi/taste/` → injected again, no circuit breaker; (2) fork inflation — same prompt replayed into N files: "restart running clients?" 17 files/2 days, "no fork icon" 13/2, "no jj commit and push" 3 files/1 day, worktree→parent openspec 153/38; up to ~8×; confidence MUST be distinct DAYS; (3) abort demoted ★★★★★→★★ — 190/518 candidates post-abort, dominant post-abort prompt = "go on"; abort = liveness (spinning/rate-limited/stuck), NOT reject. Worktree-openspec ALREADY SOLVED, mechanism before prose: commit `351d33737` (2026-06-05) `worktreeInit` in `.pi/settings.json` with existence gate; commit `41ab03185` (2026-06-19) prose rule (AGENTS.md line 136); corrections May 1, Jun 12 (5 days), Jul 3 (2 days), Aug 0; root cause `.pi/.gitignore` line 2 `skills/openspec-*/**` (9 skills on disk, 2 tracked); earlier "190 corrections/38 days proves enforcement" = 92% injection artifact, thesis UNPROVEN. TRUE inventory: 6985 → 323 candidates → 18 clusters ≥2 days → ~5 learnings. TIER 1 live: dev-server hygiene ~20 days Jun→Aug 26 ("stop vite" 9d/14 hits, "stop it and commit" 6d/7, "stop the frontend dev server"/"stop mockup server" 5d/5, "stop mockup server, seems good" 2d) = "agent leaves dev servers running; kill before finishing", absent from AGENTS.md + skills = TOP REAL SIGNAL; "no. merge it and delete branch, worktree" 2026-05-31→08-09 durable; "dont use jj. Please move to git worktree" 05-28/29 stale; "Why npm is used instead of pnpm?" 07-13/21, small enforcement gap. TIER 2 NOT taste: bug reports, defects, feature requests, operator noise, accepts wearing reject clothing ("no, its ok" vs "no. merge it" open identically, mean opposite). Harvest gates: strip machinery (26% not human); confidence = DAYS (forks ≤8×); short prompts <400 ch ideally <160; LLM classify not regex; abort = liveness. Verdict: CommandCode taste = CODE STYLE, corpus ≈ ZERO style corrections across 6985 prompts — all recurring = WORKFLOW (git-not-jj, stop dev servers, rebase, merge+delete worktree, pnpm, openspec); user reviews at PR level via CodeRabbit, style never enters transcript; wrong tool (fits FUZZY+UNENFORCEABLE+high-freq, corpus CRISP+ENFORCEABLE+low-freq); injection economics — active learning injected every turn forever to prevent ~5 corrections/quarter; AGENTS.md already trimmed ~58% (`fa2558186`), taste store = bloat generator. RECOMMENDED: (1) dev-server hygiene as MECHANISM (session-end kill of spawned servers), (2) harvest as periodic read-only report, (3) nothing else — no `.pi/taste/`, no decay, no promotion thresholds, no injection loop. FUTURE SPIKE: code-taste lives in git not sessions (edit-after-accept Δ, commit style delta, CodeRabbit FIXED vs DISPUTED); verdict flips on evidence of silent file edits — then transcript = wrong sensor. Reproduce: `/tmp/taste-spike/{signals.json,signals.md,themes.json}` ephemeral, 518 candidates; walk `~/.pi/agent/sessions/*pi-agent-dashboard*/*.jsonl`, `JSON.parse` per line, `o.message?.message ?? o.message`, keep role user, text from string or `content[].type === "text"`, reject `/^<(skill|project_instructions|available_skills|system-reminder|memory-policy|user)\b/` or >400 ch, candidate when prompt index >1 + cue regex on first 140 ch, cluster Jaccard ≥ 0.34 stopword-stripped, rank by distinct `o.timestamp.slice(0,10)` DAYS.
