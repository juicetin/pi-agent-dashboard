# research/test-suite-performance.md — index

Research dossier. WHY `npm test` takes 12 min (728.81s wall, 1456 files / 16206 tests). RESEARCH ONLY, no change / no impl. Inner vitest aggregates sum ≈ 3547 worker-s ÷ 8 workers (`maxWorkers: "50%"`, 16-core Mac) → ~443s floor; wall 728.81s → two problems: too much work + poor packing. 4 bottlenecks: (1) `scripts/__tests__/async-semantics-mutation.test.mjs` = 238.0s = 33% wall — shells full vitest per mutation, per-change teeth-check for ARCHIVED `cleanup-async-semantics-server-extension` (test-plan #X15); irreducible critical path, `npm test` can never go below ~4 min; (2) every `packages/*/vitest.config.ts` pins `maxWorkers: "50%"` → 8/16 cores idle; client experiment (project `@blackbelt-technology/pi-dashboard-web`, 467 files): forks 50% 178.0s, forks 100% 143.5s, threads 100% 124.2s (1.44x, green, config-only), forks 50% `--no-isolate` 103.0s but 55 files / 185 tests fail = latent hygiene debt; (3) jsdom env 743.98s vs tests 169.25s (4.4x), ~1.6s DOM build/file, `--no-isolate` did NOT cut env (771.24s); (4) vitest 4 `groupSpecs`: `maxWorkers: 1` projects (`image-fit-extension` 57s, `video-transcription`, `video-production`, `nano-banana`, `mockup-loop`, `kb-extension`) run strictly serial AFTER main pool — ~60s one core, fifteen idle. Histogram: 15 files >20s = 886s = 49% work; 1178 files <0.5s. Prior art: archive `2026-06-07-parallelize-test-suite` (50%, `setup-home-perfile.ts`, `createTestServer()`/`port: 0` ×18) = why 50% safe today. Levers A–G: A `test:gates` project off `npm test` (−473s/−238s, S), B threads+100% jsdom/pure (S), C retire archived mutation harnesses (−238s, S policy), D merge `maxWorkers: 1` or `sequence.groupOrder` (−60s, M), E `--changed`/`related` dev loop (S), F `--shard=i/N` CI (S), G jsdom triage `@vitest-environment node` (up to −500s, L). A+B+D projection: ~1330s work drop, critical path ~102s → ~3–4 min. Reproduce: `HOME=$(mktemp -d) npx vitest run --reporter=json --outputFile=/tmp/pi-test-timing.json --reporter=default`; client: `--project '@blackbelt-technology/pi-dashboard-web' --pool=threads --maxWorkers=16`.
