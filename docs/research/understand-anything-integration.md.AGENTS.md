# research/understand-anything-integration.md — index

Research dossier. Adapt Egonex-AI/Understand-Anything (MIT; turns any codebase/docs into interactive knowledge graph) into pi-dashboard. Explore-mode, no change / no impl. Verdict: ~70% redundant. UA tree-sitter structural plane ≈ add-codegraph-code-plane (federated CodeGraph); UA LLM semantic plane ≈ add-kb-semantic-annotation-plane (Tier-2); UA docs graph ≈ shipped kb Tier-1; UA /understand-chat Q&A ≈ agent + kb_search natively; fingerprint incremental ≈ kb content-hash gate. GAP = visual graph explorer + guided tours (UA 'graphs that teach' layer); pi-dashboard holds nodes/edges data but has NO viewer. Chosen adaptation: kb graph-visualization panel in packages/kb-plugin — new GET /api/kb/graph route (bounded BFS via neighbors() vs full dumpGraph() under node-count guard) + KbGraphPanel client slot adapting UA @xyflow/react + elkjs + graphology-communities-louvain (louvain.ts community color = zero-LLM layer proxy). kb Tier-1 sparse: GraphNode{type file|heading|tag|entity, name, path}, GraphEdge{src,dst,rel child_of|links_to|references|has_tag} — no summary/layer today. Staging v1 structural map (louvain color, lazy kb_get tooltip) → v2 auto-enriches when annotation plane lands (entity nodes, CURIE edges, layers) → v3 overlays codegraph code plane. Wrinkles: no dump-graph API (KbStore only neighbors/backlinks/counts); heading nodes name-keyed on breadcrumb → cross-file collisions. Client packages/client React 19; no graph libs present. Sources: github.com/Egonex-AI/Understand-Anything, openspec/changes/{add-codegraph-code-plane,add-kb-semantic-annotation-plane,add-kb-code-symbol-index(superseded)}, packages/kb/src/types.ts, packages/kb-plugin/.
