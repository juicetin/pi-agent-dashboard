# research/user-browser-in-editor-view.md — index

Explore-mode research record. `user_browser` tool: agent drives browser, page renders in dashboard editor tab. No OpenSpec change, no implementation. Date 2026-08-06. CDP screencast RBI measured: 529 MB/browser, 61 KB/10 s idle-page bandwidth (change-driven frames, ~0 idle). agent-browser `stream` protocol reverse-engineered from :4848 dashboard bundle (`90ec4543e77acccc.js`): `input_mouse`/`input_keyboard`, base64 JPEG frames, modifiers bitmask. Topologies A1 (relay stream)/A2 (server speaks CDP, allowlist, local ack — adopted)/A′ (raw CDP passthrough — REJECTED: host RCE + 1/RTT fps cap). Relay MUST NOT enable `Runtime` domain. Escalation ladder L0–L3: transfers TASK via `storageState`, not browser. Named-browser providers (`work`/`local`), capability-detected, never silent fallback. Control: input iff takeover pending OR session idle; takeover rides `prompt_request` via PromptBus (bridge.ts:2317 bypass = stuck-"Needs you" defect). Steel feasibility blocked: no `editor-viewer` slot, DinD.
