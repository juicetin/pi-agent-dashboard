# research/webgl-field-and-glass-bands.md — index

Implementation record. pi-dashboard.dev visual system: `site/field.js` WebGL slab field + `site/index.html` glass bands. Shipped 2026-08-26/27, commits `c52745af0` + `e305c361b`, live https://pi-dashboard.dev. FINDING A (3 rewrites, user "ping-pong"): scroll drives RATES, never DISPLACEMENTS — any reversible scroll displacement (position parallax, flow heading, camera dolly/roll, bank angle) must undo itself on the way back = ping-pong; `sin(t*rate)` tumble JUMPS on rate change; angles INTEGRATED per frame (`s.spin += rate*dt`). Model: unsigned energy pool `Math.min(2.4, energy + Math.min(1, d/90) * 0.85)` (~90px saturates, 0.85 gain), decay `energy *= Math.exp(-dt * 0.45)` (~1.5s half-life), multiplies per-slab integrated spin rates (yaw 0.14–0.38, pitch 0.04–0.13, roll 0.03–0.10, `spinResp` 0.45–1.5); camera pointer-parallax ONLY, deaf to scroll; constant one-way travel; removing flow/bank trig bought ~5fps (30→35-39). FINDING B: placement STRATIFIED not random — sides alternate, one vertical cell per side across `2*Y_SPAN` with ±45% jitter (`rnd(-0.45, 0.45) * cell`); x/z from golden ratio `(i * 0.6180339887) % 1`, depth +⅓ turn; `Y_SPAN = 24`, `WRAP_Y = Y_SPAN + 3` shares half-height spawn/wrap band or first pass denser; bug: stratification read `state.count` while `build()` caps `n` narrow-mode → `makeSlab(i, n)` takes real count. FINDING C: `driftY` = layer (1.1 near / 0.68 mid / 0.37 far) × `rnd(0.94, 1.06)`; old `rnd(0.5, 1.25)` let fast slabs lap slow → clumps in minutes. FINDING D: accumulated spin rotated hero accent card into diamond at t+75s → BOUNDED SWAY (`swayA` via `sin` of integrated spin phase, `step()` branches on `s.swayA`); free tumble → slabs edge-on = "flying shards". FINDING E (user 13fps): `curveSegments` 10→3 + `bevelSegments` 3→1 (~¼ triangles); FOUR shared body materials not per-slab; `castShadow = layer !== 2` (near+mid cast, far receives); software raster ~24→~34fps same count; NO EffectComposer/DOF/bloom (bloom = banned AI-glow; depth = fog + scale + slow far layer); reduced-motion = composed still (240 headless `step(1/60)`), not blank; pauses on `document.hidden`; WebGL down → `null` + `console.warn`, no fallback image by design. FINDING F: white emissive at `emissiveIntensity 2.4` clips to blown-out bar under ACES → `pal.emissive` ~1.1. FINDING G: `.glass` 3 layers, order matters — (1) tint `color-mix(in srgb, var(--bg-secondary) var(--glass-tint,86%), transparent)` = CONTRAST FLOOR not decoration; (2) `backdrop-filter: blur(18px) saturate(1.15)` baseline; (3) `@supports (backdrop-filter: url(#chroma))` SVG filter: blur once `stdDeviation 9`, `feColorMatrix` isolates R/G/B, `feOffset` red `dx="-1.4"` / blue `dx="1.4"`, `feBlend screen`, saturate 1.15 — Chromium+Firefox SVG, Safari plain blur (loses fringe only). 2 non-obvious reqs: `color-interpolation-filters="sRGB"` (SVG default linearRGB silently BRIGHTENS) + widened region `x="-20%" y="-20%" width="140%" height="140%"` (else edge fade → dark seam); ±1.4px = lens-defect scale, more = broken display; applied `.strip` + `#download`. FINDING H (moving-backdrop contrast — sample across frames, ONE frame proves nothing): hide text runs, screenshot band, sample backdrop pixels in each run's box over SIX field frames, WCAG vs computed colour; `.stat-k` **4.27:1** < 4.5 AA; tint alone cannot fix — `--text-tertiary` 4.98:1 on opaque = 0.48 margin spent by translucency, 89% tint → 4.46 straddle = fail; fix at source `.glass .spec .stat-k,.glass .rel .when,.glass .foot-note{color:var(--text-secondary)}` → glass MORE transparent `--glass-tint` 78%→**74% dark / 70% light**, worst run **7.2:1**; specificity trap `.glass .stat-k` (0,2,0) vs later `.spec .stat-k` (0,2,0) — equal specificity, source order wins. FINDING I: `--text-tertiary` `#808080` 4.98:1 dark / `#777777` 4.48:1 light ("decorative only"); footer link at tertiary = 4.48:1 light FAIL; `--text-secondary` 9.13:1 dark / 9.74:1 light; rule: decorative caption ~2:1 OK, INTERACTIVE element inside it not. FINDING J: glass costs 44→**35fps** at 1440×900 software raster (20%) — full-width backdrop-filter recomputed EVERY frame over animating field; mitigations: ≤720px flat-tint fallback (small screens pay most, bands tallest there) + `@media(prefers-reduced-transparency:reduce)` off entirely; production 390px computed `backdrop-filter: none`. FINDING K (CSS traps shipped): `.copy{position:absolute}` code-copy rule matched hero `<div class="copy">` → headline out of flow, intro under video → renamed `.copy-btn`; blanket `body > *:not(#field){position:relative;z-index:1}` after `header{position:sticky}` equal-specificity killed sticky → `:not(#field):not(header)`; `<figure>` UA margin `16px 40px` → `.film{margin:0}` or video insets 40px. FINDING L: `.reveal` hidden = `translateY(var(--rv,34px)) scale(.985)`, transitions `opacity .5s ease` + `transform .72s cubic-bezier(.22,.61,.36,1)`; observer NEVER unobserves, exit flips `--rv` to sign of `entry.boundingClientRect.top` → block continues scroll direction, no retrace (lesson A); only opacity+transform animate = compositor-only, no frame-budget cost to field; hidden state applied by JS at runtime, NEVER static CSS — reduced-motion rule kills transitions → permanent blank. FINDING M: header sheds in explicit order, Octocat changed it — buttons overflowed (measured 1133px in 1024px box); order = least-affordable-loss: labels first ≤1080px (`.btn.ic .lbl`), GitHub button ≤860px (repeated in footer), in-page nav ≤720px (reachable by scrolling), wordmark ≤360px; install + theme never leave; dropping LABEL beats DESTINATION (109px→41px, destination kept 220px further down); verified 0px overflow at 14 widths 320–1440. FINDING N: one-path Tux loses beak/feet/belly at 22px = ghost → redrawn in 4 parts (body, white front, beak, feet); belly CONSTANT light `#f3f4f6`, not theme token (`--bg-secondary` near-invisible light theme, `--bg-primary` = hole through bird in dark).
