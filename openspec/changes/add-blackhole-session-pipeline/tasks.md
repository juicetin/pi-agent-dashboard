# Tasks — add-blackhole-session-pipeline

## 1. Gate mechanism review (discipline gate)

- [x] 1.1 Run `doubt-driven-review` on mechanism #6 (design D1–D4) against the five-failure table in proposal.md; record verdict + any adjustment in design.md and verify each of mechanism 5's four defects (truth source, vitest guard, retry, re-render nudge) has a concrete counterpart in the design — done during planning: 3 cross-model cycles, findings folded into design/specs
- [x] 1.2 Run `scenario-design` over the two spec files now that the gate is settled; fold automated scenarios into these tasks and verify test-plan.md exists in the change directory — done during planning: test-plan.md written, rows folded below

## 2. Platform delta — dashboard-plugin-runtime + server wiring

- [ ] 2.1 TDD `isPiExtensionInstalled` capability: add optional method to `ServerPluginContext` + `ServerContextDeps` in `packages/dashboard-plugin-runtime/src/server/server-context.ts`; verify a context built without the dep yields `undefined` (no throw) and typechecks
- [ ] 2.2 Wire the capability in `packages/server/src/server.ts` from `packageManagerWrapper.listInstalled("global")` ∪ `listInstalled("local")` with the probe's `installedMatchesName` matching (id/name/displayName/source incl. `sourcesMatch`) and a ~30 s cache; scan failure rejects the promise (never resolves `false`); verify unit tests show repeated calls within the window trigger one scan, boolean-only answers, and a throwing `listInstalled` rejecting; cover local-only install matched by displayName → `true`, and rejection-not-cached → next call rescans (test-plan E14, E15, X4; exemplar: sibling tests in `packages/dashboard-plugin-runtime/src/server/`)
- [ ] 2.3 TDD the slot-claims invalidation store: module-level version store + exported `bumpSlotClaimsVersion()` in the client runtime; subscribe `useSlotHasClaimsForSession` (and `shouldRender`-evaluating slot consumers) via `useSyncExternalStore` invoked BEFORE the registry-null early return (rules of hooks); verify a bump re-invokes `shouldRender` on mounted wrappers with no session broadcast, and an unbumped store leaves existing gate tests green (test-plan F2, F3; exemplar: existing slot-consumer tests in `packages/dashboard-plugin-runtime/`)
- [ ] 2.4 Verify the whole existing suite still passes (`set -o pipefail; npm test 2>&1 | tee /tmp/pi-test.log` + grep summary) — the additions must be behaviour-neutral until used

## 3. Blackhole plugin server routes

- [ ] 3.1 TDD `GET /api/plugins/blackhole/status`: capability present → its answer alone (negative NOT overridden by config file); capability absent → `existsSync(resolveBlackholeConfigPath(env))` fallback; verify tests cover capability-yes, capability-no + file-exists → `{ installed: false }`, absent-capability + file-exists → `{ installed: true }`, absent-capability + no-file → `{ installed: false }`, and capability rejection → 503 (never `{ installed: false }`) (test-plan E1–E4, X3; exemplar: `packages/blackhole-plugin/src/server/__tests__/routes.test.ts`)
- [ ] 3.2 TDD session-id validation for `GET /api/plugins/blackhole/session/:id`: RFC 4122 syntax with unrestricted version nibble; verify tests accept a UUIDv7 (`019fe770-…`), reject `..`/separator/`%2e%2e`/truncated/non-hex garbage with no fs call (spy asserts), and a version-nibble sweep (1,4,7,8 all accepted) (test-plan E5–E8; exemplar: `routes.test.ts`)
- [ ] 3.3 TDD path confinement: resolved absolute path asserted inside `<agentDir>/pi-blackhole/` before any read; verify a test with a hostile-but-syntax-valid id never escapes and the `security-hardening` checklist for this route is run and noted in the PR description (test-plan E9)
- [ ] 3.4 TDD the response assembly: per-session pending state (absent/torn file → no-activity, never an error), global fields (`compactAfterTokens`, `memory`, `compaction` via `readConfig`, parse failure → nulls not 500), cooldown-derived resolved-model info; add `SOURCE-VERSION PIN: mirrored from pi-blackhole@<version>` comments for the `<id>-pending.json` and `pi-blackhole-cooldown.json` shapes per `config-path.ts` precedent; verify injected-Fastify tests per `routes.test.ts` precedent — absent file → no-activity `200`; truncated JSON → no-activity; malformed global config → null fields not 500; absent/garbage cooldown file → no advisory (test-plan X5–X7)
- [ ] 3.1a Repoint `blackhole-api.isExtensionInstalled()` at `GET /api/plugins/blackhole/status`, preserving its fail-open-on-unknown posture; verify existing settings-surface tests stay green and one test covers the repointed source
- [ ] 3.5 Verify read-only contract: only `GET` handlers registered on both routes and a test asserting `<id>-pending.json` bytes+mtime unchanged after a served request (test-plan X8)

## 4. Client gate

- [ ] 4.1 TDD `installed-gate.ts`: exported synchronous `shouldRenderMemorySubcard()` failing closed until resolved; `resolveInstalled({ fetchImpl })` fetching `/status`, capped-backoff retry on network/5xx then slow fixed interval until first success (never gives up; no re-poll after success), success sets module state + calls `bumpSlotClaimsVersion()`; vitest-specific guard (`process.env.VITEST` / injected override, not `MODE`); verify unit tests with injected fetch + fake timers cover: sync fail-closed before resolve (F1); fails ×4 then succeeds → 3 backoff retries + slow-interval attempt, gate flips true, exactly one bump, no further polling (X1); 404/500/garbage-body each treated transient, value never finalized (X2) (test-plan F1, X1, X2)
- [ ] 4.2 Guard the module-scope kick against the test environment; verify importing the client entry under vitest issues no network request (test asserts fetch spy uncalled) while `resolveInstalled` stays explicitly invokable (test-plan F4)
- [ ] 4.3 Add the `session-card-memory` claim to the manifest with `shouldRender` as the exported function name string; verify manifest test, and repo-lint scenario that `packages/shared/src/dashboard-plugin/slot-types.ts`/`slot-props.ts` are unmodified in the diff (test-plan E13)

## 5. MEMORY subcard

- [ ] 5.1 TDD subcard state derivation from the single `/session/:id` response: healthy one-row, no-activity-yet, workers-off (no meter, "compaction still runs" copy), degraded cooldown advisory (names cooling model + remaining time), manual-mode pending-batches advisory (count + flush hint); verify component tests per state (test-plan E10; exemplar: existing blackhole client tests under `packages/blackhole-plugin/src/client/`)
- [ ] 5.2 TDD worker indicators: textual identifier per worker (observer/reflector/dropper) + accessible name describing state, never colour-alone; verify a11y-name assertions in tests (test-plan F7)
- [ ] 5.3 TDD exact cursor lag readout (tip − cursor, e.g. 450−412=38, not marked approximate; cursor==tip → "0"; cursor AHEAD of tip → distinct stale-cursor indicator, no numeric/negative lag) and the proximity meter: label marks approximate, reachable explanation of non-convertibility, no exact tokens/percentage, no threshold marks/scale, omitted when `contextTokens` or `compactAfterTokens` unavailable while indicators+lag still render, never raises an alert; verify component tests per spec scenarios (test-plan E11, F5, F6)
- [ ] 5.4 TDD the always-present detail-view affordance independent of proximity value; verify test renders it in every state (test-plan F10)

## 6. Content-view drill-in

- [ ] 6.1 TDD the `content-view` claim: predicate false by default, activates only on explicit navigation, return-to-chat affordance present; verify tests plus manifest checks — manifest-wide `priority` bumped in `packages/blackhole-plugin/package.json` from the shipped `100` (ties flows; `pluginId` tie-break would make blackhole win) to a higher number (test asserts blackhole priority strictly > flows'), and no per-claim `priority` field (test-plan F8, E12)
- [ ] 6.2 TDD detail content with provenance: cursors attributed to the pending file, resolved model + reason attributed to the cooldown file, proximity attributed to dashboard accounting with the non-convertibility caveat, and absence of in-memory-only values (`consolidationInFlight`, counts, last errors) with the transcript pointer; verify component tests per spec scenarios (test-plan F11)
- [ ] 6.3 Verify session-scoping: detail view reached from the session surface and the global settings page renders no per-session state (test on `BlackholeSettings` untouched-by-this-change assertion)

## 7. Integration + docs

- [ ] 7.0 Author a Playwright spec asserting no MEMORY subcard appears on any seeded session card when `pi-blackhole` is not installed in the harness — input: docker harness without the extension, seeded sessions; trigger: dashboard loads; observable: zero MEMORY subcards (test-plan F9; see `tests/e2e/blackhole-settings.spec.ts` for harness glue; read the port from `.pi-test-harness.json`, never hardcode)
- [ ] 7.1 Full suite green (`set -o pipefail; npm test 2>&1 | tee /tmp/pi-test.log` + grep summary); build client (`npm run build`) and verify manual smoke in the dashboard: installed → subcard appears (including on an idle card after boot) (test-plan: manual-only, M1), and approximate-vs-exact readouts visually distinguishable (test-plan: manual-only, M2)
- [ ] 7.2 Update directory `AGENTS.md` rows for every new/changed file (blackhole-plugin client/server, dashboard-plugin-runtime server + client, server wiring) and delegate any `docs/` prose (plugin-context API docs gain `isPiExtensionInstalled` + invalidation store) to DocScribe; verify `kb dox lint` reports no missing/stale rows for touched dirs
