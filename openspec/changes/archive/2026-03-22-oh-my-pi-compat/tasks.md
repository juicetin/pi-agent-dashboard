# Oh My Pi Compatibility

## 1. Package Configuration

- [x] 1.1 Update `package.json`: add `@oh-my-pi/pi-coding-agent`, `@oh-my-pi/pi-ai`, `@oh-my-pi/pi-tui` to `peerDependencies` with `"*"` range. Add `peerDependenciesMeta` marking all six peer deps as optional.

## 2. Documentation

- [x] 2.1 Update `README.md`: add "Oh My Pi" section under Installation with install command. Note that the extension is compatible with both runtimes.
