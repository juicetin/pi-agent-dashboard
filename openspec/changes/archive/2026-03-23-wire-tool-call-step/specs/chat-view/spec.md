<!-- No spec changes required. The existing chat-view spec already defines
     tool call rendering with ToolCallStep, click-to-expand behavior, and
     all associated scenarios. This change fixes the implementation to match. -->
