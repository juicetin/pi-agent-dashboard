## 1. CSS Change

- [x] 1.1 Add `overscroll-behavior: none` to `html` and `body` in `src/client/index.css`

## 2. Verification

- [x] 2.1 Verify normal in-page scrolling still works (chat views, sidebars)
- [x] 2.2 Update `mobile-resilience` spec via openspec sync (delta spec ready; will sync at archive)
