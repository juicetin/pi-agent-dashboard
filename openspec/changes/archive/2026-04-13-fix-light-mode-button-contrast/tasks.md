## 1. Update button color classes

- [x] 1.1 In `InstallButton.tsx`, change `text-[var(--text-muted)]` to `text-[var(--text-tertiary)]` (keep hover as `--text-secondary`)
- [x] 1.2 In `TunnelButton.tsx`, change `text-[var(--text-muted)]` to `text-[var(--text-tertiary)]` on the button icon (keep hover as `--text-secondary`)
- [x] 1.3 In `SessionList.tsx`, change the Settings button from `text-[var(--text-muted)]` to `text-[var(--text-tertiary)]` (keep hover as `--text-secondary`)
- [x] 1.4 In `SessionList.tsx`, change the Pin directory button from `text-[var(--text-muted)]` to `text-[var(--text-tertiary)]` (keep hover as `--text-secondary`)

## 2. Verify

- [x] 2.1 Run existing tests to confirm no regressions (`npm test`)
- [x] 2.2 Visually verify buttons are visible in both light and dark themes
