# Tasks — Support zrok v2

## 1. Binary resolution (zrok2 / zrok)
- [x] 1.1 `tunnel-providers/zrok.ts`: `getZrokBinary()` resolves first of `["zrok2","zrok"]` via `ToolResolver`; cache path. → verify: unit test injects a resolver that finds only `zrok2`, only `zrok`, and neither.
- [x] 1.2 `detectZrokBinary()` true if either name resolves. → verify: `detectBinary()` test both-names matrix.
- [x] 1.3 `tunnel-enroll.ts`: `zrok:auth-token` recipe resolves the same dual name instead of literal `"zrok"`. → verify: enroll test asserts resolved binary.

## 2. v2 reserved/persistent shares (namespaces + names)
- [x] 2.1 Add `tunnel.zrok.reservedName?: string` and `tunnel.zrok.persistent?: boolean` (default false) to config schema (`config.ts`). → verify: type + default shape test.
- [x] 2.2 Replace the `spec.reserve`-wired `reserveShare` with a `connect`-invoked helper `mintReservedName(port)` (NOT a `ChildProviderSpec.reserve` field — that field is removed in 2c.3): generate DNS-safe `pi-dash-<8hex>` when no name; `zrok2 create name -n public <name>` (reuse-on-exists-for-this-account; taken-by-other → warn + ephemeral, no silent rotate); persist `reservedName`; return name. → verify: test create-name success, already-exists reuse, taken→ephemeral+warn, failure→null.
- [x] 2.3 `buildArgs` (flags-first, target-last): reserved → `["share","public","--headless","-n",`public:${name}`,`localhost:${port}`]`; ephemeral → `["share","public","--headless",`localhost:${port}`]`. → verify: buildArgs matrix test.
- [x] 2.4 `processMarker` matches BOTH `zrok share` and `zrok2 share` (e.g. `/\bzrok2? share\b/`); `endpointMarker` stays `localhost:<port>` used in conjunction. → verify: scavenge test whose ps line is the real flags-first `<abs>/zrok2 share public --headless -n public:x localhost:PORT` form (MUST match) + a `zrok share` v1 line (MUST match) + an unrelated `localhost:PORT` line (MUST NOT match).
- [x] 2.5 Persistence: reserved name SURVIVES `disconnect`/restart — `deleteTunnel` MUST NOT `delete name`. `releaseShare` → `["delete","name",<name>]` invoked ONLY by an explicit "forget reserved URL" path that reads `reservedName` before clearing config. → verify: disconnect keeps the name (list-names still shows it); explicit-forget deletes it.
- [x] 2.6 Reserved path opts out of release-on-retry: a transient `share public` failure retries the SAME name (no `release`+regenerate); only ephemeral may fall back to a fresh URL. → verify: core retry test with a reserved name asserts no `delete name` and same name reused.

## 2b. Plumb reservedName end-to-end (connect path)
- [x] 2b.1 `tunnel-provider.ts`: add `TunnelConnectOpts.reservedName?: string`. → verify: type test.
- [x] 2b.2 `cli.ts` + `server.ts` + `routes/system-routes.ts`: name-minting lives in the zrok provider's `connect` (only when `persistent`); source `reservedName`/`persistent` from config and pass through; STOP passing v1 `reservedToken` into the v2 zrok provider. → verify: connect test asserts named-share served when `persistent`+name, ephemeral when not, and a stray legacy `reservedToken` is ignored (ephemeral, not `-n public:<v1tok>`).
- [x] 2b.3 `config-api.ts`: ensure `tunnel.zrok.reservedName`/`persistent` round-trip through `writeConfigPartial` deep-merge (non-secret; the `keepSecret` path only covers redacted keys). → verify: partial write toggling `tunnel.enabled` preserves both fields.
- [x] 2b.4 `POST /api/tunnel-disconnect` (`system-routes.ts`): accept optional `{forget:true}` → provider `release` (`delete name`) + clear `tunnel.zrok.reservedName` + `persistent=false`; no body / `{forget:false}` = plain disconnect that PRESERVES the name (back-compat). → verify: forget deletes + clears; plain disconnect keeps name and config.

## 2c. Generic core (tunnel-core.ts) — shared with ngrok, keep back-compat
- [x] 2c.1 `ChildProviderSpec.processMarker: string | RegExp`; `scavengeOrphans` matcher = `typeof m === "string" ? line.includes(m) : m.test(line)`. ngrok keeps its string marker. → verify: matcher test for string (ngrok) and regex (zrok `/\bzrok2? share\b/`) paths.
- [x] 2c.2 Guard the crash-exit retry path in `createInner` on `!callerProvidedToken` (as the timeout path already is), so a caller-provided reserved name is never `release`d/regenerated by the core — only re-served. → verify: exit-retry test with a caller-provided token asserts no `release` call + same token reused.
- [x] 2c.3 Remove `reserve` from the zrok `ChildProviderSpec`; the core no longer auto-mints. Name-minting moves into the zrok provider `connect` (guarded by `persistent`), passing the name as a caller-provided token. → verify: ephemeral connect (no `persistent`) never calls `create name`.

## 3. Headless enrollment + token length
- [x] 3.1 Enroll recipe args → `["enable", tok, "--headless"]`. → verify: recipe test asserts `--headless` present, token still argv-only.
- [x] 3.2 `re.zrokToken` min length 20 → 8 (`/^[A-Za-z0-9._-]{8,200}$/`); charset + max unchanged. → verify: validator accepts a real 12-char token (`RX1EuRvs9H8s`-shape), still rejects a metacharacter-bearing value and an over-200 value.

## 4. URL + CORS (fold landed patch + regression tests)
- [x] 4.1 **Anchor** `urlRegex` so the `[^\s"]*` tail cannot swallow a following dot into a spoofed host: end the host at a boundary (e.g. `\.shares?\.zrok\.io(?=[\s"/:]|$)` before any path). Keep `normalizeUrl` prepend-scheme. → verify: matches bare `x.shares.zrok.io` and schemed v1 `https://x.share.zrok.io`; **does NOT** match `foo.shares.zrok.io.attacker.com` as a `*.shares.zrok.io` host; normalize yields `https://…`.
- [x] 4.2 Keep `tunnel-core.ts` `normalizeUrl?` hook + call site. → verify: core test that a bare match is normalized before `activeTunnelUrl` is set.
- [x] 4.3 Keep `cors-origin.ts` `.shares.zrok.io` allow. → verify: cors test allows a v2 host, denies `foo.shares.zrok.io.attacker.com` (real over-match string, not `evilshares.zrok.io.…`).

## 5. Doctor (api-v2 + version-compat)
- [x] 5.1 "zrok API reachable" resolves `api-v2.zrok.io`, or the enrolled env `api_endpoint` when present; fix stale `api-v1.zrok.io` suggestion text. → verify: doctor test with/without enrolled env + suggestion no longer names api-v1.
- [x] 5.2 NEW check "zrok version compatible": short-circuit to unavailable when `detectZrokBinary()` false (NEVER spawn); else parse `<resolved-zrok> version`; major `<2` (v1=`0.4.x`) → warn+upgrade remedy; unparseable → warn(unknown); `>=2.0.0` → ok. Register in `SECTION_OF` (tunnel) + suggestion factory. → verify: doctor-core test for v0.4.x(warn)/v2.0.4(ok)/`2.0.0-rc.1`(ok)/garbage(warn)/missing-binary(unavailable, no spawn).
- [x] 5.3 Update "zrok binary" detection + suggestion to mention `zrok2`. → verify: suggestion-lint test still passes.

## 6. Config migration
- [x] 6.1 `normalizeTunnelConfig`: preserve legacy `reservedToken` (downgrade) but do NOT promote it to `reservedName`; surface `zrok.reservedName` when present. → verify: migration tests — legacy token kept + ignored, explicit reservedName preserved, idempotent.

## 7. Docker → zrok v2
- [x] 7.1 `Dockerfile`: `ARG ZROK_VERSION=2.0.4`; extract `zrok2` from tarball; `ln -s zrok2 zrok`; `zrok2 version` smoke. → verify: `docker build` succeeds; `docker run … zrok2 version` and `zrok version` both report v2.
- [x] 7.2 `entrypoint.sh`: enrollment path uses `zrok2 enable "$ZROK_TOKEN" --headless`. → verify: container with `ZROK_TOKEN` enrolls headless (or logs a clear skip when unset).
- [x] 7.3 `.env.example` / `compose.yml` comments: note v2, `~/.zrok2`, `ZROK2_*` if any zrok env used. → verify: doc-only diff.

## 8. Install hints + Gateway UI
- [x] 8.1 `tool-registry/definitions.ts`: macOS `brew install zrok`; Linux package-repo/`zrok2`; Windows `zrok2` on PATH; v2 doc URLs. → verify: `install-hints.test.ts` / `tool-registry-definitions.test.ts`.
- [x] 8.2 `Gateway/*` copy: install + enroll text reflects v2 (`zrok enable <token>` headless, `zrok2` on non-brew). → verify: component render test / snapshot.

## 9. Docs
- [x] 9.1 `api-reference.md` + `doctor` skill: `api-v1.zrok.io` → `api-v2.zrok.io`; note version-compat check. (docs write → DocScribe / caveman style)
- [x] 9.2 `docker/README.md` + `docs/architecture.md` tunnel section: v2 binary/env/reserved-name flow. (docs → DocScribe)
- [x] 9.3 Directory `AGENTS.md` rows for every changed source file (`zrok.ts`, `tunnel-core.ts`, `cors-origin.ts`, `tunnel-enroll.ts`, `doctor-core.ts`, `config.ts`, `definitions.ts`, docker files).

## 9b. Tests — folded from test-plan.md (automated rows; one per manifest id)

### L1 (vitest) — exemplars: `packages/server/src/__tests__/tunnel.test.ts`, `tunnel-provider.test.ts`, `tunnel-enroll.test.ts`, `cors.test.ts`; `packages/shared/src/__tests__/{doctor-tunnel-checks,tunnel-config-migration}.test.ts`
- [x] T-E1 binary detect: resolver finds only `zrok2` · `getZrokBinary()` · chosen=`zrok2`, available=true (see tunnel.test.ts) (test-plan #E1)
- [x] T-E2 binary detect: resolver finds only `zrok` · `getZrokBinary()` · chosen=`zrok` (test-plan #E2)
- [x] T-E3 binary detect: neither resolves · `detectZrokBinary()` · available=false, version-check unavailable no-spawn (test-plan #E3)
- [x] T-E4 buildArgs: no name, port 8000 · `buildArgs(8000,undefined)` · `["share","public","--headless","localhost:8000"]` (test-plan #E4)
- [x] T-E5 buildArgs: name `myapp`, port 8000 · `buildArgs(8000,"myapp")` · `["share","public","--headless","-n","public:myapp","localhost:8000"]` (test-plan #E5)
- [x] T-E6 token validator: 12-char `RX1EuRvs9H8s` · `re.zrokToken.test` · accepted (see tunnel-enroll.test.ts) (test-plan #E6)
- [x] T-E7 token validator: 7-char · validator · rejected (test-plan #E7)
- [x] T-E8 token validator: 201-char AND `&`-bearing · validator · both rejected (test-plan #E8)
- [x] T-E9 version: `v2.0.4` · version check · ok (see doctor-tunnel-checks.test.ts) (test-plan #E9)
- [x] T-E10 version: `v0.4.51` · version check · warn + upgrade remedy (test-plan #E10)
- [x] T-E11 version: `2.0.0-rc.1` · version check · ok (test-plan #E11)
- [x] T-E12 version: garbage · version check · warn(unknown), no throw (test-plan #E12)
- [x] T-E13 url: bare `abc.shares.zrok.io` · match+normalize · `https://abc.shares.zrok.io` (test-plan #E13)
- [x] T-E14 url: v1 `https://abc.share.zrok.io` · match · unchanged (test-plan #E14)
- [x] T-E15 url anchor: `foo.shares.zrok.io.attacker.com/x` · match · NOT a zrok host (test-plan #E15)
- [x] T-E16 cors: `https://x.shares.zrok.io` · `isCorsOriginAllowed` · allowed (see cors.test.ts) (test-plan #E16)
- [x] T-E17 cors: `https://foo.shares.zrok.io.attacker.com` · `isCorsOriginAllowed` · denied (test-plan #E17)
- [x] T-E18 install-guide routing: serverOs darwin/linux/win32/unknown · render copy · per-OS commands (test-plan #E18)
- [x] T-E19 migration: `{reservedToken}` no provider · `normalizeTunnelConfig` · zrok/public, token preserved, NOT promoted, idempotent (see tunnel-config-migration.test.ts) (test-plan #E19)
- [x] T-E20 schema: fresh config · read `tunnel.zrok` · `persistent` default false, `reservedName` unset (test-plan #E20)
- [x] T-X1 share fail: share exits non-zero pre-URL · `createTunnel` · null, inactive, no throw (test-plan #X1)
- [x] T-X2 spawn timeout: no URL 30s · `createInner` timeout · killed, warn, null (see tunnel.test.ts) (test-plan #X2)
- [x] T-X3 scavenge: ps `<abs>/zrok2 share public --headless localhost:8000` · `/\bzrok2? share\b/` · matched; bare `localhost:8000` NOT; v1 `zrok share` matched (see tunnel-cleanup.test.ts) (test-plan #X3)
- [x] T-X4 retry no-recycle: caller-provided name, share exits once · crash-exit retry · `!callerProvidedToken` guard, no `delete name`, same name re-served (test-plan #X4)
- [x] T-X5 no auto-mint: `persistent=false` · provider `connect` · never calls `create name` (test-plan #X5)
- [x] T-X6 legacy token: stray `reservedToken`, no name · connect · ephemeral, NOT `-n public:<v1tok>` (test-plan #X6)
- [x] T-X7 partial write: `zrok.reservedName` set · `writeConfigPartial({tunnel:{enabled:false}})` · name+persistent preserved (see tunnel-config-redaction.test.ts) (test-plan #X7)
- [x] T-X8 api probe: `api-v2.zrok.io` DNS fail · doctor api-reachable · failure row, no api-v1 in text (test-plan #X8)
- [x] T-X9 api endpoint: enrolled env `api_endpoint` present · doctor api-reachable · probes recorded endpoint (test-plan #X9)
- [x] T-X10 name taken: `create name` taken-by-other · provider mint · warn + ephemeral, no persist/rotate (test-plan #X10)

### L3 (Playwright e2e, docker harness `.pi-test-harness.json` dashboardPort — never :18000) — exemplar: `tests/e2e/change-summary-table.spec.ts`
- [x] T-F1 `/tunnel-setup` on linux server · open route · Install/Enroll/Verify + docs link + package-repo commands (test-plan #F1)
- [x] T-F2 tunnel status active `*.shares.zrok.io` · sidebar button · connected indicator + copyable v2 URL (test-plan #F2)
- [x] T-F3 forget control · click "Forget reserved URL" · fires `disconnect {forget:true}`, button → disconnected (test-plan #F3)

### L2 (qa smoke) — exemplar: `qa/tests/01-install.sh`
- [x] T-M6 docker image · `docker run … zrok2 version` && `zrok version` · both report v2.0.4 (symlink resolves) (test-plan #M6)

## 10. Validate
- [x] 10.1 `npm test` green (HOME-isolated). → verify: full suite.
- [x] 10.2 `openspec validate support-zrok-v2 --strict`.
- [x] 10.3 Fold-completeness: every `test-plan.md` automated row has a T-task above; every manual-only row is a 10.4 item.
- [x] 10.4 Manual on a v2-enrolled host (manual-only rows; `ship-change` defers post-merge): headless enroll no `/dev/tty` (test-plan: manual-only #M1); ephemeral connect serves 200 (#M2); persistent stable URL across restart (#M3); plain disconnect preserves name (#M4); `disconnect {forget:true}` deletes name (#M5); doctor all-green on v2 host (#M7).
