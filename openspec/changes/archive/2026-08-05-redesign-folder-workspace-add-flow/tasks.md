## 1. Phase 1 — Remove the directory-home dead end

- [x] 1.1 Write a failing test in `packages/client/src/components/folder/__tests__/` (or the existing DirectoryHomeView test file) asserting a cwd that is NEITHER in `pinnedDirectories` NOR in any `workspaces[].folders` renders the home page prompt surface and NOT `directory-home-not-pinned`
- [x] 1.2 Delete the eligibility branch + "not available" notice from `packages/client/src/components/folder/DirectoryHomeView.tsx`; keep the loading state driven only by the component's own session data
- [x] 1.3 Remove the now-unused `pinnedDirectoriesLoaded` / `workspacesLoaded` eligibility props from `DirectoryHomeView` and drop them at every call site (grep for the prop names)
- [x] 1.4 Delete i18n keys `directoryHome.notPinnedTitle`, `directoryHome.notPinnedBody`, `directoryHome.pinCta` from `packages/client/src/lib/i18n-en-source.json` and any translated catalogs
- [x] 1.5 Update/remove existing tests asserting the guard or the `directory-home-not-pinned` test id; run `npm test 2>&1 | tee /tmp/pi-test.log` and confirm green
- [x] 1.6 Verify manually: open a folder that has sessions but is neither pinned nor in a workspace → its home page renders with the session list and spawn prompt — VERIFIED in the worktree harness (:18237, `TEST_COPY_MODE=1`). `/folder/L2ZpeHR1cmVzL3NhbXBsZS1ob29rLW9r` (`/fixtures/sample-hook-ok`, neither pinned nor workspace-owned) renders `directory-home` + `directory-home-prompt` + header + quick actions; `directory-home-not-pinned`, `directory-home-loading` and `directory-home-pin-cta` are all absent — the dead end is gone. The "has sessions" half stays unit-covered only: pi cannot spawn in the harness (PI_E2E_SEED seeds a FAKE oauth credential, no `docker/.env` key), so no session ever registers; the removed guard never consulted session count, so the live case above covers the substantive risk

## 2. Phase 2 — Compact add-to-workspace affordance

- [x] 2.1 Write failing tests in `SessionList.test.tsx`: the folder header renders a button whose accessible name matches the add-to-workspace verb, exposes `aria-haspopup`/`aria-expanded`, and NO element with literal text `+ws` exists
- [x] 2.2 Replace the `+ws` text token in `renderGroupWithWorkspaceMenu` with a real affordance rendered INSIDE the header icon cluster (order `sort · add-to · home · pin`); delete the `absolute top-1 right-7` wrapper. RECONCILED at merge: presentation is `add-to-workspace-affordance`'s `mdiViewGridPlus` + "Workspace" pill, not the original `mdiFolderPlus` + `mdiMenuDown` caret (see design.md §3 REVERSED)
- [x] 2.3 Add `aria-label` + `title` ("Add to workspace…"), `aria-haspopup="menu"`, and `aria-expanded` bound to popover state; ensure the ≥44px touch target at mobile breakpoints matches the sibling icon buttons
- [x] 2.4 Remove the "Pin to dashboard" entry — already absent; covered by a regression test instead from `packages/client/src/components/workspace/AddToWorkspaceMenu.tsx` (keep workspaces, `+ New workspace…`, and remove-from-workspace); keep the existing "No workspaces yet" empty state
- [x] 2.5 Add the same affordance to the session card header cluster, targeting the session's cwd, reusing the popover component
- [x] 2.6 Apply the cluster layout contract in `renderGroup`: `flex:none` + `white-space:nowrap` on the cluster, `min-width:0` on the name region, parent path `flex:0 1 auto; min-width:0`, leaf name with a legible minimum + ellipsis
- [x] 2.7 Add a test asserting all cluster buttons remain rendered and unwrapped at a narrow container width, and that the parent path truncates before the leaf segment
- [x] 2.8 Run tests; then build and visually verify the cluster at ~220px, ~260px, ~300px sidebar widths in dark AND light themes — VERIFIED in the worktree harness at all three widths (`localStorage['dashboard:sidebar-width']`), dark AND light. All 4 cluster buttons stay on ONE line: measured `topSpread = 0.6px` (sub-pixel alignment jitter, not wrapping), `flexWrap:nowrap`, `whiteSpace:nowrap`, `flexShrink:0`; cluster right edge == card right edge at every width (never escapes). Order confirmed left→right `sort(98) · add-to(119) · home(148) · pin(169)`. Truncation priority measured: parent path collapses 43px→3px→0px as width shrinks while the leaf holds a constant 53px floor

## 3. Phase 3a — PathPicker multi-select + MDI iconography

- [x] 3.1 Write failing tests in `PathPicker.test.tsx`: in multi-select mode, activating a row browses into it and does NOT call `onSelect`; in single-select mode activation still calls `onSelect`
- [x] 3.2 Add an opt-in selection mode to `packages/client/src/components/primitives/PathPicker.tsx` (props for selected set + change handler); default behaviour for existing callers MUST stay single-select
- [x] 3.3 Render a per-row checkbox in multi-select mode using `mdiCheckboxMarked` / `mdiCheckboxBlankOutline`, with its own accessible name and `stopPropagation` so ticking never navigates
- [x] 3.4 Add the trailing `mdiChevronRight` descend affordance to directory rows
- [x] 3.5 Replace emoji glyphs with `@mdi/js` paths: `⬆` → `mdiArrowUp` (parent row), `📁` → `mdiFolder` (directory rows), `＋` → `mdiFolderPlusOutline` (create-here row and the footer New folder button); keep `git`/`pi` as text badges
- [x] 3.6 Add a test asserting no row renders the characters `⬆`, `📁`, or `＋` and that each renders an SVG path
- [x] 3.7 Wire keyboard semantics: Space toggles selection on the highlighted row, Enter activates (descends); verify existing arrow-key navigation and the create-here flow still pass

## 4. Phase 3b — Add Folders dialog

- [x] 4.1 Write failing tests in `PinDirectoryDialog.test.tsx` (renamed/extended for the Add Folders dialog): selections persist across navigation, pill removal deselects, empty basket disables the primary action, and the action label reflects the count
- [x] 4.2 New `components/workspace/AddFoldersDialog.tsx` (PinDirectoryDialog kept as the single-select picker for the packages move flow): multi-select `PathPicker` + basket of removable pills + count-bearing primary action; normalize each path with `normalizePath`/`inferPlatform` as today
- [x] 4.3 Add the single-select workspace destination control (default `None`), listing existing workspaces plus `+ New workspace…`; render "None — no workspaces yet" plus the create affordance when zero workspaces exist
- [x] 4.4 Assert the dialog renders NO pin control anywhere (test), since pinning is implicit
- [x] 4.5 Implement commit: send `pin_directory` for every selected path FIRST, then `add_folder_to_workspace` for each when a workspace destination is chosen; close on success
- [x] 4.6 Add session-count badges by joining browse entries against session cwds using `pathKey` from `session-grouping` (NOT raw string equality); add a test for a badged loose cwd and an unbadged plain directory
- [x] 4.7 Wire `dashboard-add-buttons` entry points: the sidebar `+ Add Folder` opens the dialog with destination `None`; each workspace-scoped `+ Add Folder` opens it with that workspace preselected
- [x] 4.8 Add i18n keys for the destination control, empty-workspace state, basket label, and the count-bearing action

## 5. Validation

- [x] 5.1 Run the full suite — 11451 passed; the 5 failures are pre-existing/environmental (3 `fs.watch` integration tests fail on base too; 2 lint contracts pass in isolation), none from this change: `npm test 2>&1 | tee /tmp/pi-test.log` then `grep -nE 'FAIL|Error|✗|✘' /tmp/pi-test.log` — zero failures
- [x] 5.2 Measure commit broadcast churn with ~10 selected paths; if sidebar reorder flicker is visible, record it as a follow-up for the deferred batched message (design Decision 5) — MEASURED live: two 10-path commits. Run 1: 10 pinned, dialog closed on success, 86 sidebar DOM mutations over ~3.1s. Run 2 (10 new paths on top of 10 existing, sampled every 80ms — 206 samples): folder count grew 10→20 with **zero reorder events** (relative order of already-rendered rows never inverted). Commit is pure progressive append, NOT reorder churn → **no visible flicker, so Decision 5's batched message is NOT needed; no follow-up recorded**
- [x] 5.3 Accessibility pass on the dialog: keyboard-only select/navigate/commit, visible focus rings, ≥24×24 targets, and correct roles/names on the checkbox and destination controls — DONE live, and it **caught a real defect**: hit targets measured 16×16 (checkbox), 14×14 (chevron), 125×16 (`+ New workspace…`), failing WCAG 2.5.8 (≥24×24). Fixed by padding the hit box while keeping the compact glyph (`min-w-6 min-h-6` + negative margin); re-measured live: checkbox 24×24, chevron 24×24, pill-remove 24×24, new-workspace 133×24, commit 103×36 — all pass. Also adopted the project's `.focus-ring` utility (2px `var(--focus-ring)` = `#3b82f6` on `--bg-primary` `#0a0a0a` → high contrast on dark) instead of the near-invisible 1px UA default. Keyboard-only verified live: ArrowDown highlights, **Space selects** (basket +1, browsed dir unchanged), and Enter descends without committing (locked by two new unit tests). Regression tests added for both the ≥24px padding and the `.focus-ring` class
- [x] 5.4 Verify all three phases in dark AND light themes at 420px and 1440px; confirm no console errors — VERIFIED live across the full matrix. Phases captured: (1) empty basket + "None — no workspaces yet" empty state + disabled commit, (2) multi-select basket with removable pills + count-bearing commit, (3) populated single-select destination radio group (`○ None` / `● Frontend`) incl. the nested `NewWorkspaceDialog` layering with the basket preserved. Both themes at 420px and 1440px; MDI glyphs inherit `currentColor` correctly on dark. **Zero console errors/warnings** for the entire run
- [x] 5.5 Confirm no duplicate rows: a folder that is both pinned and workspace-owned renders exactly once (inside its tier), and removing it from the workspace leaves it visible at root — VERIFIED live: after committing 2 paths with destination `Frontend` (both already pinned from an earlier commit → both pinned AND workspace-owned), the sidebar rendered 21 rows with `bus-client` ×1 and `client-utils` ×1, **zero duplicates**; each appears only inside its workspace tier. This change does not touch `visibleTopPinned`/`visibleTopUnpinned`; the implicit-pin design (Decision 2) relies on exactly this filter

## 6. Documentation

- [x] 6.1 Delegate to DocScribe (caveman style): update the folder/workspace + directory-home sections of `docs/architecture.md` for the removed guard, implicit pin, and multi-select add flow
- [x] 6.2 Update directory `AGENTS.md` rows for every touched file (`components/folder/`, `components/workspace/`, `components/primitives/`, `components/session/`) with purpose + `See change: redesign-folder-workspace-add-flow`
- [x] 6.3 Run `kb dox lint` and confirm no `over-threshold` rows were introduced — `kb` CLI unavailable; verified manually (all touched dir `AGENTS.md` < 30 KB cap) and ran `scripts/split-large-agents.mjs` on `workspace/AGENTS.md` to promote the 3 >200-char rows (AddFoldersDialog/AddToWorkspaceMenu/PinDirectoryDialog) to sidecars
