# Tasks — fix-pi-install-node26-and-omit-dev-build

Ordering note: §2 is TDD-ordered — the assertion corrections (2.1) land BEFORE the cap change
(2.2–2.5) so the suite is never left knowingly red across a multi-task gap.

## 1. git-install `--omit=dev` build fix

- [x] 1.1 In `packages/client/package.json`, move `vite`, `@vitejs/plugin-react`, `@tailwindcss/vite`, `tailwindcss` from `devDependencies` → `dependencies` (same version ranges).
- [x] 1.2 Add `tsx` (`^4.21.0`, matching root/server) to `packages/client/package.json` `dependencies`; leave the root `tsx` devDependency and the `packages/server` runtime `tsx` dependency untouched.
- [x] 1.3 Refresh `pnpm-lock.yaml` with `pnpm install` (the repo has no `package-lock.json`; `pnpm-workspace.yaml` sets `nodeLinker: hoisted` and AGENTS.md forbids `npm install`) and confirm no workspace version drift.
- [x] 1.4 Add `scripts/verify-release-deps.mjs` RULES asserting `vite`, `@vitejs/plugin-react`, `@tailwindcss/vite`, `tailwindcss`, `tsx` live in `packages/client/package.json` `dependencies` (mirror the existing `tsx`/`openspec` rule shape + evidence pointing at this change). Mirror the existing server `tsx` rule's `minVersion: "4.21.0"` on the new client `tsx` rule; the four Vite/Tailwind rules need no `minVersion`.

## 2. Node 26 engines cap raise (`<26` → `<27`)

- [x] 2.1 (test-plan #E9) FIRST — correct the pre-existing assertions that encode the OLD cap, so 2.2–2.4 turn them green rather than red. While retargeting the probes to `"v27.0.0"`, also rename the `"explains the EBADENGINE / floor link"` case — v27 trips the CAP, not the floor, so the existing name becomes misleading. Triple: input the current suites · trigger `npm test` after the cap raise · observable no assertion still claims Node 26 is out of engines range. Rewrite: `packages/server/src/__tests__/node-guard.test.ts` — the `"returns true for v26.x and above (engines cap)"` case (`v26.0.0`/`v26.8.1` → now `false`, keep `v27.0.0` → `true`), the no-`v`-prefix case (`"26.0.0"` → now `false`), the `/>=22\.19\.0 <26/` range match → `<27`, and the four `buildEnginesRangeMessage("v26.0.0")` probes → `"v27.0.0"` (v26 is no longer a refused version); `packages/shared/src/__tests__/node-version.test.ts` — the `["v26.0.0", false]` accept-set row → `true`, and the accept-set header comment `(>=22.19.0 <26)` → `<27`. See `packages/server/src/__tests__/node-guard.test.ts`.
- [x] 2.2 Root `package.json#engines.node`: `>=22.19.0 <26` → `>=22.19.0 <27`.
- [x] 2.3 `packages/shared/src/node-version.ts` — change every `<26`/`>= 26` site, enumerated so none is missed: (a) `isOutOfEnginesRange` arithmetic `major >= 26` → `major >= 27`; (b) that function's JSDoc header `engines cap (>=22.19.0 <26)` → `<27`; (c) the same JSDoc's `Too new: major >= 26.` → `major >= 27.`; (d) the module doc-comment engines-cap line `(>=22.19.0 <26)` → `<27` and its `At/above the cap (>=26): untested` → `(>=27)`; (e) the `isUsableNodeVersion` accept-set comment (now accepts 26, rejects 27+). APPEND a new cap-history entry for `<26` → `<27` — do NOT rewrite the existing `<25`→`<26` provenance. Also correct the stale path in the module doc header: the server guard is `packages/server/src/auth/node-guard.ts`, not `packages/server/src/node-guard.ts`. `isAffectedNode` is unchanged (Node 26 is outside the Fastify-affected range).
- [x] 2.4 `packages/server/src/auth/node-guard.ts` `buildEnginesRangeMessage`: update the hardcoded range string `Required: >=22.19.0 <26` → `<27`, AND the adjacent line `At/above the cap: untested; raise the cap when ready.` — Node 26 is CI-validated after this change, so only 27+ is untested. Without 2.4 the message contradicts the new cap and scenario E4 cannot pass. The predicates are re-exports and need no change.
- [x] 2.5 (test-plan #E10) Close the single-source hole 2.4 exposes: the hardcoded range string is a THIRD location the lockstep contract does not cover (the spec claims "when `engines.node` changes, only `node-version.ts` changes"). Triple: input root `package.json#engines.node` + the string `buildEnginesRangeMessage` emits · trigger call the builder and compare · observable the emitted `Required: …` substring contains the engines range verbatim. Add it as a repo-lint so the next cap change cannot forget the literal. See `packages/shared/src/__tests__/bundled-node-meets-pi-floor.test.ts` (reads package.json + asserts a derived invariant).
- [x] 2.6 Sweep the remaining live `<26` claims outside the changed modules (the contract forbids any surviving assertion that Node 26 is out of range): `openspec/specs/doctor-diagnostic/spec.md` (hardcodes `(>=22.19.0 <26)` + "26+" in the doctor gate) and `examples/chat-embed-tester/INTEGRATION.md` + `INTEGRATION.agent.md` ("Node `>=22.19.0 <26` … Matches this repo's engines"). The E5 scan only covers TS cap arithmetic and catches none of these. EXCLUDED deliberately: (a) `openspec/specs/server-startup-node-version-guard/spec.md` — its stale `<26`, `major >= 26`, `ci.yml` and `src/node-guard.ts` lines all sit inside the three requirements this change's delta MODIFIES, so `openspec archive --sync` replaces them wholesale; hand-editing it would conflict with the sync. Do NOT skip the sync step in `ship-change`. (b) `electron-qa-smoke-test-inventory.md` — auto-generated by batch workers across archived proposals; a hand edit is overwritten on next regeneration.

## 3. CI — validate Node 26

- [x] 3.1 `.github/workflows/_smoke.yml` `standalone-install-smoke-linux` matrix: add `node:26-bookworm-slim` + `node:26-alpine` legs (`node-version: 26`), mirroring the existing 25 entries. Also update the file header comment (line 4) — `Linux × 6: Node 22/24/25` → `Linux × 8: Node 22/24/25/26`.
- [x] 3.2 Refresh the lockstep contract note in `packages/shared/src/node-version.ts` (the "when `package.json#engines.node` … changes, only this file changes" comment) so it names the `_smoke.yml` Node-major set. Note: `ci.yml` carries a single `node-version: 22` setup-node step and no Node-major matrix — there is no lockstep list there to update.
- [x] 3.3 (test-plan #X3) Drop the `--config.engine-strict=false` override on the Node 26 Linux legs so the EBADENGINE half of #357 is regression-tested in always-on CI. SCOPE LIMIT — be honest about what this proves: the leg runs `pnpm install --frozen-lockfile`, whereas pi runs `npm install --omit=dev`; pnpm and npm resolve `engine-strict` through different config cascades, and `scripts/test-standalone-npm-install.sh` installs tarballs into a temp dir where the repo's root `.npmrc` does not apply. So X3 proves the manifest engines range admits Node 26 under a strict installer, NOT the exact pi codepath. Full-fidelity npm+engine-strict coverage comes from the strict arm added to the qa test in 4.9 and, finally, M1. Evidence the override is unnecessary on Linux: `_smoke.yml:75-76` justifies it as "the transitive appdmg engine range fail-fasts on Node 24/25", but `pnpm-lock.yaml:4079-4082` records `appdmg@0.6.6` with `engines: {node: '>=8.5'}` and `os: [darwin]` — no upper bound, and platform-filtered out of Linux installs entirely; the true historical cause per `.npmrc` was native compilation of `macos-alias`, not an engines range. A whole-lockfile scan finds exactly one upper-bounded engines range (`>=6 <7 || >=8`), which admits 26. Correct the false `_smoke.yml:75-76` appdmg rationale UNCONDITIONALLY — it is wrong regardless of how the strict leg turns out, and leaving it invites the next engineer to re-derive the same wrong conclusion. If the strict leg unexpectedly reds, restore the override on that leg and record the REAL blocker in the comment.

## 4. Tests (folded from test-plan.md — automated rows)

- [x] 4.1 (test-plan #E1) Extend `packages/shared/src/__tests__/node-version.test.ts`. Triple: input `"v26.0.0"`/`"v26.5.0"` · trigger `isOutOfEnginesRange(v)` + `isUsableNodeVersion(v)` · observable `isOutOfEnginesRange` false, `isUsableNodeVersion` true. SCOPE vs 2.1: task 2.1 already flips the existing `["v26.0.0", false]` accept-set row to `true` — do NOT re-add that row here. E1's remaining delta is the mid-major case `"v26.5.0"` plus explicit direct `isOutOfEnginesRange` assertions (the accept-set table only drives `isUsableNodeVersion`). See `packages/shared/src/__tests__/node-version.test.ts`.
- [x] 4.2 (test-plan #E2) Extend `node-version.test.ts`. Triple: input `"v27.0.0"` · trigger `isOutOfEnginesRange(v)` · observable returns true, `isUsableNodeVersion("v27.0.0")` false. See `packages/shared/src/__tests__/node-version.test.ts`.
- [x] 4.3 (test-plan #E3) Extend `node-version.test.ts`. Triple: input `"v22.19.0"`,`"v22.18.0"`,`"v24.2.0"` · trigger `isUsableNodeVersion(v)` · observable `true`/`false`/`false` (floor + Fastify range unchanged). See `packages/shared/src/__tests__/node-version.test.ts`.
- [x] 4.4 (test-plan #E4) Extend the existing `describe("buildEnginesRangeMessage")` block in `packages/server/src/__tests__/node-guard.test.ts` (the builder lives in the server package, not shared). Triple: input `buildEnginesRangeMessage("v27.0.0")` · trigger call builder · observable string contains `cannot start on Node v27.`, `Required: >=22.19.0 <27`, `nvm install`, `PATH="$HOME/.pi-dashboard/node/bin`, `brew install node`. See `packages/server/src/__tests__/node-guard.test.ts`.
- [x] 4.5 (test-plan #E5) New repo-lint file `packages/shared/src/__tests__/node-cap-single-source.test.ts` (there is no existing single-source scan to extend — a scan for `major >=` arithmetic across the shared+server test dirs returns zero hits). Triple: input the repo `src/` tree · trigger scan for engines-cap arithmetic · observable the literal `major >= 27` appears only in `packages/shared/src/node-version.ts`; no stray `major >= 26` cap check. MUST scope the walk to `packages/*/src/**` EXCLUDING `__tests__/` — otherwise the test matches its own source (both literals appear in its assertions) and is vacuously self-satisfying. DEFINE THE PATTERN MECHANICALLY rather than as the prose "engines-cap arithmetic" (two engineers would write different regexes): match `/major\s*>=\s*2\d/` and assert the only file with a hit is `node-version.ts` and that its hit is `27`. Note this catches CODE only — prose like `At/above the cap (>=26)` has no `major` token and will NOT match; that comment is covered by 2.3(d), and the message string by E10, not here. See `packages/shared/src/__tests__/no-managed-dir-reference.test.ts` (repo-source-tree walk exemplar — `node-version.test.ts` is a pure unit test with no FS traversal and is the WRONG pattern here).
- [x] 4.6 (test-plan #E6) New repo-lint `packages/shared/src/__tests__/client-build-deps-runtime.test.ts`. Triple: input `packages/client/package.json` · trigger inspect deps vs devDeps · observable `dependencies` ⊇ {`vite`,`@vitejs/plugin-react`,`@tailwindcss/vite`,`tailwindcss`,`tsx`} and `devDependencies` ∩ first four = ∅. See `packages/shared/src/__tests__/bundled-node-meets-pi-floor.test.ts` (repo-lint-over-package.json exemplar).
- [x] 4.7 (test-plan #E7) Assert the `verify-release-deps` guard fires when a client build dep regresses. Triple: input a package.json tree with a build dep back in `devDependencies` · trigger evaluate the client-deps rules · observable a failure naming the missing client `dependencies` entry. IMPLEMENTATION — DECIDED, do not re-litigate: export `collectFailures({ repoRoot })` from `scripts/verify-release-deps.mjs` (it is currently unexported with `REPO_ROOT` hardcoded to `scripts/..`, and always also runs the openspec-floor + pi-pin gates needing `packages/server/package.json`, `packages/extension/package.json`, `docker/Dockerfile`) and drive it against a tmp fixture tree containing those files. Keep the CLI entry point behaviour unchanged. Do NOT mutate a tracked file from a vitest run, and do NOT `execSync` the script — no existing vitest test spawns a subprocess to assert an exit code, so there is no exemplar for that shape.
- [x] 4.8 (test-plan #E8) New repo-lint file `packages/shared/src/__tests__/smoke-node-matrix.test.ts` — a NEW file, not an extension of `publish-workflow-contract.test.ts` (that file covers `publish.yml` + `_electron-build.yml` and has no `_smoke.yml` assertions; appending would mix concerns). Triple: input `.github/workflows/_smoke.yml` `standalone-install-smoke-linux` matrix · trigger parse the Node matrix · observable the matrix `node-version` majors equal the declared supported set `{22, 24, 25, 26}`. Extract the `include:` block with the repo's established job-body line-slicing helper pattern, NOT a YAML library: `packages/shared` declares no `yaml` dependency (it would resolve only by hoisting from `packages/client` — exactly the fragile implicit resolution this change fixes for `tsx`), and `publish-workflow-contract.test.ts` documents the deliberate convention of avoiding a YAML lib for stable 2-space-indent workflow files. Slice the `standalone-install-smoke-linux` job body, then collect `node-version:` scalars with a line regex. See `packages/shared/src/__tests__/publish-workflow-contract.test.ts` for the workflow-lint shape only.
- [x] 4.9 (test-plan #X1) New qa install-smoke `qa/tests/15-omit-dev-build.sh` (prefixes 01–14 are taken; `02-` is `02-server-start.sh`). Triple: input clean checkout, no `node_modules`/`packages/client/dist` · trigger `npm install --omit=dev` at repo root · observable exit `0` and `packages/client/dist/index.html` exists. Cover BOTH resolution engines AND both engine-strict modes, three arms total: (1) `npm install --omit=dev --engine-strict=false` — isolates the BUILD failure (fidelity: npm is what pi runs; with no `package-lock.json` it resolves fresh from the registry, a non-reproducible canary by design); (2) `npm install --omit=dev` with root `.npmrc` `engine-strict=true` in force and NO override — the only full-fidelity reproduction of the actual #357 codepath (npm + engine-strict + git clone); (3) `pnpm install --prod --frozen-lockfile` — reproducibility against the locked CI tree. Verify pnpm's `--prod` is still the correct flag for the installed pnpm major (newer pnpm prefers `--omit=dev`; the wrong flag can succeed while silently not omitting). A pass on one arm and a fail on another is the signal that the engines have diverged. Comment in the script that the `--engine-strict=false` in arm 1 is a test-only bypass scoping that arm to the BUILD failure, not a recommended install workaround. Arm 2 MUST run on Node >= 26 (pin via nvm inside the script, or SKIP with a clear message when the runner Node is below 26) — on the CI-default Node 22 the engine-strict assertion passes trivially and the arm is vacuous. Resolve the pnpm flag up front rather than at runtime: use `--prod` if the installed pnpm major still honours it, else `--omit=dev`; a silently-accepted no-op flag makes arm 3 vacuous too. Register the new file in the `TESTS` array of `qa/tests/run-all.sh` (currently ending at `14-pi-resources-parity.sh`) — otherwise it never executes in the qa suite. See `qa/tests/01-install.sh`.
- [x] 4.10 (test-plan #X2) Node 26 install-smoke leg. NOTE: no separate test artifact — task 3.1 IS the implementation and the CI run is the verification; this row exists to keep the manifest fold 1:1. Triple: input standalone install on Node 26 runner · trigger `_smoke.yml` linux leg `node-version: 26` · observable install-smoke job passes on Node 26. See `.github/workflows/_smoke.yml` `standalone-install-smoke-linux`.

## 5. Manual verification (folded from test-plan.md — manual-only)

- [x] 5.1 (test-plan: manual-only) M1: on a fresh machine with Node 26 and pi installed, run `pi install git:github.com/BlackBeltTechnology/pi-agent-dashboard` and confirm it completes with no `EBADENGINE` and no `Cannot find module 'vite/package.json'`, and the dashboard loads. Deferred to post-merge per `ship-change`.

## 6. Close-out

- [x] 6.1 Run `npm test 2>&1 | tee /tmp/pi-test.log` and confirm the folded L1 tests pass.
- [x] 6.2 Add directory `AGENTS.md` rows for every new file, per the Documentation Update Protocol: `qa/tests/15-omit-dev-build.sh` → `qa/tests/AGENTS.md`; `client-build-deps-runtime.test.ts`, `node-cap-single-source.test.ts`, `smoke-node-matrix.test.ts` → `packages/shared/src/AGENTS.md` (there is NO `src/__tests__/AGENTS.md`; that dir's files are indexed from the parent with an `__tests__/` path prefix — see the existing `__tests__/nightly-workflow-contract.test.ts` row); refresh the `verify-release-deps.mjs` row in `scripts/AGENTS.md` (it enumerates the current rules) for the 5 new client rules; and update the EXISTING `node-version.ts` row in `packages/shared/src/AGENTS.md` (line ~30) — it currently states `isOutOfEnginesRange` is true when `>=26` and `Reject: … 26+`, both false after the cap raise.
- [x] 6.3 Update `CHANGELOG.md` `## [Unreleased]` (Fixed), matching the existing entry style: Node 26 support (cap → `<27`) + git-install `--omit=dev` client build fix, referencing issue #357.
- [x] 6.4 (test-plan: manual-only) Comment on #357 and close once the change lands (Node 26 leg green + qa omit-dev build passing). Post-merge admin action — not implementable before the PR merges, so it carries an explicit defer tag rather than gating the ship.
