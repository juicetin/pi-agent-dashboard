## 1. Ledger and baseline

- [x] 1.1 Re-derive both counts at repo root and record them: `npx biome lint --only=lint/nursery/noFloatingPromises . --max-diagnostics=20000` and the same for `noMisusedPromises`. Take the site count from Biome's own reported total, and include `.cjs` in any extraction — a `.ts/.js`-only filter drops `packages/server/src/rpc-keeper/keeper.cjs:141`.
- [x] 1.2 Confirm the ledger still sums: floating 143 = 88 (this change) + 55 (sibling: extension 37, server 17, electron 1); misused 11 = 5 (this change) + 6 (electron, sibling). If the tree has drifted, update all four ladder documents together, not just this one.
- [x] 1.3 Verify the electron handoff is recorded in `openspec/changes/cleanup-async-semantics-server-extension/proposal.md` (scope, fix vocabulary for the inherited misused sites, tunnel-core coupling note) and in the `add-typeaware-lint-gate` ownership table. These edits were made during planning — confirm they survived any rebase.

## 2. Instrumentation (lands first, per design D7)

- [x] 2.1 Add the shared `reportError()` helper in `packages/client/src/lib/`, forwarding the rejection reason to the client's console-error path. This is the client's "existing logging path" for every handler added by this change.
- [x] 2.2 Install the global unhandled-rejection handler in the client bundle entrypoint (`packages/client/src/main.tsx`), before `ReactDOM.createRoot(...).render(...)`, routing through `reportError`.
- [x] 2.3 Install the global unhandled-rejection handler in `packages/electron/src/main.ts` via `process.on("unhandledRejection", …)`, routing through the existing `log()` function. This is instrumentation only — do not fix the electron promise sites, which belong to the sibling change.
- [x] 2.4 Run the suite and triage anything the new handlers surface. Pre-existing rejections unrelated to this change are recorded, not silenced.

## 3. Misused-promise sites (5)

- [x] 3.1 Narrow `packages/client/src/components/worktree/WorktreeActionsMenu.tsx:48` using `!== undefined` — the declared type is `Promise<boolean> | undefined`, so `!== null` is a TypeScript no-overlap error here.
- [x] 3.2 Narrow `packages/client/src/hooks/useHostPlatform.ts:44` and `packages/client/src/hooks/useLaunchSource.ts:29` using `!== null`. Preserve the inflight-memoization semantics exactly; do not add `await` or `.catch`.
- [x] 3.3 Narrow `packages/server/src/tunnel/tunnel-core.ts:156` using `!== null`, preserving the `pendingCreate` dedupe.
- [x] 3.4 Fix the real defect at `packages/server/src/tunnel/tunnel-core.ts:167` — `new Promise(async (resolve) => { … })` swallows a throw in the async executor, so the outer promise never settles. Restructure the executor so a throw rejects (or resolves `null`) rather than hanging. Coordinate with the sibling's floating fix at `:160` in the same `createTunnel`/`createInner` block.

## 4. Floating-promise sites (88) — classify before editing

- [x] 4.1 For each site, choose one of the four allowed fixes — `await`, return the promise, `Promise.all`/`allSettled`, or `.catch(handler)` — and record the choice. Bare `void` is banned; a discard is written `void p.catch(handler)` and every handler body is non-empty and routes to `reportError`.
- [x] 4.2 Fix `scripts/nightly-verdaccio-serve.mjs:70` (`main();`) — the repo-root orphan that blocks graduation.
- [x] 4.3 Fix the 1 site in `packages/automation-plugin` and the 1 in `packages/subagents-plugin`.
- [x] 4.4 Fix the 3 sites in `packages/shell`.
- [x] 4.5 Fix the 5 sites in `packages/roles-plugin`.
- [x] 4.6 Fix the 7 sites in `packages/flows-plugin`.
- [x] 4.7 Fix the 2 test-file sites in `packages/client/src/lib/__tests__/plugin-config-write.test.ts` using test-idiomatic fixes — return/await the promise or `await expect(p).rejects/.resolves`. Never `.catch(log)` in a test; it hides the failure the test exists to catch.
- [x] 4.8 Fix the remaining 68 `packages/client` sites, densest files first (`UnifiedPackagesSection.tsx` 7, `ProviderAuthSection.tsx` 5, `EmlPreview.tsx` 3, `NetworkDiscoverySection.tsx` 3). In a `useEffect`, do not return the promise — React requires `void` or a cleanup function; use an inner async function plus `.catch(handler)`, with a cancellation guard when the result touches state.
- [x] 4.9 Record the fix-type distribution across the 70 client sites (excluding the 2 test-file sites) as an input to `add-typeaware-lint-gate`'s `error`-vs-`warn` severity decision for `packages/client`.

## 5. Tests

- [x] 5.1 Author an L1 test that a throw inside the `createInner` async executor makes the outer promise settle rather than hang — see `packages/server/src/__tests__/tunnel.test.ts` for harness glue. Triple: `TunnelCore` whose `spec.detectBinary()` throws synchronously (input) · `createTunnel(8000)` awaited with a 2s timeout (trigger) · the promise rejects with the thrown error or resolves `null`, and does not remain pending past the timeout (observable). (test-plan #E1)
- [x] 5.2 Author an L1 test that the `undefined`-variant narrowing preserves inflight memoization — see `packages/client/src/hooks/__tests__/useAsyncAction.test.tsx`. Triple: `ghAvailablePromise` and `ghAvailableCache` both `undefined`, `fetchTool` stubbed to resolve after 50ms (input) · `probeGhAvailable()` invoked twice in the same tick (trigger) · `fetchTool` called exactly once and both calls return the identical promise reference (observable). (test-plan #E2)
- [x] 5.3 Author an L1 test that the `null`-variant narrowing preserves inflight memoization in `useHostPlatform`, `useLaunchSource`, and `tunnel-core.ts:156` — see `packages/client/src/hooks/__tests__/useAsyncAction.test.tsx` and `packages/server/src/__tests__/tunnel.test.ts`. Triple: guard variable `null` with the underlying fetch stubbed to resolve after 50ms (input) · probe or `createTunnel` invoked twice concurrently (trigger) · underlying fetch or `createInner` invoked exactly once and both callers observe the same result (observable). (test-plan #E3)
- [x] 5.4 Author an L1 decision-table test over the guard's three reachable states — see `packages/client/src/hooks/__tests__/useAsyncAction.test.tsx`. Triple: the states (cache set, inflight null), (cache unset, inflight set), (cache unset, inflight null) (input) · probe invoked once per state (trigger) · state 1 returns the cached value with zero fetches, state 2 returns the in-flight promise with zero new fetches, state 3 performs exactly one fetch (observable). (test-plan #E4)
- [x] 5.5 Author an L1 test that the graduation site-enumeration covers every linted extension — see `scripts/__tests__/verify-release-deps-openspec-floor.test.mjs` for harness glue. Triple: Biome floating-promise output containing `packages/server/src/rpc-keeper/keeper.cjs:141` (input) · the enumeration is run over that output (trigger) · the enumerated count equals Biome's reported diagnostic total and the `.cjs` site is present (observable). (test-plan #E5)
- [x] 5.6 Author an L1 test that the ladder ledger sums to the live repo totals — see `scripts/__tests__/verify-release-deps-openspec-floor.test.mjs`. Triple: the claimed counts from this change (88 floating / 5 misused) and the sibling (55 floating / 6 misused) (input) · the sum is compared to a live repo-root Biome run (trigger) · floating sums to 143 and misused to 11, each equal to the live totals, with no site claimed twice (observable). (test-plan #E6)
- [x] 5.7 Add an L3 perf assertion that no touched interaction regresses p95 by more than 10% vs the pre-change baseline — see `tests/e2e/chat-render-perf.spec.ts` for harness glue and sample window. Triple: the interaction that spec already exercises, run against the pre-change and post-change commits (input) · the measured run (trigger) · p95 regression ≤ 10% (observable). (test-plan #P1)
- [x] 5.8 Author an L3 spec for the `UnifiedPackagesSection` surface — see `tests/e2e/navigation.spec.ts` for the `pageerror` listener glue and `tests/e2e/eml-preview.spec.ts` for surface-navigation glue. Triple: the packages surface loaded against the docker harness (input) · search, install-toggle and refresh actions exercised (trigger) · zero `unhandledrejection` and zero `pageerror`, surface converges to a settled rendered state (observable). (test-plan #F1)
- [x] 5.9 Author an L3 spec for the `ProviderAuthSection` surface — see `tests/e2e/navigation.spec.ts`. Triple: the provider-auth settings surface loaded (input) · each async auth action exercised (trigger) · zero `unhandledrejection` / `pageerror`, surface converges to a settled state (observable). (test-plan #F2)
- [x] 5.10 Extend `tests/e2e/eml-preview.spec.ts` with a rejection assertion for the `EmlPreview` surface — that file is its own exemplar. Triple: an `.eml` artifact opened in preview (input) · the preview loads and its async parse/render actions run (trigger) · zero `unhandledrejection` / `pageerror` and the preview renders content (observable). (test-plan #F3)
- [x] 5.11 Author an L3 spec for the `NetworkDiscoverySection` surface — see `tests/e2e/navigation.spec.ts`. Triple: the connectivity/network-discovery surface loaded (input) · a discovery scan triggered and allowed to settle (trigger) · zero `unhandledrejection` / `pageerror` and discovery reaches a terminal state, results or empty-state (observable). (test-plan #F4)
- [x] 5.12 Author an L1 test that a `useEffect` fix does not break the cleanup contract — see `packages/client/src/hooks/__tests__/useMessageHandler.asset-register.test.tsx` for RTL harness glue. Triple: a component whose `useEffect` performs the touched async work with the fetch stubbed to resolve after 100ms (input) · the component unmounted before the promise settles (trigger) · no state update after unmount, cleanup ran, and the effect callback returned `undefined` or a function rather than a promise (observable). (test-plan #F5)
- [x] 5.13 Author an L3 test that the global handler is installed before any application work — see `tests/e2e/navigation.spec.ts`. Triple: the client bundle loaded with a promise rejection fired at the earliest script execution point (input) · page load (trigger) · the rejection is captured and reported rather than lost to a not-yet-installed listener (observable). (test-plan #F6)
- [x] 5.14 Author an L3 fault-injection test that an aborted request is reported rather than swallowed — see `tests/e2e/navigation.spec.ts` and `tests/e2e/error-lifecycle.spec.ts`. Triple: the network request behind a touched client action aborted mid-flight (input) · the action invoked on one of the F1–F4 surfaces (trigger) · zero `unhandledrejection`, the failure reported through `reportError` as a console error record, and the surface degrades to a visible error/empty state rather than hanging (observable). (test-plan #X1)
- [x] 5.15 Add an electron-level assertion that the main-process handler fires — see `.github/workflows/ci-electron.yml` for the existing electron job. First verify that job can assert on main-process log output; if it cannot, record the row as manual-only rather than dropping it. Triple: a promise rejected in electron main with no local handler (input) · the main process runs the rejecting path (trigger) · the `unhandledRejection` handler fires and the reason reaches `log()` in `packages/electron/src/main.ts` (observable). (test-plan #X2)
- [x] 5.16 Author an L1 test that the global handler does not swallow — see `packages/client/src/lib/__tests__/artifact-view-gate.test.ts` for harness glue. Triple: a rejection whose reason is a distinctive `Error` with a known message and stack (input) · the handler processes it (trigger) · the emitted record contains that message, the reason is not replaced by a generic placeholder, and the reporting path is not silently short-circuited (observable). (test-plan #X3)
- [x] 5.17 Author an L1 test that `reportError()` is the client's logging path — see `packages/client/src/lib/__tests__/artifact-view-gate.test.ts`. Triple: a rejecting promise handled by a site handler written per D1 (input) · the handler runs (trigger) · `reportError` invoked exactly once with the rejection reason and forwarding to the console-error path (observable). (test-plan #X4)
- [x] 5.18 Review every `.catch` handler body introduced by this change and confirm none is empty or swallowing (test-plan: manual-only). Enforcement is deliberately review-only — enabling `noEmptyBlockStatements` is a severity flip owned by `add-typeaware-lint-gate`.

## 6. Validate

- [x] 6.1 Re-derive both lint counts and confirm the 93 claimed sites are clear. DONE: this change's scope reports 0 for both rules; 54 floating + 6 misused remain, all in the sibling's scope (server 16 + extension 37 + electron 1 floating; electron 6 misused). Note the boundary moved: `tunnel-core.ts:160` was absorbed here, so the split settled at 89 + 54, not 88 + 55.
- [x] 6.2 Confirm no new `noMisusedPromises` findings were introduced by `await`s added in section 4 — re-derive both rules together, not just floating.
- [x] 6.3 Run the full suite and the E2E harness. DONE with a stated limit: the full vitest suite is green (12314 passed, 0 failed) and every scenario this change added passes against the docker harness (F1, F2, F3, F4, F6, X1). The full 80-spec browser suite was NOT run to completion — the harness container goes unhealthy under sustained load on the dev host. Every failure encountered was reproduced identically against a harness built from the base commit, so none is attributable to this change; they are tracked in the follow-up issue for develop's red E2E baseline. Do not read this box as 'the whole E2E suite is green'.
- [x] 6.4 Confirm the fix-type distribution from 4.9 is recorded where `add-typeaware-lint-gate` can consume it.
