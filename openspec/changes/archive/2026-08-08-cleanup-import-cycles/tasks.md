## 1. Baseline

- [x] 1.1 Record the starting probe count: `npx biome lint --only=lint/suspicious/noImportCycles . --max-diagnostics=20000` reports 17. Re-run after every cut below; the predicted drops are D1 −2, D2 −2, D4a −2, D3 −5, D4b −6, ending at 0. A cut that misses its predicted drop did not do what it claimed — stop and re-diagnose rather than stacking the next cut.
- [x] 1.2 Resolve **C1** (test-plan clarification, blocks task 5.7a): determine what `GET /api/file?cwd&path=diff:<rel>` returns today, so the expected observable for an oversized pseudo-tab is established before E8 is authored. Likely a miss → `size` 0 → the gate never fires → removal is a no-op; confirm rather than assume.

## 2. D1 — extract `isLoopback` (server, −2)

- [x] 2.1 Extract `isLoopback` and `LOOPBACK_ADDRESSES` from `packages/server/src/auth/localhost-guard.ts` into a new leaf module under `packages/server/src/auth/`. Both `localhost-guard.ts` and `tunnel/tunnel-block-events.ts` import it; leave no re-export edge behind in `localhost-guard.ts`.
- [x] 2.2 Split the import at `packages/server/src/__tests__/localhost-guard.test.ts:2` — it imports 7 symbols (`isLoopback`, `isBypassedHost`, `matchCidr`, `ipToNum`, `createNetworkGuard`, `netmaskToCidrBits`, `networkAddress`), of which only `isLoopback` moves. Take `isLoopback` from the new leaf module and leave the other 6 importing from `localhost-guard.js`. This is a split, not a path-swap.
- [x] 2.3 Extend the `isLoopback` unit test for the extracted module — see `packages/server/src/__tests__/localhost-guard.test.ts`. Triple: each member of `LOOPBACK_ADDRESSES`, plus `203.0.113.1` and `undefined` · call `isLoopback` from its new leaf module · `true` for every set member, `false` for the other two, identical to pre-extraction (test-plan #E2).
- [x] 2.4 Run the existing `localhost-guard.test.ts` and `tunnel-block-events.test.ts` suites unchanged — see `packages/server/src/__tests__/tunnel-block-events.test.ts`. Triple: both existing suites with updated import paths · run them · every assertion passes unchanged (test-plan #E3).

## 3. D2 — extract `formatCost` (flows-plugin, −2)

- [x] 3.1 Extract `formatCost` from `packages/flows-plugin/src/client/FlowAgentCard.tsx` into a leaf module under `packages/flows-plugin/src/client/`; `FlowAgentCard` and `FlowAgentDetail` both import it.
- [x] 3.2 Unit-test the extracted `formatCost` — see `packages/flows-plugin/src/__tests__/flow-agent-cost.test.tsx`. Triple: `0`, `0.005`, `0.999`, `1`, `1.5`, `1234.567` · call the extracted `formatCost` · hybrid precision identical to today's `FlowAgentCard.tsx:27-29`, which is `n >= 1 ? toFixed(2) : toFixed(4)` — so `$0.0000`, `$0.0050`, `$0.9990`, `$1.00`, `$1.50`, `$1234.57`. It is NOT an always-2-decimal formatter; writing it as one is a sub-dollar rendering regression (test-plan #E4).
- [x] 3.3 Render-test both flow agent surfaces — see `packages/flows-plugin/src/__tests__/flow-agent-cost.test.tsx`. Triple: a flow agent with cost `>= 1` and one with cost `< 1` · render `FlowAgentCard` and `FlowAgentDetail` · both mount, cost renders identically in each at 2 decimals for `>= 1` and 4 decimals for `< 1`, no `undefined` component from a circular import (test-plan #F7).

## 4. D4a — extract `isExternalHref` (client, −2)

- [x] 4.1 Extract `isExternalHref` from `packages/client/src/components/preview/MarkdownContent.tsx` into a leaf module under `preview/`; `MarkdownContent` and `FrontmatterProperties` both import it. Move only this util — leave `tableToMarkdown`, `tableToTsv`, `isFencedBlockComplete` in place.
- [x] 4.2 Unit-test the extracted predicate — see `packages/client/src/components/preview/__tests__/is-external-href.test.ts`. Triple: `https://example.com/x`, a same-origin absolute URL, `#anchor`, `undefined` · call `isExternalHref` from its new module · `true` only for the cross-origin URL, `false` for the other three, identical to pre-extraction (test-plan #E9).
- [x] 4.3 Regression-test external-link hardening — see `packages/client/src/components/__tests__/MarkdownContent.test.tsx` (anchor target handling). Triple: external URL, same-origin URL, fragment-only, loopback URL · render markdown containing each · external gets `target="_blank" rel="noopener noreferrer"`, fragment + same-origin stay in-document, loopback click opens the split live-server viewer (test-plan #F6).

## 5. D3 — split `viewerRegistry` + `EditorPane` dispatch (client, −5)

- [x] 5.1 Create the neutral leaf `packages/client/src/components/editor-pane/viewer-kinds.ts` FIRST. It imports NO viewer components. Export `PSEUDO_TAB_VIEWERS` and `OPEN_PATH_VIEWERS` (const tuples, 4 and 14 members), `PseudoTabViewer`, `OpenPathViewer = Exclude<ViewerKind, PseudoTabViewer>`, the guard `isPseudoTabViewer(v: ViewerKind): v is PseudoTabViewer`, and FOUR compile-time checks built on `type _AssertNever<T extends never> = T`: `_Uncovered` (coverage), `_NoExtraOpen` and `_NoExtraPseudo` (no stray member in either array), and `_NoOverlap` (`Extract<...>` of the two arrays is `never`, i.e. disjoint). Do NOT write these as `const _x: SomeType[] = []` — that form is VACUOUS (an empty array literal is assignable to every array type, including `never[]`), verified by running this repo's `tsc` against a missing member, a spurious member and an overlapping member: all three exited 0. Confirm each of the four checks actually errors before trusting it. Note what they do NOT prove: they establish that the two arrays totally and disjointly partition `ViewerKind`, but NOT that each kind is in the *correct* half — swapping a member between the arrays passes all four checks. Correct-half assignment is proven only by the runtime test 5.6 (#E6), so do not treat the compile-time checks as covering it. The arrays are needed because `ViewerKind` is a type with no runtime representation and `packages/shared` is out of scope. This module must NOT live in half (b) — `CappedViewer` imports `OpenPathViewer` from it, so putting it in (b) re-forms `CappedViewer → (b) → DiffViewer → DiffPanel → DiffFilePreview → CappedViewer` and the gate stays nonzero.
- [x] 5.2 Split `viewer-registry.tsx` into half (a) `fileKind()`-returnable viewers and half (b) explicitly-opened pseudo-tab viewers, AND add the `EditorPane` dispatch, as ONE atomic commit. `EditorPane` branches on `isPseudoTabViewer(activeTab.viewer)`: pseudo-tab kinds render half (b) directly, everything else goes to `CappedViewer`. The discriminator MUST be `activeTab.viewer` — NOT `fileKind(...)` (its `.viewer` is wrong for pseudo-tab paths) and not a path-prefix test. These two edits cannot be separate commits: `EditorPane` cannot import half (b) before 5.2 creates it, and splitting the registry without the dispatch routes every pseudo-tab to a missing key. Only (b) imports `DiffViewer`; make each half's `Record` total over its own subset. Also unify the duplicate pseudo-tab set already hardcoded at `EditorPane.tsx:120-126` (`tabActionTarget`) onto `isPseudoTabViewer` — but that branch is NOT uniform over the four kinds: `url` returns `{kind:"url", url}` while `live-server`/`diff`/`terminal` return `null`. Test `activeTab.viewer === "url"` BEFORE the guard; a literal collapse to `isPseudoTabViewer(v) ? null : {kind:"file"}` silently drops the system-open action for `url:` tabs with no type error. Preserve the url derivation verbatim — it is `url: activeTab.path.replace(/^url:/, "")` (`EditorPane.tsx:123`), NOT `activeTab.path`; dropping the `.replace` ships the `url:` prefix inside the url field, again with no type error. Finally, DO update the `EditorPane.tsx:3` comment: "the active viewer (resolved via the viewer registry)" becomes misleading after this change, because pseudo-tab kinds are dispatched straight into half (b) and bypass the registry entirely.
- [x] 5.3 Point `DiffFilePreview` at the guard: it passes `fileKind(...).viewer`, typed `ViewerKind` in `packages/shared` and out of scope to narrow at source, so use `isPseudoTabViewer` to discharge it to `OpenPathViewer`, rendering its existing not-found path on the branch `fileKind()` provably never produces. TypeScript does NOT narrow a string union through a bare `Set.has`, so the guard is required, not optional.
- [x] 5.4 Narrow `CappedViewer`'s `viewer` prop from the full `ViewerKind` union to `OpenPathViewer`, and point its lookup at half (a) only. Do this only after 5.1 and 5.3 — narrowing first converts a compile-time guarantee into a runtime crash. Do NOT add an `as`/`as never` cast at either call site to force this through; a cast erases the compile-time guarantee that is D3's whole justification (note `DiffFilePreview` already carries one `kind={meta.kind as never}` — do not add a second).
- [x] 5.5 Update `packages/client/src/components/editor-pane/__tests__/viewer-registry.test.tsx:53-62`, which asserts `Object.keys(viewerRegistry)` equals the full 18-key list, to cover both halves. Triple: both registry halves plus the `OPEN_PATH_VIEWERS`/`PSEUDO_TAB_VIEWERS` const arrays · take the key union and intersection and compare against the arrays · union === all 18 members (14 + 4), intersection empty, each half's `Record` total over its subset. Enumerate the const arrays, not the `ViewerKind` type — the type has no runtime representation; the `_Uncovered`/`_NoExtra` checks from 5.1 are what prove the arrays equal the union (test-plan #E5).
- [x] 5.6 Test kind routing — see `packages/client/src/components/editor-pane/__tests__/viewer-registry.test.tsx`. Triple: all 18 `ViewerKind` members · look each up in both halves · the 14 `fileKind()`-returnable kinds resolve from (a) and are absent from (b); the 4 pseudo-tab kinds resolve from (b) and are absent from (a). Half (a) MUST contain `binary-warn` and `monaco` — both are `fileKind()`-returnable and are the two most likely to be dropped (test-plan #E6).
- [x] 5.7 Test that the size gate is preserved for real files — see `packages/client/src/components/editor-pane/__tests__/viewer-registry.test.tsx`. Triple: `size` = `MAX_PREVIEW_BYTES-1`, `= MAX_PREVIEW_BYTES`, `= MAX_PREVIEW_BYTES+1` for a non-`monaco` file-kind viewer · mount `CappedViewer` · first two render the viewer, third renders `TooLargePreview`; `monaco` bypasses at all three (test-plan #E7).
- [x] 5.7a Test the oversized pseudo-tab boundary once C1 (task 1.2) is resolved — see `tests/e2e/editor-pane.spec.ts`. Triple: a `diff:` tab whose content exceeds `MAX_PREVIEW_BYTES` · open the tab · observable per the C1 resolution. If C1 resolves to "the probe always missed, so the gate never fired", this row is a no-op and D3's "owned behaviour change" is retired rather than tested (test-plan #E8).
- [x] 5.8 E2E the diff tab — see `tests/e2e/editor-pane.spec.ts`. Triple: a session with a diff · open a `diff:` tab in the editor pane · `DiffPanel` body renders, no blank pane, no `<undefined/>` React throw, no console error (test-plan #F1).
- [x] 5.9 E2E the other three pseudo-tabs — see `tests/e2e/editor-pane.spec.ts`. Triple: a `url:` tab, a `live:` tab, a `term:` tab · open each · each converges to its own body (url viewer / live-server viewer / terminal keep-alive layer), none takes the file-kind path (test-plan #F2).
- [x] 5.10 Test the `DiffFilePreview` missing-file path is intact — see `packages/client/src/components/__tests__/FilePreviewOverlay.test.tsx`. Triple: `GET /api/file` returns 404 / non-file · open diff "Preview" mode · `data-testid="diff-preview-not-found"` renders and the panel does not crash (test-plan #X1).
- [x] 5.11 Add the type-level guard that an unregistered kind cannot ship — see `packages/client/src/components/editor-pane/__tests__/viewer-registry.test.tsx`. Triple: a member added to `ViewerKind` but registered in neither half · `tsc --noEmit` · compile error, not a runtime `undefined` lookup (test-plan #X2).

## 6. D4b — invert `MarkdownContent → FileLink` (client, −6)

- [x] 6.1 Add an optional `fileLink` renderer field to `ToolContext` (`packages/client/src/components/tool-renderers/types.ts`) and move `MarkdownContent`'s linkification trigger from "`context` present" to "`context.fileLink` present" at ALL THREE trigger sites — the `p` and `li` overrides AND the `code` override at `MarkdownContent.tsx:540`; `renderInlineString` and `linkifyChildren`, which render `<FileLink>` directly, both move to `context.fileLink`. Missing the `code` override silently drops linkification inside inline code spans. Do NOT introduce a renderer registry module — it cannot import `FileLink` without re-forming the cycle, and would need a mutable attach-before-render singleton.
- [x] 6.2 Extract the `ToolContext` construction into an exported `makeToolContext(...)` builder in its OWN module `packages/client/src/components/tool-renderers/make-tool-context.tsx` — NOT in `tool-renderers/types.ts`, which must stay type-pure (every SCC-B consumer imports it via `import type`, an erased edge; adding a value import of `FileLink` there makes that edge real the moment anyone value-imports `types.ts`). It MUST be a pure function taking its dependencies as arguments — if it closes over module scope instead, task 6.4 exercises a different path than production and the test is worthless. Call it from BOTH production sites: `packages/client/src/App.tsx:1126` (a `useMemo` in `App`'s body) and `packages/client/src/main.tsx:112` (an inline literal in `ToolCallStepPrimitive`). Attach `fileLink` inside the builder. The extraction is what makes task 6.4 feasible — neither inline site is reachable from an L1 test without mounting the whole app shell, so without it the D4b regression test degenerates into a fixture that cannot catch the regression. Missing the `main.tsx` site is the concrete silent-regression path.
- [x] 6.2a Verify `LinkifiedText.tsx:5` is unaffected. `FileLink` has TWO production importers, not one — `MarkdownContent.tsx:24` (cut here) and `LinkifiedText.tsx:5`. `LinkifiedText` is not in SCC B, forms no cycle, and keeps its static `FileLink` import unchanged; the `context` → `context.fileLink` trigger change must not alter its linkification.
- [x] 6.3 Merge a default `fileLink` into the context inside `packages/client/src/components/chat/ChatView.tsx`, so external embedders keep linkification with no code change. `ChatView` receives `toolContext` as a prop at `ChatView.tsx:51` and does NOT construct one — do not call `makeToolContext` here, that would shadow the embedder's own context. Merge ONCE at the top of the component in a `useMemo` keyed on `[toolContext]` — `useMemo` is REQUIRED, not a suggestion: `MarkdownContent` is `React.memo`'d (`MarkdownContent.tsx:453`), so an inline merge hands it a new `context` reference every render and defeats the memo. Pass the merged value to EVERY consumer — the complete list is `ChatView.tsx:873` (`ToolBurstGroup`), `:878` (`CollapsedToolGroup`), `:981`, `:997`, `:1106`, `:1140`. **`:873` and `:878` are the easily-missed ones**: they take `toolContext={toolContext}` and render `MarkdownContent` with it internally (`ToolBurstGroup.tsx:285`, `CollapsedToolGroup.tsx:89`), so leaving them on the raw context silently drops linkification inside tool-burst and collapsed-tool-group messages. Merging at only some sites silently drops linkification at the rest. Do NOT try to attach it in `packages/client/src/chat-embed/index.ts` — that file is a pure type-only re-export barrel with no runtime `ToolContext` construction, so an attachment there is dead code and the embedder regression would ship. Cycle-safety is verified: nothing in SCC B imports `ChatView`. Separately, correct the stale doc-comment at `chat-embed/index.ts:39-40` (it lists an `editors` field that does not exist).
- [x] 6.3b Document `fileLink` in the `chat-embed/index.ts` doc-comment as an optional field headless embedders should attach. `chat-embed` also exports the headless half (`useSessionState`, `createSessionAccumulator`, `applySessionMessage` at `index.ts:83-86`), so an embedder can render `MarkdownContent` without ever mounting `ChatView`, bypassing the 6.3 merge and silently losing linkification with no type error. This path is an accepted documented trade-off, not a bug to fix — making the field required would break the published surface.
- [x] 6.4 Test linkification at every REAL context builder — see `packages/client/src/components/tool-renderers/__tests__/FileLink.test.tsx`. Triple: the contexts returned by the extracted `makeToolContext(...)` (task 6.2) in both production configurations — with `cwd` and cwd-less — called directly rather than re-declared, plus a bare embedder `ToolContext` rendered through `ChatView`, plus no context · render `MarkdownContent` with a file mention in a `p`, an `li`, and an inline `code` span · a `FileLink` is produced for the first three at all three positions, plain text for the fourth, matching today. A hand-declared fixture proves only that `MarkdownContent` honours the field and stays green through the exact regression this test exists to catch (test-plan #F3).
- [x] 6.5 Test the `hostManaged` dual-mode is preserved — see `packages/client/src/components/tool-renderers/__tests__/FileLink.test.tsx`. Triple: a `FileLink` inside a `FilePreviewProvider` and one outside it · click to open a preview · inside, `hostManaged` true with exactly one overlay from `FilePreviewHost` and none from the leaf; outside, the leaf-local overlay renders; zero double-mounts (test-plan #F4).
- [x] 6.6 Test the public `ToolContext` surface is not broken — see `packages/client/src/components/tool-renderers/__tests__/FileLink.test.tsx`. Triple: an embedder building a `ToolContext` without `fileLink`, rendered through `ChatView` · typecheck + render · compiles (field optional) and linkification still works via `ChatView`'s merged default (test-plan #X4).
- [x] 6.7 E2E that file preview survives message churn — see `tests/e2e/file-preview-survives-churn.spec.ts`. Triple: open a file preview in chat, then stream new messages · the message list re-renders / remounts · the overlay stays open on the same target (test-plan #F5).

## 7. Verify

- [x] 7.1 Terminal gate — see `.github/workflows/ci.yml`. Triple: the whole repo after all five cuts · run `npx biome lint --only=lint/suspicious/noImportCycles . --max-diagnostics=20000` · `Found 0 warnings`, exit 0; any nonzero count is a failure, not partial progress (test-plan #E1).
- [x] 7.2 `isolatedModules` conformance — see `.github/workflows/ci.yml`. Triple: all five extracted leaf modules · `tsc --noEmit` · exit 0, every type re-export using `export type` (test-plan #E10).
- [x] 7.3 Production bundle builds and boots — see `.github/workflows/ci.yml`. Triple: the full repo after all cuts · `npm run build` then load the built bundle in the harness browser · build exits 0 and the app boots with no module-evaluation `undefined` binding error in console. `vite build` alone does not satisfy this — it compiles without executing, so it cannot observe an eval-order defect (test-plan #X3).
- [x] 7.4 Run the full suite: `set -o pipefail; npm test 2>&1 | tee /tmp/pi-test.log` then grep for failures.
- [x] 7.4a Run the E2E layer against a harness REBUILT FROM LOCAL CODE, and require it green. Use `docker/test-up.sh` / the `run-dashboard-e2e-local-changes` skill, then `npm run test:e2e`. Plain `npm run test:e2e` runs Playwright against the `docker/` all-in-one harness, which by default uses a CACHED image — i.e. the pre-change bundle — so F1, F2, F5 and X3 would all pass vacuously against code this change never touched. E2E is opt-in repo-wide but REQUIRED here: per design D6 these are the sole oracle able to catch a D3 mis-route or a D4b silent linkification drop. A green default CI is not sufficient to merge.
- [ ] 7.5 Manual visual parity check across the markdown-heavy chat transcript, the file-preview overlay, the four pseudo-tab kinds, and the flow agent card/detail, compared against `develop` (test-plan: manual-only).

## 8. Docs

- [x] 8.1 Update the directory `AGENTS.md` rows for every added or materially changed file (`packages/server/src/auth/`, `packages/flows-plugin/src/client/`, `packages/client/src/components/preview/`, `.../editor-pane/`, `.../tool-renderers/`, `.../chat-embed/`), per the Documentation Update Protocol.
