## 1. Baseline and ledger

- [x] 1.1 Re-derive both counts over `packages/{server,extension,electron}`: `npx biome lint --only=lint/nursery/noFloatingPromises <pkg> --max-diagnostics=20000` and the same for `noMisusedPromises`. Take counts from Biome's reported total and include `.cjs` — a `.ts/.js`-only filter drops `packages/server/src/rpc-keeper/keeper.cjs:141`.
- [x] 1.2 Confirm the ladder still sums: floating 143 = 55 (this change) + 88 (`cleanup-client-plugin-promises`); misused 11 = 6 (this change, electron) + 5 (sibling). If the tree drifted, update this change, the sibling, and the `add-typeaware-lint-gate` ownership table together.
- [x] 1.3 Re-confirm the production-vs-test split before planning work: 41 sites in test files (37 extension, 4 server) and 20 production (13 server, 7 electron). The change's task grouping depends on it.

## 2. Non-promise inference artifacts (3 sites)

- [x] 2.1 Annotate `withPiResolve` in `packages/server/src/__tests__/pi-resource-activation-timeout.test.ts:22` with a `: void` return type, clearing the diagnostics at `:46,59,65`. Do NOT `await` these calls — the helper returns `undefined`, so `await` would be a no-op documenting a defect that does not exist.

## 3. Test-file sites (38)

- [x] 3.1 Triage every site into missing-await or deliberately-in-flight before editing. Fix vocabulary is `await` / `return` only — `void` and `.catch` are banned in tests because they suppress the failure the test exists to detect.
- [x] 3.2 Fix the 17 sites in `packages/extension/src/__tests__/prompt-bus.test.ts`. For deliberately-in-flight requests, capture the promise and await it after the assertions; match the promise's actual polarity (these typically resolve, so `.rejects` would change what the test proves).
- [x] 3.3 Fix the 14 sites in `packages/extension/src/__tests__/prompt-bus-wiring.test.ts`, same triage.
- [x] 3.4 Fix the 6 sites in `packages/extension/src/__tests__/tui-prompt-adapter.test.ts`, same triage.
- [x] 3.5 Fix the 1 site in `packages/server/src/embed-lifecycle/__tests__/visitor-session-registry.test.ts`.
- [x] 3.6 Flag any site whose promise never settles — awaiting it hangs the test to the Vitest timeout and `.catch` is banned, so it needs the adapter made to answer rather than a forced fix.

## 4. Production server sites (13)

- [x] 4.1 Classify each of the 13 sites into `await`, `return`, `Promise.all`/`allSettled`, or `.catch(handler)` and record the choice. Bare `void` is banned; a discard is `void p.catch(handler)` with a non-empty handler routed to the existing logging path. Prefer `.catch(handler)` over `await` unless ordering is demonstrably load-bearing.
- [x] 4.2 Fix `browser-handlers/subscription-handler.ts:220,243,249`.
- [x] 4.3 Fix `server.ts:2144,2180` (boot path: zrok cleanup, session discovery).
- [x] 4.4 Fix `browser-handlers/directory-handler.ts:226,247`.
- [x] 4.5 Fix `tunnel/tunnel-core.ts:160`. Coordinate with `cleanup-client-plugin-promises`, which narrows `:156` and restructures the async executor at `:167` in the same `createTunnel`/`createInner` block — whichever change lands second re-derives the diagnostics for this file rather than assuming its patch still applies.
- [x] 4.6 Fix `session/session-load-worker-pool.ts:254` and `openspec/openspec-poll-worker-pool.ts:278`. Note these run on worker threads and plausibly do not route to the `cli.ts:493` crash-safety net, so an unhandled rejection here may be genuinely invisible.
- [x] 4.7 Fix `lifecycle/recovery-server.ts:370` and `pairing/browser-gateway.ts:621` (`handleShutdown` in the WS message dispatch).
- [x] 4.8 Fix `rpc-keeper/keeper.cjs:141` (`startServer(true).then(resolve)` in the backoff retry). CommonJS file — confirm the fix style matches the surrounding module.

## 5. Inherited electron sites (7)

- [x] 5.1 Narrow `packages/electron/src/lib/server-lifecycle.ts:454` and `packages/electron/src/lib/doctor-window.ts:52` with `!== null`, preserving inflight-memoization semantics exactly. Do not `await` — prefer narrowing for minimality.
- [x] 5.2 Fix the 4 `noMisusedPromises` sites at `packages/electron/src/main.ts:531,557,607,654`. These are the same `createTray(() => mainWindow, quit, { … })` call repeated four times, where `async function quit(): Promise<void>` (`main.ts:469`) is passed as `createTray`'s `onQuit: () => void` (`lib/tray.ts:86`). Fix the pattern once and apply it four times, or widen `onQuit` to accept an async callback and handle it inside `tray.ts`. The wrapper must be `() => { void quit().catch(<handler>); }` — the natural `() => { void quit(); }` is bare `void`, which this change bans, and making `quit` sync is infeasible because it awaits `stopServerIfNeeded()`.
- [x] 5.3 Fix the floating site at `packages/electron/src/main.ts:675`. Rebase against `cleanup-client-plugin-promises`, which installs the global unhandled-rejection handler in the same file.

## 6. Tests

- [x] 6.1 Author an L1 test that the `: void` annotation clears the inference-artifact diagnostics without an await — see `packages/server/src/__tests__/tunnel.test.ts` for harness glue. Triple: `withPiResolve` annotated `: void` (input) · Biome run over `pi-resource-activation-timeout.test.ts` (trigger) · zero diagnostics for that file and no `await withPiResolve(` in the source (observable). (test-plan #E1)
- [x] 6.2 Author an L1 guard that no `await` was applied to a non-promise — see `scripts/__tests__/ship-it-no-weakening.test.mjs` for diff-scanning glue. Triple: the post-change diff (input) · scan for `await` on an expression whose static type is not a promise (trigger) · no such site exists (observable). (test-plan #E2)
- [x] 6.3 Author an L1 guard that this change adds no bare `void` discard across the 3 packages — see `scripts/__tests__/ship-it-no-weakening.test.mjs`. **Scope corrected during implementation:** 54 bare discards PRE-DATE this change in those packages, so "no bare `void` discard exists" was never achievable here and would have failed on untouched code. The guard is therefore change-scoped: a frozen pre-change baseline (`fixtures/bare-void-baseline.json`, generated at the merge-base) records those 54, and the live multiset must stay a SUBSET of it — shrinking allowed, growth is a regression. Driving the baseline to zero belongs to a later rung. Triple: the post-change tree measured against the frozen baseline (input) · scan for `void <expr>` whose statement lacks a non-empty `.catch(` (trigger) · zero ADDED bare discards, and the discards this change did add are the guarded `void p.catch(handler)` form (observable). Implemented as a tree guard rather than a diff guard because CI checks out at depth 1, where a diff-scoped check resolves an empty diff and passes vacuously. (test-plan #E3)
- [x] 6.4 Author an electron-runner L1 test that the narrowed guards still dedupe — see `packages/electron/src/__tests__/doctor-window.test.ts`. Triple: `server-lifecycle.ts:454` and `doctor-window.ts:52` narrowed `!== null`, underlying work stubbed to resolve after 50ms (input) · guarded entrypoint invoked twice concurrently (trigger) · underlying work runs once, both callers observe the same result, `noMisusedPromises` zero for both files (observable). (test-plan #E4)
- [x] 6.5 Author an electron-runner L1 test for the `createTray` wrapper — see `packages/electron/src/__tests__/server-lifecycle-spawn-options.test.ts`. Triple: `createTray` wrapped per task 5.2 with `quit` stubbed to reject (input) · tray Quit item clicked (trigger) · rejection reaches the wrapper's handler, no bare `void`, `noMisusedPromises` zero for `main.ts` (observable). (test-plan #E5)
- [x] 6.6 Author an L1 graduation assertion — see `scripts/__tests__/verify-release-deps-openspec-floor.test.mjs`. Triple: the post-change tree (input) · `biome lint --only` for both rules over the 3 packages (trigger) · `noFloatingPromises` zero for extension/electron/server, `noMisusedPromises` reporting only the 2 sibling-owned `tunnel-core.ts:156,167` sites (observable). (test-plan #E6)
- [x] 6.7 Extend `packages/server/src/__tests__/browser-gateway-load.test.ts` to drive `openspec_refresh`, `openspec_bulk_archive` and `shutdown` messages, then assert the oracle actually reaches the sites it claims — that file is its own exemplar. Triple: the extended harness (input) · the new message drives run (trigger) · `directory-handler.ts:226,247` and `browser-gateway.ts:621` each execute at least once inside the measured window, asserted via spy/counter rather than a coverage report (observable). (test-plan #P1)
- [x] 6.8 Assert the extended harness's existing budgets do not regress — see `packages/server/src/__tests__/browser-gateway-load.test.ts`. Triple: the extended harness run pre- and post-change (input) · the scenario matrix A–E (trigger) · existing flush/backpressure budgets do not regress (observable). (test-plan #P2)
- [x] 6.9 Move the replay path inside the measured window and assert no serialization — see `packages/server/src/__tests__/browser-gateway-load.test.ts`. Triple: harness extended so `subscription-handler.ts:220,243,249` run inside the measured window rather than as empty-replay setup (input) · subscribe with a non-empty replay backlog (trigger) · replay drain latency does not regress vs the pre-change baseline (observable). (test-plan #P3)
- [x] 6.10 Author a per-site assertion for `subscription-handler.ts:220` — see `packages/server/src/__tests__/subscription-handler.test.ts`. Triple: the operation at `:220` rejects (input) · a subscribe reaching that path (trigger) · rejection observed by the site's handler, gateway does not hang, no unhandled rejection escapes (observable). (test-plan #X1)
- [x] 6.11 Author a per-site assertion for `subscription-handler.ts:243` — see `packages/server/src/__tests__/subscription-handler.test.ts`. Triple: the operation at `:243` rejects (input) · replay path exercised (trigger) · rejection observed, subscribe still settles (observable). (test-plan #X2)
- [x] 6.12 Author a per-site assertion for `subscription-handler.ts:249` — see `packages/server/src/__tests__/subscription-handler.test.ts`. Triple: the operation at `:249` rejects (input) · replay path exercised (trigger) · rejection observed, subscribe still settles (observable). (test-plan #X3)
- [x] 6.13 Author a per-site assertion for `server.ts:2144` — see `packages/server/src/__tests__/recovery-server.test.ts` for boot-path glue. Triple: zrok cleanup rejects during boot (input) · server `start()` invoked (trigger) · boot continues to a listening state and the rejection is logged, not silently swallowed (observable). (test-plan #X4)
- [x] 6.14 Author a per-site assertion for `server.ts:2180` — see `packages/server/src/__tests__/recovery-server.test.ts`. Triple: session discovery rejects during boot (input) · server `start()` invoked (trigger) · boot completes, rejection logged, no unhandled rejection (observable). (test-plan #X5)
- [x] 6.15 Author a NEW test file for `directory-handler.ts:226` — no sibling test exists; see `packages/server/src/browser-handlers/__tests__/session-action-handler.test.ts` for harness glue. Triple: the openspec refresh operation rejects (input) · an `openspec_refresh` message dispatched (trigger) · rejection observed and the gateway remains responsive to a subsequent message (observable). (test-plan #X6)
- [x] 6.16 Add a per-site assertion for `directory-handler.ts:247` to that new file — same exemplar. Triple: the bulk-archive operation rejects (input) · an `openspec_bulk_archive` message dispatched (trigger) · rejection observed, gateway remains responsive (observable). (test-plan #X7)
- [x] 6.17 Author a per-site assertion for `tunnel-core.ts:160` — see `packages/server/src/__tests__/tunnel.test.ts`. Triple: `createInner` rejects so the `promise.finally()` at `:160` runs on a rejected promise (input) · `createTunnel(port)` called (trigger) · `pendingCreate` cleared, rejection observed, and a subsequent `createTunnel` starts a fresh attempt rather than returning the dead promise (observable). (test-plan #X8)
- [x] 6.18 Author a per-site assertion for `session-load-worker-pool.ts:254` — see `packages/server/src/__tests__/session-load-worker.test.ts`. Triple: the pooled worker task rejects (input) · a session load dispatched to the pool (trigger) · rejection observed by the pool and the worker slot released rather than leaked (observable). (test-plan #X9)
- [x] 6.19 Author a per-site assertion for `openspec-poll-worker-pool.ts:278` — see `packages/server/src/__tests__/openspec-poll-worker.test.ts`. Triple: the poll worker task rejects (input) · a poll cycle dispatched (trigger) · rejection observed, slot released, polling continues (observable). (test-plan #X10)
- [x] 6.20 Author a per-site assertion for `recovery-server.ts:370` — see `packages/server/src/__tests__/recovery-server.test.ts`. Triple: the recovery operation rejects (input) · the recovery path triggered (trigger) · rejection observed and the recovery server remains in a defined state (observable). (test-plan #X11)
- [x] 6.21 Author a per-site assertion for `browser-gateway.ts:621` — see `packages/server/src/__tests__/helpers/load-fixtures.ts` for gateway construction glue. Triple: `handleShutdown` rejects (input) · a `shutdown` message dispatched (trigger) · rejection observed rather than floating, shutdown still reaches a terminal state (observable). (test-plan #X12)
- [x] 6.22 NOT AUTHORABLE (documented, see below). Author a NEW test file for `keeper.cjs:141` — see `packages/server/src/__tests__/keeper-manager.test.ts` for harness glue; confirm the vitest project resolves `.cjs` before authoring. **Resolved: no.** The server vitest `include` is `src/**/__tests__/**/*.test.ts` (`.ts` only), `keeper.cjs` has zero exports and self-executes `main()` on require (binds a UDS socket, spawns pi, calls `process.exit`), and `startServer` never rejects — it always `resolve(s)`/`resolve(null)` — so the `onRejected` branch is unreachable by construction. The fix still clears the diagnostic and settles the outer promise defensively; no behavioural test is possible without changing production source. Triple: `startServer(true)` rejects on the backoff retry (input) · the retry branch entered after a failed first start (trigger) · rejection observed and the outer promise settles rather than hanging (observable). (test-plan #X13)
- [x] 6.23 Author an electron-runner per-site assertion for `main.ts:675` — see `packages/electron/src/__tests__/server-lifecycle-spawn-options.test.ts`. Triple: `main()` rejects (input) · the electron main entrypoint runs (trigger) · the rejection is handled by the bootstrap handler and does not float (observable). (test-plan #X14)
- [x] 6.24 Build the scripted mutation harness and run it over the 4 touched test files — new infra; see `scripts/__tests__/ship-it-no-weakening.test.mjs` for scripted-check glue. Triple: the harness applied to each touched test file (input) · a behaviour each touched test covers is temporarily flipped (trigger) · every touched test file goes red under mutation, and a file that stays green fails the harness (observable). Build it reusable by later ladder rungs. (test-plan #X15)
- [x] 6.25 Author an L1 test that a deliberately-in-flight resolving request keeps its assertion — see `packages/extension/src/__tests__/prompt-bus.test.ts`. Triple: a deliberately-in-flight `bus.request(...)` that resolves, settled per design D3 (input) · the touched extension test runs (trigger) · the test passes, still asserts the same adapter interaction, and no `.rejects` assertion was applied to a resolving promise (observable). (test-plan #X16)

## 7. Validate

- [x] 7.1 **Confirmed: it does NOT.** `packages/electron` is excluded from the root `vitest.config.ts` projects and no `ci.yml` step runs it, so 6.4/6.5/6.23 pass locally but never execute in CI. Exclusion is pre-existing and tracked as a separate cleanup; not widened here. The graduation oracle (#E6) is unaffected — `ci.yml` runs `biome lint .` repo-wide, covering electron. Recorded in `classification.md`. Original task: Confirm CI actually runs the electron vitest project. `packages/electron` is deliberately excluded from the root `vitest.config.ts` projects and runs via `cd packages/electron && npm test`; if CI does not invoke it, tasks 6.4, 6.5 and 6.23 never execute.
- [x] 7.2 Re-derive both rules over all three packages and confirm this change's 61 claimed sites are clear, leaving only the sibling's `tunnel-core.ts:156,167` misused sites.
- [x] 7.3 Confirm no new `noMisusedPromises` findings were introduced by `await`s added in sections 3–5 — re-derive both rules together, not just floating.
- [x] 7.4 Run the full suite plus the extended load harness; confirm no budget regressed and the new per-site tests pass.
- [x] 7.5 Confirm the recorded fix-type distribution is available to `add-typeaware-lint-gate` for its severity decision.
