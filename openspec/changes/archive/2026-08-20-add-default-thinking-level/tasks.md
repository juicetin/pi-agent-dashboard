## 1. Shared config: `defaultThinkingLevel` field

- [x] 1.1 Add `defaultThinkingLevel: string` to the config type in `packages/shared/src/config.ts`, with default `""` in the defaults object, mirroring the `defaultModel` field placement and doc comment.
- [x] 1.2 In the config parser (`packages/shared/src/config.ts`), read `defaultThinkingLevel` with a `typeof parsed.defaultThinkingLevel === "string" ? … : defaults.defaultThinkingLevel` guard — exactly the pattern used for `defaultModel` (:1065) so a non-string falls back to `""`. Ensure the sanitizer (:891) passes it through only when a string.

## 2. Bridge: apply the level alongside the default model

- [x] 2.1 In `packages/extension/src/bridge.ts` `applyDefaultModel()`, inside the `setModel(found).then(...)` success callback, read `freshConfig.defaultThinkingLevel` and, when non-empty, call `(pi as any).setThinkingLevel?.(level)` BEFORE `sendModelUpdateIfChanged()`. `pi.setThinkingLevel` is synchronous, so no await ordering is needed. Applying it at this single site covers both the gate call site (:2619) and the pending-provider retry (:2915).

## 3. Settings UI: thinking-level control paired with Default Model

- [x] 3.1 In `packages/client/src/components/settings/SettingsPanel.tsx`, render `ThinkingLevelSelector` inside the Default Model `--severity-info-*` callout beside the Default Model selector; bind `current` to `defaultThinkingLevel` and include `defaultThinkingLevel` in the `PUT /api/config` partial on change (follow the existing `defaultModel` partial wiring).
- [x] 3.2 Derive `supportedLevels` from the selected Default Model's `supportedThinkingLevels` (same lookup as `packages/client/src/components/chat/CommandInput.tsx:847`); when no Default Model is selected, pass `["off"]` so only `off` renders, and make the locked-state `onSelect` a no-op that does NOT add `defaultThinkingLevel` to the partial and does NOT write `"off"` (persisted value stays `""`).

## 4. Roles UI: thinking level encoded in the role ref

- [x] 4.1 In `packages/roles-plugin/src/RolesSettingsSection.tsx`, add the `ModelInfo.supportedThinkingLevels?: string[]` optional field (:39) — the `models_list` rows already carry it (D10); no payload change.
- [x] 4.2 Add pure helpers beside `inferProviderForBareId` (:58): `splitRefLevel(ref) -> { base, level? }` (split on the LAST `:` only when the tail is a canonical level, so a provider id containing `:` is unharmed) and `joinRefLevel(base, level?)` (omit the suffix for the no-override option). Keep them exported for direct unit tests.
- [x] 4.3 Feed the model selector the STRIPPED base: `current={inferProviderForBareId(splitRefLevel(effective(editingRole)).base, models)}` (:676). Feeding the suffixed ref matches no model row and blanks the pill (D7).
- [x] 4.4 Render `useUiPrimitive(UI_PRIMITIVE_KEYS.thinkingLevelSelector)` beside the picker inside the `roles-model-picker` block (:669-680), with `current` = the split level and `supportedLevels` = the picked model's `supportedThinkingLevels`. Follow `mockups/ui-plan.md` §ModelLevelPair: one bordered container, model left / level right, persistent `MODEL`+`THINKING` captions, ref echo below.
- [x] 4.6 Show the role pill's level as a `:<level>` chip beside the model name, and render the drop notice (`--severity-warning-*`) when a model switch clears an unsupported level (mockup S2, state 2).
- [x] 4.7 A11y fix carried by the mockup (defect #2, pre-existing): the selected role pill's accent-on-accent text is 2.66:1 dark / 3.58:1 light. Promote selected-pill text to `--text-primary`; keep the accent as the 2px outline. Single-column pill grid below 640px.
- [x] 4.5 Route BOTH controls through the existing `setRole(role, modelLabel)` (:246) with a rejoined ref, so level picks reuse the pending/dirty/Save seam verbatim — no new dispatch. On a model change, keep the staged level only when the new model supports it, else drop the suffix.

## 5. Automation UI: thinking level suffixed on the `model` field

- [x] 5.1 In `packages/automation-plugin/src/client/CreateAutomationDialog.tsx`, widen the local models type (:181) with `supportedThinkingLevels?: string[]`.
- [x] 5.2 On the direct-model branch only (:829-838, inside `data-testid="create-model-selector"`), render the `UI_PRIMITIVE_KEYS.thinkingLevelSelector` primitive beside the existing `ModelSelector` in the same `ModelLevelPair` shape; hold the level in a sibling state and write `modelValue` as `base[:level]`. The `@role` branch (the `roleOptions` select) renders NO control, and shows the hint "Thinking level comes from `@<role>`" in its place (D9, mockup S3 state 2).
- [x] 5.3 Split the suffix when seeding state from an existing `model` value so the selector shows the base and the level control the level (edit path parity with 4.3).
- [x] 5.4 Verify NO server change is needed: `resolveModel()` passes non-`@` values through with only `.trim()`, `engine.ts:581` forwards `resolved.model`, and `sessionFlagsToArgv()` (`packages/shared/src/platform/spawn-mechanism.ts:140`) emits `["--model", flags.model]`. Do not add validation anywhere in that chain (D8).

## 6. Tests — folded from test-plan.md (all automated)

- [x] 6.1 L1: `defaultThinkingLevel` defaults to `""` when key absent. Triple: config.json without the key (input) · `loadConfig()` (trigger) · returns `""` (observable). See `packages/shared/src/__tests__/config.test.ts` defaultModel-default test. (test-plan #E1)
- [x] 6.2 L1: set value round-trips. Triple: `{ "defaultThinkingLevel": "high" }` (input) · `loadConfig()` (trigger) · returns `"high"` (observable). See `packages/shared/src/__tests__/config.test.ts:149`. (test-plan #E2)
- [x] 6.3 L1: non-string falls back to `""`. Triple: `{ "defaultThinkingLevel": 3 }` (input) · `loadConfig()` (trigger) · returns `""` (observable). See `packages/shared/src/__tests__/config.test.ts:161`. (test-plan #E3)
- [x] 6.4 L1: partial-merge preserves siblings. Triple: existing config with `port`+`defaultModel`, partial `{ "defaultThinkingLevel": "low" }` (input) · partial merge (trigger) · `defaultThinkingLevel==="low"` AND `port`/`defaultModel` unchanged (observable). See `packages/shared/src/__tests__/config.test.ts:718`. (test-plan #E4)
- [x] 6.5 L1: empty level → bridge does not call setThinkingLevel. Triple: gate passes with `defaultThinkingLevel===""` (input) · `applyDefaultModel()` success (trigger) · `setModel` called, `setThinkingLevel` NOT called (observable). See `packages/extension/src/__tests__/bridge-thinking-level-select.test.ts`. (test-plan #E5)
- [x] 6.6 L1: control renders inside the Default Model callout. Triple: Sessions page with a Default Model selected (input) · render (trigger) · thinking-level control present in the info callout (observable). See `packages/client/src/components/__tests__/SettingsPanel.test.tsx`. (test-plan #F1)
- [x] 6.7 L1: levels filter to the selected model. Triple: model with `supportedThinkingLevels=["medium","high"]` selected (input) · open dropdown (trigger) · only `medium`+`high` render (observable). See `packages/client/src/__tests__/ThinkingLevelSelector.test.tsx`. (test-plan #F2)
- [x] 6.8 L1: levels re-derive when the model changes. Triple: model A (has `xhigh`) → model B (no `xhigh`) (input) · change Default Model selector (trigger) · dropdown converges to model B's levels, `xhigh` gone (observable). See `packages/client/src/components/__tests__/SettingsPanel.test.tsx`. (test-plan #F3)
- [x] 6.9 L1: locked-off with no model persists nothing. Triple: no Default Model selected (input) · render + interact with control (trigger) · displays `off`, only `off` selectable, PUT partial has NO `defaultThinkingLevel`, persisted stays `""` (observable). See `packages/client/src/components/__tests__/SettingsPanel.test.tsx`. (test-plan #F4)
- [x] 6.10 L1: selecting a level persists it. Triple: Default Model selected, pick `high` (input) · select in control (trigger) · PUT partial includes `defaultThinkingLevel:"high"` (observable). See `packages/client/src/components/__tests__/SettingsPanel.test.tsx:418`. (test-plan #F5)
- [x] 6.11 L1: brand-new startup applies both model and level. Triple: gate passes, `defaultThinkingLevel==="high"` (input) · `applyDefaultModel()` success (trigger) · `setModel(found)` then `setThinkingLevel("high")` called before `sendModelUpdateIfChanged` (observable). See `packages/extension/src/__tests__/bridge-thinking-level-select.test.ts`. (test-plan #X1)
- [x] 6.12 L1: unsupported level clamped by pi, bridge does not throw. Triple: model supports `["off","medium"]`, `defaultThinkingLevel==="xhigh"` (input) · `applyDefaultModel()` success (trigger) · bridge passes `"xhigh"` unchanged to `setThinkingLevel`, no bridge exception (observable). See `packages/extension/src/__tests__/bridge-thinking-level-select.test.ts`. (test-plan #X2)
- [x] 6.13 L1: resumed session applies neither model nor level. Triple: non-zero message history, `defaultThinkingLevel==="high"` (input) · session start, gate false (trigger) · neither `setModel` nor `setThinkingLevel` called for the default (observable). See bridge-default-model-gate tests. (test-plan #X3)
- [x] 6.14 L1: custom-provider-late applies level on resolution. Triple: default model on a not-yet-available custom provider, `defaultThinkingLevel==="high"` (input) · provider models arrive, pending retry `applyDefaultModel()` succeeds (trigger) · `setThinkingLevel("high")` called at the same success branch as `setModel` (observable). See `packages/extension/src/__tests__/bridge-thinking-level-select.test.ts`. (test-plan #X4)

- [x] 6.15 L1: `splitRefLevel`/`joinRefLevel` round-trip. Triple: refs `"p/m"`, `"p/m:high"`, and a base whose id itself contains `:` (input) · split then join (trigger) · output equals input; only a canonical-level tail is split off (observable). See `packages/roles-plugin/src/__tests__/RolesSettingsSection.test.tsx`. (test-plan #R1)
- [x] 6.16 L1: level control renders beside the role picker, filtered. Triple: role picker open, picked model with `supportedThinkingLevels=["medium","high"]` (input) · render (trigger) · thinking control present, only `medium`+`high` offered (observable). (test-plan #R2)
- [x] 6.17 L1: level pick stages a suffixed ref. Triple: `@planning`, model `anthropic/claude-sonnet-4-5`, level `high` (input) · select level (trigger) · pending value === `"anthropic/claude-sonnet-4-5:high"`, no separate level field (observable). (test-plan #R3)
- [x] 6.18 L1: no-override strips the suffix. Triple: staged `"p/m:high"` (input) · select the no-override option (trigger) · staged value === `"p/m"` (observable). (test-plan #R4)
- [x] 6.19 L1: suffixed ref splits for display without dirtying. Triple: persisted `"anthropic/claude-sonnet-4-5:high"` (input) · render (trigger) · selector shows base, control shows `high`, pill NOT dirty (observable). (test-plan #R5)
- [x] 6.20 L1: unsupported level dropped on model switch. Triple: staged `"p/a:xhigh"`, pick `p/b` without `xhigh` (input) · select model (trigger) · staged value === `"p/b"` (observable). (test-plan #R6)
- [x] 6.21 L1: level pick defers like a model pick. Triple: role with a persisted model (input) · change ONLY the level (trigger) · pill dirty AND no `role_set` dispatched until Save (observable). (test-plan #R7)
- [x] 6.22 L1: automation direct-model branch writes the suffix. Triple: model `anthropic/claude-sonnet-4-5` + level `high` (input) · submit create dialog (trigger) · written `model` === `"anthropic/claude-sonnet-4-5:high"` (observable). See `packages/automation-plugin/src/__tests__/CreateAutomationDialog.test.tsx`. (test-plan #A1)
- [x] 6.23 L1: `@role` branch renders no level control. Triple: dialog switched to the `@role` branch (input) · render (trigger) · no thinking control present, written `model` is the bare `@role` token (observable). (test-plan #A2)
- [x] 6.24 L1: resolver passes a suffixed ref through unchanged. Triple: `model: "anthropic/claude-sonnet-4-5:high"` and `model: "@planning"` → role ref `"anthropic/claude-sonnet-4-5:high"` (input) · `resolveModel()` (trigger) · returns the suffixed ref verbatim in both cases (observable). See `packages/automation-plugin/src/__tests__/` model-resolver tests. (test-plan #A3)
- [x] 6.25 L1: suffix reaches argv. Triple: `SessionFlags.model === "p/m:high"` (input) · `sessionFlagsToArgv()` (trigger) · argv contains `["--model","p/m:high"]` unchanged (observable). Fails-on-revert guard for D8. (test-plan #A4)

## 7. Docs

- [x] 7.1 Update the affected directory `AGENTS.md` rows (`packages/shared/src`, `packages/extension/src`, `packages/client/src/components/settings`) per the Documentation Update Protocol, noting the new `defaultThinkingLevel` field and its bridge/UI wiring with a `See change:` marker.
- [x] 7.2 Update the `packages/roles-plugin/src` and `packages/automation-plugin/src/client` `AGENTS.md` rows for the paired thinking control and the `:<level>` ref-suffix encoding, with a `See change:` marker.
- [x] 7.3 Keep `mockups/ui-plan.md` as the surface contract while implementing; if a rule changes during PROMOTE, update the plan in the same commit.
- [x] 7.4 Delegate to DocScribe (caveman style) a `docs/architecture.md` note recording the rule this change establishes: every model selector that configures a RUN pairs a thinking level; reference-listing selectors (Model Proxy preferred list, alias table) do not.
