## 1. Shared search-options helper (design D2, spec R1)

- [x] 1.1 Implement `packages/kb/src/search-opts.ts` exporting `searchOptsFromConfig(cfg, { sources, overrides })` mapping all 12 ranking keys (`fieldWeights`, `proximityBoost`, `diversity`, `sourceDedup`, `laneQuota`, `coverageRerank`, `queryExpansion`, `prf`, `expandParent`, `expandGraph`, `rerank`, `rootPriority`); re-export from `src/index.ts`
- [x] 1.2 Test (test-plan #E1, automated, L1 — exemplar: `packages/kb-extension/src/__tests__/kb-search-tool.test.ts`): default config + sources → helper with `{expandGraph:false, rerank:false}` deep-equals the exact object `extension.ts:95` passes today, field-by-field. Triple: default config (input) → helper call with extension overrides (trigger) → field-by-field deep-equal of all 12 keys (observable)
- [x] 1.3 Test (test-plan #E2, automated, L1 — exemplar: `packages/kb/src/__tests__/kb.test.ts`): decision table — all-knobs-flipped config, each CLI override (`--no-source-dedup`, `--no-lane-quota`, `--no-coverage-rerank`, `--no-expand-parent`, `--expand-parent`, `--expand-query`, `--expand-graph`, `--rerank`) applied one at a time flips exactly its field; the other 11 stay at config values; eval path ignores CLI-only flags. Triple: one flag at a time over flipped config (input) → helper call (trigger) → exactly one field changes per override (observable)
- [x] 1.4 Test (test-plan #E3, automated, L1 — same exemplar): structural key-coverage — `Object.keys(helper(cfg, src))` covers the canonical 12-key ranking list (`fieldWeights`, `proximityBoost`, `diversity`, `sourceDedup`, `laneQuota`, `coverageRerank`, `queryExpansion`, `prf`, `expandParent`, `expandGraph`, `rerank`, `rootPriority`) so an unmapped new SearchOpts ranking field fails the test. Triple: canonical key list (input) → helper call (trigger) → key set covers every canonical key (observable)
- [x] 1.5 Replace the inline object in `packages/kb/src/cli.ts` `search` with the helper, passing flag-derived overrides (`--no-*`, `--expand-*`, `--rerank` OR-compositions); verify `kb search` output unchanged on a sample query
- [x] 1.6 Replace the inline object in `packages/kb-extension/src/extension.ts` `kb_search` with the helper + explicit `{expandGraph:false, rerank:false}` overrides (its current behaviour, now written down)
- [x] 1.7 Wire `cli.ts` `eval` to the helper with the EXTENSION's overrides (eval measures the tool path, spec R1); replace `evaluate(store, golden, { k, docType })`
- [x] 1.8 Convert `eval/run-fixtures.ts` (BASELINE/variants → overrides over shared base) and `eval/measure-render.ts` to the helper; leave `src/dox.ts` + `verify.ts` as documented store-default consumers Review note: the '+ PRF (D4b) = shipped default' and '--sweep-with-d4' variants now run expandParent=cfg default (true) where the pre-change base hardcoded false — deliberate correction (the shipped tool runs cfg.expand.parent), matching the variant's name.

## 2. Golden fixture loading (design D3, spec R2)

- [x] 2.1 Implement `loadGolden(raw, file)` in `packages/kb/src/eval.ts`: array | `{items}` accepted, both-shapes diagnostic thrown otherwise, item shape validated (string `q`/`expect`, file+index on rejection), provenance header to stderr; wire into `cli.ts` `eval` replacing the raw cast
- [x] 2.2 Test (test-plan #E4, automated, L1 — exemplar: `packages/kb/src/__tests__/kb.test.ts`): bare array and `{items:[...]}` both load; `intent`/`minedAt` header goes to stderr, stdout untouched. Triple: bare-array file + bundled-object-shape file (input) → `loadGolden` (trigger) → item array returned; stderr header only (observable)
- [x] 2.3 Test (test-plan #E5, automated, L1 — same exemplar): JSON string, `{}`, `{items:"x"}`, and the real `eval/golden.provenance.json` each throw naming BOTH accepted shapes + file. Triple: malformed inputs incl. provenance.json (input) → `loadGolden` (trigger) → throw naming both shapes + path (observable)
- [x] 2.4 Test (test-plan #E6, automated, L1 — same exemplar): `{items:[{q:1,expect:"a"}]}` and missing-`expect` rejected with file name + array index (today: silent zero). Triple: bad item shapes (input) → `loadGolden` (trigger) → throw with file+index (observable)
- [x] 2.5 Verify `kb eval --golden packages/kb/eval/golden.source-intent.json` loads (no `golden is not iterable`) — VERIFIED: `kb eval --golden packages/kb/eval/golden.source-intent.json` loads via the documented command (n=97+7 unreachable, Recall@10 0.495); no `golden is not iterable`; provenance header on stderr.

## 3. Root normalization + reachability (design D4, spec R3)

- [x] 3.1 Implement in `packages/kb/src/eval.ts`: `roots` option, two-tier candidates (longest separator-checked prefix strip + raw kept), substring match over both, first-segment reachability rule (relPrefix first segments ∪ top-level entries of root dirs), `unreachable` metric, exclusion from all rank + redundancy + latency metrics, `n = total − unreachable`, `--verbose` path list; build `roots` in `cli.ts` from `relative(cfg.cwd, source.dir)` — VERIFIED: eval.ts roots implemented; E7-E11 green (eval-roots.test.ts).
- [x] 3.2 Test (test-plan #E7, automated, L1 — exemplar: `packages/kb/src/__tests__/kb.test.ts`, mock store): roots `[{relPrefix:"packages"}]`, expect `packages/foo/AGENTS.md`, indexed `foo/AGENTS.md` → rank 1 (spec scenario verbatim). Triple: repo-relative expect + configured root (input) → score (trigger) → first-hit rank 1 (observable) — VERIFIED by test E7 (eval-roots.test.ts, spec scenario verbatim).
- [x] 3.3 Test (test-plan #E8, automated, L1 — same exemplar): separator boundary — roots `[{relPrefix:"packages"}]`, expect `packages-x/foo.md` is NOT stripped (separator check); raw candidate still scored, item not unreachable. Triple: boundary expect (input) → strip attempt (trigger) → no strip across missing separator; item scored (observable) — VERIFIED by test E8 (separator boundary, rank-geometry no-strip observable).
- [x] 3.4 Test (test-plan #E9, automated, L1 — same exemplar): longest prefix — roots `[{relPrefix:"packages"},{relPrefix:"packages/client"}]`, expect `packages/client/src/a.md` strips against the deeper root → candidate `src/a.md`, not `client/src/a.md`. Triple: nested root set + expect under deeper root (input) → strip (trigger) → longest-prefix candidate generated (observable) — VERIFIED by test E9 (longest prefix, MRR 0.5 rank-2 observable).
- [x] 3.5 Test (test-plan #E10, automated, L1 — same exemplar): decision table `faq.md`→scored, `changes/…`→scored (top-level of openspec root), `tests/…`/`Documents/…`→unreachable; empty-relPrefix root → only rule (b) classifies; unreachable excluded from all 8 metrics, `n + unreachable === total`, no search call for them. Triple: five-shape fixture (input) → classify+score (trigger) → per-class outcome + metric exclusions + sum invariant (observable) — VERIFIED by test E10 (decision table + metric exclusions + call-count invariant + verbose list).
- [x] 3.6 Test (test-plan #E11, automated, L1 — same exemplar): real `golden.markdown-intent.json` (108 items) vs the old `path.includes(expect)` matcher over reachable items → identical first-hit ranks (no-shrink invariant). Triple: real fixture + mock store (input) → new vs old matcher (trigger) → identical ranks for all 104 reachable items (observable) — VERIFIED by test E11 (real 108-item fixture vs old matcher replay: identical P@1/P@5/R/MRR/nDCG; n+unreachable=108).

## 4. Vacuous-run guard (design D5, spec R4)

- [x] 4.1 Implement in `cli.ts` `eval`: guard on empty scored set (`n===0` after unreachable exclusion) and zero recall (`Recall@K===0, n>0`); stderr diagnostic naming fixture shape + root normalization, metrics still on stdout, exit non-zero; `--allow-zero` escape hatch; document both in `HELP` — VERIFIED: cli.ts guard (n===0 incl. empty fixture; Recall@K===0 with n>0), stderr diag + stdout metrics + exit 1, --allow-zero exit 0, HELP documents both.
- [x] 4.2 Test (test-plan #X1, automated, L1 — exemplar: `packages/kb/src/__tests__/kb.test.ts` spawning the CLI handler): `items:[]` fixture and all-unreachable fixture → stderr diagnostic + stdout metrics + non-zero exit; `--allow-zero` → exit 0 with metrics. Triple: empty/all-unreachable fixture (input) → eval run (trigger) → diagnostic+exit≠0; `--allow-zero` → exit 0 (observable) — VERIFIED by X1 (eval-guard.test.ts, subprocess: empty + all-unreachable fixtures).
- [x] 4.3 Test (test-plan #X2, automated, L1 — same exemplar): zero-hit store, n>0 fixture → non-zero exit; `--allow-zero` → exit 0 with `Recall@K: 0` printed. Triple: degenerate store (input) → eval run (trigger) → exit≠0; escape hatch → exit 0 (observable) — VERIFIED by X2 (eval-guard.test.ts, zero-recall degenerate store).

## 5. Build/engine parity (design D1, spec R5)

- [x] 5.1 Implement shared dependency-free `scripts/lib/kb-engine-fingerprint.mjs`: recursive `src/**/*.ts` minus `__tests__`, LF-normalized sorted-path SHA-256 (`srcHash`) + tsconfig-chain hash + `distHash`; bump `packages/kb` `engines` to `>=22.13.0` (`node:sqlite` unflagged floor)
- [x] 5.2 Test (test-plan #E12, automated, L1 — exemplar: `packages/kb/src/__tests__/kb.test.ts`): same tree twice → identical hash; CRLF vs LF → identical (normalization); reordered files → identical; edited src or tsconfig → different hash. Triple: determinism/normalization inputs (input) → lib recompute (trigger) → hash equality/inequality per case (observable)
- [x] 5.3 Make `npm run build` write `packages/kb/engine-fingerprint.json` (tsc + fingerprint), add it to package `files`
- [x] 5.4 Implement committed plain-JS `bin/kb.mjs` (branch table: match → import; dev mismatch/missing fingerprint → rebuild via nearest `node_modules/.bin/tsc` + refresh + import; installed mismatch → stderr warning + import; dist missing + installed → hard error), repoint `bin.kb` to `./bin/kb.mjs`, add `bin` to `files`
- [x] 5.5 Test (test-plan #E13, automated, L1 — same exemplar, sandbox package dir): (a) hashes match → imports dist, exit 0, silent; (b) srcHash mismatch + tsconfig resolvable → rebuilds (dist mtime advances), exit 0; (c) mismatch without tsconfig → stderr warning, imports stale dist, exit 0; (d) dist missing without tsconfig → non-zero exit naming the divergence. Triple: four sandbox states (input) → spawn `bin/kb.mjs` (trigger) → per-branch import/warn/rebuild/exit behaviour (observable)
- [x] 5.6 Implement `scripts/check-kb-dist-fresh.mjs` (recompute srcHash+tsconfigHash vs committed fingerprint; mismatch → non-zero + "the `kb` bin and the extension would run different engines — rebuild and commit the fingerprint") and wire into `.github/workflows/ci.yml` (exemplar pattern: the `node scripts/verify-published-imports.mjs` step)
- [x] 5.7 Test (test-plan #C1, automated, ci — exemplar: the `scripts/*.mjs` step pattern in ci.yml): mutated src after committed build → check script non-zero with the mandated message; clean tree → exit 0. Triple: stale-fingerprint checkout (input) → run check script (trigger) → non-zero + message; clean → 0 (observable)
- [x] 5.8 Test (test-plan #C2, automated, ci — exemplar: `scripts/verify-published-imports.mjs` packaging assertions): `npm pack --dry-run` of `packages/kb` contains `bin/kb.mjs`, `engine-fingerprint.json`, `dist/cli.js`, `src/`, no `__tests__`. Triple: package tarball listing (input) → parse (trigger) → required files present, tests excluded (observable)
- [x] 5.9 Rebuild `packages/kb/dist`, then verify (doubt-driven-review) `dist/sqlite-store.js`/`dist/cli.js` actually contain `laneQuota` and the fingerprint matches — do not accept a green build as proof

## 6. Re-baseline and document

- [x] 6.1 Re-run `kb eval` on `golden.source-intent.json` + `golden.markdown-intent.json` against `.pi/dashboard/knowledge_base.json` (roots docs/openspec/packages/.pi); record metrics incl. `unreachable` in this file's checked-off task notes — VERIFIED: fresh dist bin vs `.pi/dashboard/knowledge_base.json` (roots docs/openspec/packages/.pi), worktree index. source-intent: n=97, unreachable=7, P@1 0.041, P@5 0.402, Recall@10 0.495, MRR 0.200, nDCG@10 0.271, avgLatency 118ms. markdown-intent: n=104, unreachable=4, P@1 0.173, P@5 0.519, Recall@10 0.635, MRR 0.303, nDCG@10 0.382, avgLatency 140ms. Census matches design D4 (97+7=104, 104+4=108).
- [x] 6.2 Run the `doc-example` fixtures with `--allow-zero` and confirm the D5 guard fires (guard validation, not a baseline); confirm `laneQuota` 0-vs-configured now produces differing output (no longer a dead parameter) — VERIFIED: golden.doc-example.json without --allow-zero → exit 1 + stderr `VACUOUS RUN: all 20 of 20 golden items are unreachable ... check root normalization` (metrics still on stdout); with --allow-zero → exit 0. laneQuota 0 vs 0.5 now produces DIFFERING `kb search` output (parameter no longer dead).
- [x] 6.3 Run `npm test` for `packages/kb` + `packages/kb-extension`; confirm no ranking-behaviour regression — VERIFIED: packages/kb vitest 226 passed + 1 skipped (doc-example corpus absent in worktrees), packages/kb-extension 29 passed; kb tsc clean.
- [x] 6.4 Update `packages/kb/README.md` eval section (accepted fixture shapes, `unreachable`, `--allow-zero`, `--verbose`); update `packages/kb/AGENTS.md` rows for `search-opts.ts`, `bin/kb.mjs`, `eval.ts`, `cli.ts`, `engine-fingerprint.json` with `See change: fix-kb-eval-measurement-integrity` — VERIFIED: README eval section (shapes/unreachable/--allow-zero/--verbose + engine freshness note); AGENTS.md rows updated in packages/kb (bin shim, fingerprint lib+json, run-fixtures, measure-render, verify), packages/kb/src (search-opts.ts, eval.ts, cli.ts, index.ts), scripts (check-kb-dist-fresh.mjs, lib re-export, both new tests).
