# Tasks — investigate-bridge-cwd-asymmetric-immunity

Investigation only — no shipped behaviour change. Evidence and verdicts
accumulate in `findings.md` in this change directory. Method: `design.md`
D1–D7; discipline: `systematic-debugging`.

## 1. Forensics on surviving artifacts (no replay — D1)

- [x] 1.1 Extract the matrix window (~2026-08-28/29) from `~/.pi/dashboard/server.log`: all seven arms' `session registered` lines, any duplicate registrations per session id, `[bridge-transport]` endpoint-resolution lines per session (the channel pre-dates the matrix; an arm with no rows is vintage evidence, not missing data — design Constraints), `servers_updated`/discovery events, restarts. Verify: `findings.md` gains a per-arm timeline table covering all seven arms (or records which arms are absent from the log). (test-plan #E2: manual-only)
- [x] 1.2 Inventory surviving keeper logs (`keeper-<sessionId>.log`) and any capture the parent investigation enabled for the matrix arms. Verify: inventory table in `findings.md` names which arms have extension-side output and which do not.
- [x] 1.3 Identify the exact commit the live server ran during the matrix (server.log startup banner, git reflog/branch history around 2026-08-28) and whether that commit contains the spawn pin (`buildSpawnEnv` `PI_DASHBOARD_URL`/`PI_DASHBOARD_SOCKET`) and the D3 pinned-resolution ladder; record every commit/reflog move inside the matrix window (working-tree drift = per-arm code-vintage evidence, design D2). Verify: commit hash (or nearest + caveat) + yes/no for each mechanism + window drift list recorded in `findings.md`.
- [x] 1.4 Establish per-arm extension load count (H-dual-load static half): from the matrix window, count registrations per session id **paired with token/pid** (raw line counts cannot distinguish dual-load from a legitimate re-register after `/reload`/reconnect); from git/config history (`~/.pi/agent/settings.json` backups, repo `.pi/settings.json` + root `package.json` `pi` field at the matrix date), derive which arms could double-load the bridge. Verify: per-arm load-source/load-count table with disambiguation evidence in `findings.md`.
- [x] 1.5 Verify the constant-route premise and establish per-arm vintage inputs (H-resolution-path forensic half): confirm from the log that all seven arms were dashboard-spawned (parent: "varying only cwd"), whether spawn carried explicit extension args (`SessionOptions.extensions`), and each arm's session start time for vintage correlation with the 1.3 drift list. Verify: per-arm route/env/start-time table in `findings.md`; any route anomaly explicitly flagged.

## 2. Hypothesis ranking (D5, D6)

- [x] 2.1 Score H-dual-load, H-resolution-path, H-config, H-timing against the group-1 evidence; kill what the evidence kills; for each verdict cite the specific finding (log line, commit, config state); a composite factor set counts as one candidate (design D6). Verify: `findings.md` has an alive/dead verdict with citation for all four hypotheses. (test-plan #E3: manual-only)
- [x] 2.2 Record the go/no-go: if a surviving hypothesis already predicts all seven original outcomes, mark groups 3–4 scope (flip-only vs full replay); if forensics is inconclusive, proceed to full replay. Verify: explicit go/no-go decision with rationale in `findings.md`.

## 3. Isolated pre-fix replay (D2, D3, D4 — only per the 2.2 decision)

- [x] 3.1 Create a worktree pinned to the 1.3 commit and install/build it; confirm the migration guard is absent there (the parent's `ConnectionManager` re-target test fails or does not exist at that commit). If 2.2 left vintage alive, pin a second worktree at the other end of the 1.3 drift window (design D2 vintage dimension). Verify: worktree path(s) + guard-absence check recorded. (test-plan #E1: manual-only)
- [x] 3.2 Stage the poisoned-discovery environment per design D4: temp HOME **seeded** with a settings surface replicating the matrix-time global bridge load (load-surface parity — every arm must be able to register), non-8000 ports, multicast contained — docker harness with a local uncommitted compose override re-enabling mDNS (the test profile sets `PI_DASHBOARD_NO_MDNS=1`), or a second machine per parent 6.1; bare same-host replay is excluded. Verify containment both directions — advertisement invisible from the host (60 s pre-flight browse before arms start) AND a known-migrating canary arm (wedge-repro cwd) migrates within 60 s inside the isolation before any other arm is scored — plus zero test arms visible on the live dashboard (`/api/sessions` unchanged) — abort criteria for every subsequent replay task. (test-plan #X2 #X3: manual-only)
- [x] 3.3 Re-run the seven-arm cwd matrix against the isolated pre-fix server with `keeperLog.capturePiOutput=true` (plus temporary endpoint-resolution stdout probes if needed; `[bridge-transport]` lines and `/dashboard where` are the primary views). Outcome states: migrated / kept / **never-registered** (a never-registered arm is a staging defect, not evidence — fix parity and re-run). The dashboard-repo arm's cwd is the pinned worktree itself, never the live repo (design D4 code parity). If multiple vintages are staged (incl. the pre/post-gateway boundary probe when 2.2 mandates it), run the discriminating arms at each — sequential phases, load surface re-pointed between phases, order recorded. Verify: per-arm(×vintage) outcome table + captured logs for every arm; deviations from the original matrix explicitly noted. (test-plan #X1 #X4: manual-only)
- [x] 3.4 Diff the immune arm's endpoint-resolution log against a migrated arm's; name the first decision point where they diverge. Verify: divergence point recorded in `findings.md` with quoted log lines from both arms.

## 4. Confirmation by flip (D6)

- [x] 4.1 Flip A — remove the candidate factor from the dashboard-repo arm (e.g. drop the repo-local extension entry in the pinned worktree, or strip the pin env — never editing the live repo) and re-run that arm. Verify: predicted outcome (migration) observed and recorded, or the hypothesis is demoted with the observed outcome. (test-plan #E4: manual-only, spans 4.1–4.3)
- [x] 4.2 Flip B — add the candidate factor to a previously migrating arm and re-run it (for H-resolution-path the flip is the same arm at the other pinned vintage, both directions). Verify: predicted outcome (immunity) observed and recorded, or the hypothesis is demoted.
- [x] 4.3 If the surviving hypothesis is timing-sensitive (H-timing or flip results are inconsistent), repeat the flipped arms ≥3× and report per-arm outcome counts. Verify: run counts + outcomes tabulated; conclusion states a probability, not a single run.

## 5. Close-out

- [x] 5.1 Write the final `findings.md` narrative: confirmed mechanism with the seven-arm prediction check (or the bounded negative result per D7), and what — if anything — remains unexplained. Verify: `findings.md` answers the parent's Open Question in one paragraph up top, with the D7 middle-case verdict direction honoured. (test-plan #X5: manual-only)
- [x] 5.2 If a still-live defect surfaced (pin bypass, double load, config hole), file it as its own change directory with a `## Discipline Skills` section; otherwise record "no live defect" with justification. Verify: new change exists or the explicit none-needed entry is in `findings.md`.
- [x] 5.3 If a durable architecture fact emerged (e.g. extension load-source matrix, spawn-pin behaviour), delegate a `docs/` update to DocScribe per the Documentation Update Protocol; otherwise record none-needed. Verify: docs updated (or none-needed noted) and any directory `AGENTS.md` rows touched by new files added.
- [x] 5.4 Tear down: remove temporary probes, the pre-fix worktree(s), staged dashboards/temp HOMEs, and any test cwds; kill stray processes. Verify: `git status` clean, no `wedge`/`w-a`-style arms on the live dashboard, no leftover listeners on the recorded isolation ports. (test-plan #E6: manual-only)
- [x] 5.5 Scope audit before ship: review the final branch diff. Verify: only `openspec/changes/investigate-bridge-cwd-asymmetric-immunity/**`, filed follow-up change dirs, and 5.3-delegated `docs/`+`AGENTS.md` rows are touched; zero hunks in production code (`packages/`, `docker/` runtime files). (test-plan #E5: manual-only)
