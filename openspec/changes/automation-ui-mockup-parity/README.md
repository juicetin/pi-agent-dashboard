# automation-ui-mockup-parity

Bring the shipped Automation board + editor up to the redesign mockups (session-card visual language, editor polish) and add the two mockup features left unbuilt: Stop a running run, and a findings count in the runs table.
