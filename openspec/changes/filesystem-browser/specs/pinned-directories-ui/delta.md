## Modified Requirements

### Pin directory dialog gains filesystem browser

- Pin directory dialog (`PinDirectoryDialog.tsx`) SHALL include a "Browse" button that opens the `FilesystemBrowser` component for visual directory selection
- The existing text input SHALL remain as an alternative entry method
- On selection from the browser, the selected path SHALL populate the text input field
