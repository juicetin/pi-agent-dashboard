# migrate-flows-jsx-to-slots

Migrate FlowActivityBadge and SessionFlowActions to render via plugin slots (session-card-badge / session-card-action-bar) instead of direct JSX in SessionCard.tsx. Unblocks the deferred wiring from wire-plugin-registry-into-shell.
