# Tasks

**Execution protocol:**

1. **No auto-commits — I write a commit script, you run it.** When a phase is complete and ready to commit, I will:
   - Write a shell script at `B:/tmp/pi-commit-<N>-<slug>.sh` (on this system, the `Write` tool resolves paths starting with `/tmp/` to `B:/tmp/` because the agent's cwd is on the B: drive — so I use the explicit drive-letter form to avoid confusion).
   - The script makes **multiple small commits** when a phase introduces more than one logically-distinct artifact. Each commit gets a **one-liner** conventional-commits message — no commit-body paragraphs, no bullet lists. Format:
     ```
     <type>(<scope>): <one-short-sentence>
     ```
     where `<type>` is one of `feat`, `fix`, `refactor`, `test`, `chore`, `docs`.
   - Prefer splitting a phase into 2–4 one-line-message commits over writing one long-body commit. E.g., Phase 0 produces three commits (one for each planning doc + one for the proposal), not one mega-commit.
   - Use paths rooted at the repo (`cd B:/Dev/BB/pi-agent-dashboard` at the top, then relative paths for `git add`).
   - Use `set -e` so the script aborts if `git add` or `git commit` fails.
   - The script SHALL be **idempotent**: if the script is being re-run (detected by matching the last commit's subject against expected), it first calls `git reset --mixed HEAD~N` to un-stage the old commits (keeps working tree on disk) before applying the new version. This lets you iterate on the script without manual revert.
   - Print the invocation on a single line in the form (git-bash-friendly):
     ```
     bash B:/tmp/pi-commit-<N>-<slug>.sh
     ```
   - Wait for your "continue" signal before starting the next phase.

2. **No auto-pushes.** Nothing in this plan pushes to `origin`. You open the PR from `windows-integration` → `develop` yourself when ready.

3. **No full test suite.** `npm test` is unreliable in this repo. I will run *targeted* vitest invocations against specific test files only. When verifying a phase, I will run exactly the test files that phase touches.
   ```
   Example targeted test:
   npx vitest run packages/shared/src/__tests__/platform-paths.test.ts
   ```

4. **Stashes are already handled.** The ~640 LOC of uncommitted preload-fastify work (which you referred to as "stashed changes") is discarded in Phase 0.1a via `git checkout HEAD -- <files>` and `rm`.

5. **Commit-script cleanup.** After you confirm a commit succeeded, I delete its script (`rm B:/tmp/pi-commit-<N>-<slug>.sh`) before proposing the next one. This keeps the temp dir clean and prevents accidental reuse. If you want to keep them for audit, tell me at the start.

**Commit-script template I use for every COMMIT step (idempotent, multi-commit, one-line messages):**
```bash
#!/usr/bin/env bash
# Auto-generated by prep-for-develop-merge tasks.md phase <N>
# Idempotent: detects and undoes prior run of this phase if present,
# then applies the intended small commits.
set -e
cd B:/Dev/BB/pi-agent-dashboard

# Detect prior run by matching the LAST commit this phase makes.
LAST_SUBJECT="<type>(<scope>): <one-short-sentence> (last commit of the phase)"
PHASE_COMMIT_COUNT=<number of commits this phase makes>

if git log -1 --format=%s | grep -qxF "$LAST_SUBJECT"; then
  echo "↶ Prior run of phase <N> detected. Un-staging $PHASE_COMMIT_COUNT commits..."
  git reset --mixed HEAD~$PHASE_COMMIT_COUNT
fi

echo "→ Commit 1/N: <slug>"
git add <file1>
git commit -m "<type>(<scope>): <one-short-sentence>"

echo "→ Commit 2/N: <slug>"
git add <file2>
git commit -m "<type>(<scope>): <one-short-sentence>"

echo "✓ Phase <N> complete. Commits:"
git log -${PHASE_COMMIT_COUNT} --format="  %h %s"
```

**Commit message style — one-line only. NO body paragraphs, NO bullet lists in messages. Examples:**
- `fix(windows): restore detach:false for pi-session spawn`
- `chore(server): require Node >=22.18.0 via engines field`
- `refactor(platform): merge exec + subprocess-adapter + detached-spawn + spawn-mechanism into spawn.ts`
- `docs: add branch-comparison analysis`
- `chore(openspec): archive consolidate-platform-handlers`

Implementation order is deliberately sequenced so each phase is independently revertable and each phase's targeted tests pass before the next begins. Do not reorder without rereading `design.md` rationale.

---

## Phase 0 — Clean slate: discard the preload-fastify workaround

Prerequisite for everything else. Removing the dead code first avoids any risk of it re-entering the tree during later phases.

- [ ] 0.0 Sanity check: `git status --short` shows only the preload-fastify-related modifications and untracked files. If there's anything unexpected (stashes, untracked work in progress on other features), STOP and surface to user before proceeding.
- [ ] 0.1 Revert uncommitted working-tree preload-fastify + dynamic-imports + broken-logPath-retrofit edits
  - `git checkout HEAD -- packages/server/src/cli.ts packages/server/src/restart-helper.ts packages/server/src/routes/system-routes.ts packages/server/src/__tests__/restart-helper.test.ts packages/extension/src/server-launcher.ts packages/extension/src/__tests__/server-launcher.test.ts packages/electron/src/lib/server-lifecycle.ts packages/shared/src/platform/detached-spawn.ts packages/shared/src/__tests__/detached-spawn.test.ts packages/shared/src/platform/index.ts packages/server/src/directory-service.ts packages/server/src/process-manager.ts openspec/specs/bridge-extension/spec.md openspec/specs/dashboard-server/spec.md README.md package.json packages/server/package.json`
  - Note: `directory-service.ts` is reverted because its uncommitted edit (static import of `loadSessionEntries`) is the work product of the `remove-cargo-cult-dynamic-imports` folder being deleted in step 0.3. Both `origin/develop` and committed `origin/windows-integration` HEAD keep the dynamic `await import(...)` — reverting aligns with both branches. The maintainer flagged that in a prior change static imports here broke other things; the dynamic import is the safe default.
  - Note: `process-manager.ts` is reverted to remove the failed `logPath` retrofit (adds `logPath,` to the `spawnDetached` call in `spawnHeadlessDetached`). With `stdinMode: "pipe"`, the cmd.exe redirect branch cannot set `CREATE_NO_WINDOW` (libuv `UV_INHERIT_FD` breaks the precondition), so the retrofit is dead code. Phase 2 re-introduces the real fix (`detach: false`) cleanly.
- [ ] 0.2 Delete preload-related source and test files
  - `rm -f packages/server/preload-fastify.cjs packages/server/src/__tests__/cli-daemon-argv.test.ts packages/shared/src/platform/preload-fastify.ts packages/shared/src/platform/node-version-check.ts packages/shared/src/__tests__/platform-preload-fastify.test.ts packages/shared/src/__tests__/platform-node-version-check.test.ts`
- [ ] 0.3 Delete four misplaced openspec change folders
  - `rm -rf openspec/changes/archive/2026-04-20-fix-bridge-autostart-diagnostics openspec/changes/archive/2026-04-20-lazy-import-fastify-FAILED openspec/changes/archive/2026-04-20-preload-fastify-cjs openspec/changes/archive/2026-04-20-remove-cargo-cult-dynamic-imports`
- [ ] 0.4 Delete ad-hoc commit-helper scripts
  - `rm -f scripts/commit-route-kill-paths.sh scripts/split-commits.sh`
- [ ] 0.5 Verify `git status --short` now shows only the proposal/design/tasks/specs additions from this change.
- [ ] 0.6 **Write commit script** `B:/tmp/pi-commit-0-drop-preload.sh` that makes three one-line commits:
  1. `docs: add branch-comparison analysis for windows-integration vs develop`
  2. `docs: add merge plan for windows-integration into develop`
  3. `chore(openspec): add prep-for-develop-merge change proposal`
  Script is idempotent — detects and reverts any prior run via `git reset --mixed HEAD~3`, then re-applies.
  Print the one-liner: `bash B:/tmp/pi-commit-0-drop-preload.sh`

## Phase 1 — Add engines.node constraint

No runtime guard. Just the `engines` declaration in the server's `package.json`.

- [ ] 1.1 Add `"engines": { "node": ">=22.18.0" }` to `packages/server/package.json` (top-level object, after `"version"`).
- [ ] 1.2 Manually verify the JSON parses: `node -e "JSON.parse(require('fs').readFileSync('packages/server/package.json','utf8'))" && echo OK`
- [ ] 1.3 Manually verify the constraint is live: `cd packages/server && npm install --dry-run 2>&1 | grep -i engine` (on Node 22.22.2 no warning expected; on Node 22.17.1 you'd see `EBADENGINE`)
- [ ] 1.4 **Write commit script** `B:/tmp/pi-commit-1-engines.sh` (stages just `packages/server/package.json`) with message:
  ```
  chore(server): require Node >=22.18.0 via engines field
  ```
  Print the one-liner: `bash B:/tmp/pi-commit-1-engines.sh`

## Phase 2 — Fix the spawnDetached regressions

Two bugs in the same file. Ship as one commit so they're co-revertable.

- [ ] 2.1 Add `detach?: boolean` field to `SpawnDetachedOptions` interface in `packages/shared/src/platform/detached-spawn.ts`, with JSDoc explaining the Windows Job Object semantics
- [ ] 2.2 In `spawnDetached()`, change `detached: true,` to `detached: opts.detach ?? true,`
- [ ] 2.3 Change the `useWindowsRedirect` gate from `platform === "win32" && !!opts.logPath` to `platform === "win32" && !!opts.logPath && stdinMode === "ignore"`
- [ ] 2.4 Update `packages/shared/src/__tests__/detached-spawn.test.ts`:
  - Add cases for `detach: false` / `detach: true` / `detach: undefined` (default)
  - Add cases confirming `useWindowsRedirect === false` when `stdinMode === "pipe"` regardless of logPath
  - Add cases confirming `useWindowsRedirect === true` only when all three conditions hold
- [ ] 2.5 Targeted test: `npx vitest run packages/shared/src/__tests__/detached-spawn.test.ts` — must pass
- [ ] 2.6 In `packages/server/src/process-manager.ts` `spawnHeadlessDetached`, update the `spawnDetached()` call: add `detach: false`, ensure `stdinMode: "pipe"` remains, ensure `logPath` is NOT passed
- [ ] 2.7 Update comment in `spawnHeadlessDetached` explaining WHY `detach: false` is correct (references d331850, libuv source, pi RPC stdin requirement, "pi dies with dashboard" trade-off is unchanged)
- [ ] 2.8 Add or update `packages/server/src/__tests__/process-manager.test.ts` to assert the call-site options: `detach: false`, `stdinMode: "pipe"`, `logPath: undefined`, `logFd: <number>`
- [ ] 2.9 Targeted test: `npx vitest run packages/server/src/__tests__/process-manager.test.ts` — must pass
- [ ] 2.10 Manually verify on Windows: spawn 3 pi sessions from the dashboard UI; screen-record at 60fps; confirm no console flash in any of them
- [ ] 2.11 **Write commit script** `/tmp/pi-commit-2-detach-false.sh` (stages `detached-spawn.ts`, `process-manager.ts`, and their test files) with message:
  ```
  fix(windows): restore detach:false for pi-session spawn to eliminate console flash
  ```
  Print the one-liner: `bash /tmp/pi-commit-2-detach-false.sh`

## Phase 3 — Consolidate platform/ from 18 files to 5

Each file merge is its own commit so the diff stays reviewable. Four sub-phases.

### 3a — Merge into spawn.ts

- [ ] 3a.1 Create new `packages/shared/src/platform/spawn.ts` with section-divider comments for `exec`, `subprocess-adapter`, `detached-spawn`, `spawn-mechanism`
- [ ] 3a.2 Paste contents of `exec.ts`, `subprocess-adapter.ts`, `detached-spawn.ts`, `spawn-mechanism.ts` in that order, each under its own `// ─── <section> ───` banner
- [ ] 3a.3 Resolve any duplicate imports or internal helper name collisions
- [ ] 3a.4 Delete `exec.ts`, `subprocess-adapter.ts`, `detached-spawn.ts`, `spawn-mechanism.ts`
- [ ] 3a.5 Merge test files: `exec.test.ts` + `detached-spawn.test.ts` + `spawn-mechanism.test.ts` → `spawn.test.ts` (subprocess-adapter tests are minimal and fold in too)
- [ ] 3a.6 Update every `import ... from "<platform>/exec.js"` etc. to `"<platform>/spawn.js"` across `packages/*/src/` — use grep + sed carefully
- [ ] 3a.7 Update `platform/index.ts` to re-export from `./spawn.js`
- [ ] 3a.8 Update `no-direct-child-process.test.ts` allowlist: replace four entries with `["packages/shared/src/platform/spawn.ts"]` (the `tools.ts` entry comes in phase 3c)
- [ ] 3a.9 Targeted tests: `npx vitest run packages/shared/src/__tests__/spawn.test.ts packages/shared/src/__tests__/no-direct-child-process.test.ts`
- [ ] 3a.10 **Write commit script** `/tmp/pi-commit-3a-spawn-merge.sh` (stages new `spawn.ts`, deletions of 4 old files, merged test, all import-path updates, `no-direct-child-process.test.ts` allowlist update, `platform/index.ts`) with message:
  ```
  refactor(platform): merge exec + subprocess-adapter + detached-spawn + spawn-mechanism into spawn.ts
  ```
  Print the one-liner: `bash /tmp/pi-commit-3a-spawn-merge.sh`

### 3b — Merge into process.ts

- [ ] 3b.1 Open existing `platform/process.ts` and add section-divider banners for the current content plus upcoming `process-scan` and `process-identify` sections
- [ ] 3b.2 Paste contents of `process-scan.ts` and `process-identify.ts` under their respective sections
- [ ] 3b.3 Delete `process-scan.ts` and `process-identify.ts`
- [ ] 3b.4 Merge test files into a single `process.test.ts`
- [ ] 3b.5 Update imports across `packages/*/src/` (consumers: `extension/process-scanner.ts`, `server/editor-registry.ts`, `server/browser-handlers/session-action-handler.ts`)
- [ ] 3b.6 Update `platform/index.ts` re-exports
- [ ] 3b.7 Verify `no-direct-process-kill.test.ts` allowlist remains `["packages/shared/src/platform/process.ts"]` (should be unchanged)
- [ ] 3b.8 Targeted tests: `npx vitest run packages/shared/src/__tests__/process.test.ts packages/shared/src/__tests__/no-direct-process-kill.test.ts`
- [ ] 3b.9 **Write commit script** `/tmp/pi-commit-3b-process-merge.sh` with message:
  ```
  refactor(platform): merge process-scan + process-identify into process.ts
  ```
  Print the one-liner: `bash /tmp/pi-commit-3b-process-merge.sh`

### 3c — Merge into tools.ts

- [ ] 3c.1 Create new `packages/shared/src/platform/tools.ts` with section-divider comments for `binary-lookup`, `runner`, `git`, `npm`, `openspec`
- [ ] 3c.2 Paste contents in dependency order: binary-lookup first (ToolResolver is foundational), runner second (uses ToolResolver), then git/npm/openspec (use runner)
- [ ] 3c.3 Delete `binary-lookup.ts`, `runner.ts`, `git.ts`, `npm.ts`, `openspec.ts`
- [ ] 3c.4 Merge test files into `tools.test.ts`
- [ ] 3c.5 Update imports across `packages/*/src/`
- [ ] 3c.6 Update `platform/index.ts`
- [ ] 3c.7 Update `no-direct-child-process.test.ts` allowlist: add `"packages/shared/src/platform/tools.ts"` (final allowlist is 2 entries: spawn.ts + tools.ts)
- [ ] 3c.8 Targeted tests: `npx vitest run packages/shared/src/__tests__/tools.test.ts packages/shared/src/__tests__/no-direct-child-process.test.ts`
- [ ] 3c.9 **Write commit script** `/tmp/pi-commit-3c-tools-merge.sh` with message:
  ```
  refactor(platform): merge binary-lookup + runner + git + npm + openspec into tools.ts
  ```
  Print the one-liner: `bash /tmp/pi-commit-3c-tools-merge.sh`

### 3d — Merge into system.ts

- [ ] 3d.1 Create `packages/shared/src/platform/system.ts` with section-divider comments for `commands` and `shell`
- [ ] 3d.2 Paste contents of `commands.ts` and `shell.ts`
- [ ] 3d.3 Delete `commands.ts`, `shell.ts`
- [ ] 3d.4 Merge test files
- [ ] 3d.5 Update imports across `packages/*/src/` (consumers: `electron/main.ts`, `server/routes/provider-auth-routes.ts`, `server/terminal-manager.ts`)
- [ ] 3d.6 Update `platform/index.ts`
- [ ] 3d.7 Targeted test: `npx vitest run packages/shared/src/__tests__/system.test.ts`
- [ ] 3d.8 **Write commit script** `/tmp/pi-commit-3d-system-merge.sh` with message:
  ```
  refactor(platform): merge commands + shell into system.ts
  ```
  Print the one-liner: `bash /tmp/pi-commit-3d-system-merge.sh`

### 3e — Post-consolidation cleanup

- [ ] 3e.1 Verify final file list in `packages/shared/src/platform/` is exactly `{spawn.ts, process.ts, tools.ts, paths.ts, system.ts, index.ts}` plus `__tests__/`
- [ ] 3e.2 Update `AGENTS.md` "Key Files" table: collapse the 18 platform/ rows into 5
- [ ] 3e.3 Update `docs/architecture.md` to reference the new file names
- [ ] 3e.4 Targeted spot-check: `npx vitest run packages/shared/src/__tests__/` (runs just the shared tests dir, avoids the flaky suites)
- [ ] 3e.5 **Write commit script** `/tmp/pi-commit-3e-docs.sh` (stages AGENTS.md and docs/architecture.md) with message:
  ```
  docs: update AGENTS and architecture for consolidated platform/ structure
  ```
  Print the one-liner: `bash /tmp/pi-commit-3e-docs.sh`

## Phase 4 — Absorb develop's path-picker UX with Windows correctness

This phase implements what the develop-merge would do, pre-emptively. It makes the eventual merge cleaner.

- [ ] 4.1 Read `origin/develop`'s `packages/server/src/browse.ts` (from worktree at `B:/Dev/BB/pi-agent-dashboard-develop/packages/server/src/browse.ts`) to understand `rankTier`, `createDirectory`, and `validateMkdirName` as implemented
- [ ] 4.2 Port those additions into `windows-integration`'s `browse.ts` WHILE keeping the existing `isFilesystemRoot(resolved)` root detection. Do NOT let develop's `resolved === "/"` regression slip in.
- [ ] 4.3 Ensure `listDirectories()` returns an explicit `platform: process.platform` field in the BrowseResult
- [ ] 4.4 Add the `POST /api/browse/mkdir` endpoint in `packages/server/src/routes/file-routes.ts` (port from develop), with the same error-to-HTTP mapping (400 invalid name, 404 parent not found, 409 exists, 500 unknown)
- [ ] 4.5 Add `MkdirRequest` and `MkdirResult` types to `packages/shared/src/rest-api.ts`. Keep `BrowseResult.platform?: NodeJS.Platform` declaration.
- [ ] 4.6 Port develop's `packages/client/src/lib/browse-api.ts` `BrowseOptions` (q + signal) and `createDirectory()` helper
- [ ] 4.7 Port develop's `PathPicker.tsx` entirely — debounced fetch, AbortController, tiered result rendering, Enter state machine, "+ New folder" button, inline "Create <name> here" row
- [ ] 4.8 In the ported `PathPicker.tsx`, replace the hardcoded local `parseInput()` (develop's Unix-only version) with a call to shared `parsePathInput(value, platform)`:
  - `import { parsePathInput } from "@blackbelt-technology/pi-dashboard-shared/platform";`
  - `import { inferPlatform } from "../lib/session-grouping.js";`
  - `const platform = result?.platform ?? inferPlatform([result?.current ?? ""]);`
  - `const { parent, partial } = parsePathInput(value, platform);`
- [ ] 4.9 Add Windows-platform tests to `packages/client/src/components/__tests__/PathPicker.test.tsx`:
  - Bare drive letter `B:` fetches `B:\` listing
  - Drive-relative `B:Dev` fetches `B:\` with partial `Dev`
  - UNC root `\\server\share\` has no `..` entry
- [ ] 4.10 Add Windows root-detection tests to `packages/server/src/__tests__/browse-endpoint.test.ts` with injected `platform: "win32"`
- [ ] 4.11 Targeted tests: `npx vitest run packages/server/src/__tests__/browse-endpoint.test.ts packages/client/src/components/__tests__/PathPicker.test.tsx packages/shared/src/__tests__/platform-paths.test.ts`
- [ ] 4.12 **Write commit script** `/tmp/pi-commit-4-path-picker.sh` (stages browse.ts, file-routes.ts, rest-api.ts, browse-api.ts, PathPicker.tsx, all new/modified tests) with message:
  ```
  feat(path-picker): absorb develop's UX while preserving Windows path correctness
  ```
  Print the one-liner: `bash /tmp/pi-commit-4-path-picker.sh`

## Phase 5 — Targeted verification (no full test suite)

Full `npm test` is known-flaky in this repo. Instead, run the specific test files that this change's commits have touched.

- [ ] 5.1 Targeted test sweep:
  ```
  npx vitest run \
    packages/shared/src/__tests__/detached-spawn.test.ts \
    packages/server/src/__tests__/process-manager.test.ts \
    packages/shared/src/__tests__/spawn.test.ts \
    packages/shared/src/__tests__/process.test.ts \
    packages/shared/src/__tests__/tools.test.ts \
    packages/shared/src/__tests__/system.test.ts \
    packages/shared/src/__tests__/no-direct-child-process.test.ts \
    packages/shared/src/__tests__/no-direct-process-kill.test.ts \
    packages/shared/src/__tests__/no-direct-platform-branch.test.ts \
    packages/shared/src/__tests__/platform-paths.test.ts \
    packages/server/src/__tests__/browse-endpoint.test.ts \
    packages/client/src/components/__tests__/PathPicker.test.tsx
  ```
- [ ] 5.2 Type-check the monorepo: `npx tsc --noEmit` at repo root (catches import-path errors from consolidation)
- [ ] 5.3 Manual Windows smoke test:
  - Start dashboard via `pi-dashboard start` (Node 22.22.2)
  - Spawn 3 pi sessions via UI — visually verify no flash
  - Restart via `POST /api/restart` — verify success
  - Stop via `pi-dashboard stop` — verify port 8888 is free (`netstat -ano -p tcp | findstr 8888` should be empty)
  - Open path picker, type `B:`, `B:Dev`, `\\server\share\` — verify all parse correctly
  - Create a new folder via the picker — verify it appears
- [ ] 5.4 Manual Node v22.17 refuse-to-start test: `nvm install 22.17.1 && nvm use 22.17.1 && pi-dashboard start` — verify refuse message + exit code 1. Restore `nvm use 22.22.2` after.
- [ ] 5.5 Update `README.md` to mention `engines.node >= 22.18.0` as prerequisite
- [ ] 5.6 Update `CHANGELOG.md` `Unreleased` section with user-visible changes:
  - Fixed: Windows console flash on pi session spawn
  - Changed: Minimum Node version is now 22.18.0 (was unconstrained)
  - Added: Path picker supports creating new directories
  - Changed: Consolidated `packages/shared/src/platform/` from 18 files to 5 (no public API change)
- [ ] 5.7 **Write commit script** `/tmp/pi-commit-5-changelog.sh` (stages README.md and CHANGELOG.md) with message:
  ```
  docs: CHANGELOG and README updates for prep-for-develop-merge
  ```
  Print the one-liner: `bash /tmp/pi-commit-5-changelog.sh`

## Phase 6 — Merge origin/develop into windows-integration (local only)

This is THE merge. It stays local — no push. The user opens the PR from `windows-integration` → `develop` themselves after testing this merged state locally.

- [ ] 6.1 Fetch latest develop: `git fetch origin develop`
- [ ] 6.2 Confirm clean tree: `git status --short` is empty (all prior phases committed)
- [ ] 6.3 Create a safety tag: `git tag -a pre-develop-merge -m "windows-integration with phases 0-5 applied, about to merge develop"` (local tag only, not pushed)
- [ ] 6.4 Attempt the merge: `git merge origin/develop --no-ff --no-commit`
  - The merge will stop with conflicts in ~7–10 files. This is expected.
- [ ] 6.5 Resolve conflicts file-by-file using MERGE-PLAN.md §3 as the authoritative guide:
  - `packages/server/src/cli.ts` → keep windows-integration version (our Windows fixes stay)
  - `packages/extension/src/server-launcher.ts` → keep windows-integration version
  - `packages/server/src/process-manager.ts` → keep windows-integration version (detach:false stays)
  - `packages/shared/src/resolve-jiti.ts` → keep windows-integration version (file:// URL stays)
  - `packages/server/src/routes/system-routes.ts` → keep windows-integration version (spawnRestart, no sh -c)
  - `packages/electron/src/lib/server-lifecycle.ts` → keep windows-integration version
  - `packages/server/src/editor-manager.ts` → MERGE both: our ToolResolver imports + develop's PID registry
  - `packages/server/src/browse.ts` → ALREADY RECONCILED in phase 4, take "ours" in the merge
  - `packages/server/src/tunnel.ts` → MERGE both: our ToolResolver imports + develop's leak fix
  - `packages/extension/src/ask-user-tool.ts` → MERGE both: our registration + develop's batch method
  - `packages/shared/src/openspec-poller.ts` → keep windows-integration version (Recipe-based)
  - `packages/client/src/components/PathPicker.tsx` → ALREADY RECONCILED in phase 4, take "ours"
  - `packages/shared/src/rest-api.ts` → ALREADY RECONCILED in phase 4, take "ours"
  - `AGENTS.md`, `README.md`, `docs/architecture.md` → MERGE both: append develop's new sections to our post-phase-3e structure
- [ ] 6.6 Handle Vitest 4 config migration from develop commit `a4cced2`: the new root `vitest.config.ts` replaces `vitest.workspace.ts`. Take develop's version; adapt any platform-consolidation test config references to the new structure if needed.
- [ ] 6.7 Handle the rename/rename conflict for `openspec/changes/fix-portable-windows-package-manager/` — develop archived it to `openspec/changes/archive/2026-04-20-fix-portable-windows-package-manager/`; windows-integration archived it to `openspec/changes/archive/2026-04-19-fix-portable-windows-package-manager/`. Prefer develop's dated folder name (consistent with develop's archive naming scheme).
- [ ] 6.8 Stage all resolved files: `git add -A`
- [ ] 6.9 Type-check: `npx tsc --noEmit` — must pass
- [ ] 6.10 Targeted tests (same as phase 5.1):
  ```
  npx vitest run \
    packages/shared/src/__tests__/detached-spawn.test.ts \
    packages/server/src/__tests__/process-manager.test.ts \
    packages/shared/src/__tests__/spawn.test.ts \
    packages/shared/src/__tests__/process.test.ts \
    packages/shared/src/__tests__/tools.test.ts \
    packages/shared/src/__tests__/system.test.ts \
    packages/shared/src/__tests__/no-direct-child-process.test.ts \
    packages/shared/src/__tests__/no-direct-process-kill.test.ts \
    packages/shared/src/__tests__/no-direct-platform-branch.test.ts \
    packages/shared/src/__tests__/platform-paths.test.ts \
    packages/server/src/__tests__/browse-endpoint.test.ts \
    packages/client/src/components/__tests__/PathPicker.test.tsx
  ```
  Plus any tests that are NEW on the develop side that now apply to this tree:
  ```
  npx vitest run \
    packages/shared/src/__tests__/openspec-poller.test.ts \
    packages/server/src/__tests__/tunnel.test.ts
  ```
- [ ] 6.11 Manual smoke test on Windows (same checklist as phase 5.3) to confirm nothing regressed through the merge.
- [ ] 6.12 **Write commit script** `/tmp/pi-commit-6-merge-develop.sh` — this one is slightly different because `git merge` has pre-staged the changes and set a default merge-msg. The script SHALL just run `git commit -m "..."` (no `git add` needed because conflicts were staged in step 6.8):
  ```bash
  #!/usr/bin/env bash
  set -e
  cd B:/Dev/BB/pi-agent-dashboard
  git commit -m "chore: merge origin/develop into windows-integration"
  echo "✓ Phase 6 merge committed: $(git rev-parse --short HEAD)"
  ```
  Print the one-liner: `bash /tmp/pi-commit-6-merge-develop.sh`

- [ ] 6.13 User opens PR `windows-integration` → `develop` themselves via the GitHub UI. No automated push from this plan.

## Phase 7 — Archive superseded in-flight changes

The changes `consolidate-platform-handlers` (53/56 tasks) and `platform-path-normalization` (34/36 tasks) have their remaining work rolled into this change. They should be archived once this change lands.

- [ ] 7.1 Mark all tasks in `openspec/changes/consolidate-platform-handlers/tasks.md` as complete (remaining 3 tasks are subsumed by phase 3 of this change)
- [ ] 7.2 Mark all tasks in `openspec/changes/platform-path-normalization/tasks.md` as complete (remaining 2 tasks are subsumed by phase 4)
- [ ] 7.3 Run `openspec change archive consolidate-platform-handlers`
- [ ] 7.4 Run `openspec change archive platform-path-normalization`
- [ ] 7.5 Verify main specs (`openspec/specs/platform-primitives/spec.md`, `openspec/specs/platform-paths/spec.md`) are created/updated by the archive
- [ ] 7.6 **Write commit script** `/tmp/pi-commit-7-archive.sh` (stages the archived openspec change folders and the updated main specs that `openspec change archive` produced) with message:
  ```
  chore(openspec): archive superseded consolidate-platform-handlers and platform-path-normalization
  ```
  Print the one-liner: `bash /tmp/pi-commit-7-archive.sh`

## Done criteria

- All 7 phases complete
- No step auto-commits or auto-pushes — every commit was user-executed by running the provided `/tmp/pi-commit-<N>-<slug>.sh` one-liner
- All 12 commit scripts cleaned up from `/tmp/` after their commit succeeded
- Targeted tests green for all files touched by the change (full suite intentionally NOT run)
- Manual Windows smoke tests pass
- `windows-integration` has 12 new commits including the merge commit from phase 6
- User is ready to open PR `windows-integration` → `develop` on GitHub
