# DOX — packages/anti-slop

Files in this directory. One row per source file.

| File | Purpose |
|------|---------|
| `.pi/skills/anti-slop-frontend/SKILL.md` | NL-triggered skill: mechanical countable anti-slop checklist for AI-generated frontend. Dials VARIANCE/MOTION/DENSITY. Universal tells: no AI-purple, Inter discouraged default, zero em-dash `—`/`–`, no Jane-Doe/Acme/99.99% fake data, no div-based fake screenshots, WCAG AA contrast, motivated motion. Marketing-only: hero, eyebrow ≤ ceil(sections/3), layout repetition, CTA, decoration bans. Grep-able pre-flight. Advisory only, never overrides frontend-mockup-loop gates. Adapted Leonxlnx/taste-skill (MIT). |
| `README.md` | Package overview. pi package (skill only, no tools) catching concrete signatures an undirected model emits trying to "look designed". Flat mechanical checklist: every rule countable or binary (e.g. `eyebrow count > ceil(sections/3)`), verify pass/fail not taste. Generic: any React/Tailwind/shadcn or plain HTML project. |
