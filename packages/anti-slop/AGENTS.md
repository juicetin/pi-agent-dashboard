# DOX — packages/anti-slop

Files in this directory. One row per source file.

| File | Purpose |
|------|---------|
| `README.md` | Package overview. pi package (skill only, no tools) catching concrete signatures an undirected model emits trying to "look designed". Flat mechanical checklist: every rule countable or binary (e.g. `eyebrow count > ceil(sections/3)`), verify pass/fail not taste. Generic: any React/Tailwind/shadcn or plain HTML project. |
