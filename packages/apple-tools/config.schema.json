{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "title": "Apple Tools (iMCP) plugin config",
  "type": "object",
  "additionalProperties": false,
  "properties": {
    "imcpServerPath": {
      "type": "string",
      "description": "Absolute path to the iMCP server binary. Defaults to the canonical /Applications location; the paths requirement interpolates this key.",
      "default": "/Applications/iMCP.app/Contents/MacOS/imcp-server"
    }
  }
}
