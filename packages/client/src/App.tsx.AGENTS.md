# App.tsx — index

Two new shell-overlay routes: `/folder/:encodedCwd/view?path=…` (file preview) + `/pi-view?url=…` (URL preview). `viewMessagesMap: Map<sid, ChatMessage[]>` state populated by `view_messages_update`. `selectedState` merges raw reducer messages with view rows by `timestamp` (`useMemo`). `<CommandInput onViewLocal>` sends `{type:"inject_view_message", sessionId, target}` over WS; `currentCwd={selectedSession?.cwd}` + `sessionMessages={selectedState.messages}` threaded. Setter `setViewMessagesMap` passed to `useMessageHandler`. See change: render-file-previews. Fetches `/api/preferences/display` on mount, hydrates `<DisplayPrefsProvider>`. Runs one-shot `localStorage["show-debug-tools"]` → PATCH `{ debugTools }` → `removeItem` migration (idempotent). Mounts `<FirstLaunchDisplayModal>` when server returns `global === undefined`. Gates `<TokenStatsBar>` on `useDisplayPrefs(selectedId).tokenStatsBar`. See change: configurable-chat-display. Desktop header splits gating: `tokenStatsBar` → `showStats` prop (butterfly chart + token stats); `contextUsageBar` → `showContextBar` prop (progress bar). Both effective = override ?? global ?? true. Renders `<TokenStatsBar>` when either true; else null. Wires `setBootstrapSender(send)` once via `useEffect`. Builds `cwdVisibilityInputsRef` snapshot on every render (`pinnedDirectories`, `workspaces`, `Array.from(sessions.values()).map(s => ({cwd: s.cwd}))`) + threads as new `useMessageHandler` dep. Mounts `<SpawnErrorToastHost />` next to existing `<Toast>`. See change: harden-worktree-spawn. Pre-R3 bootstrap banner + status hook removed. `<PiUpdateBadge />` gated on `useLaunchSource() !== "electron"`. See change: eliminate-electron-runtime-install. React app with WebSocket. Owns per-session ephemeral input state alongside `sessionStates`: `drafts: Map<sid, string>` (persisted to `localStorage`, see `chat-input-draft-and-history`) + `pendingImagesMap: Map<sid, ImageContent[]>` (in-memory only, not persisted — change: lift-pending-images-to-app). Both passed to `<CommandInput>` as controlled props (`draft`/`onDraftChange`, `images`/`onImagesChange`) so they survive content-area route changes that unmount chat panel + don't leak across session switches. `wrappedHandleSend` clears both via `clearDraftForSession` + `clearImagesForSession` after send. Module-level `EMPTY_IMAGES = Object.freeze([])` gives stable empty-array ref; sessions with no pending images don't trigger child re-renders. **Shell overlays URL-driven** (change: overlay-url-routing): six shell-owned overlay routes via `useRoute` — `/folder/:encodedCwd/openspec/:changeName/:artifactId`, `/folder/:encodedCwd/openspec/archive`, `/folder/:encodedCwd/openspec/specs`, `/folder/:encodedCwd/pi-resources`, `/session/:id/diff` — plus cross-folder `/pi-resource?path=&title=` query route. `useState` overlay flags (`previewState`, `specsBrowserCwd`, `archiveBrowserCwd`, `diffViewSessionId`) replaced by route matches. Back-arrow (desktop + mobile) collapsed to single `goBack = goBackOrHome(navigate)`. Plugin content-view claims (flows-plugin) remain predicate-driven, out of scope. **Reconnect effect** (changes: fix-stale-sessions-on-reconnect, fix-reliable-live-control-events): on transition to `connected`, no longer calls `setSessionOrderMap(new Map())`; server `sessions_snapshot` replaces it atomically. It clears `subscribedRef`, removes browser-local pending interactive rows from every retained session through `reconcileInteractiveRequestsAfterReconnect`, and clears terminals. Selected-session subscribe/replay then restores only requests still pending on the server. Populates SlotRegistry from generated PLUGIN_REGISTRY at module load (see change: wire-plugin-registry-into-shell). Calls `usePluginEnabledSet(_pluginRegistry)` so disabled plugins contribute zero claims at runtime; subscribes to `/api/health` + `plugin_config_update` for live toggling. See change: add-plugin-activation-ui. See changes: unify-status-banner-and-terminal-limit-stop, fix-retry-error-lifecycle — mounts sticky `<SessionBanner>` between `ChatView` and `CommandInput`; passes `lastError.timestamp` as Retry revision and a live `sessionStatesRef` for stale-click rejection; settled Retry calls `handleRetrySession` (hidden active-bridge non-user turn), X clears only `lastError`; retry-phase Collapse never aborts or dismisses. See change: honest-mid-turn-queue-surface. wrappedHandleAbort callback removed (yank-to-draft UX produced ghost duplicates since pi.abort does not clear queues). All onAbort sites pass bare handleAbort. See change: rework-mid-turn-prompt-queue. New setDraftForSession useCallback merges followup_pulled text into per-session draft (empty: replace, non-empty: \n\n append). Threaded to useMessageHandler deps. Five bridge-owned-buffer mutation senders destructured from sessionActions + threaded to QueuePanel via onEdit/onRemove/onPromote/onPull/onClearAll props. selectedId derives from both /session/:id and /session/:id/diff matches via deriveSelectedSessionId helper (packages/client/src/lib/selectedSessionId.ts); fixes desktop fallthrough to LandingPage on diff sub-route. See change: fix-changed-files-desktop-route. Intercepts bare `!!` in `wrappedHandleSend` → `handleOpenInlineTerminal`; passes `onOpenInlineTerminal`/`onCloseInlineTerminal` props. See change: add-inline-terminal-card. favoriteModels state; threads favorites + favorite/unfavorite senders into StatusBar. See change: enrich-model-selector-capabilities-favorites. Threads `onReplaceProposal` callback down to SessionOpenSpecActions. See change: replace-proposal-dialog-with-race-handling. Settings route now `useRoute("/settings/:page?")`; manage-servers link → `/settings/remote`. See change: reorganize-settings-into-pages. Owns `loadingHistory` state `Map<sid,boolean>` + `loadingHistoryTimersRef`. Lazy-subscribe site sets flag true + arms short `SUBSCRIBE_ACK_MS` (15s) safety-net timer. Threads `setLoadingHistory` + `loadingHistoryTimersRef` to useMessageHandler. Passes selected session's flag as `loadingHistory` prop to ChatView. See change: show-chat-history-loading-indicator. useMessageHandler re-arms the timer to `HYDRATE_CEILING_MS` (90s) on the cold-hydration start marker + server heartbeats so a slow disk parse never flashes "No messages yet". See change: fix-history-loading-false-empty-flash. Owns sibling state `replayInFlight: Map<sid,boolean>` + `replayInFlightTimersRef` and `beginReplayInFlight(id)` (new sibling of `beginLoadingHistory`, which hard-codes its own setter/ref). Both flags arm together at every `subscribe` site; they diverge only on the CLEAR edge. Passes `replayInFlight` prop to `<ChatView>`. See change: show-replay-in-flight-indicator. readme overlay route + ReadmePreviewRoute removed. See change: remove-project-readme-button. Calls `useAppHidden()` to pause animations when window hidden. See change: throttle-idle-ui-animations. Holds `folderGitMap` state, wires `setFolderGitMap`, resets on server switch, passes `folderGitMap` to SessionList. See change: refresh-folder-header-branch. Adds route `/session/:id/editor` rendering `EditorPane` in content area (both mobile+desktop branches); `selectedId` derivation + `hasShellOverlayRoute` include `editorMatch`. See change: add-internal-monaco-editor-pane. Removed `queuedTexts`/`safetyTimerPaused` plumbing; `usePendingPromptTimeout` armed plainly on `!!pendingPrompt`. See change: optimistic-prompt-progress. First subscribe after reload rehydrates sessionStates+maxSeqMapRef+raw-event buffer from replayCache, subscribes lastSeq=persistedMaxSeq; reconnect skips cache read (rehydratedRef guard). See change: reduce-session-replay-traffic. route `/folder/:cwd/settings/:page?` + legacy `/folder/:cwd/pi-resources` replace-redirect → `…/settings/packages`. See change: directory-settings-page-and-scoped-md-editing. See change: refresh-model-selector-models — StatusBar onRefreshModels sends request_models for selectedId, bypasses !modelsMap.has(sid) fetch-once guard. Content router renders `SessionSplitView` (co-mounted split) in place of `/session/:id/editor` route-swap. Mounts `SplitWorkspaceProvider`; `SplitRouteSync` deep-link; `changedOnDisk` state + `watch_files` wiring. See change: split-editor-workspace. `noticeSessionIds` useMemo (state.notice && !lastError) threaded to SessionList alongside errorSessionIds/retrySessionIds. See change: fix-gemini-subagent-silent-tool-schema-failure. On `status==="connected"` fetches `fetchActiveInits()` → `initStore.seed` + `resendActiveCwdSubscriptions()` (worktree-init boot rehydration + reconnect resubscribe); mounts `<WorktreeInitStack />` beside `<Toast>`. See change: friendlier-worktree-init. `allTags` useMemo (`allTagsInUse(sessions)`) threaded to `<SessionList>` (sidebar filter + card) and `<SessionHeader allTags onSetTags>`; `onSetTags` → `handleSetSessionTags`. See change: add-session-tags. `workspacesLoaded` state (flipped by `useMessageHandler` on first `workspaces_updated`) + `workspaceFolderSet = useMemo(new Set(workspaces.flatMap(w=>w.folders)),[workspaces])`; both threaded to `<DirectoryHomeView workspaceFolders workspacesLoaded>` for the workspace-folder eligibility guard. See change: enable-workspace-folder-home-page. `usePendingPromptTimeout` arms on `status==="sending"` and its handler calls `applyPromptTimeout` (failed bubble + `lastError`) instead of dropping; `pendingPrompt` passed to `CommandInput` as the status. See change: fix-optimistic-prompt-stuck-sending. `modelRefreshErrorsMap` per-session state; `openSpecRunConfig` renamed `modelConfig` and provided via `ModelConfigProvider`. See change: upgrade-model-selector-primitives. `serverKey = useMemo(deriveServerKey(wsUrl))` + `serverKeyRef` (read at flush time); `createReplayPersister(replayCache, undefined, () => serverKeyRef.current)`; `rehydrateSession(sid, replayCache, serverKeyRef.current)`. `handleRefreshChat` delegates to `lib/chat/refresh-chat.ts` `refreshChat` (both header + mobile `onRefresh` call it, so the paths cannot drift); declared AFTER `beginReplayInFlight`, which it lists as a dep, to avoid a TDZ `ReferenceError`. See change: purge-replay-cache-on-reset-paths.
