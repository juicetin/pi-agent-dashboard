# EditorInstallGuide.tsx — index

Install guide shown when `code-server` not found. Exports `EditorInstallGuide`. Lists install commands for macOS (`brew install code-server`), Linux (curl installer), npm (`npm install -g code-server`), and `@anthropic-ai/claude-code-vscode-server` alternative. Optional `onRetry` re-detection button.
