# FolderActionBar.tsx — index

Sidebar folder-group action bar. Buttons: Terminals(N), Editor (code-server status-colored), native editors (Zed, vscode filtered out), Clean up broken (via `Confirm`), Directory Settings. Embeds `WorktreeInitButton`. Exports `FolderActionBar`.
