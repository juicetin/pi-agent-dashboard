# FolderNeedsYouPill.tsx — index

Folder-header "N need you" rollup pill. Counts chat-routed ask_user child sessions; excludes widget-bar via per-session WidgetBarProbe + useHasWidgetBarPrompt. Hidden at count 0. Click → onActivate (scroll+select first blocked). Mobile ≤375px hides "need you" label (hidden sm:inline). data-testid="folder-needs-you-pill". See change: improve-dashboard-attention-routing.
