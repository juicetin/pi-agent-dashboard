# MarkdownPreviewView.tsx — index

Markdown preview pane with header (back + title + `MarkdownSearch`) and optional tab bar (`PreviewTab[]`). Renders `MarkdownContent` with `frontmatter="properties"`. Loading/error states. Exports `MarkdownPreviewView`, `PreviewTab`.
