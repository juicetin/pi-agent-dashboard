# PackageInstallConfirmDialog.tsx — index

Pre-install confirmation dialog. Exports `PackageInstallConfirmDialog`. Shows source + optional name + scope. Scope radio (`local`/`global` with settings.json path hints) shown when `lockScope` undefined and `onScopeChange` provided; otherwise locked scope displayed read-only. See change: `unify-package-management-ui`.
