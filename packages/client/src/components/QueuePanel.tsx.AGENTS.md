# QueuePanel.tsx — index

Read-only follow-up cycler. Pi ExtensionAPI exposes no queue mutation (verified through pi 0.76.0). Renders subtitle "Follow-up — delivered when the agent finishes the turn" + ↑/↓ navigation + position counter. No mutation buttons. Mounted above CommandInput when followUp non-empty. Steering renders inline as ghost user-message bubbles in ChatView, not in QueuePanel. See change: honest-mid-turn-queue-surface. See change: rework-mid-turn-prompt-queue. Mutation buttons restored: [✎] edit (inline textarea, Cmd/Ctrl+Enter submits, Esc cancels), [✕] remove (window.confirm only when text > 50 chars), [⇧] promote-to-head (disabled when idx=0), [→ editor] pull-to-editor (round-trips followup_pulled to hydrate draft). Header [✖] clear-all shown when length > 1. Bridge owns buffer; all mutations are local to bridge, never touch pi. Steer NOT in panel (permanently pi-owned + inline ghost bubbles in ChatView).
