# RawEventCard.tsx — index

Collapsible card showing one raw event in the event log. Exports `RawEventCard`. Props: `eventType`, `content`, `timestamp`. Chevron + JSON icon header with `formatMessageTime`; expands to `<pre>` of raw content.
