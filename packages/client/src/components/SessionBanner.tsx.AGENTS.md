# SessionBanner.tsx — index

Composed session-status banner. Error anchor (red) + retry sub-line (amber) in one surface, driven by `deriveBannerState`. Dismiss ✕ state-dependent: abort+clear on retrying/retryable, clear-only on limit-exceeded. Mounted sticky between `ChatView` and `CommandInput`. Replaces `RetryBanner` + `ErrorBanner`. Preserves legacy `data-testid="error-banner"` + `error-banner-dismiss`; adds inner `data-testid="limit-exceeded-banner"` marker. See change: unify-status-banner-and-terminal-limit-stop. See change: unify-error-retry-lifecycle — renders composed surface: error anchor (red header) + retry sub-line (amber) in one wrapper. Imports `BannerState`/`BannerRetry` from event-reducer (single source). Dismiss ✕ state-dependent: `dismissAborts = !!retry \|\| error.kind==="error"` → calls `onAbort` then `onDismiss`; limit-exceeded clears only. Manual Retry button hidden while retry in flight.
