# SessionBanner.tsx — index

Single-card session-status banner. ONE bordered card composes an error header + a live retry sub-line (`retry-banner`), driven by `deriveBannerState`. No `limit-exceeded` variant — billing errors render as ordinary errors (no regex). Dismiss ✕ is CLEAR-ONLY in every state (`onDismiss` only, never aborts). The labeled "Stop (ends the session)" control (`retry-banner-stop`, present only while retrying) is the sole abort path. No manual retry control. Mounted sticky between `ChatView` and `CommandInput`. Preserves legacy `data-testid="error-banner"` (card when error set) + `error-banner-dismiss` + `retry-banner*`. Imports `BannerState`/`BannerRetry` from event-reducer (single source). See change: unify-error-retry-lifecycle. See change: simplify-error-retry-single-card — collapsed two-block surface to one card; removed `limit-exceeded`/💳, `dismissAborts`, and the `onRetry`/"Try again" re-send.
