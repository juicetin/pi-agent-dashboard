# SortableWorkspace.tsx — index

dnd-kit sortable wrapper for a workspace tier (`data.type: "workspace"`), drop indicator. Exports `SortableWorkspace`, `useWorkspaceDragHandle` (context channel to `WorkspaceHeader`). See change: `workspace-directory-drag-reorder`.
