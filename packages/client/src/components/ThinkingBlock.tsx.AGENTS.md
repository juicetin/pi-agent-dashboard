# ThinkingBlock.tsx — index

Exports `ThinkingBlock`. Collapsible reasoning panel; props `content`, `isStreaming`, `defaultExpanded`, `startedAt`, `duration`. Renders `MarkdownContent` body + `ElapsedBadge`. i18n label "Reasoning".
