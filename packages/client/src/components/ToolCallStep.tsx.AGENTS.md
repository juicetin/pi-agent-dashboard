# ToolCallStep.tsx — index

Renders tool-call card. Adds `showResultBody?: boolean` prop (default `true`); when `false` hides result body section, keeps header + status. See change: configurable-chat-display. Consults plugin `tool-renderer` slot claims before built-in registry. Resolution: plugin claim (filtered `forToolName`, `shouldRender` truthy/absent, fail-closed on throw + console.warn) → `getToolRenderer(toolName)` → `GenericToolRenderer`. One-shot at lookup via `useMemo`. Plugin component wraps in `CurrentPluginLayer`; per-tool `ErrorBoundary` catches plugin render errors, no fall-through. `useSlotRegistryOrNull()` null → skips plugin lookup. See change: wire-tool-renderer-slot. Result starts with `«` truncation marker → renders "Show full output" button via `useToolFullResult(context.sessionId, toolCallId)`; collapse re-shows truncated; 404 → "result evicted" message. See change: adopt-pi-071-072-073-features.
