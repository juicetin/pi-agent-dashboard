# WhatsNewDialog.tsx — index

Exports `WhatsNewDialog` + `WhatsNewDialogProps`. Modal rendering parsed CHANGELOG between two versions. Breaking section pinned expanded; features + other (changed/fixed) collapsible. Footer GitHub link + Cancel + Update CTA. Uses `Dialog`, `MarkdownContent`. Helpers `collectGrouped`, `collectChangedAndFixed`, `countLabel`, `BreakingSection`, `CollapsibleGroup`, `BulletGroups`, `BulletProse`.
