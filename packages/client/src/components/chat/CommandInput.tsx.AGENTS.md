# CommandInput.tsx — index

Chat composer textarea + autocomplete. Exports `CommandInput`, `parseViewCommand`, `shouldWalkFileQuery`, `MIN_FILE_QUERY_LEN`, `DASHBOARD_LOCAL_COMMANDS`. Merges `BUILTIN_COMMANDS` + `DASHBOARD_LOCAL_COMMANDS` + server commands. `/`-command (grouped by source, badges, arg hints) and `@`-file/URL autocomplete with viewport-flip dropdown. Intercepts `/view` → `onViewLocal` (never reaches bridge). History recall (ArrowUp/Down when empty). Controlled `draft`/`images` modes lifted to App.

Unified card composer (change: redesign-prompt-input): one bordered card = attachments row → textarea → inner toolbar (`＋` attach menu · `ModelSelector` chip · `ThinkingLevelSelector` chip · `Steer|Queue` delivery control · terminal · morphing action button). Model/thinking/context props (`model`, `models`, `favorites`, `onToggleFavorite`, `thinkingLevel`, `onSelectModel`, `onSelectThinkingLevel`, `onRefreshModels`, `contextUsage`) relocated here from StatusBar. Single morphing action button swaps testid+glyph by state: `send-button` (mdiSendVariant) → `stop-button` (mdiStop) → `force-stop-button` (mdiAlertOctagon) → `killing-button`; preserves `idle→aborting→killing`; `stop-after-turn` = slim secondary affordance. Delivery `Steer|Queue` (default steer) maps Enter/Alt+Enter contract. Focus-revealed footer (`composer-footer`) hint line + `composer-context-left` indicator (shown only when `contextUsage.contextWindow>0` && tokens client-side). `＋` menu (`attach-menu`): image (hidden file input → `addFiles`), file (seeds `@`), preview (seeds `/view `). Composer-card is a `@container`; toolbar folding is container-based (`@[44rem]:` folds thinking/delivery/terminal into `overflow-button`/`overflow-menu`, `@[30rem]:` gates the "after turn" label; 44rem sits above the ~689px natural inline width so Steer/Queue + stop never overflow the pane) — NOT viewport `md:`. Fixes split-pane overflow where a narrow chat pane on a wide viewport clipped the stop button. See change: fix-split-composer-overflow. Mobile: persistent `＋·model·⋯·send`. aria-label on every icon-only button; `motion-reduce:animate-none` on force-stop pulse; action button ≥44px.
