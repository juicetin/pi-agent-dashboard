# KnownServersSection.tsx — index

Settings section managing persisted known remote servers. Lists `KnownServer` rows with remove; inline add form (host/port/label). Backed by `listKnownServers`/`addKnownServer`/`removeKnownServer`. Exports `KnownServersSection`.
