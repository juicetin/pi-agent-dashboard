# PairedDevicesSection.tsx — index

Settings → Security → Paired Devices. Lists bearer-paired devices (label, last-seen), per-device revoke-with-confirm (deletes registry row via `revokePairedDevice`). Exports `PairedDevicesSection`. See change: add-server-keypair-pairing.
