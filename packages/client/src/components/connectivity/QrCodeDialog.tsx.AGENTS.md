# QrCodeDialog.tsx — index

Dialog showing tunnel URL as QR code for mobile access. Exports `QrCodeDialog`. ORPHANED by add-tunnel-providers: `TunnelButton` now opens `Gateway/GatewayDialog` instead; retained (still unit-tested) but no longer mounted. → see `QrCodeDialog.tsx.AGENTS.md`
