# DraggableChangeRow.tsx — index

dnd-kit draggable wrapper for OpenSpec change rows. Exports `DraggableChangeRow`. `id` = `change-<changeName>`, `data.type` = `openspec-change`. Applies translate transform, drag opacity 0.5, z-index 50. Used in `FolderOpenSpecSection` grouped view.
