# RichDiff.tsx — index

Pure rich-diff rendering primitive over `@git-diff-view/react` + lowlight. Exports `RichDiff` (props: `oldText`, `newText`, `filePath`, `mode`, `maxHeight`), `EXT_LANG_MAP`, `getLang`. Theme-aware via `useThemeContext`. No toolbar/toggle chrome.
