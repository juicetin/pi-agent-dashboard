# FolderActionBar.tsx — index

Sidebar folder-group action bar. Buttons: Initialize (ProjectInit/WorktreeInit), Clean up broken (conditional). Terminals + Editor buttons REMOVED — that pane is reachable from the Directory home page + ChatView. Tight content-width group (no `flex-wrap`); `SessionList` renders it right-grouped on the folder git row (variant-B compact one-row layout). Props: `cwd`, `onInitializeProject?`, `brokenSessionCount?`, `onCleanUpBroken?`. Native-editor / code-server status wiring removed. See change: remove-external-editor-integration, compact-folder-header-actions.

See change: add-folder-actions-menu — the Directory Settings `mdiCog` button is GONE (rehomed as the folder actions menu's `directory-settings` item), and with it the `onOpenPiResources` prop. The bar can now hold nothing, so it returns `null` in that case: `hasContent = showCleanUp || shouldShowProjectInit(initStatus, onInitializeProject) || !!useInitRun(cwd) || shouldShowWorktreeInitButton(initStatus)`. Both predicates are IMPORTED from the buttons that own them (`ProjectInitButton.tsx`, `WorktreeInitButton.tsx`) so the emptiness test cannot drift from what they actually render; `useInitRun` covers the live/terminal `WorktreeInitChip`, which keeps the bar populated after the gate flips. Cleanup button gained `type="button"` (Biome `useButtonType` on a file this change touches).
