# FolderOpenSpecSection.tsx — index

Slim single-line navigation entry `OpenSpec (N) →` to board route. STATE-ONLY: renders no action control. Refresh + Archive + Specs were icon-only buttons in the SlotPill trailing `actions` cluster (test-ids folder-openspec-refresh / folder-archive-btn / folder-specs-btn) — all three GONE. They are folder-actions-menu items contributed HOST-side by `SessionList` (`refresh-folder` in MAINTENANCE; `openspec-archive` + `openspec-specs` in OPEN, slot-qualified labels), because OpenSpec's callbacks are already `SessionList` props — no plugin contribution registry needed. Props now: data, cwd, onOpenBoard (onRefresh / onOpenSpecs / onOpenArchive REMOVED). Inline accordion/pills/search/DnD/session rows removed → moved to OpenSpecBoardView. See change: add-folder-task-checker-and-spawn-attach. See change: add-session-status-to-folder-proposal-rows. See change: redesign-openspec-board. See change: openspec-pill-inline-icon-actions. See change: move-slot-actions-to-menu.
