# OpenSpecProfileSection.tsx — index

Settings section. Sets global OpenSpec profile (core/expanded/custom) + workflow multiselect. Save profile → POST /api/openspec/config (writes global config only, no project mutation). Update all + collapsible per-cwd list with staleness badges → POST /api/openspec/update, GET /api/openspec/update-status. Resilient load: no hardcoded `core` default; `loading`/`ready`/`error` states; retry once on transient failure; visible error + Retry affordance; `userTouched` ref guards late-load clobber. Mounted in SettingsPanel Advanced tab. See changes: add-openspec-profile-settings, fix-openspec-profile-load-race. Standalone Save-profile button removed; profile/workflows buffer into Settings draft, commit via unified Save (registers `openspec-profile` source). See change: unify-settings-save-contract.
