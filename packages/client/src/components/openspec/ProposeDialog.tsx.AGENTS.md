# ProposeDialog.tsx — index

Name-only dialog launching `/skill:openspec-propose`. Exports `ProposeDialog`, `formatProposePrompt(name)`. Single optional field (kebab name or description); plain Enter submits. Distinct from `NewChangeDialog`: creates change + all planning artifacts in one step. See change: `add-openspec-profile-settings`.
