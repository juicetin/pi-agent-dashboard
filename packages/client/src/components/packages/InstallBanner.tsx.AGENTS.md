# InstallBanner.tsx — index

Mobile-only PWA install banner (`md:hidden`). Shows iOS Share→Add-to-Home-Screen hint or generic install button via `prompt()`. Dismiss persisted to `localStorage` key `pwa-install-dismissed`. Exports `InstallBanner`.
