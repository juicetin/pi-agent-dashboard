# PackageCard.tsx — index

Search-result card for one npm package. Exports `PackageCard`. Props: `pkg` (`NpmPackageResult`), `installed`, `installedScope`, `updateAvailable`, `operationStatus`, etc. Renders name, scope badge, weekly downloads, type badges (extension/skill/theme/prompt), install/update/uninstall/check-update actions. Click opens README.
