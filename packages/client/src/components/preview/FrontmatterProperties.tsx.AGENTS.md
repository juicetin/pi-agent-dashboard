# FrontmatterProperties.tsx — index

Obsidian-style YAML frontmatter Properties panel. Exports `extractFrontmatter` (leading `---` block parser), `inferType` (structural `ValueType` inference), `formatRelativeDate`, `FrontmatterProperties` (collapsed-by-default typed rows, `ErrorBoundary`-wrapped, raw fallback on invalid YAML, `status` key → `StatusBadge`).
