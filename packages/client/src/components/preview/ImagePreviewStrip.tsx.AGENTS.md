# ImagePreviewStrip.tsx — index

Pasted-image thumbnail grid + error banner shared by `CommandInput` and `ExploreDialog`. Consumes `useImagePaste` state; remove-X per thumb; click opens `ImageLightbox`. Renders nothing when empty + no error. Exports `ImagePreviewStrip`.
