# MarkdownContent.tsx — index

ReactMarkdown renderer (chat/thinking/flow agent detail/READMEs/previews); KaTeX math + `pi-asset:` image scheme. External-link hardening (change: harden-external-link-handling, #13): exports pure `isExternalHref(href)` + overrides `a` component so external URLs render `target="_blank" rel="noopener noreferrer"`, fragment-only + same-origin hrefs stay in-document. Optional `context?: ToolContext` prop enables prose + inline-`code` linkification via `tokenize` → `FileLink`/`UrlLink`; fenced code blocks excluded; markdown anchors not double-wrapped. See change: unify-file-link-openability. The `a` override wires `onClick`→`useLoopbackLinkOpen()`: plain click of a loopback URL opens the internal live-server split viewer (keeps `target="_blank"` so modifier/middle-click + no-context fall through to the browser). See change: open-loopback-links-in-split-viewer.

See change: virtualize-chat-transcript-tanstack (task 8.1) — `PiAssetImg` reserves `minHeight:6rem` (`reserveStyle`) before decode and clears it `onLoad` (`releaseReserved`), so an above-viewport async image load does not shift the windowed scroll offset (CR-7: accepts one reflow per image). Applied to both the resolved `pi-asset:` img and the external-URL fall-through img.

Opt-in local-image resolution (change: fix-markdown-preview-relative-images): `imageBase?: {cwd;dir}` prop delivered to `PiAssetImg` via a `useMemo`'d `ImageBaseContext` (default `null`). When set, `PiAssetImg` — after the `pi-asset:` branch, before the verbatim fall-through — calls `resolveLocalImageSrc`; a non-null result renders the rewritten `/api/file/raw` `<img>` with `onError`→swap to a non-interactive neutral "couldn't load image" placeholder (unmounts the img so the 6rem reserve dies). Verbatim branch (chat, `imageBase` null) unchanged — no `onError`. Opt-in call-sites: `FilePreviewOverlay`, editor `MarkdownViewer`, `MarkdownPreview`; `MarkdownPreviewView`-hosted surfaces out of scope.
