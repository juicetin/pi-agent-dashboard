# MarkdownSearch.tsx — index

In-markdown fuzzy search overlay. `fuse.js` index over text blocks; exact-substring match first, fuzzy fallback. TreeWalker-based `<mark>` highlighting, prev/next nav, Enter/Shift+Enter/Escape keys, match counter. Exports `MarkdownSearch`.
