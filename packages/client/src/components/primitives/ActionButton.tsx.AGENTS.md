# ActionButton.tsx — index

`<ActionButton action options pendingLabel>` thin wrapper over `useAsyncAction.bind`. Swaps children→pendingLabel while pending, disables. Optional sugar for one-action buttons. See change: add-async-action-feedback.
