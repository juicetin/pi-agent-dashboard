# PiLogo.tsx — index

Inline SVG brand mark (geometric Π). Exports `PiLogo`. Props: `size` (default 24), `className`, `title`. Fills `currentColor` so it adapts to theme; transparent background. `role="img"` + `aria-label`.
