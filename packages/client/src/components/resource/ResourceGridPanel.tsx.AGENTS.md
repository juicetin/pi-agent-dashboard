# ResourceGridPanel.tsx — index

Loading/error/refresh chrome + `ResourceReloadBanner` around `ResourceCardGrid`. Exports `ResourceGridPanel`. Shared by Directory Settings (local+global, filter) + Settings (global-only, `◇ global` pill). Caller owns the `usePiResources` fetch. See change: resources-card-tabs.
