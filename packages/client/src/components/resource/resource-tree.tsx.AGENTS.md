# resource-tree.tsx — index

Activation primitives reused by `ResourceCard`. Exports `ActivationToggle` (`role=switch`) + `ResourceReloadBanner`. Legacy `MergedScopeSection`/`ResourceItem`/`ResourceGroup`/`PackageItem` tree removed once both surfaces moved to cards. See change: folder-resource-activation-toggle, resources-card-tabs.
