# MissingRequiredBanner.tsx — index

Top banner for missing `required` recommended extensions (`useRecommendedExtensions`). Offers Activate (on-disk, cheap) or Install label; per-session dismiss via `sessionStorage` key `pi-dashboard:missing-required-dismissed`; reinstalls via `usePackageOperations`. Exports `MissingRequiredBanner`.
