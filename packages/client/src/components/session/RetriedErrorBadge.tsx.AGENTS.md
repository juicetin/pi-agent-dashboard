# RetriedErrorBadge.tsx — index

Compact badge collapsing a tool error→retry pair into one line. Click expands to full `ToolCallStep`. Exports `RetriedErrorBadge`. Props: `toolName`, `toolCallId`, `args`, `result`, `context`, `startedAt`, `duration`, `toolDetails`.
