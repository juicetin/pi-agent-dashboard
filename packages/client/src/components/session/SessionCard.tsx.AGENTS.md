# SessionCard.tsx — index

Gates both `<ContextUsageBar>` mounts on `useDisplayPrefs(session.id).contextUsageBar`. See change: configurable-chat-display. Desktop session card. Header zone (name, model, activity, cost, fork) outside subcards. Body groups OPENSPEC → WORKSPACE → PROCESS → MEMORY → FLOWS via `SessionSubcard`. `WorkspaceSubcard` helper combines `GitInfo` + `SessionCardBadgeSlot`. `MemorySubcard` helper renders `session-card-memory` plugin slot. `SessionCardActionBarSlot` stays in footer outside subcards. Mobile branch unchanged (flat layout). See change: redesign-session-card-subcards. See change: add-session-status-to-folder-proposal-rows. FLOWS subcard call site gated on (flows.length > 0 \|\| hasFlowsNew); empty FLOWS subcard no longer renders when session has no flows and no flows:new command (mirrors WorkspaceSubcard pattern). See change: hide-empty-flows-subcard. Adds `FlowsSubcard` wrapper gated by `useSlotHasClaimsForSession("session-card-flows", session)`; slot between PROCESS and MEMORY. See change: add-flows-subcard. `WorkspaceSubcard` renders `<CwdGonePill>` next to `<WorktreePill>` + `<WorktreeActionsMenu>` when `session.gitWorktree` set; resume/fork buttons `disabled` when `session.cwdMissing` (tooltip `session's directory no longer exists`). See change: add-worktree-lifecycle-actions. See change: redesign-session-card-and-composer (WORKSPACE split into GitSubcard + JjSubcard; selected card gains card-selected-ring class). PROCESS subcard now composes `SessionActivityBar` (top, in-flight `bash` toolCalls) over `ProcessList` (bottom, background-processes drawer) via internal `ProcessSubcard` helper; mobile uses `MobileProcessSubcard` with `⚠ N` chip opening bottom-sheet. `useDrawerExpansion` hook holds per-session toggle override; contextual default opens drawer when activity bar empty + drawer non-empty. New props `inflightBashTools`, `onAbortTool`. Subcard hides only when both surfaces empty. See change: redesign-process-list-activity-bar. Accepts `onSpawnSibling?(session)` prop. Renders always-visible `+Session` button (mdiPlus, data-testid `session-card-spawn-sibling`) in fork/resume pill row. NOT gated on `status === "ended"` or `sessionFile` (unlike Fork/Resume). Disabled when `session.cwdMissing` (tooltip `session's directory no longer exists`). Click calls `onSpawnSibling(session)`. SessionList wires via `onSpawnSession(s.cwd, s.attachedProposal \|\| undefined)`; clean sibling spawn inherits cwd + attachedProposal. Accepts `onSpawnWorktree?(session)` prop. Renders `+Worktree` button (mdiSourceBranchPlus, orange, data-testid `session-card-spawn-worktree`) next to `+Session`; gated upstream by `gitWorktreeEnabled`; hidden when `session.gitWorktree` set (no worktree-from-worktree); also hidden ONLY when `session.isGitRepo === false` (confirmed non-git) — `true`/`undefined` keep it (no gitBranch gate; that is a data-arrival signal). See change: gate-session-worktree-button-on-git. Disabled when `cwdMissing`. Click calls `onSpawnWorktree(session)`. SessionList routes: `attachedProposal` set → `setWorktreeForChange({cwd, changeName})` (proposal-aware WorktreeSpawnDialog, branch `os/<change>`); else `setWorktreeDialogCwd(cwd)` (plain dialog). Reuses WorktreeSpawnDialog (create worktree + bootstrap + spawn). See change: session-card-plus-session-button. Threads `onReplaceProposal` callback to SessionOpenSpecActions. See change: replace-proposal-dialog-with-race-handling. `getCardPulseClass` returns `card-input-stripes` for ask_user (was `card-input-pulse`). New `getCardStripeFxClass` maps state marker class to `.card-stripes-fx` color class. Card `<li>` gains `relative isolate`; renders `.card-stripes-fx` overlay (behind content, z-index -1) + `.card-ring-fx` overlay when selected. See change: throttle-idle-ui-animations. Card-state helpers relocated to session-status-visuals.ts; SessionCard imports + re-exports `getCardPulseClass`/`getCardStripeFxClass` for test compat. See change: port-session-card-state-visuals-to-openspec-board. ActivityIndicator label split: ask_user→"Needs you" (--status-needs-you), idle/active→"Idle" (muted); "Waiting for input" retired. `StatusShapeBadge` overlays shape marker on session-status-icon (`data-status-shape`). Threads `hasWidgetBarPrompt` into dot/rail/shape. See change: improve-dashboard-attention-routing. `GroupGitInfo` gains `folderBranch?: string\|null`; branch precedence `folderBranch ?? session.gitBranch ?? fetchedBranch`; `folderBranch===null` renders Init-git non-git state; `branchCache`/REST stays first-paint seed. See change: refresh-folder-header-branch. Renders `<WorktreeCardSectionSlot folder={{cwd: session.cwd}}>` between OPENSPEC and GIT subcards, gated on `session.gitWorktree` set. Surfaces the worktree's own KB row (worktree collapses under `gitWorktree.mainPath` in the sidebar, so its cwd never gets a folder card). Scoped to the worktree cwd, not the parent repo. See change: kb-row-on-worktree-session-card.
 Session card container gains neutral panel bevel `shadow-[inset_0_1px_0_var(--elevation-rim),0_4px_8px_var(--shadow-card)]` (desktop + mobile), replacing `shadow-md`; session-name span gains `font-semibold` (weight 600) both layouts. Selected-card treatment untouched (bevel on shared className, `isSelected` ternary only swaps border/tint/ring). No added color. See change: add-panel-elevation-system.

Accepts `hasNotice?: boolean` prop; threads into `deriveDotColorWithFlags`/`deriveStatusShape`/`deriveRailBgColor` flags. Renders non-error `notice` status ("model returned only reasoning, no answer") distinct from error. See change: fix-gemini-subagent-silent-tool-schema-failure.

Renders compact read-only `<TagStrip tags phase={openspecPhase}>` (user chips + `+N` overflow + read-only phase chip) after the OpenSpec activity badge, in both mobile-compact and desktop layouts. See change: add-session-tags.
