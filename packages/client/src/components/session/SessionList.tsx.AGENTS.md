# SessionList.tsx — index

Main sidebar session list. DnD-ordered (`@dnd-kit`) pinned/unpinned + workspace tiers, folder grouping,… `renderGroup` folder-home open affordance (`folder-open-home-<cwd>`) renders when `isPinned || inWorkspace` (unpinned workspace folder has `folder.pinned===false`). See change: enable-workspace-folder-home-page. Whole folder header name-row (`folder-home-row-<cwd>`) navigates to `buildFolderHomeUrl(cwd)` on click (all folders); collapse moved solely to chevron `folder-toggle-btn` in drag gutter. See change: directory-card-clickable-select. `renderGroup` card wrapped in `relative pt-[9px]` with a non-interactive folder-tab nub (`aria-hidden`, `pointer-events-none`, `top-0 left-3.5 w-[78px] h-3`, behind the card) so the card reads as a folder; the old `folder-3d.svg` watermark layer is removed. See change: folder-card-tab-nub. `renderGroup` now ENCLOSES the Create tray + session list + "N ended" row inside a `folder-body-<cwd>` wrapper (header `rounded-t`/`border-b-0`+inset-rim-only shadow when expanded, body `border-t-0`/`rounded-b`, no seam shading — fold-shadow child removed, `CREATE` separator alone marks the junction; junction is FLUSH: expanded header gets `pb-0`, `CREATE` separator `mt-0 mb-2`; session-list container carries the DIRECTORY RAIL — `relative pl-[18px]` + `before:` 2px `--rail-directory` line at `left-[7px]`, which each `SessionCard` ticks into and whose band hosts the card's drag bead, replacing the deleted per-card status gutter, see change: session-card-directory-rail) so the card reads as a folder; adds a `SESSIONS` separator mirroring `CREATE`; top-level (`!inWorkspace`) folders get an accent-tinted surface via `folderTint` (color-mix on `--accent-blue`). See change: folder-card-enclosure. Sidebar tag/phase filter area sits under ONE master collapse (`tag-area-toggle`, `aria-expanded`, default-collapsed via `getTagAreaOpen`/`setTagAreaOpen`); collapsed header shows `N tags · M phases` (`tag-area-count`) + an active-selection badge (`tag-area-active-indicator`) with a fold-free clear (`clear-tag-filters-collapsed`) when filters active; expanded renders `TagFilterGroup` user (cap=10, per-chip ✕ → `TagDeleteConfirmDialog` → `onRemoveTagGlobally`) + read-only phase group. See change: sidebar-tag-collapse-and-delete. → see `SessionList.tsx.AGENTS.md`

See change: redesign-folder-workspace-add-flow — the `+ws` text token (old `absolute top-1 right-7` layer) is GONE. `renderGroupWithWorkspaceMenu` now builds the affordance and passes it as `renderGroup(..., headerAction)` (a `React.ReactNode` slot — contract from `add-to-workspace-affordance`; when `headerAction` is present the cluster's `ml-auto` moves onto it). The header trailing action buttons are wrapped in a non-wrapping cluster (`folder-header-cluster-<cwd>`, `flex-none whitespace-nowrap`); the name region (`folder-header-name-<cwd>`, `min-w-0`) absorbs the squeeze — parent path (`folder-header-parent-<cwd>`, `flex-[0_1_auto] min-w-0`) collapses first, leaf (`folder-header-leaf-<cwd>`, `min-w-[6ch]`) keeps a floor. Cluster order: sort · add-to · home · pin (· remove on workspace rows). `renderAddToWorkspaceButton(cwd,label,scopeKey,testId,wrapperClass)` renders the `mdiViewGridPlus` + "Workspace" labelled pill (`aria-haspopup=menu`, `aria-expanded` bound to `addToWsMenuFor`, keyed by SCOPE not cwd so a card + its folder row don't co-open) + `AddToWorkspaceMenu` popover; top-level rows only (`onCreateWorkspace || workspaces.length`). See change: redesign-folder-workspace-add-flow — the original `mdiFolderPlus`+`mdiMenuDown` caret icon button is REVERTED; `add-to-workspace-affordance` promoted the labelled pill to a main spec while this branch was unmerged, so presentation is theirs and the scope-keyed state + a11y contract are ours. SessionCard gets the same button via a `renderAddToWorkspace` render prop (`session-card-add-to-workspace-<id>`). Workspace-scoped `+ Add Folder` now opens `AddFoldersDialog` (that ws preselected) instead of `PinDirectoryDialog`.

Folder-membership drags: `DndContext` uses `compatibleClosestCenter` + `measuring.droppable.strategy = MeasuringStrategy.Always` (spring-load mounts folder droppables MID-DRAG; the default `Optimized` strategy would not remeasure them). `handleDragEnd`'s `activeType !== overType` early return is REPLACED by per-active-type dispatch: `session`/`workspace` keep their same-type wall as a per-branch guard, folder-like actives route through `resolveFolderMove` → `onMoveFolderToWorkspace(path, toWorkspaceId, index?)`. New state: `activeDragType` (gates the empty-tier eject zone), `springOpen: Set<wsId>` (add-only for the drag; cleared ONLY on drag end/cancel — clearing on `over` change would re-collapse the instant the cursor entered the revealed children, a flicker loop) and a dwell-timer ref keyed on the resolved WORKSPACE id (closestCenter jitters `over.id` at Voronoi boundaries). Render precedence: `displayCollapsed = springOpen.has(id) ? false : (forceCollapsed.has(id) || ws.collapsed)`. Neither set ever emits `set_workspace_collapsed`. See change: drag-folders-across-workspaces.

See change: add-folder-actions-menu — the header trailing cluster (`folder-header-cluster-<cwd>`) collapses to EXACTLY ONE control: a `FolderActionsMenu` trigger. `folder-urgency-sort-<cwd>`, `pin-dir-btn`/`unpin-dir-btn`, `ws-remove-<wsId>-<cwd>` and `folder-open-home-<cwd>` are GONE as cluster buttons; `mdiOpenInNew` is DELETED outright (the header row `folder-home-row-<cwd>` is the only open affordance, and the leaf gains `group-hover:underline` under a `group` row). New state `folderMenuFor` keyed by SCOPE `folder:<cwd>` (mirrors `addToWsMenuFor`; a cwd key would co-open a folder row and a same-cwd card). New builder `folderMenuItems({group,isPinned,inWorkspace,workspaceId,headerAction})` maps the old buttons to items — workspace group: `add-to-workspace` (the `headerAction` node, so `add-to-workspace-btn-<cwd>` + its popover survive verbatim), `remove-from-workspace`; directory group in order `pin` · `urgency-sort` · `directory-settings`. Gating is preserved, not widened: add-to-workspace still only where `renderGroupWithWorkspaceMenu` supplies the node (top-level, `onCreateWorkspace || workspaces.length`), remove only on workspace-owned rows, pin only outside a workspace container. `renderAddToWorkspaceButton` gains a 6th param `asMenuItem` (adds `role="menuitem"`), and its `onPick`/`onNewWorkspace`/`onRemoveFromWorkspace` now also clear `folderMenuFor` so a terminal action does not leave the outer menu open. Pinned state is an INERT `mdiPin` indicator (`folder-pinned-indicator-<cwd>`, `aria-hidden`) in the NAME region — deliberately not in the cluster, which must stay at one control. The `renderAddToWorkspace` render prop passed to `SessionCard` is REMOVED. Prop `onOpenPiResources` renamed `onOpenDirectorySettings` (route unchanged; only the name lagged behind the rendered "Directory Settings" label).

Keyboard route to the directory home (change: add-folder-actions-menu, CodeRabbit review): `folder-header-name-<cwd>` carries `role="link"` + `tabIndex={0}` + an Enter `onKeyDown` that `preventDefault`/`stopPropagation`s then `navigate(buildFolderHomeUrl(cwd))`, plus `focus-ring`. Semantics sit on the NAME region, not on `folder-home-row-<cwd>`: the row also hosts the `FolderActionsMenu` trigger, and a `button` may not nest inside a `role="link"`. Deleting `folder-open-home-<cwd>` removed the only focusable open control, so without this the whole-row gesture was pointer-only.

Folder header card carries NO `overflow-hidden` (change: fix-folder-menu-clip). The card is the `FolderActionsMenu` popover's positioning ancestor; clipping it hid the menu at the card bounds. Nothing inside needs clipping — the tab nub is a sibling. Regression test in `SessionList.test.tsx`: walk `folder-actions-menu-panel-<cwd>` ancestors, assert none match `overflow-hidden`.
