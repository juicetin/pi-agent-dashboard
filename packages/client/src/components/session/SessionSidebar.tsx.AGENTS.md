# SessionSidebar.tsx — index

Legacy compact session sidebar. Active + ended (`<details>`) lists with status dots, source icons, model, token/cost counters, current tool. Inline rename via `InlineRenameInput`. Exports `SessionSidebar`.
