# SortablePinnedGroup.tsx — index

dnd-kit sortable wrapper for a pinned folder group (`data.type: "pinned-group"`), drop indicator via `dropIndicatorProps`. Exports `SortablePinnedGroup`, `FolderDragHandleCtx`, `useFolderDragHandle` (context channel for drag-handle props).
