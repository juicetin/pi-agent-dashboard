# SpawnErrorBanner.tsx — index

Renders structured spawn error: code→hint, preflight reasons, stderr details, timeout banner. Error shades → `--severity-error-*`; `TimeoutBanner` amber → `--severity-warning-*`. See change: spawn-failure-diagnostics, unify-message-severity-colors.
