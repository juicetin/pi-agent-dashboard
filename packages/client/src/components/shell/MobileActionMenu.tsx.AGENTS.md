# MobileActionMenu.tsx — index

Kebab session-action menu for mobile. Rows: rename, hide/unhide, resume/fork, native editors, OpenSpec Explore/+New Change/Continue/FF/Apply/Verify/Archive/Detach (state-aware via `deriveChangeState`), refresh, exit (confirms if streaming). Opens `ExploreDialog`/`NewChangeDialog` via `DialogPortal`. Exports `MobileActionMenu`.
