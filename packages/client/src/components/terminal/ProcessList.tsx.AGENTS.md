# ProcessList.tsx — index

Repurposed as BackgroundProcessesDrawer (filename kept). Renders bridge PGID scan as collapsible drawer under SessionActivityBar. Controlled `expanded` + `onToggle` props; parent (SessionCard) owns per-session toggle state. Summary row `⚠ N background process(es)` clickable, `aria-expanded`. Per-row `✕` invokes `onKill(pgid)` (force-kill PGID via bridge `force_kill`); tooltip `Force-kill process tree` exported as `KILL_TOOLTIP`. MIN_SLOTS skeleton padding removed; SessionActivityBar above provides stable surface. MAX_VISIBLE=5 ceiling + `+N more processes` overflow tail preserved. `compact` prop for mobile. Exports: `ProcessList`, `computeVisibleRows`, `KILL_TOOLTIP`, `ProcessEntry`. See change: tighten-process-list-ux. See change: redesign-process-list-activity-bar.
