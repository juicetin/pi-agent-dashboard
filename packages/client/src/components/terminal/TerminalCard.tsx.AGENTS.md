# TerminalCard.tsx — index

Sidebar terminal card. Cyan border, console icon, name (`InlineRenameInput` rename), relative age, rename/close actions, cwd subline. Props: `terminal` (`TerminalSession`), `selectedId`, `onSelect`, `onClose`, `onRename`, `now`. Exports `TerminalCard`.
