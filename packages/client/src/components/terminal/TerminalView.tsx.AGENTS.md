# TerminalView.tsx — index

xterm.js terminal emulator wrapper with keep-alive. Adds `heightPx?` bounded fixed-height variant for inline use (explicit height, not flex-1). See change: add-inline-terminal-card. `cursorBlink: false` (was true), stops per-second cursor repaint. See change: throttle-idle-ui-animations.
