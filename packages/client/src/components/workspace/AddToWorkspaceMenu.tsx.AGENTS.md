# AddToWorkspaceMenu.tsx — index

Popover menu listing workspaces plus `+ New workspace…` entry + remove-from-workspace (when owned). Surfaced from the folder-row + session-card add-to-workspace icon buttons (`renderAddToWorkspaceButton` in SessionList). NO "Pin to dashboard" entry — a sidebar folder is already visible. Exports `AddToWorkspaceMenu`. Closes on outside click / Escape. See change: redesign-folder-workspace-add-flow.
