# PinDirectoryDialog.tsx — index

Single-select pin-directory dialog (wraps `PathPicker` single-select). Retained ONLY for the packages move-to-local picker (`UnifiedPackagesSection`); the sidebar/workspace add flows now use `AddFoldersDialog`. See change: redesign-folder-workspace-add-flow.
