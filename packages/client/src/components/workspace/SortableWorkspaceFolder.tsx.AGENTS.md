# SortableWorkspaceFolder.tsx — index

dnd-kit sortable wrapper for a folder inside a workspace (`data.type: "workspace-folder"`, carries `wsId`). Reuses `FolderDragHandleCtx`. Drop indicator on `isOver`. Exports `SortableWorkspaceFolder`. See change: `workspace-directory-drag-reorder`.
