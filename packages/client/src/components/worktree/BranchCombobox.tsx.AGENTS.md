# BranchCombobox.tsx — index

Collapsed typeahead combobox. Trigger button + popover with filter input + `BranchListbox`. WAI-ARIA combobox pattern. Used by `WorktreeSpawnDialog` base-branch field. See change: worktree-base-branch-typeahead
