# BranchListbox.tsx — index

Presentational branch list. Splits local/remote with separator. Current-branch `●` marker. Remote badge. Exports `useBranchListboxKeyboard` hook (ArrowUp/Down + Enter). Shared by `BranchPicker` and `BranchCombobox`. See change: worktree-base-branch-typeahead
