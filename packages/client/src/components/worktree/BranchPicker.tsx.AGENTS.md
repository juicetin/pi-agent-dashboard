# BranchPicker.tsx — index

Typeahead branch picker with keyboard navigation. Delegates row rendering + keyboard nav to `BranchListbox`. Owns fetching, filter input, loading/error states, Cancel button. See change: worktree-base-branch-typeahead
