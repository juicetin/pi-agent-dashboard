# MergeConfirmDialog.tsx — index

Fetches `/api/git/worktree/diff-stat`; renders 5-line summary; delete-branch checkbox. `merge_conflict` stderr in `<details>`. See change: add-worktree-lifecycle-actions. Migrated to client-utils Dialog/Confirm shell. See change: unify-dialog-system.
