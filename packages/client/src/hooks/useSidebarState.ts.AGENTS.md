# useSidebarState.ts — index

Persists sidebar `width` + `collapsed` to `localStorage` (`dashboard:sidebar-width`, `dashboard:sidebar-collapsed`). Clamps width 180–500. Exports `useSidebarState`, `SidebarState`, `MIN_WIDTH`/`MAX_WIDTH`/`DEFAULT_WIDTH`/`WIDTH_KEY`/`COLLAPSED_KEY`.
