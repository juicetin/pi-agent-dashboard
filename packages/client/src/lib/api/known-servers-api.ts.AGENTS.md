# known-servers-api.ts — index

Client fetch helpers for known-servers management. Exports `listKnownServers`, `addKnownServer(host, port, label?)`, `removeKnownServer(host, port)`, `discoverServers`. Hits /api/known-servers + /api/discover-servers.
