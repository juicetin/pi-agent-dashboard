# gateway-providers.ts — index

Client provider matrix metadata. Exports `GatewayProviderId`, `GATEWAY_PROVIDERS` (zrok/ngrok public; tailscale both; zerotier private), `providerMeta`, `supportsMode`. Mirrors server `TunnelProvider` capability flags. See change: add-tunnel-providers.
