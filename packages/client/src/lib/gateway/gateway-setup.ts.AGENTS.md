# gateway-setup.ts — index

Per-provider setup-step model (D3 taxonomy). Exports `SetupStepKind` (install/auth-token/activate/browser-auth/external), `SetupStep`, `GATEWAY_SETUP_STEPS`. Presentational; server enforces the real recipe/param boundary. See change: add-tunnel-providers.
