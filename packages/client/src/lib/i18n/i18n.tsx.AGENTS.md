# i18n.tsx — index

i18n provider + `t()` translator. Exports `Language` ("en"|"zh-CN"|"hu"), `LANGUAGE_OPTIONS`, `t(key, vars?, fallback?)`, `I18nProvider` (React context, persists choice to `localStorage` key `pi-dashboard-language`, detects via stored→`VITE_PI_DASHBOARD_DEFAULT_LANGUAGE`→browser), `useI18n`. Auto-generated `zh-CN` string map.
