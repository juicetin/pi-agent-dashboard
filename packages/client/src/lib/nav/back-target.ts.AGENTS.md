# back-target.ts — index

Pure `computeBackTarget(route): string \| null` — maps route to parent route one shell depth up. depth 0 → null; depth 1 → `/`; `/session/:id/diff` → `/session/:id` (strip `/diff`); ambiguous depth-2 overlays (openspec/pi-resources/pi-resource/view) → `/`. Exports `routeDepth(url)` + `parseRouteDepthInput(url)` — parse URL string to `MobileDepthInput`, reuse `getMobileDepth` (single-source depth derivation). See change: fix-mobile-back-depth-aware. readme overlay dropped. See change: remove-project-readme-button. `/folder/:cwd/settings*` → depth 1. See change: directory-settings-page-and-scoped-md-editing. Added `/session/:id/editor` static descriptor (depth 2, computeParent=parentSession) to STATIC_DESCRIPTORS. Internal Monaco editor pane route missing from table → routeDepth 0 → goBack dead no-op. See change: fix-plugin-and-scoped-back-navigation (5.2 smoke fix).
