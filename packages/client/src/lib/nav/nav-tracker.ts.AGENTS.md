# nav-tracker.ts — index

In-app depth-tagged nav stack. `Array<{url, depth}>` via `routeDepth`. `recordNavigation(url, {replace})` appends, dedupes consecutive identical url (StrictMode guard), `replace` overwrites stack top. `predecessor()` returns `stack[len-2]` or undefined. `popNav()` drops top. `initNavTracker()` attaches single `popstate` listener, returns detach fn; `handlePopState(url)` realigns (pop on back, record on forward). `resetNavStack(initialUrl?)` seeds. Feeds depth-aware `goBack`. See change: fix-mobile-back-depth-aware. `initNavTracker()` patches `history.pushState`/`replaceState` (composes over wouter patch); navigations bypassing App-wrapped `navigate` (plugin raw `useLocation`, session-card routing, `Link`) record into nav stack. Fixes run-monitor Back going to `/` instead of launching route. Restore idempotent; teardown unpatches. See change: fix-plugin-and-scoped-back-navigation.
