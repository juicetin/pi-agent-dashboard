# openspec-groups-api.ts — index

Fetch helpers for `/api/openspec/groups` CRUD + assignment endpoints. Adds `setChangeOrder(cwd, {groupId, order})` → PUT `/api/openspec/groups/change-order`. See change: add-openspec-change-grouping. See change: redesign-openspec-board.
