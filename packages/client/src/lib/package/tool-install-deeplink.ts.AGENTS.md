# tool-install-deeplink.ts — index

Deep-link bus between `MissingToolInlineError` and `ToolsSection`. `OPEN_TOOL_INSTALL_EVENT` window event + `requestToolInstall()`/`consumePendingToolInstall()` pending-target. Leaf module, no cycle. See change: register-bash-and-tool-install-help.
