# file-icon.ts — index

`fileIcon(pathOrName)` → `{ iconPath, colorClass }`. Extension-keyed `@mdi/js` glyph + accent color for editor-pane tree rows + tabs (#2). Finer than `fileKind` coarse kind (separates `.ts`/`.json`). Fallback `mdiFileOutline`. See change: improve-content-editor.
