# mdi-icon-lookup.ts — index

Extension UI System icon resolver. Exports `resolveMdiIcon(key)` — maps `"mdiCheckCircle"`-style key to `@mdi/js` SVG path string, returns `null` for missing/non-`mdi` keys (never errors). See change: add-extension-ui-modal.
