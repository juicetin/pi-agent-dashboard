# tree-visible.ts — index

`useTreeVisible(sessionId)` + load/save. Persists editor-pane rail show/hide boolean under `pi-dashboard:tree-visible:<sessionId>`. Defaults collapsed (`DEFAULT_VISIBLE=false`); user reveal persists per session. Mirrors `rail-width.ts`. See change: improve-content-editor (#6). See change: collapse-files-panel-by-default.
