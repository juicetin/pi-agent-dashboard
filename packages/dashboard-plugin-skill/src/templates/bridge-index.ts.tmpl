/**
 * {{ displayName }} — pi bridge extension entry.
 *
 * Loaded as a pi extension on every pi session start. Use this only if your
 * plugin needs in-pi-process state (activity detection, custom event emission,
 * tool registration). Most plugins don't need a bridge entry.
 *
 * Auto-registered at ~/.pi/agent/extensions/dashboard-{{ id }}/ when the
 * plugin is enabled; auto-deregistered on disable.
 */
import type { ExtensionContext } from "pi-coding-agent";

export default function activate(ctx: ExtensionContext): void {
  void ctx;
  // Example: register a tool that the dashboard's tool-renderer slot will render.
  // ctx.pi.registerTool({
  //   name: "{{ ToolName }}",
  //   description: "Example tool from {{ displayName }}",
  //   parameters: { type: "object", properties: {}, required: [] },
  //   handler: async () => ({ ok: true }),
  // });

  // Example: emit a custom event the server entry can listen for.
  // ctx.events.emit("{{ id }}:ready", { at: Date.now() });
}
