/**
 * {{ displayName }} — dashboard plugin client entry.
 *
 * Each exported component below is claimed by `pi-dashboard-plugin.claims[]`
 * in package.json. Remove sections whose claims you don't need.
 */
import React from "react";
import {
  usePluginConfig,
  usePluginSend,
  useSessionState,
  useAllSessions,
} from "@blackbelt-technology/dashboard-plugin-runtime/context";
import type { SlotProps } from "@blackbelt-technology/pi-dashboard-shared/dashboard-plugin/slot-props";

export interface {{ ConfigTypeName }} {
  // TODO: Define your plugin's config shape. Should match configSchema.json.
}

{{ slotSections }}
