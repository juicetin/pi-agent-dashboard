# browser/references/electron.md — index

Vendored agent-browser Electron-app automation reference. Launch w/ `--remote-debugging-port`, `agent-browser connect <port>`, tab/webview targets, `--session`. Worked example: PI Dashboard `--debug-cdp` / `PI_DEBUG_CDP`, port 9222, `npm run dev:cdp` (packages/electron).
