# browser/references/web.md — index

Vendored agent-browser core reference. Core loop: `snapshot -i` → @eN refs; waits, semantic `find`, tabs, iframes, dialogs, `--session`, `network route`, react devtools, `doctor`. Bundles authentication/commands/profiling/proxy/session-management refs.
