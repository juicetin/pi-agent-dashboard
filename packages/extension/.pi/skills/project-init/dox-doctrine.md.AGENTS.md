# project-init/dox-doctrine.md — index

Canonical DOX doctrine; seeds one block into project AGENTS.md (marker `<!-- dox-doctrine -->`). Delimited write/read blocks; `start`/`end` marker pairs `dox:write`, `dox:read:kb`, `dox:read:manual`. Maintain section outside markers — NOT seeded; `kb_search "dox maintenance"`.
