# auto-session-namer.ts — index

Automatic session topic-naming (bridge-side). Pure helpers `shouldSkipByPrefilter`, `parseTitle`, `buildTranscriptWindow`, `isEligible`, `classifyNameChange`, `generateTitle` (in-process pi-ai `streamSimple` + `registry.getApiKeyAndHeaders`; hard-error on unknown model / OAuth-only-unauthable, soft-error on transient), `SUMMARIZER_SYSTEM_PROMPT`. `createAutoNamer(hooks)` = per-session state machine: eligibility gate, pre-filter, model call, `NULL`/over-long wait, once-only auto-name, provenance latch (auto→external→user lockout via `onObservedName`), one-shot `emitError`. `sanitizeSessionName` (newline-collapse + trim, pi's own sanitization) applied inside `classifyNameChange` both sides so a newline-bearing self-title matches the sanitized `session_info_changed` name (self-lockout regression guard). See change: add-auto-session-naming, adopt-pi-074-080-features.
