# bridge-context.ts — index

Shared mutable bridge state + pure predicates. Exports `BridgeContext`, `DASHBOARD_NATIVE_COMMANDS`, `filterHiddenCommands`, `isExtensionSlashCommand`, `hasDispatchCommand`, `isHeadlessRpcSession`, `extractFirstMessage`, `extractFirstAssistantReply` (first assistant text, ≤2000 chars, for the auto-name transcript window; see change: add-auto-session-naming), `getCurrentModelString`. `filterHiddenCommands` also maps pi's `sourceInfo.path` onto the declared `CommandInfo.path` (tolerating absence, never overwriting an existing `path`) — it is the one chokepoint all five `commands_list` senders pass through, so the server's retained list always carries joinable skill paths. See change: fix-skill-discovery-parity. Stops 14+ closure vars passing to every extracted fn.
