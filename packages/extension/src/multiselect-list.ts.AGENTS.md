# multiselect-list.ts — index

TUI multi-select component implementing pi-tui `ComponentLike`. Exports `MultiSelectList`, `ComponentLike`. Keyboard: arrows/jk move, space toggle, enter confirm, esc cancel. Legacy fallback renderer for `polyfillMultiselect`.
