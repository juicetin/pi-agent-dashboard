# role-model-tools.ts — index

Agent-facing tools registered via `pi.registerTool` (capability agent-role-model-tools). Exports `registerRoleModelTools(pi,{getRegistry})`, `buildModelRows`, `buildModelsResult`, `ModelRow`, `ModelsResult`. `list_models` (read, roles-independent, source = `getRegistry().getAvailable()` = ModelSelector path; `annotated` derives `excludedReason` from `getAll()`\`getAvailable()` since pi ModelRegistry lacks `getAllAnnotated()`; custom flag via `getCustomProviderNames()`; result carries `registryReady` discriminator via `buildModelsResult` — falsy registry ⇒ `{ models:[], registryReady:false, reason }` (spawn-before-hydration, retry), else `registryReady:true`; disambiguates empty-vs-unhydrated; annotated obeys same discriminator. See change: fix-list-models-empty-on-unhydrated-registry), `list_roles` (read, bound-only, no models key), `update_roles` (write, discriminated `action`, every mutation gated behind `ctx.ui.confirm`, routes through role-manager accessors + atomic write). See change: add-agent-role-model-tools.
