# component-architecture/SKILL.md — index

Reusable component patterns: folder structure (`ui/layout/sections/cards/forms/shared/seo`), `ServiceCard`/`ServicesPreview`/`Container`/`SectionHeader`/`CTAButton` templates, compound `PricingCard` composition, props-interface naming conventions, `EmptyState` pattern. Trigger on new-component/refactor work.
