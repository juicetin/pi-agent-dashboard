# DOX — packages/kb/src

Files in this directory. One row per source file.

| File | Purpose |
|------|---------|
| `chunker.ts` | Structural heading chunker. Fence-safe, breadcrumb-aware. Line-based fenced-code state machine. |
| `cli.ts` | kb CLI. `search` flags `--no-source-dedup`/`--no-lane-quota`/`--no-coverage-rerank`; `--limit` bounds distinct SOURCES. `dox lint --source-rows` enables the D9 source arm; text mode prints a coverage line (`N files, M rows scanned, K findings`) and `--json` carries `filesScanned`/`rowsScanned`. See change: fix-dox-lint-blind-rows. `get` appends a `(+N more sections …)` marker on a path-only fetch. See change: fix-kb-search-retrieval-quality. Commands init\|index\|search\|neighbors\|backlinks\|get\|agents\|dox\|eval\|config (`dox init\|lint\|triage`). Dev run NODE_OPTIONS=--experimental-sqlite tsx src/cli.ts. `index` routes through `runIndexAtomic` (index-run.ts) and does NOT `openStore` — opening pre-creates the DB husk. `search`/`eval` still `openStore` + auto-reindex. `search` non-json branch calls shared `renderHits(leading:"score")`. `search` + `eval` build SearchOpts via `searchOptsFromConfig` (search-opts.ts); `eval` accepts both fixture shapes (`loadGolden`), normalizes expects against roots, reports `unreachable`, and exits non-zero on a vacuous run (0 scored items or 0 recall) unless `--allow-zero`. See change: harden-kb-index-failure-atomicity; slim-kb-search-output; fix-kb-eval-measurement-integrity. |
| `config.ts` | Config layering. project .pi/dashboard/knowledge_base.json → global ~/.pi/dashboard/knowledge_base.json → defaults. No file-count cap default. `ranking.sourceDedup` (default ON), `ranking.laneQuota` 0..1 (default **0.5** — agents-lane share of the page; validated in `validateConfig`; 0 disables), `ranking.laneLeadMargin` 0..1 (default **0** = OFF — relative score margin letting the reserved agents lane take slot 1; validated like `laneQuota`; inert when `laneQuota` is 0 or an explicit `docType` is passed; rollback is an explicit `0`, NOT removing the key. Measured: no grid margin cleared the paired-fixture bar — see `openspec/changes/fix-kb-search-lane-composition/measurements.md`. See change: fix-kb-search-lane-composition), `ranking.coverageRerank` (default **OFF**), `queryExpansion.mode` (default **off**) + `queryExpansion.prf {terms,topK,dfCeiling}`. coverageRerank/prf implemented but default-off: measured net regression on the bundled fixtures (see `openspec/changes/fix-kb-search-retrieval-quality/measurements.md`). See change: fix-kb-search-retrieval-quality. |
| `__tests__/lane-lead.test.ts` | Vitest for the lead-slot rule (change fix-kb-search-lane-composition). `laneLeadMargin` config BVA; margin 0 ⇒ interleaving byte-identical to pre-change; competitive `agents` candidate leads slot 1 (and only once under source dedup); non-competitive leaves the page unchanged; explicit `docType` and `laneQuota: 0` make the knob inert; slot 2 returns to the main lane; margin 1 leads unconditionally; a `coverageRerank` fixture pins the decision to RAW scores. Lane gaps are asserted as PRECONDITIONS so corpus drift fails loudly instead of passing on the wrong branch. |
| `__tests__/sweep-rows.test.ts` | Vitest for `eval/sweep-rows.ts` paired-fixture invariant: a sweep row carries both metric groups, and a missing or zero-scored fixture throws. See change: fix-kb-search-lane-composition. |
| `__tests__/dox-triage.test.ts` | Vitest for `dox-triage.ts`. Row parsing (targeted replace, byte-identical elsewhere, no-op on absent path) + `resolveBaseline`/`buildWorkItems` against a real temp git repo. Asserts the diff spans UNCOMMITTED working-tree edits. |
| `__tests__/gitignore.test.ts` | Vitest for `gitignore.ts`. Matcher decision table (bare/dir//glob/`**`/anchor/case forms, negation, malformed-line skip), loader seeding (up-walk from nested walk root), negation + deeper-override, graceful no-gitignore degrade. See change: fix-dox-lint-blind-rows. |
| `__tests__/verdict.test.ts` | Vitest for `verdict.ts`. All five labels, worst-of aggregation + counts, cap 8/1 MiB boundaries, stat-baseline read skip (counting fake fs), v1/v2/future sidecar tolerance, read-only + fault injection (EACCES, git failure), D1 order identity, disk-body resolution. Temp-git-repo pattern. |
| `dox-triage.ts` | Stale-row triage. `kb dox lint` stale = bytes changed, NOT row wrong. `resolveBaseline` walks file history for the commit whose blob sha256 == acked hash → `git diff <commit> --` (working tree, NOT `..HEAD`: staleness hashes the working tree, so `..HEAD` misses uncommitted edits and yields no evidence). Exports `parseRows`, `replaceRowPurpose` (row-targeted, byte-identical elsewhere), `resolveBaseline`, `buildWorkItems` (reuses `resolveRowPath` so triage and lint never disagree), `applyDecisions`, `ackTargets`. `baselineFound:false` = no trustworthy baseline, needs human eyes. |
| `dox.ts` | DOX tree. Directory-level AGENTS.md scaffold + audit. kb agents <path> nearest-applicable chain. Detect-don't-write: dox init/--fix fill PATH columns + prune orphans only. `over-threshold` lint splits two arms: `arm:"bytes"` (actionable — bytes > `AGENTS_BYTE_CAP` 30 KB, auto-injected per turn, remedy sidecar split) + `arm:"rows"` (informational — inline count > `ROW_CAP` 40, no per-turn cost, optional foldering). `countInlineRows(af)` counts INLINE rows only (excludes sidecar-pointer rows matching `/→ see \`<File>.AGENTS.md\`/`) — sibling to `parseRowPaths` (public export consumed by kb-extension; signature untouched, stays complete path list). Splitting a heavy row to its sidecar drops both byte + counted-row totals. `ROW_CAP`/`AGENTS_BYTE_CAP` numeric values unchanged. `isMdFile` excludes `*.AGENTS.md` sidecars + `*.agent.md` companions (both DOX index artifacts, not documentable source). `DEFAULT_EXCLUDE` skips build output (`out`) + electron bundled/vendored trees (`bundled-extensions`, `electron/resources/server`) + `.reverse-spec-scratch` (transient reverse-spec experiment output) — all gitignored, fs-walk-only, else bogus missing/companion rows; `server` scoped to `electron/resources/server` so real `server` source dirs stay indexed. `resolveRowPath(agentsDir,cwd,rp)` resolves rows relative to their OWN AGENTS.md dir, repo-root `cwd` fallback (nested AGENTS.md may document a file living at project root) before orphan. See change: fix-dox-lint-blind-rows. `parseRowPaths` + `doxLint` recognize rows by TABLE HEADER (`| File | Purpose |`, case-sensitive, via shared `scanDoxRows(text)` state machine) — never by heading state; second delimiter closes the table (adjacent prose table's backticked cells stay unscanned); `emptyFileTables` yields `zero-row-table` findings. `DoxLintResult` carries `filesScanned`/`rowsScanned` (CLI prints the coverage line). Walks (`walkMd`/`walkSource`/`walkAgents`) honour `.gitignore` via `gitignore.ts`, conservative dir-prune (descend when a deeper `.gitignore` could negate). `--fix` prunes orphan `lineIndex`es only; `--fix` missing arm + `doxInit` share one table-aware row-append helper (create-template carries header+delimiter; idempotent). `doxLint` `walkAgents` tests `DEFAULT_EXCLUDE` on the cwd-RELATIVE path (mirrors `walkFiles`) so running inside `.worktrees/` does not exclude the whole tree. `rowPaths`/staleness keyed cwd-relative. `DEFAULT_EXCLUDE` scopes `.pi` narrowly: walks `.pi/skills/`, `.pi/agents/`, `.pi/prompts/` (carry DOX rows); skips `.pi/{dashboard,npm,git,flows}` (caches, kb index, mirrors, run state). Blanket `.pi` exclusion previously blinded the orphan check on the whole tree. `doxLint({sourceFileRows:true})` adds the D9 SOURCE arm: a `.ts`/`.tsx` in a covered area with no row in ANY ancestor AGENTS.md and no `<file>.AGENTS.md` sidecar reports `missing` with `arm:"source"` (markdown findings carry no arm). Opt-in (CLI `--source-rows`), OFF by default — 70 findings on this repo, so default-on would red-wall CI — and never auto-fixed in `--fix` (a blank purpose row is worse than an honest finding). See change: split-components-agents-dedup-rollup. See change: fix-dox-lint-false-positives. See change: fold-oversized-agents-directories. See change: fix-kb-search-retrieval-quality. |
| `eval.ts` | Retrieval-quality eval. Scores search against golden set. Gates ranking changes. `EvalMetrics` adds result-redundancy metrics `distinctSourcesAtK`, `duplicateSlotShare`, `singleSourcePageRate` — P@1/recall are blind to page composition by construction (rank-1 hit scores the same whether ranks 2–10 are nine files or nine copies). Sources keyed `(root, path)`. `loadGolden(raw, file)` accepts bare array + `{items}` object, validates item shapes (string q/expect; file+index on rejection), provenance header to stderr. `evaluate` gains `roots` (two-tier expect normalization: longest separator-checked prefix strip + raw candidate kept) and filesystem-anchored reachability — `unreachable` items are counted in the `unreachable` metric, never searched, excluded from every rank/redundancy/latency metric; `n + unreachable === total`; path list behind `--verbose`. See change: fix-kb-search-retrieval-quality; fix-kb-eval-measurement-integrity. |
| `gitignore.ts` | Gitignore-honouring walk filter (design D3). Case-sensitive git semantics: bare names, `dir/`, `*.ext`, mid-name globs, `**` leading/mid/trailing, bare `*` + file negations, `!` negation (dir + content + root-level file forms), leading-slash anchors; last-match-wins within a file, deeper `.gitignore` overrides shallower. Pattern stack seeded by up-walk from walk start to repo root (`.git` boundary or `cwd`), then descend; `hasDeeperGitignore` gates conservative dir-pruning so deeper negations survive. Malformed line (unbalanced bracket) skipped, never thrown. Exports `loadGitignoreMatcher`, `patternMatches`, `GitignoreMatcher`. NOT implemented: tracked-file override, character classes. Consumers: `dox.ts` walks, `indexer.ts` walk (gated on `respectGitignore`). See change: fix-dox-lint-blind-rows. |
| `index-run.ts` | Atomic `kb index` orchestration. `runIndexAtomic({dbPath,sources,indexOpts,explicit})` returns `IndexStats & {counts}` (counts read from the still-open store so `index` never re-`openStore`s). First index (no dbPath) builds into `<dbPath>.tmp-<pid>` + `rename()` on success (survives OOM/SIGKILL — real path only on success); incremental (dbPath exists) indexes in place (mid-run failure leaves prior DB valid & queryable). All-sources-missing + explicit-`--source`-missing throw BEFORE any store opens (no husk). `sweepOrphanTemps` unlinks stale `.tmp-*` but SKIPS a temp whose PID is a live process (concurrent peer's active build). See change: harden-kb-index-failure-atomicity. |
| `index.ts` | Public API barrel for @blackbelt-technology/pi-dashboard-kb. Exports `renderHits` + `RenderOpts`; exports `searchOptsFromConfig` + `SearchOptsOverrides` (shared ranking-option mapping, See change: fix-kb-eval-measurement-integrity). |
| `indexer.ts` | Indexer. `indexSource` is ASYNC: walks source, mtime→sha256 change detection, structural chunking, Tier-1 graph extraction, BATCHED transactional upsert — commits + `await setImmediate` every `YIELD_EVERY`=100 files so the walk yields the event loop (a concurrent `/stats` reader observes `indexing`+live counts) and releases the WAL write lock; trades whole-walk atomicity for responsiveness (mid-walk throw leaves earlier batches committed; reindex idempotent). All callers must `await`. A source whose `dir` does not exist is skipped with `console.warn` (returns `missing:true`), not a thrown ENOENT — partial source set still indexes what exists. `docTypeOf` classifies `*.AGENTS.md` per-file index sidecars as `agents` doc_type (searchable; name != `AGENTS.md` so pi never auto-injects them). Walk honours `.gitignore` via `gitignore.ts` when `respectGitignore` is not explicitly false (config default true) — makes the long-declared default real; `cwd` option bounds the up-walk. See change: fix-dox-lint-blind-rows. See change: split-components-agents-dedup-rollup; fix-kb-index-feedback; harden-kb-index-failure-atomicity. |
| `init.ts` | kb init. Scaffolds + validates knowledge_base.json. --global writes global file. --force, --dry-run flags. gitignores dbPath. |
| `migrate-file-index.ts` | One-time migration core. Re-homes `docs/file-index-<area>.md` purposes into per-dir `AGENTS.md` tree. Pure + testable. Exports `parseFileIndex`, `mergeIndex`, `planDirs`, `tier0Rows`, `renderAgentsMd`, `validateAuthored`, `finalRows`, types `IndexRow`, `FileEntry`, `DirPlan`, `AuthoredRow`. Classifies dirs tier 0 (all hits, deterministic) vs tier 1 (≥1 miss, needs subagent). |
| `migrate-runner.ts` | Big-bang resumable runner for file-index → AGENTS.md migration (one-time; source splits since deleted). Owns plan, batching, grounding gate, idempotent per-dir write, checkpoint persistence. Exports `loadSplitTexts`, `buildDirPlans`, `groundingCheck`, `knownStems`, `makeBatches`, `subagentPrompt`, `parseAuthoredBatch`, `recordAuthored`, `writeDir`, `loadState`, `saveState`, `PACKAGES_ROOT_RE`. Drives `@fast` subagent fan-out for Tier-1 authoring. Rollup export (`exportRollup`/`treeRows`/`SPLIT_AREAS`) REMOVED — the legacy docs/ file-index pages are retired (splits since deleted), per-directory `AGENTS.md` tree is sole per-file record. See change: split-components-agents-dedup-rollup. See change: migrate-file-index-to-agents-tree. |
| `node-sqlite.d.ts` | Ambient shim `declare module "node:sqlite"` (DatabaseSync + StatementSync surface used by `sqlite-store.ts`). Pins @types/node 20.x lacks `node:sqlite` typings (ship in >= 22.5) so root `tsc`/kb build fail TS2307; shim resolves without a dep bump. Delete when @types/node >= 22.5. See change: adopt-pi-074-080-features. |
| `render.ts` | Shared hit renderer for CLI + kb_search tool. `renderHits(hits, { leading:"score"\|"rank", parentGlyph, multiline })`. Same fields/order both callers; differ on leading token (BM25 score vs 1-based rank ordinal), parent glyph, line structure. Renders the **leaf** heading (`leafHeading()` splits `headingPath` on ` > `), not the breadcrumb — 47% of a page was breadcrumb, 38% of it repeated inside one file group. Emits `(+N more sections)` from `KbHit.suppressedSections`. Emits a record-type mark `[agents]`/`[source-md]` into the same `marks` array; `doc` (the unmarked majority) emits nothing. Marks the RECORD type, not the lane origin. See change: fix-kb-search-lane-composition. BREAKING vs the legacy byte-identical CLI form. Measured −32.8% tokens/page at 2x distinct sources. See change: slim-kb-search-output; fix-kb-search-retrieval-quality. |
| `search-opts.ts` | Shared `searchOptsFromConfig(cfg, { sources, overrides })` — the ONE mapping of resolved config → ranking SearchOpts (13 keys: fieldWeights, proximityBoost, diversity, sourceDedup, laneQuota, laneLeadMargin, coverageRerank, queryExpansion, prf, expandParent, expandGraph, rerank, rootPriority). Callers: cli search/eval, kb-extension kb_search, eval/run-fixtures, eval/measure-render. `src/dox.ts` + `verify.ts` are deliberate store-default consumers. See change: fix-kb-eval-measurement-integrity. |
| `sources.ts` | Pluggable source resolvers. fs/npm/git/https → local dir. KB reads markdown only, never executes source. |
| `sqlite-store.ts` | Default KbStore backend over node:sqlite. FTS5. WAL + `PRAGMA busy_timeout=5000` (concurrent reader waits for a batch commit, not `SQLITE_BUSY`). Zero runtime deps. Requires --experimental-sqlite. better-sqlite3 drop-in fallback. `readonly dbPath`. `finalizeRename(dest)`: TRUNCATE-checkpoint + close BEFORE `rename` (WAL ordering load-bearing), then drop stale sidecars. `closeAndUnlink()`: close + unlink db + `-wal`/`-shm` (failed first-index cleanup). `expandParent` collapses `h.parent` to `{ headingPath }` (dropped root/path/docType/chunkId/score/snippet — same-file dups/constants). `search()` pipeline: body-hash collapse → **source dedup** (one hit per `(root,path)`, rep = best BM25, rest counted in `suppressedSections`) → MMR → optional coverage rerank → **lane interleave**. `limit` bounds distinct SOURCES (BREAKING); candidate pool `limit*6` when source dedup is on, capped 4000. Lane quota runs a SECOND FTS pass restricted to `doc_type='agents'` (shallow pool `limit*share*2`) and interleaves — `doc_type` is an UNINDEXED FTS5 column so that pass is a full match scan and is the change's main latency cost (~25→53 ms median at 31k chunks). Explicit `docType` bypasses the quota. **Lead-slot rule**: the running-share test `(taken+1)/(out.length+1) <= share` is structurally false at slot 1 for every share < 1, so `laneLeadMargin > 0` adds a separate slot-1 contest — reserved leads when `r0 - m0 <= margin * \|m0\|` on RAW BM25(+proximity) bests captured BEFORE `coverageRerank` re-sorts a lane (MMR reorders only). Sign-safe because scores are negative/ascending-better; at margin 1 it degenerates to an unconditional lead. The lead pick is an ordinary reserved take (increments `taken`, populates `seen`), so slot 2 reads `2/2 > share` and yields. See change: fix-kb-search-lane-composition. `coverageRerank()` = IDF-weighted coverage of ORIGINAL query terms (appended PRF terms at 0.5x) with BM25 tiebreak; `prfTerms()` = RM3 mining gated on `MIN_FEEDBACK_DOCS` 5. df/IDF from a lazily created `chunks_vocab` fts5vocab shadow table keyed by exact porter stems (`stems()` temp-table `kb_stem` mapping; exact df, not prefix-range estimate; read-only-DB safe). `getChunk(root,path)` without a heading now reports `suppressedSections`; `getChunks()` returns the whole file. See change: fix-kb-index-feedback; harden-kb-index-failure-atomicity; slim-kb-search-output; fix-kb-search-retrieval-quality. |
| `trust.ts` | TOFU trust store for remote sources. fs sources skip trust. npm/git/https confirm on first fetch. Keyed by sha256(canonical(SourceSpec)). |
| `types.ts` | KbStore interface + chunk types. Storage accessed only through KbStore. `KbHit.parent` narrowed to `{ headingPath } \| null` — display-only, non-recursive, NOT a refetch key. BREAKING public-type change. Adds `KbHit.suppressedSections` (further matching sections of the same source collapsed by source dedup; 0 = matched once) + `Chunk.suppressedSections` (path-only `getChunk` truncation marker) + `SearchOpts.{sourceDedup,laneQuota,coverageRerank,prf}` + `SearchOpts.laneLeadMargin` (0 = off; lead-slot rule, see change: fix-kb-search-lane-composition). See change: slim-kb-search-output; fix-kb-search-retrieval-quality. Adds `KbHit.verdict` (`HitVerdict \| null` — trust label, never ranking) + `KbHit.coverage` (opt-in 0..1) + `VerdictLabel/VerdictCounts/HitVerdict`. See change: add-kb-trust-verdicts-and-search-guard. |
| `verdict.ts` | Query-time trust verdicts (arm A). `enrichHits(hits, ctx)` labels `agents` hits worst-of `FRESH/STALE/MOVED/GONE/UNVERIFIED` over the section's resolvable DOX rows (cap 8, row order) + per-label counts incl. total. Read-only post-search, store untouched (D10); bodies default from DISK (chunks table is FTS5 — every getChunkById is a full scan). Existence precedes hash gate; stat baseline (sidecar v2) skips the read; >1 MiB / binary never hashed; MOVED via ONE batched `git diff HEAD --name-status -M`, non-git/ambiguous → GONE; unreadable → UNVERIFIED. Coverage = separate opt-in field, 256 KB cap. Label-only: ordering byte-identical on/off (D1). See change: add-kb-trust-verdicts-and-search-guard. |
