# DOX — packages/nano-banana/src/bin

Files in this directory. One row per source file.

| File | Purpose |
|------|---------|
| `nano-banana.ts` | `pi-nano-banana` CLI. Parses `<prompt> [--file] [--output/-o] [--model] [--flash] [--api-key]`, calls `generateImage`. Runs as TS via jiti. Exit 1 on missing prompt or generation failure. |
