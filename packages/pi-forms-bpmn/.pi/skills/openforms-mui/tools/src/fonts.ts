/**
 * Bundles the Roboto webfont (400/500/700) with the component so typography does
 * not silently fall back to a system face (task 10.6). Importing this module
 * (done by OpenFormsMui) pulls the font files into the consuming bundle.
 */
import "@fontsource/roboto/400.css";
import "@fontsource/roboto/500.css";
import "@fontsource/roboto/700.css";
