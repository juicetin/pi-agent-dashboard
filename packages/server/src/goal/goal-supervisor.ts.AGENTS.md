# goal-supervisor.ts — index

Goal session supervisor (main-server; owns GoalStore). `createGoalSupervisor(deps)` rides `dispatchPluginSessionEnded` (C2a, never reassigns `onUnregister`). `onDriverDeath(sessionId)`: classify death for the CURRENT `driverSessionId` only (C2e) — terminal/paused→ignore; `!autoRespawn`→`paused "session ended"`; `!headlessAvailable`→paused; crash-loop breaker (3 no-progress deaths / rolling 5min after `lastProgressAt` epoch, derived from persisted `respawns[]`)→`failed "crash loop"`; else `respawning`+`recordRespawn`+backoff timer. progress = strict `totalTurnsUsed` > `currentDriverBaselineTurns` (missing baseline=unknown, not breaker-counted). `performSpawn`: generation-guarded, mints token BEFORE launch, persists `inFlightSpawn`, resume (`spawnPiSession` continue-mode via resolved sessionFile) unless poisoned (`POISON_K=2` consecutive no-progress resume-deaths→fresh re-primed); stale-generation completion killed. `abort(cwd,id,terminal)`: `store.finalize` (gen++ + terminal) BEFORE host kill (`killByToken`\|`killBySession`) — kill-death is a no-op. `reconcileOnBoot()` classifies orphaned pursuing/respawning goals (deferred 30s in server.ts). Backoff `RESPAWN_BACKOFF_MS=[5s,15s,45s]` by consecutive-no-progress. See change: add-goal-session-supervisor.
