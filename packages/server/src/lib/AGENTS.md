# DOX — packages/server/src/lib

Files in this directory. One row per source file.

| File | Purpose |
|------|---------|
| `artifact-roots.ts` | NEW. Artifact-root allowlist for /api/file/raw image previews (layer ③). artifactRoots() returns realpath'd, cached list: join(homedir, ".agent-browser", "tmp") default + AGENT_BROWSER_SCREENSHOT_DIR env when set (same env var agent-browser CLI honors). Missing-dir roots dropped, not fatal. resetArtifactRootsCache() test-only. isImageUnderArtifactRoot(resolved) layer-③ check: ext in IMAGE_EXTS AND safeRealpath(resolved) within any root; realpath collapses ../symlink (D4); missing file resolves nearest ancestor → allowed → fs.stat 404 not 500 (A3/D7). IMAGE_EXTS mirrors client FilePreviewOverlay set. --screenshot-dir CLI flag uncovered (A1, best-effort). See change: serve-agent-artifact-previews. |
| `decode-file-uri.ts` | NEW. `decodeFileUri(value)` strips `file://` scheme + percent-decodes payload. Returns non-`file://` input and decode failures unchanged. See change: unify-file-link-openability. |
| `grep.ts` | Content grep engine. `runGrep` prefers `rg` (`parseRipgrepJson`,`grepWithRipgrep`), else bounded `grepWithJsScan` (IGNORE_DIRS + .gitignore, caps files/bytes/matches). `GrepMatch{path,line,col,snippet}`. See change: split-editor-workspace. |
| `md-candidates.ts` | `enumerateMdCandidates({cwd?,home?})`. Bounded `.md`/`.mdx` walk (maxDepth 6, cap 500, skips node_modules/.git/etc). Filters through isWritableMdTarget so picker ⊆ guard. See change: directory-settings-page-and-scoped-md-editing. |
| `mime-types.ts` | Exports `extToContentType(ext: string): string`. Maps `.pdf .png .jpg .jpeg .gif .svg .webp .mp4 .webm .mov .html .htm .txt .md .adoc .asciidoc`. Case-insensitive. Default `application/octet-stream`. See change: render-file-previews. |
| `path-containment.ts` | NEW. Shared path-containment helper. `within(p, base)` logical subtree compare. `gitRoot(cwd)` = dirname of `git rev-parse --path-format=absolute --git-common-dir`; fail-closed to cwd; path.resolve-normalized; not cached. `isAllowed(resolved, {anchors})` layered: ① logical within(cwd) fast path, ② realpath'd within(gitRoot) widening; allow if any anchor passes; unconditional widening, no loopback gate. Used by file-routes.ts + system-routes.ts guards. Worktree/repo-subdir sessions read whole git common root. Uses node:child_process execFile + promisify (platform exec wrapper loses {stdout,stderr} promisify shape); `// ban:child_process-ok` marker. See change: git-root-file-containment. safeRealpath now exported for artifact-roots.ts reuse. See change: serve-agent-artifact-previews. |
| `writable-md-target.ts` | Security boundary. `isWritableMdTarget(absPath,{cwd?,home?}): Promise<boolean>`. Realpath-normalize then containment. Dir scope = markdown under cwd; global scope (no cwd) = markdown under ~/.pi/agent only. Rejects non-.md/.mdx, .. traversal, symlink-escape. Fail-closed. See change: directory-settings-page-and-scoped-md-editing. |
