# DOX — packages/server/src/lifecycle

Files in this directory. One row per source file. See change: fold-oversized-agents-directories.

| File | Purpose |
|------|---------|
| `boot-parent-liveness.ts` | Boot-parent liveness + live-ppid reader for `/api/health` + kill-decision helper. → see `boot-parent-liveness.ts.AGENTS.md` |
| `bounded-startup.ts` | `runBoundedStartup({core, teardown, deadlineMs})` + `StartupDeadlineError`. Wraps `server.start()`: on throw OR deadline it tears down (gateway first) then rethrows the ORIGINAL error; on success teardown never runs. Stops a post-`piGateway.start()` failure leaving a process holding the gateway port. See change: fix-worktree-server-autostart-leak. |
| `dashboard-source-decision.ts` | Pure decision: stamp `source:"dashboard"` on `session_register`? Exports `decideDashboardSource(input)` →… → see `dashboard-source-decision.ts.AGENTS.md` |
| `ephemeral-parent-watch.ts` | Independent interval (default 5s) active ONLY in `--ephemeral` mode: on a proven-dead boot parent (`isBootParentProvablyDead`) invokes `onParentDead` exactly once (wired to `server.stop({exitIntent:"ephemeral"})` — graceful drain + recovery-suppressing intent, never `process.exit`). Exports `startEphemeralParentWatch(deps) → {start, stop}`. The idle timer cannot host this check (early-returns without `autoShutdown`; raw-exit). Flag-only opt-in — standalone/Electron excluded by construction. See change: fix-autostart-discovery-precedence (D5). |
| `home-lock-release.ts` | Installs SIGINT/SIGTERM/SIGHUP/SIGBREAK + `exit` handlers that release the per-HOME dashboard lock exactly… → see `home-lock-release.ts.AGENTS.md` |
| `home-lock.ts` | Per-HOME advisory lock ensuring one dashboard instance per `getDashboardConfigDir()/server.lock` (`$HOME`-honoring root via `dashboard-paths.ts`, NOT the `$HOME`-immune `canonicalHomedir`). → see `home-lock.ts.AGENTS.md` |
| `home-rendezvous.ts` | Production caller of `home-lock` (task 2.0a — it had none, so no `server.lock`/record ever existed). `establishHomeRendezvous({httpPort,piPort,version,identity,hooks?,installHandlers?,promotionIntervalMs?,env?})` → `HomeRendezvous {mode:"acquired"|"attach"|"disabled", meta, identity, checkNow(), stop(), release?}`. Owns the two decisions `home-lock` leaves out: attach-mode semantics (serve pinned bridges, never write the record — 2.0c) and promotion (periodic + on-demand owner re-check; a crashed owner never releases — 2.0j/2.0j-i). Called from `server.ts::_startCore` before `piGateway.start`, released in `stop()`. See change: add-pi-gateway-transport-identity. |
| `instance-id.ts` | Persisted per-instance rendezvous id at `<configDir>/instances/<piPort>.id` (0600 in 0700). Exports `getInstanceIdPath`, `ensureInstanceId`, `INSTANCE_ID_HEALTH_FIELD`, `instanceIdHealthFields`. Stable across restarts, distinct per instance — NOT the per-HOME Ed25519 fingerprint. Single publish site for the `/api/health` `instanceId` field that `home-lock`'s liveness probe reads. See change: add-pi-gateway-transport-identity. |
| `launch-source-effective.ts` | `computeEffectiveLaunchSource({raw, activeBridgeCount, uptimeMs})` → `LaunchSourceEffective`… → see `launch-source-effective.ts.AGENTS.md` |
| `recovery-server.ts` | Pure `node:http` recovery server. `startRecoveryServer({port, error})` spawned by `cli.ts` `runForeground`… → see `recovery-server.ts.AGENTS.md` |
