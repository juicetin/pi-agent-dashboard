# DOX — packages/server/src/pi

Files in this directory. One row per source file. See change: fold-oversized-agents-directories.

| File | Purpose |
|------|---------|
| `bridge-contention.ts` | Pure decision logic + record tracker for the one-live-bridge-per-session-id invariant. Exports `decideClaim`, `resolveProbe`, `isSocketAlive`,… → see `bridge-contention.ts.AGENTS.md` |
| `pi-core-checker.ts` | Discovers installed pi-ecosystem CORE packages (global `npm list -g` + `~/.pi-dashboard/node_modules`… → see `pi-core-checker.ts.AGENTS.md` |
| `pi-core-updater.ts` | Runs `npm install -g <pkg>@latest` (global) or `npm install <pkg>@latest` in `~/.pi-dashboard/` (managed) for… → see `pi-core-updater.ts.AGENTS.md` |
| `pi-dev-version-check.ts` | pi.dev version-check client. Queries `https://pi.dev/api/latest-version`; returns `{version, packageName?}`… → see `pi-dev-version-check.ts.AGENTS.md` |
| `bridge-upgrade-auth.ts` | Pure per-transport gate for bridge WS upgrades. `decideBridgeUpgrade({transport,remoteAddress?,url?,secWebSocketProtocol?,requireTicketOnLoopback?,consumeTicket})` → allow/refuse with a named cause; `isLoopbackAddress`. unix ⇒ allowed uncredentialed (kernel decided, D5); remote TCP ⇒ valid single-use bridge-scoped ticket, no grace; GENUINELY-local TCP ⇒ bounded deprecation window (refused from 1.0.0). The grace requires loopback AND no proxy-forwarding header — a locally-terminating relay (zrok/ssh -L/socat/nginx/docker userland proxy) otherwise makes an internet peer present as 127.0.0.1. Installed as `verifyClient` on the TCP listener in `pi-gateway.ts::start`. Loopback also accepts a valid `X-Pi-Local-Token` via injected `verifyLocalToken` (POSITIVE credential, checked BEFORE the grace so the log shows a credential not a horizon); refusals carry `cause` ∈ `local-token-missing`|`local-token-invalid`|`no-ticket`. Neither token nor grace ever reaches a remote or relayed peer. See change: add-pi-gateway-transport-identity (D10b, tasks 6.3, 5.3/5.4). |
| `gateway-transport-policy.ts` | Pure D10 listener decision. `isTcpOptIn(env)` (`PI_GATEWAY_TCP`, default OFF) + `decideGatewayListeners({local,tcpOptIn,host,piPort})` → socket-only by default; TCP only on opt-in (configured host) or as the no-socket fallback pinned to `127.0.0.1`. Wired in `server.ts` (TCP first, then `startOnSocket`). See change: add-pi-gateway-transport-identity (tasks 8.1/8.6, 5.2). |
| `gateway-socket-bind.ts` | Serialized probe/unlink/bind for the gateway UDS path; never unlinks a live socket. → see `gateway-socket-bind.ts.AGENTS.md` |
| `pi-gateway.ts` | WebSocket server for bridge extension connections. Routes `ExtensionToServerMessage` → `SessionManager`;… → see `pi-gateway.ts.AGENTS.md`  **Socket upgrades are tagged in a `unixUpgrades` WeakSet** and read back per connection: both transports share one `WebSocketServer` (D10), so `verifyClient` and the handler's `transport` label applied to socket peers too — enabling the TCP listener (the shipped container) refused every local bridge with 401 and attributed the survivors as REMOTE. See change: add-pi-gateway-transport-identity. |
| `pi-resource-activation.ts` | Activation-state bridge to pi's own resolver. Loads pi via ToolRegistry; `resolveActivation(cwd, agentDir)`… → see `pi-resource-activation.ts.AGENTS.md` |
| `pi-resource-scanner.ts` | Discovers extensions, skills, prompts, agents from local `.pi/`, global `~/.pi/agent/`, and installed… → see `pi-resource-scanner.ts.AGENTS.md` |
| `pi-runtime.ts` | Server-side composition of the shared enumerator: binds the live locations (managed dir, `npm root -g`, PATH, repo root, bare-import anchor) and maps each consumer's current resolution back onto a candidate. Exports `piRuntimeSnapshot`, `consumerDivergenceMessage`, `PI_SPAWN_TOOL`, `PI_MODULE_TOOL`, types `PiRuntimeSnapshot`/`PiRuntimeDeps`. CONSUMER divergence is realpath'd package-DIRECTORY inequality, not version inequality — the same axis the sync checkbox uses, so two different installs at one version cannot read "in sync" on one surface and "diverged" on another. Unresolvable on either side is "unknown", never "diverged". INSTALL-SET divergence is reported separately. `PI_DASHBOARD_PI_FIXTURE_DIR` redirects enumeration at a fixture tree for L3 harness states no real host can produce; env-only, inert when unset, never network-settable. See change: select-pi-runtime-install (design D5/D10). |
| `pi-version-skew.ts` | Pi compatibility range reader. `readPiCompatibility` reads `piCompatibility` from… → see `pi-version-skew.ts.AGENTS.md` |
| `provisional-registration.ts` | `createProvisionalRegistry()` + `PROVISIONAL_TTL` (30s) — intent-only registration for D11 moves. Claims no routing entry/contention slot; `refuseForWire()` returns a constant `provisional_rejected` (no cause → no enumeration oracle, 9.3a-iv) while logging the true cause; commit is single-use and TTL-bounded (#E20). See change: add-pi-gateway-transport-identity (task 9.3a). |
| `resource-activation-toggle.ts` | Writes the pi-standard settings form for a resource's ORIGIN (4 forms; project package delta needs… → see `resource-activation-toggle.ts.AGENTS.md` |
| `resource-entry-ownership.ts` | Machine-local record of the plain settings entries this dashboard wrote, at… → see `resource-entry-ownership.ts.AGENTS.md` |
| `resource-origin.ts` | Origin classification by longest-prefix path match + narrow mirrors of unexported pi semantics… → see `resource-origin.ts.AGENTS.md` |
| `resource-toggle-trust.ts` | Project-trust gate for the resource-activation write. `resolveToggleTrust` → proceed \| refused \| prompt… → see `resource-toggle-trust.ts.AGENTS.md` |
| `session-skill-registry.ts` | Live half of the Resources skill list. `SessionCommandRegistry` retains the latest `commands_list` per… → see `session-skill-registry.ts.AGENTS.md` |
