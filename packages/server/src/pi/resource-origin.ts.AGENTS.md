# resource-origin.ts — index

Origin classification + the narrow pi-semantics mirrors the project-scope write needs. `classifyResourceOrigin`/`classifyFrom` pick the LONGEST-PREFIX matching base dir over `collectOriginCandidates` (package roots + `.agents` bases pi reported, `<cwd>/.pi`, `agentDir`, `~/.agents`) → `{kind: project-loose|agents-loose|global-loose|package, baseDir, packageSource?}`. Never keys on `metadata.scope`/`source`/`baseDir` — a project-scope disable mutates exactly those, and longest-prefix survives `cwd === $HOME` where `<cwd>/.pi` is an ancestor of `~/.pi/agent`. Mirrors of unexported pi internals: `normalizeExactPattern`, `exactSpellings` (rel/abs + SKILL.md parent forms `matchesAnyExactPattern` accepts), `packageIdentity` (npm → name without version, git → `host/path` across SSH+HTTPS, local → resolved path). Also exports `toPosix`, `stripPrefix`, `Candidate`, `OriginKind`, `ResourceOrigin`. See change: project-scope-disable-global-resources.
