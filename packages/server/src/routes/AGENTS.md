# DOX — packages/server/src/routes

Files in this directory. One row per source file.

| File | Purpose |
|------|---------|
| `doctor-routes.ts` | Fastify plugin. `GET /api/doctor` returns `{checks, summary, generatedAt}`. Calls `runSharedChecks` with server deps. Auth-gated identically to `/api/config`. Top-level `try/catch` returns 200 fallback row on internal throw (never 500). Omits Electron-only rows. Tunnel section wired via `resolveZrokBinary` (delegates to `getDefaultRegistry().resolve("zrok")` so diagnostic matches Settings ▸ Tools) and `getTunnelWatchdogStatus`. See change: harvest-bootstrap-survivor-fixes, add-tunnel-diagnostic-checks. |
| `editor-routes.ts` | Registers code-server lifecycle routes. Exports `registerEditorRoutes`. Endpoints: `POST /api/editor/start`, `POST /api/editor/:id/heartbeat`, `POST /api/editor/:id/stop`, `POST /api/editor/:id/theme`, `GET /api/editor/status`. Uses `EditorManager` + `networkGuard`. |
| `file-routes.ts` | REST routes for file read/browse. `/api/file` decodes leading `file://` on `path` via `decodeFileUri`. Absolute paths accepted but still gated by known-session-cwd + traversal check. See change: unify-file-link-openability. Adds GET /api/favorite-models → {labels}. See change: enrich-model-selector-capabilities-favorites. 4 guard sites (`/api/file`, `/api/file/exists`, `/api/file/raw`, `/api/file/render`) route through `isAllowed`. exists keeps pinned-dir anchor + `"unknown cwd"`/`"path outside cwd"` strings. See change: git-root-file-containment. /api/file/raw adds layer ③ artifact-root image anchor via isImageUnderArtifactRoot (image-only, realpath-contained); only raw route, not /api/file or /api/file/render. See change: serve-agent-artifact-previews. GET /api/file file branch sniffs first 1024 bytes, calls `fileKind(resolved, sniff)`, returns `{ type:"file", kind, mimeType, size, content? }`. content read only for monaco/markdown viewers. /api/file/raw (pre-existing) serves image/pdf bytes. See change: add-internal-monaco-editor-pane. Adds `POST /api/file/write` (body `{cwd?,path,content,mtime}`; isWritableMdTarget gate → 403; mtime mismatch → 409; atomic tmp+rename; returns new mtime), `GET /api/file/md-candidates` (scoped picker list), `GET /api/file/md-read` (scoped markdown read, same guard, serves global ~/.pi/agent that /api/file cannot). `/api/file` read now returns `mtime`. See change: directory-settings-page-and-scoped-md-editing. |
| `git-routes.ts` | REST routes: git branches, checkout, init, stash-pop. Adds `GET /api/git/head`, `GET /api/git/worktrees`, `POST /api/git/worktree` (body `{cwd, branch, base?, path?}`) forwarding structured `{code, stderr}` errors from `addWorktree`. See change: add-worktree-spawn-dialog. Adds `POST /api/git/worktree/{remove,merge,push,pr}` + `GET /api/git/worktree/diff-stat`. Localhost-gated. Takes `sessionManager` + `browserGateway` deps. `/remove` pre-flight calls `activeSessionsUnder` → returns `{code:"active_sessions", sessionIds}` when non-empty; on success broadcasts `cwdMissing:true` per session under removed path. See change: add-worktree-lifecycle-actions. Adds `GET /api/git/worktree/bootstrap-status?cwd=<path>` (returns `{needsBootstrap, reason}` per decision tree: `not_required` / `ok` / `no_node_modules` / `stale_lockfile`) and `POST /api/git/worktree/bootstrap` (body `{cwd, requestId?}`; runs install in existing worktree). `POST /api/git/worktree` body extended with optional `requestId`; runs `detectBootstrapRequirement(repoRoot)` + `runBootstrap(...)` post-add; streams `worktree_bootstrap_progress` / `_done` / `_failed` to requesting browser via `worktreeBootstrapRegistry`. Response shape gains `bootstrap: {ran, durationMs?, skippedReason?}`. New error code `bootstrap_failed`. Takes `worktreeBootstrapRegistry` dep. See change: harden-worktree-spawn. worktree-init endpoints: `GET /api/git/worktree/init-status` (cached gate eval → {hasHook,needsInit,trusted}), `POST /api/git/worktree/init` (TOFU-gated; untrusted → init_untrusted carrying {hook,hash}; runs hook, streams progress). `POST /api/git/worktree` no longer auto-runs init. Replaced bootstrap + bootstrap-status endpoints. See change: generalize-worktree-init-hook. Adds `GET /api/git/pull-requests?cwd=` (lists open PRs via `listPullRequests`, `networkGuard` + gh resolution) + `POST /api/git/worktree/from-pr` (body `{cwd, prNumber, path?}`; runs `addWorktreeFromPr`). See change: add-worktree-from-pull-request. `POST /api/git/worktree` body `newBranch` now optional (string when present, else checkout mode); base still required. See change: worktree-checkout-existing-branch. |
| `goal-routes.ts` | REST routes for folder GoalRecords. `parseJudge(raw)` validates `judge` on POST/PATCH (clamp-or-reject, mirrors parseBudget). Rejects missing provider/modelId. See change: sophisticate-goal-authoring-and-control. |
| `grep-routes.ts` | `GET /api/grep?cwd&q&regex`. Gates: known-session cwd, min-3 q, per-match cwd containment. See change: split-editor-workspace. |
| `known-servers-routes.ts` | Known-servers config + mDNS discovery routes. Exports `registerKnownServersRoutes`. Endpoints: `GET/POST/DELETE /api/known-servers`, `POST /api/discover-servers`. Persists via `writeConfigPartial`, returns peer servers from `getPeerServers`. |
| `manifest-route.ts` | Dynamic `/manifest.json` route. Exports `stripPort`, `resolveManifestSource`, `buildManifestBody`, `loadStaticManifest`, `registerManifestRoute`. Resolves source: `config.dashboardName` → request `Host` header (port stripped, IPv6-safe) → `os.hostname()` → `"Pi-Dash"`. `buildManifestBody` sets `id="/"`, `name="Pi-Dash · <src>"`, `short_name=src.slice(0,12)`. Registered before `@fastify/static` so explicit route wins over on-disk asset. Re-reads config per request via `loadConfig()`; Settings change propagates without restart. Sets `Cache-Control: no-cache, must-revalidate` + `Content-Type: application/manifest+json`. See change: add-dynamic-pwa-manifest-naming. |
| `model-proxy-api-key-routes.ts` | Proxy API key CRUD (JWT-gated management surface). Exports `registerModelProxyApiKeyRoutes`, `ApiKeyRoutesDeps`. Endpoints: `GET/POST /api/model-proxy/api-keys`, `POST .../:id/revoke` (soft-delete), `DELETE .../:id` (hard-delete). Cleartext revealed once on create. Scopes + expiry. Admin vs owner filtering. |
| `model-proxy-diagnostics-routes.ts` | `GET /api/model-proxy/diagnostics`. JWT-gated, main instance only (NOT second `/v1` proxy port). `registerModelProxyDiagnosticsRoutes(fastify)`. Returns `getAllAnnotated()` → `{id, provider, excludedReason}` per model (`null` \| `no-credential` \| `oauth-incompatible`). Sibling to `model-proxy-refresh-routes.ts`; resolves registry via `getModelRegistry()` singleton. 503 when pi-ai unresolved. See change: filter-oauth-incompatible-models. |
| `model-proxy-refresh-routes.ts` | Force-refresh model proxy registry. Exports `registerModelProxyRefreshRoutes`. `POST /api/model-proxy/refresh` → `refreshModelRegistry`; 503 `MODEL_PROXY_RUNTIME_MISSING` on failure. |
| `model-proxy-routes.ts` | OpenAI- + Anthropic-compatible proxy endpoints fronting model registry via pi-ai `streamSimple`. Exports `registerModelProxyRoutes`, `ModelProxyRouteDeps`, `ModelProxyRegistry`, `StreamSimpleFn`. Endpoints: `GET /v1/models`, `POST /v1/chat/completions`, `POST /v1/messages`. Concurrency tracking, SSE + non-streaming, request logging, abort on client disconnect. |
| `openspec-group-routes.ts` | REST routes for `/api/openspec/groups` CRUD + assignment. Adds PUT `/api/openspec/groups/change-order` (body `{groupId, order:string[]}`) → `store.setChangeOrder`. See change: add-openspec-change-grouping. See change: redesign-openspec-board. |
| `openspec-routes.ts` | REST routes: openspec-archive, pi-resources, pi-resource-file. Adds POST /api/openspec/config (core→CLI preset, expanded/custom→atomic JSON write, busts configCache), POST /api/openspec/update (per-cwd or all known cwds, records signature), GET /api/openspec/update-status (up-to-date|needs-update|unknown). knownCwds = union(session cwds, pinned dirs). GET /api/openspec/config reads via `configListOrAsync` (async, non-blocking). `currentGlobalSignature` async + computed ONCE per request (profile is global). GET /api/openspec/update-status + POST /api/openspec/update signature step no longer spawn `openspec config list` per-cwd — was ~11s blocking for 11 projects, starved concurrent config GET. See changes: add-openspec-profile-settings, fix-openspec-profile-load-race. |
| `package-routes.ts` | Extension/package management routes. Exports `registerPackageRoutes`. Endpoints: `GET /api/packages/search`, `GET /api/packages/readme`, `GET /api/packages/installed`, `POST /api/packages/install|remove|update|move|check-updates`. Uses `PackageManagerWrapper`; move returns phases based on `parseSourceKind`. |
| `pi-changelog-routes.ts` | `GET /api/pi-core/changelog?pkg&from&to`. Whitelist-validates `pkg` against `CORE_PACKAGE_NAMES`. Bootstrap-gated (503 unless ready). 200 + empty release list when CHANGELOG missing. See change: pi-update-whats-new-panel. |
| `pi-core-routes.ts` | Pi CLI core package version check + update. Exports `registerPiCoreRoutes`, `PiCoreRouteDeps`. Endpoints: `GET /api/pi-core/versions` (refresh query), `POST /api/pi-core/update`. Broadcasts `pi_core_update_complete` via `onUpdateComplete` so other tabs refetch. |
| `plugin-activation-routes.ts` | REST routes: `GET /api/plugins` (returns `PluginStatus[]` with `displayName`, `requirements`, `missingRequirements`) + `POST /api/plugins/:id/toggle` (writes `plugins.<id>.enabled` via config-api, broadcasts `plugin_config_update`). Auth-gated. Effective at next restart (compared client-side against `/api/health.startedAt`). See change: add-plugin-activation-ui. |
| `plugin-config-routes.ts` | Plugin partial config write. Exports `registerPluginConfigRoutes`. `POST /api/config/plugins/:id` validates body against plugin `configSchema`, merges into `~/.pi/dashboard/config.json`, applies schema defaults, broadcasts `plugin_config_update` to browsers. Rejects disabled/unknown plugins. |
| `preferences-display-routes.ts` | REST routes `GET /api/preferences/display` (returns `{ global: DisplayPrefs|undefined, sessionOverrides: Record<sessionId, Partial<DisplayPrefs>> }`) + `PATCH /api/preferences/display` (deep-merges `toolCalls` via `preferences-store.setDisplayPrefs`, broadcasts `display_prefs_updated`). Auth-gated. See change: configurable-chat-display. |
| `preferences-worktree-init-routes.ts` | REST routes `GET /api/preferences/worktree-auto-init` (returns `{autoInitWorktreeOnSpawn:boolean}`) + `PATCH /api/preferences/worktree-auto-init` (body `{value:boolean}` → `preferences-store.setAutoInitWorktreeOnSpawn`). Stores flag only; trusted auto-trigger lives client-side. See change: auto-init-worktree-on-spawn. |
| `provider-auth-routes.ts` | Browser-based pi provider OAuth + API-key auth. Exports `registerProviderAuthRoutes`. Endpoints: `GET /api/provider-auth/providers|handlers|status`, `POST .../authorize` (auth-code + callback server), `POST .../device-code`, `GET .../device-status/:flowId`, `PUT .../api-key`, `DELETE .../:provider`. In-memory PKCE/device-code flow store with 10-min TTL. Notifies bridges `credentials_updated` + eager-refreshes model registry. |
| `provider-routes.ts` | Custom LLM provider read/write to `~/.pi/agent/providers.json`. Exports `registerProviderRoutes`. Endpoints: `GET /api/providers` (redacted), `PUT /api/providers` (merge, blank-name guard, recursion guard, `***` masked-sentinel preserve), `POST /api/providers/test` (probe without saving). Broadcasts `credentials_updated` + refreshes registry. |
| `recommended-routes.ts` | Curated recommended-extensions list enriched with npm/GitHub meta, install scope, activeInPi, updateAvailable, skills, requirements probes. Exports `registerRecommendedRoutes`, `invalidateRecommendedCache`. `GET /api/packages/recommended`, 60s cache. Re-exports `parseSourceKey`, `sourcesMatch`, `SourceKey`. |
| `route-deps.ts` | Shared route dependency types. Exports `NetworkGuard` type, `RouteDeps` interface wiring `SessionManager`, `EventStore`, `PreferencesStore`, `MetaPersistence`, `DirectoryService`, `ServerConfig`, `networkGuard`. |
| `session-routes.ts` | Session REST routes. Exports `registerSessionRoutes`. Endpoints: `GET /api/sessions`, `GET /api/events/:sessionId/:seq`, `GET /api/sessions/:sessionId/tool-result/:toolCallId`, `GET /api/session-diff`, `GET /api/session-file` (path confined to session cwd). Uses `extractFileChanges` + `enrichWithVcsDiff`. |
| `system-routes.ts` | REST routes: config, health, shutdown, tunnel, editors. **`/api/health`** adds `bundleHash` field via `pluginRegistryHash(discoverPlugins())` + enriches every `plugins[]` entry with `bridgeLoadedFrom` via `enrichWithBridgeSource(settings, id)` (`"packages[]"`/`"dashboardPluginBridges"`/`"both"`/`"none"`) + `lastProbe` from plugin-status-store. Consumed by `PluginStalenessBanner` + flows-anthropic-bridge diagnostics. See change: fix-pi-flows-end-to-end. Adds `startedAt` ISO timestamp; client `usePluginEnabledSet` + `PluginsSection` compare against snapshot to surface restart-required banner after toggle. See change: add-plugin-activation-ui. `POST /api/restart` handler passes `dev: useDev` into `spawnRestart({…})` so orchestrator picks mode-matched deadline (matches `--dev` argv already forwarded to child). See change: fix-mode-aware-server-ready-deadlines. `/api/open-editor` decodes leading `file://` on `file` via `decodeFileUri`. See change: unify-file-link-openability. `/api/health` adds `platform: process.platform` for host-OS install-hint filtering. See change: register-bash-and-tool-install-help. `/api/health` gains additive `eventLoopDelay {meanMs,p99Ms,maxMs}` (`perf_hooks` histogram, resets window each read) + `hydration` (`HydrationMetrics.snapshot()`, ≤20 newest-first). See change: instrument-session-hydration-timing. `/api/open-editor` target guard routes through `isAllowed({anchors:[cwd]})`. See change: git-root-file-containment. `/api/health` returns `compatibility: BootstrapCompatibility | null`; 30s-cached via `readCurrentPiVersion`+`computeCompatibility`; null when pi unresolvable. See change: restore-pi-version-skew-surface. |
| `tool-routes.ts` | REST routes: `GET /api/tools`, `GET /api/tools/:name`, `POST /api/tools/rescan`, `PUT/DELETE /api/tools/:name`, `POST /api/tools/diagnostics` (text/plain export). `GET /api/tools` rows carry `installHints` (`ToolListEntry[]`). See change: register-bash-and-tool-install-help. |
