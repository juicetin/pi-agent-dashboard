# route-deps.ts — index

Shared route dependency types. Exports `NetworkGuard` type, `RouteDeps` interface wiring `SessionManager`, `EventStore`, `PreferencesStore`, `MetaPersistence`, `DirectoryService`, `ServerConfig`, `networkGuard`.
