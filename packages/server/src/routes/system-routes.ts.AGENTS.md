# system-routes.ts — index

REST routes: config, health, shutdown, tunnel. External-editor endpoints `/api/editors`, `/api/editor/detect`, `/api/open-editor` removed (change: remove-external-editor-integration). **`/api/health`** adds `bundleHash` field via `pluginRegistryHash(discoverPlugins())` + enriches every `plugins[]` entry with `bridgeLoadedFrom` via `enrichWithBridgeSource(settings, id)` (`"packages[]"`/`"dashboardPluginBridges"`/`"both"`/`"none"`) + `lastProbe` from plugin-status-store. Consumed by `PluginStalenessBanner` + flows-anthropic-bridge diagnostics. See change: fix-pi-flows-end-to-end. Adds `startedAt` ISO timestamp; client `usePluginEnabledSet` + `PluginsSection` compare against snapshot to surface restart-required banner after toggle. See change: add-plugin-activation-ui. `POST /api/restart` handler passes `dev: useDev` into `spawnRestart({…})` so orchestrator picks mode-matched deadline (matches `--dev` argv already forwarded to child). See change: fix-mode-aware-server-ready-deadlines. `/api/health` adds `platform: process.platform` for host-OS install-hint filtering. See change: register-bash-and-tool-install-help. `/api/health` gains additive `eventLoopDelay {meanMs,p99Ms,maxMs}` (`perf_hooks` histogram, resets window each read) + `hydration` (`HydrationMetrics.snapshot()`, ≤20 newest-first). See change: instrument-session-hydration-timing. `/api/health` gains additive `eventLoopSpikes` (`EventLoopSpikeMetrics.snapshot()`, ≤50 newest-first `{at,ms,turn}`; sampler `turn:null` + poll per-turn self-records) via `deps.eventLoopSpikes`, failure-isolated like the other telemetry reads. See change: attribute-openspec-poll-eventloop-stalls. `/api/health` returns `compatibility: BootstrapCompatibility \| null`; 30s-cached via `readCurrentPiVersion`+`computeCompatibility`; null when pi unresolvable. See change: restore-pi-version-skew-surface. `/api/health` adds ownership/liveness fields: `bootParentPid` (static), `ppid` (live, reparenting-aware via `readLivePpid()`), `bootParentAlive` (`computeBootParentAlive()`, both from `boot-parent-liveness.ts`), `activeBridgeCount` (`piGateway.connectionCount()`), `launchSourceEffective` (`computeEffectiveLaunchSource(...)` — promotes stale `bridge`→`bridge-orphaned`). Feed Electron tray ownership, zombie detection, Doctor version-skew. See change: electron-attach-ownership-fixes. Gateway (tunnel) routes (all networkGuard-gated): `GET /api/tunnel/endpoints` (`collectEndpoints` tagged "Accessible at"), `POST /api/tunnel/enroll` (`runEnrollStep` whitelisted `(provider,step)`, 400 unknown/invalid-param, 422 exec-failed), `GET /api/tunnel/block-events` (`blockEvents.list()`). Trust/remove reuses `PUT /api/config` (`trustedNetworks`). See change: add-tunnel-providers. `/api/health` gains additive `storeTrim` (`deps.eventStore.getTrimStats?.()` → `{trimmedEvents:{total,toolExecutionEnd,bySession},evictedSessions}`, `?? all-zero`) beside `droppedFrames` — third silent tool_execution_end loss path (in-memory store trim/evict) made observable. `server.ts` passes `eventStore` into `registerSystemRoutes` deps. See change: instrument-event-store-trim. `/api/restart` records `exitIntent:"restart"` and `/api/shutdown` records `"shutdown"` (or `"user-quit"` for body `{userQuit:true}`) into the boot record BEFORE exiting, so the next cold start knows the sessions were not lost; `RESTART_QUIESCE_MS` now imported from shared `recovery-timing.ts`. See change: fix-recovery-exit-intent. `/api/health` gains `notifyLog: {evictedEntries, bySession}` from `browserGateway.getNotifyLogStats()` — notify-log cap evictions, beside `droppedFrames`/`storeTrim`. See change: split-notify-from-prompt-request. `droppedFrames.serverToBrowser` adds `forcedReconnects`, one termination attempt per over-cap browser socket; it does not claim replay completion. See change: fix-reliable-live-control-events. Adds `DELETE /api/config/auth/providers/:id` (`deleteAuthProvider`, same `networkGuard` as `PUT /api/config`, idempotent, 409 + lockout copy for the last provider without `?force=true`, `_reloadAuth` on an actual deletion) and `GET /api/auth/diagnostics` (`{redirectBase, source, authActive, providerCount}` from `resolveRedirectBase`; guarded but loopback-reachable, which is how the `doctor` module reads it without a JWT). `PUT /api/config` + the DELETE pass the FULL reloaded config to `_reloadAuth` so the top-level `trustedNetworks` merge survives a reload. `/api/tunnel/endpoints` sources manual entries via `resolvePublicBaseUrls`. See change: config-override-oauth-redirect-base. `GET /api/config` gains a failure-isolated `reachability` object (guarded surface, never `/api/health`); a `PUT` that moves `pendingBindHost` broadcasts `reachability_updated`; `/api/network-interfaces` delegates to `network-interfaces.ts`. See change: warn-unreachable-trusted-networks.
