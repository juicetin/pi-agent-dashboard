# server.ts — index

Dashboard HTTP + WebSocket server. Exports `ServerConfig`, `DashboardServer`, `createServer(config)`. Wires Fastify + CORS (origin callback delegates to `isCorsOriginAllowed` in `cors-origin.ts`; trusted-network origins allowed for LAN-to-LAN switching — change: fix-remote-connect-cors-gates) + compress + auth, registers all route modules, session/terminal/editor managers, pi/browser gateways, event forwarding, mDNS discovery, zrok tunnel, plugin runtime, OpenSpec polling. ~2190 lines. host provides cross-plugin service `host.knownFolderCwds` (live getter: session cwds ∪ pinned dirs) via pluginServiceRegistry; kb-plugin consumes for cwd validation. Trusted plugin ctx gains `abortSpawnedRun({sessionId?,spawnToken?,graceful?})` (RENAMED from `abortAutomationRun` — generic spawned-run kill, goal is a 2nd caller; gate priority<=100): graceful sends `{type:"shutdown"}` hint THEN `killBySessionId` (mirrors handleShutdown — hint dropped when WS not OPEN); hard kills by sessionId else falls back to `killByToken`. See change: add-kb-folder-slot, fix-automation-stop-zombie-runs, add-goal-session-supervisor. `pluginSessionEndSubs` + `dispatchPluginSessionEnded` fan `sessionManager.onUnregister` to `ctx.onSessionEnded` subscribers AND the main-server `goalSupervisor.onDriverDeath` (C2a). Builds `goalSupervisor` (createGoalSupervisor): `spawnGoalDriver` (headless-forced, token+goalId register, continue-mode resume / fresh reprime), boot reconcile deferred 30s. goal-routes `abortGoalSupervision` + budget guard on cumulative `totalTurnsUsed`. See change: finalize-automation-run-on-session-death, add-goal-session-supervisor. `worktree_init_subscribe`/`_unsubscribe` handlers accept `cwd` (registry.subscribeCwd/unsubscribeCwd) in addition to `requestId`. See change: friendlier-worktree-init. `sessionManager.onChange` debounced `.meta.json` save delegates to `sessionToMeta(session)` (full-overwrite enumeration; extracted for testability). See change: add-session-tags. Plugin ctx `registerBrowserHandler` wrapper routes `type==="plugin_action"` to `browserGateway.registerPluginActionHandler(plugin.manifest.id, ...)` (manifest-authoritative pluginId, not self-declared) so plugins fan out; other custom types stay single-owner via `registerHandler`. See change: fix-plugin-action-fanout-and-handlers.
