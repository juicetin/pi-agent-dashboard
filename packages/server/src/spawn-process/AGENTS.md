# DOX — packages/server/src/spawn-process

Files in this directory. One row per source file. See change: fold-oversized-agents-directories.

| File | Purpose |
|------|---------|
| `cwd-policy.ts` | Host cwd-keyed capability-policy registry (Part B, #475). Exports `CwdPolicyRegistry` (`register`/`unregister`/`resolve`/`dropPlugin`; entries keyed `(pluginId, canonicalCwd)`, STORED as a list so same-path re-reg composes never overwrites), `mergeCwdPolicy(policy, options)` (pure, non-weakening: `tools`/`skills` INTERSECT, `excludeTools` UNION, `no*` sticky-OR; all commutative+associative → order-independent; empty policy = identity, returns `options` ref for byte-identical argv), `CwdPolicy`/`CwdCapabilityFields`/`CwdPolicyRejectedError`. `register` REJECTS `extensions`/`extensionConfig` (B3) + overly-broad targets (fs root, home, non-workspace-root, B7); deep-freezes the stored copy. `resolve` composes EVERY ancestor-or-equal entry, matching on canonical OR lexical form (fail-toward-applying symlink robustness, B6). Wired into `spawnPiSession` via `setCwdPolicyRegistry` + every plugin ctx (server.ts). Accepted limits: TOCTOU, literal-token intersection. See change: add-plugin-spawn-scope. |
| `headless-pid-registry.ts` | Registry mapping headless child processes → session IDs. Exports `createHeadlessPidRegistry`,… → see `headless-pid-registry.ts.AGENTS.md` |
| `idle-timer.ts` | Auto-shutdown timer with sleep-wake resilience. Exports `IdleTimer`, `HasActiveTerminals`,… → see `idle-timer.ts.AGENTS.md` |
| `process-classifier.ts` | Pure process classifier. Enriches scanned `process_list` entries with `kind`, `label`, `sessionRef` by… → see `process-classifier.ts.AGENTS.md` |
| `process-manager.ts` | Spawns/kills pi sessions. Exports `spawnPiSession`, `buildSpawnEnv`, `buildHeadlessArgs`, `buildTmuxCommand`, `setCwdPolicyRegistry`… `spawnPiSession` resolves+merges the cwd policy into options BEFORE argv/env (every spawn); no match ⇒ byte-identical. `buildSpawnEnv` projects `extensionConfig` → `PI_EXT_<NAME>_<KEY>` (camelCase-split + uppercase + non-alnum→`_`; scalar verbatim, array `JSON.stringify`, D8). → see `process-manager.ts.AGENTS.md` `getKeeperManager()` passes `loadConfig().keeperLog.maxBytes` into `createKeeperManager` (composition root, D7); `spawnHeadlessViaKeeper` sets `PI_KEEPER_LOG_MAX_BYTES`/`PI_KEEPER_LOG_CHECK_INTERVAL_MS` per spawn (spawn-time read, alongside `PI_KEEPER_CAPTURE_PI_OUTPUT`). See change: fix-runaway-keeper-log-growth. `spawnPiSession` env without a `spawnRuntime` follows the family SELECTION (`prependSelectedNodeToPath`) with the legacy managed prepend as no-selection fallback — the unconditional managed prepend is gone from this path. See change: add-node-runtime-family-selection. |
| `restart-helper.ts` | Cross-platform restart orchestrator for POST /api/restart. → see `restart-helper.ts.AGENTS.md` |
| `server-pid.ts` | PID file management at `~/.pi/dashboard/server.pid`. Exports `writePid`, `readPid`, `removePid`,… → see `server-pid.ts.AGENTS.md` |
| `spawn-failure-log.ts` | Appends/reads rolling NDJSON log of failed spawns (`~/.pi/dashboard/sessions/spawn-failures.log`). → see `spawn-failure-log.ts.AGENTS.md` |
| `spawn-preflight.ts` | Pure sync preflight: checks cwd exists/is-dir/writable + pi+node resolvable. → see `spawn-preflight.ts.AGENTS.md` |
| `spawn-recovery-window.ts` | Single source of `RECOVERY_GRACE_MS`/`ORDERING_MARGIN_MS` + `deriveSpawnCorrelationTtlMs`; every spawn TTL… → see `spawn-recovery-window.ts.AGENTS.md` |
| `spawn-register-watchdog.ts` | Arms per-spawn timer. Fires `spawn_register_timeout` if pi never registers, then RECLAIMS — kills the unregistered spawn via its env spawn token (tmux panes included). byPid + byCwd + byToken maps. → see `spawn-register-watchdog.ts.AGENTS.md` |
| `spawned-turn-log.ts` | Build redacted `server.log` lines for spawned-session turn outcomes. → see `spawned-turn-log.ts.AGENTS.md` |
