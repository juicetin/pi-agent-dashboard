# DOX — packages/shared/src/dashboard-plugin

Files in this directory. One row per source file.

| File | Purpose |
|------|---------|
| `index.ts` | Barrel re-export of `slot-types`, `manifest-types`, `slot-props`, `plugin-status`, `ui-primitives`, `intent-types`. Import via subpath `@blackbelt-technology/pi-dashboard-shared/dashboard-plugin/index.js`. |
| `intent-types.ts` | Wire-format intent protocol types. Exports `IntentNode`, `ActionDescriptor`, `PluginIntentsMessage`, `PluginActionMessage`, `PluginEventBroadcast`. Plugins emit `IntentNode` trees; client `IntentRenderer` resolves primitive name + wires `ActionDescriptor` → `PluginActionMessage`. See change: adopt-server-driven-intent-rendering, add-goal-continuation-plugin. |
| `manifest-types.ts` | `PluginManifest` + `PluginClaim` + `PluginRequirements` types declared via `pi-dashboard-plugin` field in package.json or `dashboard-plugin.json`. `PluginClaim` carries per-slot fields (`component`, `command`, `trigger`, `toolName`, `tab`, `path`, `sessionParam`, `depth`, `parentPath`, `predicate`, `shouldRender`). `requires`/`dependsOn` declare activation deps. See change: add-plugin-activation-ui, fix-flows-plugin-polish, fix-plugin-and-scoped-back-navigation, auto-hide-empty-session-subcards. |
| `plugin-status.ts` | Status types for `/api/health.plugins[]` + WS broadcasts. Exports `BridgeLoadSource`, `PluginRequirementReport`, `BridgeProbeSnapshot`, `PluginStatus`, `PluginConfigUpdate`. `PluginStatus` carries `enabled`/`loaded`/`error`/`claims`/`bridgeLoadedFrom`/`lastProbe`/`requirements`/`missingRequirements`/`dependsOn`/`dependents`/`missingDeps`. See change: fix-pi-flows-end-to-end, add-plugin-activation-ui. |
| `route-descriptor.ts` | `RouteDescriptor` + `claimsToRouteDescriptors(claims)` + `interpolateParentPath(parentPath, params)` + `RouteClaimLike`. Shared depth-aware back-action type; client + runtime both depend on shared (avoids cycle). Emits back descriptors from `shell-overlay-route` claims. See change: fix-plugin-and-scoped-back-navigation. |
| `slot-props.ts` | `SlotPropsMap` (slot id → props type) + `SlotProps<S>` helper + `FolderDescriptor`/`ToolRendererImage`/`ToolRendererContext`/`AnyPluginContext`. Opaque `AnyPluginContext` keeps types-only package free of runtime dep. See change: wire-tool-renderer-slot. |
| `slot-types.ts` | Frozen slot taxonomy. Exports `SlotId` union, `Multiplicity`, `PayloadTier`, `SlotDefinition`, `SLOT_DEFINITIONS` map, `SettingsTab` + `VALID_SETTINGS_TABS` (adds resource page ids `skills`/`agents`/`extensions`/`prompts`/`themes`; change: resources-card-tabs), `SlotPredicateInput<S>` classifier. Adding slot = minor; removing/renaming = major. See change: slot-generic-claim-entry. Adds folder-scoped `worktree-card-section` slot (KB row inside worktree session cards). See change: kb-row-on-worktree-session-card. |
| `ui-primitives.ts` | Defines `UI_PRIMITIVE_KEYS` + `UiPrimitiveMap` + per-primitive prop interfaces (`UiAgentCardProps`, `UiMarkdownContentProps`, `UiConfirmDialogProps`, `UiDialogPortalProps`, `UiSearchableSelectDialogProps`, `UiZoomControlsProps`, format helpers). Adds `modelSelector: "ui:model-selector"` key + `UiModelSelectorProps {current?, models?, onSelect(modelLabel)}`; imports `ModelInfo` from `../types.js`. See change: add-ui-model-selector-primitive. Adds `dialog: "ui:dialog"` key + `UiDialogProps` + `UiDialogComponent` (static Footer/Cancel/Action) + intent/footer/cancel/action prop interfaces. `ui:confirm-dialog` contract unchanged (re-skinned via adapter). See change: unify-dialog-system. |
