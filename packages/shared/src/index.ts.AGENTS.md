# index.ts — index

Barrel re-exports for cumbersome symbols. Re-exports `doctor-core.js`, `node-version.js`, `ViewTarget` type, `fileKind` + `TEXT_EXTENSIONS` + `IMAGE_EXTENSIONS` + `ViewerKind`/`FileKind`/`FileKindResult` from `file-kind.js`, `isValidRoleName` + `RoleNameValidation` from `role-name-validation.js`. Most consumers import per-file via `exports` map. See change: add-custom-roles-ui.
