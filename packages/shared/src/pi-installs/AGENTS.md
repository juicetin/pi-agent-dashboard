# DOX — packages/shared/src/pi-installs

Files in this directory. One row per source file.

| File | Purpose |
|------|---------|
| `__tests__/candidates.test.ts` | L1 for enumeration + validation (test-plan #E1-#E13, #E18-#E20, #X10, #X11). Hermetic tmp fixture trees; `npm root -g` + PATH injected so the suite never spawns. #E9 wraps every subprocess in a counter — the mechanical proof of "no `pi --version` ever" and "zero spawns on a cached second enumeration". #E5 fails CLOSED: a directory value must be rejected, so the drift guard cannot pass vacuously. See change: select-pi-runtime-install. |
| `__tests__/installs.test.ts` | L1 for the promoted doctor helpers, moved verbatim from `packages/extension/src/__tests__/doctor/checks.test.ts` (behaviour-preserving move) plus new single-floor-reader cases (#D11): `readPiFloor` null on missing vs `resolvePiFloor` falling back to `PI_COMPATIBILITY_FALLBACK.minimum`. See change: select-pi-runtime-install. |
| `candidates.ts` | Candidate enumeration mirroring `piExecutorDef`'s chain locations (bare-import anchor · managed dir · npm-global · repo-root), plus a read-only `current` row when the live resolution matches none (#D10). Exports `enumeratePiCandidates`, `invalidatePiCandidatesCache` (wired into `ToolRegistry.rescan()`), `pkgDirForEntry`, `samePackageDir`, `PI_PKG_ALIASES`, `PI_SPAWN_ENTRY`, `PI_MODULE_ENTRY`, types `PiCandidate`/`PiCandidateKey`/`EnumerateDeps`. Each candidate carries per-consumer ENTRY FILES (`spawnEntry` = `dist/cli.js`, `moduleEntry` = `dist/index.js`) — never a directory, which is illegal for both consumers (EACCES on spawn, ERR_UNSUPPORTED_DIR_IMPORT on import). An ABSENT location is a row with null paths, never an omission. Versions are read from `package.json` only: no `pi --version` spawn, ever. Locating is NOT spawn-free (`npm root -g`, PATH), hence the module-level cache. See change: select-pi-runtime-install (design D2/D2a/D3). |
| `index.ts` | Public entry. Re-exports everything from `candidates.ts`, `installs.ts`, `validate.ts`, `versions.ts`. THE single implementation of pi enumeration/version/floor logic — the doctor skill's `_lib/checks.ts` and the server's `pi-version-skew.ts` both re-export from here rather than keeping copies. See change: select-pi-runtime-install (design D1). |
| `installs.ts` | Promoted verbatim from the doctor skill: `PiInstall`, `enumeratePiInstalls`, `piVersionDivergence`, `readPkgVersion`. Plus the ONE floor reader (#D11): `readPiCompatibilityRange` (null when absent/malformed), `readPiFloor` (null — "unknown floor", doctor semantics), `resolvePiFloor` (falls back to `PI_COMPATIBILITY_FALLBACK.minimum`, used by BOTH the picker and `/api/health` so the old null-vs-`0.6.7` split cannot recur). `piVersionDivergence` answers INSTALL-SET divergence only — a different question from CONSUMER divergence; the two are never conflated. See change: select-pi-runtime-install. |
| `validate.ts` | Override-path validation at the WRITE boundary for `pi` + `pi-coding-agent` only. Exports `validatePiOverridePath`, `isPiOverrideTool`, `PI_OVERRIDE_TOOLS`, types `PiOverrideCheck`/`PiOverrideValidation`. Accepts EITHER a resolvable pi package dir (yields a version) OR a bare executable file (a Windows `.cmd` shim has no adjacent pi `package.json`; requiring one would make a legitimate install permanently unpinnable). ALWAYS rejects a directory. Result names the failed check so the route can 400 with a reason. NOT a sandbox: TOCTOU between validate and spawn is accepted, and the real control on that surface is the route's `networkGuard`. See change: select-pi-runtime-install (design D6). |
| `versions.ts` | `parseVersion`, `compareVersions`, `isBelow`, `isAbove` — moved verbatim from `packages/server/src/pi/pi-version-skew.ts`, which re-exports them so its public surface is unchanged. Deliberately minimal; no `semver` dep. See change: select-pi-runtime-install (design D11). |
