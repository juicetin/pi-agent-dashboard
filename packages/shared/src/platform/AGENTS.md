# DOX — packages/shared/src/platform

Files in this directory. One row per source file.

| File | Purpose |
|------|---------|
| `binary-lookup.ts` | Unified binary resolution. Exports `ToolResolver` — `which`, `resolvePi`, `resolveTsx`, `resolveNode`, `resolveJiti`, `buildSpawnEnv`. Exports `whichSync`, `isAppImageSelfHit`, `MANAGED_PI_PACKAGES`, `JITI_PACKAGES`, `ResolveJitiOpts`, `AppImageSelfHitOpts`. Search order: managed bin → extraBinDirs → system PATH → login shell (macOS/Linux GUI). jiti hook resolution walks managed-pi → system-pi → caller anchor → `process.argv[1]`. Strips Electron env vars from spawn PATH. |
| `commands.ts` | Cross-platform OS-command primitives. `openBrowser(url,opts)` opens default browser (darwin `open`, win32 `start`, linux `xdg-open`). `isVirtualMachine(opts)` best-effort VM detect: darwin `sysctl -n hw.model`, linux `systemd-detect-virt`, win32 PowerShell `Get-CimInstance Win32_BIOS`+`Win32_ComputerSystem` via `spawnSync` (no wmic; wmic removed by default Win 11 22H2+). `parseVmProbeOutput(text)` pure regex helper `VMware\|VirtualBox\|VBOX\|Parallels\|Virtual Machine\|Hyper-V`. Injectable `platform`/`exec`/`asyncExec`/`spawnSync` opts for tests. See change: replace-wmic-with-powershell. |
| `detached-spawn.ts` | OS-aware detached-child spawn primitives. Exports `spawnDetached`, `waitForNoCrash`, `waitForReady` + option/result interfaces. Invariants: `detached: true` default, `shell: false` always, `stdio[0]="ignore"`, `windowsHide: true`. `logFd` routes both stdout+stderr to single fd. `detach:false` ties child to parent Job Object (Windows) / process group (POSIX). |
| `ensure-bundled-git.ts` | `ensureBundledGitOnPath()` + `resolveBundledGitDir()`. Prepends bundled git/sh dirs to PATH (`cmd`, `usr/bin`, `<libdir>/bin`). Sets `GIT_EXEC_PATH` + `SSL_CERT_FILE`. `<libdir>` arch-specific: `mingw64` (x64) / `clangarm64` (arm64). Shell = `usr/bin/sh.exe` (dugite-native ships NO `bash.exe`). Idempotent. Only module allowed to hardcode `resources/git` paths (`no-hardcoded-bundled-git-paths.test.ts`). See change: embed-git-bash-on-windows. |
| `ensure-windows-path.ts` | `ensureWindowsSystemPath(env, opts?)`: on win32 prepends to PATH (only when dir exists on disk + not already substring-present, case-insensitive): `C:\Windows\System32`, `C:\Windows`, `\System32\Wbem`, `\System32\WindowsPowerShell\v1.0`, `\System32\OpenSSH`, `%LOCALAPPDATA%\Microsoft\WindowsApps`. Idempotent. Non-win32 no-op. Shallow-clones `env`, never mutates `process.env`. Test seam via `{ platform, exists }` opts. Called from `ToolResolver.buildSpawnEnv` (binary-lookup.ts) so every spawned child inherits System32 even when parent PATH lacks it (Electron / minimal-env launchers). Re-exported by `packages/electron/src/lib/ensure-windows-path.ts` for back-compat. See change: fix-windows-path-system32-missing. Sibling pattern to `ensure-bundled-git.ts`. See change: embed-git-bash-on-windows. |
| `exec.ts` | Safe `child_process` wrappers forcing `windowsHide: true`. Exports `execSync`, `execFileSync`, `spawnSync`, `exec`, `execFile`, `spawn`, `execAsync`, `execFileAsync`, `buildSafeArgv`, `SafeArgv`. `buildSafeArgv` wraps `.cmd`/`.bat` + extensionless names in `cmd.exe /d /s /c` on Windows; native `.exe` + Unix spawn direct. Never `shell: true`. Re-exports child_process types. |
| `git-source.ts` | Process-lifetime git-source cache. `setWindowsGitSourceSetting`, `getActiveGitSource(which)`, `augmentEnvWithGitSource(env, which)`, `getGitSourceReadout(which)` (Windows-only, cached), `invalidateGitSourceCache`. `which` injected to avoid binary-lookup cycle. See change: embed-git-bash-on-windows. |
| `git.ts` | Recipe-based git API: recipes (`GIT_DIFF`, `GIT_DIFF_ALL`, `GIT_NUMSTAT`, `GIT_STATUS_V2`, …) + `GIT_RECIPES`; typed `Result<T>` funcs + `*Or` fallbacks; async `runAsync` variants (`diffAllOr`/`isGitRepoOrAsync`/`statusPorcelainOrAsync`/`numstatOrAsync`/`headShaOrAsync`, no `spawnSync`); pure `parseGitStatusV2`. → see `git.ts.AGENTS.md` |
| `index.ts` | Barrel re-export of platform modules. Exports `binary-lookup`, `process`, `process-scan`, `shell`, `commands`, `exec`, `runner`, `detached-spawn`, `spawn-mechanism`, `process-identify`, `subprocess-adapter`, `node-spawn` flat; `git`, `openspec`, `npm`, `paths` as namespaces. |
| `managed-node-path.ts` | `getManagedNodeBinDir(managedDir, platform?)` + `prependManagedNodeToPath(env, managedDir, platform?)`: shallow-clones `env`, prepends `<managedDir>/node/` (Win) or `<managedDir>/node/bin` (Unix) to `PATH`. No-op when runtime absent. Never mutates `process.env`. Post add-node-runtime-family-selection consumers: pi-core-updater (managed-tree mutations, managed-first by landed spec) + the no-selection fallback inside `node-installs/child-path.ts`; NOT the process-manager else-lane (uses `prependSelectedNodeToPath`) and NOT server-launcher (builds env via `buildSpawnEnv`). See change: embed-managed-node-runtime. |
| `native-module-abi.ts` | Extension-tree native-module ABI guard rail. Exports `isNapiModuleFile` (byte-level `napi_register_module_v1` symbol check — layout-blind, never distribution-format), `probeNativeModuleAbi`/`ProbeOutcome` (Tier-B out-of-process dlopen probe; parses `NODE_MODULE_VERSION X … requires Y`; crash/garbage → null, never throws), `readNativeModuleAbi` (two-tier: N-API skip → probe), `walkNativeModuleFiles`/`buildNativeModuleManifest`/`checkManifestDrift`/`findAbiMismatches` (depth-capped 8-hop discovery, stat-signature drift catches in-place rebuilds). Consumed by doctor-core's ABI rows + pre-spawn checks. See change: unify-pi-runtime-identity. |
| `node-spawn.ts` | Canonical `node --import <loader> <entry>` argv builder. Exports `spawnNodeScript`, `buildNodeImportArgvParts`, `isTsxLoader`, `isJitiLoader`, `shouldUrlWrapEntry`, `toFileUrl`, `SpawnNodeScriptOptions`. URL-wraps loader always; entry wrap per `shouldUrlWrapEntry` (tsx+jiti reject `file://` entries; Windows default Node wraps). Single source for `--import` argv shape; lint `no-raw-node-import` enforces. |
| `npm.ts` | Recipe-based npm CLI API. Exports `Recipe` consts `NPM_ROOT_GLOBAL`, `NPM_OUTDATED`, `NPM_OUTDATED_GLOBAL`, `NPM_INSTALL`, `NPM_INSTALL_GLOBAL`, `NPM_VIEW_VERSION` + `NPM_RECIPES`. Typed funcs `rootGlobal`/`outdated`/`outdatedGlobal`/`install`/`installGlobal`/`viewVersion` return `Result<T>`; `*Or` variants swallow errors. Long install timeout 120s; outdated tolerates exit 1. |
| `openspec.ts` | Recipe-based OpenSpec tool module. Adds `OPENSPEC_CONFIG_PROFILE` + `OPENSPEC_UPDATE` recipes, `configProfile()`/`update()` runners, `writeOpenSpecConfigFile()` (atomic tmp+rename), `workflowSetSignature()`, `openSpecConfigFilePath()`. Adds `configListAsync` + `configListOrAsync` (async, non-blocking sibling of `configList`/`configListOr`, uses `runAsync`). Re-exports `CORE_WORKFLOWS`/`EXPANDED_WORKFLOWS` from types. See changes: add-openspec-profile-settings, fix-openspec-profile-load-race. Adds `OPENSPEC_INIT` (argv exactly `openspec init <cwd> --tools pi --force`, 60s) + `OPENSPEC_INIT_HELP` recipes and `initAsync`/`initHelpAsync` wrappers (registry-resolved binary, never bare PATH). Pinned against CLI 1.11.0 (only --tools/--force/--profile registered). See change: add-openspec-init-affordances. `parseJsonOrNull()` recovers from a leading NON-JSON preamble (retries from the first `[`/`{`): openspec prints a one-off telemetry notice ahead of its JSON on the first run under a given HOME, and strict parsing turned that into a null the poller read as 'no changes' -- empty OpenSpec panel on every fresh HOME (container/new user/CI). Trailing junk still fails; preamble-with-no-JSON still returns null. See test: __tests__/openspec-json-preamble.test.ts. |
| `paths.ts` | Isomorphic OS-aware path primitives (no `node:path`; browser-safe). Exports `normalizePath`, `samePath`, `parsePathInput`, `withTrailingSep`, `joinForDisplay`, `isFilesystemRoot`. Windows drive-letter + UNC root aware; case-insensitive on win32/darwin, case-sensitive on linux. Every helper takes injectable `platform` param. Used by pin storage, session grouping, path-picker UI. Adds `caseInsensitiveFilesystem(platform?)` (darwin/win32⇒true) for embed-lifecycle canonical-cwd case-normalization. See change: add-embed-session-lifecycle. |
| `process-identify.ts` | Process identification by command-line marker. Exports `isPiCommandLine`, `findPidByMarker`, `findPidsBySpawnToken`, `isProcessLikePi`, `ProcessIdentifyOpts`. Unix: `ps -eo pid,command` + sentinel match; Windows stubs `[]`/`true` (delegates to headlessPidRegistry). Injectable `platform`/`exec` for tests. `findPidsBySpawnToken` matches `PI_DASHBOARD_SPAWN_TOKEN` in the process ENVIRONMENT (linux `/proc/*/environ`, darwin `ps -E`), narrowed to leaf `comm == "pi"` — the token is inherited by the tmux server + dashboard, so an un-narrowed match is a catastrophic kill target; probe forces `exit 0` (a `for` loop's status is the last iteration's). See change: fix-tmux-session-shutdown-leak. |
| `process-scan.ts` | Cross-platform process enumeration. Exports `parseEtime` (ps `etime` → ms), `isProcessRunning` (win32 tasklist, unix pgrep), `ProcessScanOpts`. Injectable `platform`/`exec`. Best-effort: failures return false/0. |
| `process.ts` | Cross-platform process primitives. Exports `parseNetstatListeners`, `findPortHolders`, `isProcessAlive`, `signalZero`, `killProcess`, `killPidWithGroup`, `ProcessOpts`, `KillProcessResult`. Unix: `lsof`/SIGTERM→SIGKILL; Windows: `netstat -ano`/`taskkill /F /T`. Injectable `platform`/`exec`/`kill`. `signalZero(pid, {kill?})` → `"alive"\|"esrch"\|"other-errno"` — the errno-aware kill-decision probe (sole allowlisted signal-0 site): `esrch` = proof of absence; `EPERM`/other errno = alive. See change: fix-autostart-discovery-precedence (D6). |
| `runner.ts` | Recipe execution engine. Exports `run`, `runAsync`, `unwrap`, `buildSpawnEnvForArgv`, `electronAsNodeRequired`, `resetResolverCache`, types `Recipe`, `RunCtx`, `Result`, `ExecError`. Resolves argv[0] via tool registry (executor form) → `ToolResolver.which` fallback; routes through `buildSafeArgv`; enforces timeout + tolerated exit codes; normalizes errors to `Result<T>` (never throws). `electronAsNodeRequired(argv0, deps?)` = shared predicate: `Boolean(electronVersion) && argv0 === execPath`; `buildSpawnEnvForArgv` sets `ELECTRON_RUN_AS_NODE=1` via it when node-wrapping Electron execPath. Predicate reused by `process-manager.buildSpawnEnv` + `keeper-manager` so every executor spawn site agrees. See change: fix-nodescript-argv-electron-execpath-fallback. Timeout ExecErrors now carry partial `stdout`/`stderr` (init endpoint surfaces partial stderr from a killed 60s spawn). See change: add-openspec-init-affordances. |
| `select-git-source.ts` | `selectGitSource()` tri-state `windowsGitSource` decision (`auto`/`host`/`bundled`) → `"host"`\|`"bundled"`. Pure, injectable `which`/`platform`. Non-win32 → host. `auto` needs git+bash on host PATH atomically else bundled. See change: embed-git-bash-on-windows. |
| `shell.ts` | Cross-platform shell + terminal env primitives. Exports `detectShell` (win32 COMSPEC/powershell, unix SHELL/bash), `getTerminalEnvHints` (win32 sets `TERM=cygwin`), `ShellOpts`. Injectable `platform`/`env`. |
| `spawn-mechanism.ts` | Session-spawn mechanism selection. Exports `selectMechanism`, `buildWtArgs`, `sessionFlagsToArgv`, types `SpawnMechanism`, `UserSpawnStrategy`, `SpawnMechanismContext`, `WtArgsOptions`, `SessionFlags`. Mechanisms: tmux / wt / wsl-tmux / headless. `selectMechanism` pure; electronMode or userStrategy "headless" forces headless. `buildWtArgs` argv form reuses MRU WT window. `sessionFlagsToArgv` appends `--name` (when `SessionFlags.name` non-empty; single argv element, no shell-split), `--session`/`--fork`/`--model`, composing across all three return paths. See change: adopt-pi-074-080-features. `SessionFlags` gains capability-scope fields `tools`/`excludeTools`/`noBuiltinTools`/`noTools`/`skills`/`noSkills`/`extensions`; `sessionFlagsToArgv` (via private `scopeFlags`) appends `--tools a,b,c`/`--exclude-tools a,b,c` (comma-joined single arg), bare `--no-builtin-tools`/`--no-tools`/`--no-skills`, repeatable `--skill <p>` + `-e <p>` per entry. Empty array ⇒ no flag. NO `--no-extensions` ever emitted (would kill the dashboard bridge — control channel must survive; design D2/D6). Scope flags append AFTER name/session/model (prefix byte-identical). See change: add-plugin-spawn-scope. |
| `spawn-runtime.ts` | Pi spawn-runtime resolution ladder ("follow, don't lead"). Exports `resolveSpawnRuntime`/`ResolvedRuntime` (4-rung total ladder: override → selection → user Node (arm-dependent order + fs-only version-manager probe) → managed → bundled/execPath, recorded skip trail), `evaluateVersionGate` (pi floor + ¬nodejs/node#58515; cap excess accepted + flagged), `readPiEnginesFloor`/`parseEnginesFloor`, `classifyNodeSource` (VENDORED from manage-node-runtime-updates — convergence contract), `readRuntimeOverride`/`readToolOverrideNode`, `validateResolvedRuntime`/`ensureFreshRuntime` (stat-signature re-validation; per-spawn probe for shim paths), `buildPublishedRuntimeBlock` (path-free bundled shapes), `resolvedFamilyEntries` (per-member node/npm entry files), `detectSpawnArm`, `defaultVersionProbe`. Registry-free by design (D5). See change: unify-pi-runtime-identity. |
| `subprocess-adapter.ts` | Strategy-pattern OS-aware subprocess adapter. Exports `SubprocessAdapter` interface, `createSubprocessAdapter`, `getDefaultSubprocessAdapter`, `_resetDefaultSubprocessAdapter`. Windows adapter routes `.cmd`/extensionless via `buildSafeArgv` (`cmd.exe /d /s /c`); Unix adapter direct spawn `shell:false`. Lazy process-wide singleton; injectable for tests. |
