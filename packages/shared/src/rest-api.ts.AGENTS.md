# rest-api.ts — index

REST endpoint request/response types. Sessions, events, spawn, file read/write (`FileWriteRequest` mtime conflict), browse + browse/flags, tunnel status, pi resources (`PiResource.enabled` scope-derived activation + `"theme"`/`"agent"` types + optional `model`/`tools`/`colors`; `PiResourceScope.agents`; changes: folder-resource-activation-toggle, resources-card-tabs), git ops, PRs, provider auth, package mgmt, pi-core versions, known servers, recommended extensions, tool registry, model-proxy OpenAI/Anthropic wire types, model-proxy API keys, OpenSpec groups + change-order. `InstalledPackage.publishedVariantSource?`/`publishedVariantVersion?` = canonical published spec for a local/git override row (recommended manifest OR npm-name lookup); drives dual source line + reset. See change: reset-override-to-npm.
