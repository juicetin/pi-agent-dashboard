# DOX — packages/shared/src/tool-registry

Files in this directory. One row per source file.

| File | Purpose |
|------|---------|
| `definitions.ts` | Registers standard tool set. Each definition declares ordered strategy chain (override → bare-import → managed → npm-global → where) + classifier (strategy → source). Includes build-time tools `electron` + `node-pty` (registered as `kind: "module"` returning package directory; resolved hoist-aware via `bareImportPackageDirStrategy` with optional `searchPaths`). See change: register-build-time-tools. Registers `binaryDef("bash")` on every platform. `INSTALL_HINTS` map (bash, git, node, gh, zrok [v2: doc URLs `docs.zrok.io/getting-started`, win32 `zrok2` on PATH; see change: support-zrok-v2], ngrok, tailscale, zerotier-cli) attached via `binaryDef`; registers `ngrok`/`tailscale`/`zerotier-cli` binaries for the tunnel providers (See change: add-tunnel-providers). `npx` unchanged, no installHints. See change: register-bash-and-tool-install-help. bash chain now override → bundled-git-bash → managed → where (win32 surfaces bundled sh.exe before PATH). classify maps `bundled-git-bash` → source `bundled`. See change: resolve-bundled-bash-on-windows. wmic no longer registered on win32 (removed by default Win 11 22H2+); win32 set = powershell, tasklist, taskkill. See change: replace-wmic-with-powershell. npx chain now override → bundled-node → managed-runtime → managed-bin → where (`managedRuntimeStrategy("npx")` — managed Node runtime visible to whole node/npm/npx family). See change: fix-node-family-resolution-gaps. `npmCliBesideNodeStrategy` (win32 npm) anchors on the PEER-resolved node via `deps.resolvePeer` → falls back to `deps.execPath`; probes via `deps.exists` — never a direct `process.execPath` read; stale "global registry hook" comment corrected. — see also: peer-resolution seam, change add-node-runtime-family-selection (section 3b). |
| `index.ts` | Tool registry public entry. Re-exports `types`, `OverridesStore`/`defaultOverridesPath`, `ToolRegistry`, `registerDefaultTools`, `strategies`. Exports `getDefaultRegistry` (lazy process-wide singleton, publishes on `globalThis[Symbol.for("pi-dashboard.tool-registry")]`), `peekGlobalRegistry` (cycle-safe accessor for `platform/runner.ts`), `_resetDefaultRegistry` (test-only). getDefaultRegistry binds the PRODUCTION peer seam via `bindPeerResolution(defaultRegistry.resolve)` (design D2); `bindPeerResolution` = peer seam + in-flight re-entrancy guard at the binding (refuses self/re-entrant lookups with null). See change: add-node-runtime-family-selection (section 3b). |
| `overrides.ts` | Persistent per-tool path overrides at `~/.pi/dashboard/tool-overrides.json`. Exports `OverridesStore`, `defaultOverridesPath`, `OverridesStoreDeps`. Schema `{version:1, overrides:{<name>:{path:<abs>}}}`. Read-through + write-through in-memory cache; lazy disk load; atomic tmp+rename persist. Malformed file → empty, no throw. |
| `registry.ts` | `ToolRegistry` service — single-source resolver for every external binary/module (pi, pi-coding-agent, openspec, npm, node, tsx, git, zrok, pi-dashboard). Ordered strategy chain per tool, per-resolution diagnostic trail, in-memory cache, override-aware. `list()` returns `ToolListEntry[]`: each row carries `def.installHints` opaquely (omitted when absent). `resolve()` unchanged. See change: register-bash-and-tool-install-help. `rescan()` also invalidates the node candidates cache (`invalidateNodeCandidatesCache()`); new `listOverrides()` = override snapshot (hand-set source of truth for the family picker). See change: add-node-runtime-family-selection. |
| `strategies.ts` | Reusable resolution strategies (pure over `StrategyCtx`). Exports `overrideStrategy`, `managedRuntimeStrategy`, `bundledNodeStrategy`, `bundledGitBashStrategy`, `managedBinStrategy`, `managedModuleStrategy`, `npmGlobalStrategy`, `whereStrategy`, `bareImportStrategy`, `StrategyDeps`. `whereStrategy` filters AppImage self-hits via `isAppImageSelfHit`. `bareImportStrategy` resolves via `createRequire` → exports-map dir-walk → `import.meta.resolve`. That ESM step is LAST and is an INERT GUARD, not a fallback: this module is itself jiti-loaded from a `data:` URL where `import.meta.resolve(<bare id>)` throws, so the step has never produced a value in production and the dir-walk has carried every step-1 miss. Behaviour preservation holds BECAUSE step 2 was already dead — do not "restore" the obvious order. Unreachability is contingent on bare (not subpath) ids + the default `anchor`. The ESM call MUST stay a direct `import.meta.resolve(id)`: a TypeScript cast makes the node `TSAsExpression`, defeats jiti's erasure, and forces its `data:`-URL ESM fallback (fatal on Bun hosts, issue #408) — enforced by `scripts/__tests__/jiti-cjs-transpile-safety.test.mjs`. See change: fix-jiti-cjs-transpile-safety. | NOTE: `StrategyDeps` gains optional `resolvePeer(name, forTool)` — the peer-resolution seam (design D1: a dependency on the injection surface, NOT StrategyCtx; optional + undefined-safe; bound at ONE production site). See change: add-node-runtime-family-selection (section 3b). |
| `types.ts` | `ToolDefinition`, `Strategy`, `StrategyResult`, `Resolution`, `Source`, `UnknownToolError`, `ModuleResolutionError`. Adds `InstallHints` + `PlatformInstallHint` interfaces. Adds `ToolDefinition.installHints` optional field. Adds `ToolListEntry` (`Resolution` + `installHints`). See change: register-bash-and-tool-install-help. |
| `definitions.ts` | Registers standard tool set. Each definition declares ordered strategy chain (override → bare-import → managed → npm-global → where) + classifier (strategy → source). Includes build-time tools `electron` + `node-pty` (registered as `kind: "module"` returning package directory; resolved hoist-aware via `bareImportPackageDirStrategy` with optional `searchPaths`). See change: register-build-time-tools. Registers `binaryDef("bash")` on every platform. `INSTALL_HINTS` map (bash, git, node, gh, zrok [v2: doc URLs `docs.zrok.io/getting-started`, win32 `zrok2` on PATH; see change: support-zrok-v2], ngrok, tailscale, zerotier-cli) attached via `binaryDef`; registers `ngrok`/`tailscale`/`zerotier-cli` binaries for the tunnel providers (See change: add-tunnel-providers). `npx` unchanged, no installHints. See change: register-bash-and-tool-install-help. bash chain now override → bundled-git-bash → managed → where (win32 surfaces bundled sh.exe before PATH). classify maps `bundled-git-bash` → source `bundled`. See change: resolve-bundled-bash-on-windows. wmic no longer registered on win32 (removed by default Win 11 22H2+); win32 set = powershell, tasklist, taskkill. See change: replace-wmic-with-powershell. Media/skill-facing tools: ffmpeg (override → static-npm(ffmpeg-static) → where), ffprobe (static-npm @ffprobe-installer/ffprobe), imagemagick (where `convert`; win32 platformStrategies probe `magick` — System32 convert.exe is the NTFS tool), chromium (pw-browser), agent-browser (where; hint `pi install npm:pi-agent-browser`), pi-doc-engine (docker-image; hint `npm run build:image` requiresConfirm) — installHints with docsAnchors install-ffmpeg/install-ffprobe/install-imagemagick/install-chromium. See change: add-skill-tool-provisioning. |
| `default-registry.ts` | Process-wide `ToolRegistry` singleton. Own module (NOT index.ts) so ensure.ts can import getDefaultRegistry without an index.js round trip (Biome noImportCycles). Exports `getDefaultRegistry` (lazy, publishes on `globalThis[Symbol.for("pi-dashboard.tool-registry")]`), `peekGlobalRegistry` (cycle-safe accessor for `platform/runner.ts`), `_resetDefaultRegistry` (test-only). See change: add-skill-tool-provisioning. |
| `ensure-cli.ts` | `pi-dashboard-ensure` CLI (design D5). Reads a package's `pi.tools`, ingests, ensures, prints report. Flags `--install`, `--json` (--json always exits 0); exit 0 when all required present/installed. TTY confirm prompt for requiresConfirm; headless auto-deny. Run via tsx launcher `packages/shared/bin/pi-dashboard-ensure.mjs`. See change: add-skill-tool-provisioning. |
| `ensure.ts` | `ensureTools` — library face of tool ensuring. Recommend-only default; `autoInstall:true` runs ONLY resolved first-party `installHints.commands[pkgmgr]`; `requiresConfirm` hints need per-invocation confirm (no callback = auto-deny). Actions present/recommended/installed/degraded/blocked; `ok:false` ⇔ required tool not present/installed. Never throws for missing tools. See change: add-skill-tool-provisioning. |
| `index.ts` | Tool registry public entry. Re-exports `types`, `OverridesStore`/`defaultOverridesPath`, `ToolRegistry`, `registerDefaultTools`, `strategies`, `parseSkillTools`/`ingestSkillTools`/`discoverSkillManifests`/`ingestInstalledSkillTools`, `ensureTools`. Exports `getDefaultRegistry` (lazy process-wide singleton, publishes on `globalThis[Symbol.for("pi-dashboard.tool-registry")]`), `peekGlobalRegistry` (cycle-safe accessor for `platform/runner.ts`), `_resetDefaultRegistry` (test-only). npx chain now override → bundled-node → managed-runtime → managed-bin → where (`managedRuntimeStrategy("npx")` — managed Node runtime visible to whole node/npm/npx family). See change: fix-node-family-resolution-gaps. |
| `definitions.ts` | Registers standard tool set. Each definition declares ordered strategy chain (override → bare-import → managed → npm-global → where) + classifier (strategy → source). Includes build-time tools `electron` + `node-pty` (registered as `kind: "module"` returning package directory; resolved hoist-aware via `bareImportPackageDirStrategy` with optional `searchPaths`). See change: register-build-time-tools. Registers `binaryDef("bash")` on every platform. `INSTALL_HINTS` map (bash, git, node, gh, zrok [v2: doc URLs `docs.zrok.io/getting-started`, win32 `zrok2` on PATH; see change: support-zrok-v2], ngrok, tailscale, zerotier-cli) attached via `binaryDef`; registers `ngrok`/`tailscale`/`zerotier-cli` binaries for the tunnel providers (See change: add-tunnel-providers). `npx` unchanged, no installHints. See change: register-bash-and-tool-install-help. bash chain now override → bundled-git-bash → managed → where (win32 surfaces bundled sh.exe before PATH). classify maps `bundled-git-bash` → source `bundled`. See change: resolve-bundled-bash-on-windows. wmic no longer registered on win32 (removed by default Win 11 22H2+); win32 set = powershell, tasklist, taskkill. See change: replace-wmic-with-powershell. npx chain now override → bundled-node → managed-runtime → managed-bin → where (`managedRuntimeStrategy("npx")` — managed Node runtime visible to whole node/npm/npx family). See change: fix-node-family-resolution-gaps. |
| `index.ts` | Tool registry public entry. Re-exports `types`, `OverridesStore`/`defaultOverridesPath`, `ToolRegistry`, `registerDefaultTools`, `strategies`. Exports `getDefaultRegistry` (lazy process-wide singleton, publishes on `globalThis[Symbol.for("pi-dashboard.tool-registry")]`), `peekGlobalRegistry` (cycle-safe accessor for `platform/runner.ts`), `_resetDefaultRegistry` (test-only). |
