# DOX — packages/shared/src/tool-registry

Files in this directory. One row per source file.

| File | Purpose |
|------|---------|
| `definitions.ts` | Registers standard tool set. Each definition declares ordered strategy chain (override → bare-import → managed → npm-global → where) + classifier (strategy → source). Includes build-time tools `electron` + `node-pty` (registered as `kind: "module"` returning package directory; resolved hoist-aware via `bareImportPackageDirStrategy` with optional `searchPaths`). See change: register-build-time-tools. Registers `binaryDef("bash")` on every platform. `INSTALL_HINTS` map (bash, git, node, gh, zrok) attached via `binaryDef`. `npx` unchanged, no installHints. See change: register-bash-and-tool-install-help. bash chain now override → bundled-git-bash → managed → where (win32 surfaces bundled sh.exe before PATH). classify maps `bundled-git-bash` → source `bundled`. See change: resolve-bundled-bash-on-windows. wmic no longer registered on win32 (removed by default Win 11 22H2+); win32 set = powershell, tasklist, taskkill. See change: replace-wmic-with-powershell. |
| `index.ts` | Tool registry public entry. Re-exports `types`, `OverridesStore`/`defaultOverridesPath`, `ToolRegistry`, `registerDefaultTools`, `strategies`. Exports `getDefaultRegistry` (lazy process-wide singleton, publishes on `globalThis[Symbol.for("pi-dashboard.tool-registry")]`), `peekGlobalRegistry` (cycle-safe accessor for `platform/runner.ts`), `_resetDefaultRegistry` (test-only). |
| `overrides.ts` | Persistent per-tool path overrides at `~/.pi/dashboard/tool-overrides.json`. Exports `OverridesStore`, `defaultOverridesPath`, `OverridesStoreDeps`. Schema `{version:1, overrides:{<name>:{path:<abs>}}}`. Read-through + write-through in-memory cache; lazy disk load; atomic tmp+rename persist. Malformed file → empty, no throw. |
| `registry.ts` | `ToolRegistry` service — single-source resolver for every external binary/module (pi, pi-coding-agent, openspec, npm, node, tsx, git, zrok, pi-dashboard). Ordered strategy chain per tool, per-resolution diagnostic trail, in-memory cache, override-aware. `list()` returns `ToolListEntry[]`: each row carries `def.installHints` opaquely (omitted when absent). `resolve()` unchanged. See change: register-bash-and-tool-install-help. |
| `strategies.ts` | Reusable resolution strategies (pure over `StrategyCtx`). Exports `overrideStrategy`, `managedRuntimeStrategy`, `bundledNodeStrategy`, `bundledGitBashStrategy`, `managedBinStrategy`, `managedModuleStrategy`, `npmGlobalStrategy`, `whereStrategy`, `bareImportStrategy`, `StrategyDeps`. `whereStrategy` filters AppImage self-hits via `isAppImageSelfHit`. `bareImportStrategy` resolves via `createRequire` → `import.meta.resolve` → exports-map dir-walk. |
| `types.ts` | `ToolDefinition`, `Strategy`, `StrategyResult`, `Resolution`, `Source`, `UnknownToolError`, `ModuleResolutionError`. Adds `InstallHints` + `PlatformInstallHint` interfaces. Adds `ToolDefinition.installHints` optional field. Adds `ToolListEntry` (`Resolution` + `installHints`). See change: register-bash-and-tool-install-help. |
