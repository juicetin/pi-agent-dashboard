# DOX — packages/video-transcription

Files in this directory. One row per source file.

| File | Purpose |
|------|---------|
| `.pi/skills/video-transcription/SKILL.md` | NL-triggered skill (/transcribe): video/audio → speaker-diarized SRT. Backs pi-transcribe CLI (TS port, no Python). TWO backends via TRANSCRIBE_BACKEND: soniox (default, `<name>.srt`, SONIOX_API_KEY) and assemblyai (opt-in, `<name>.diarize.srt`, ASSEMBLY_AI_KEY, EU endpoint) — distinct suffixes so both can run on one source for diarization A/B. `both`/`all`/comma list runs every backend in ONE pass: audio extracted once, both SRTs written; skip + failure are per backend. In-place sibling .mp3 + subtitle. Args: none → ~/Movies, dir, or file paths. Pool TRANSCRIBE_CONCURRENCY (default 8, clamp 1-100). Duration cap (Soniox 5h / AAI 10h) → split MAX_CHUNK_HOURS (default 4.5h / 9h) chunks, merge with absolute timestamps (ffprobe probes duration, not MB). Overrides TRANSCRIBE_SRT_SUFFIX / TRANSCRIBE_LANGUAGE / TRANSCRIBE_MAX_SPEAKERS. Needs ffmpeg/ffprobe; key env → gitignored .env. See change: add-assemblyai-transcription-backend. |
| `README.md` | Package overview. Transcribe local video/audio in-place to speaker-diarized SRT via Soniox async API (default) or AssemblyAI (opt-in, EU `api.eu.assemblyai.com`). Full TS port of standalone `video-transcription` pi skill, no Python. Only runtime dep `@blackbelt-technology/pi-dashboard-shared`. Exposed as pi skill (`.pi/skills/video-transcription`, `/transcribe`) + CLI bin `pi-transcribe`. Backend comparison table + env-override table. Needs `ffmpeg`/`ffprobe` on PATH. See change: add-assemblyai-transcription-backend. |
| `vitest.config.ts` | Vitest config for video-transcription package. `include` `src/**/__tests__/**/*.test.ts`, `environment` node, `pool` forks, `maxWorkers` 1, `testTimeout` 30000. |
