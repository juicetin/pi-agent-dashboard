# DOX — qa

Files in this directory. One row per file. Non-source area (migrated from `docs/file-index-skills-misc.md`; source of truth now here). See change: migrate-file-index-to-agents-tree.

| File | Purpose |
|------|---------|
| `.gitignore` | Ignores VM build artifacts: `output/` images, `iso/` downloads, `packer_cache/`, `clones/` working dirs, `crash.log`. |
| `fixtures/faux-provider.ext.ts` | pi extension fixture. Registers faux provider via pi-ai `registerFauxProvider({api:"faux"})`. Surfaces `faux/faux-1` in CLI catalog via `pi.registerProvider("faux",{api:"faux",streamSimple})`. `streamSimple` grabbed via `getApiProvider("faux")` so faux stream survives rpc `rebindSession()`. Reads `FAUX_SCRIPT` (scenario id) + `FAUX_TPS`. Unknown scenario → loud "faux: no scenario". Imports `@earendil-works/pi-ai` unpinned (resolves pi's bundled copy). Exports `resolveActiveStep(context)`: walks messages backward to last user msg matching `/\[\[faux:(id)\]\]/`, `stepIndex` = assistant turns after it; no sentinel → `FAUX_SCRIPT` fallback anchored at start. Self-perpetuating router factory replaces static `setResponses`: re-appends itself, resolves `SCENARIOS[id]`, replays `script[stepIndex]`, calls factory steps. `FAUX_SCRIPT` kept as fallback. See change: add-faux-model-integration-tests. See change: add-e2e-faux-model-roundtrip. |
| `fixtures/faux-scenarios.ts` | Shared scenario catalog `SCENARIOS: Record<id,{script,expect}>`. `script` = `FauxResponseStep[]` from `fauxText`/`fauxThinking`/`fauxToolCall`/`fauxAssistantMessage` + factories. Imported by server + client test layers. Covers plain-text, slow-stream, model-error, isolation-a/b, ask-select-roundtrip, `tool-*` renderer matrix, `ask-*` interactive matrix, text-realfile, echo-system-context, mermaid-colorize. `mermaid-colorize` streams a fenced flowchart (A/C default, `style B fill:#ff0000`) for MermaidBlock default-node colorization e2e. See change: colorize-mermaid-default-nodes. Adds `text-realfile` scenario streaming `./hello.txt` (explicit `./` prefix linkifies; resolves to real `/fixtures/sample-git/hello.txt`); used by file-preview-survives-churn e2e for live-content overlay. Adds `echo-system-context` scenario: factory step echoes `context.systemPrompt` dashboard fragment back as assistant text. Exports `extractDashboardFragment`, `DASHBOARD_CONTEXT_DELIMITER`, `NO_DASHBOARD_CONTEXT_MARKER`. `FauxContext` gains `systemPrompt?`. Used by e2e session-context-injection spec. See change: add-faux-model-integration-tests. See change: fix-file-preview-survives-message-churn. See change: inject-session-context-into-agent. Reworked `tool-bash-large` to `seq 1 500` (>200 lines) for Strategy B replay-truncation E2E. See change: reduce-session-replay-traffic. |
| `fixtures/README.md` | Documents faux fixtures: purpose, `registerFauxProvider`+`pi.registerProvider` recipe, `streamSimple` survives rpc rebind, `faux/faux-1` token form, `FAUX_SCRIPT`/`FAUX_TPS` env contract, scenario-catalog shape, how to add scenario. See change: add-faux-model-integration-tests. |
| `Makefile` | Build/test/manual/clean targets for QA VMs. Targets: `build-{linux-x86,linux-arm,windows,macos-x86,macos-arm,-all}`, `test-{linux-x86,linux-arm,windows,macos-x86,macos-arm,-all}`, `manual-<target>`, `clean`, `test-windows-remote-nsis SETUP=<Setup.exe>`. Preflight `check_placeholders` / `check_user_data` reject unfilled `REPLACE_WITH_` var files. Calls `scripts/run-test.sh`, `scripts/vm-clone.sh`, `scripts/vm-destroy.sh`. SSH user `qa`, key `~/.ssh/qa_vm_key`, `VMRUN` path hard-coded to VMware Fusion. |
| `packer/http/autounattend.xml` | Windows 11 unattended install answer file. `windowsPE` pass: EFI/MSR/Primary partition layout, Win11 hardware-check bypass via `LabConfig` reg keys (`BypassTPMCheck`, `BypassSecureBootCheck`, `BypassRAMCheck`, `BypassCPUCheck`, `BypassStorageCheck`), `/IMAGE/NAME=Windows 11 Enterprise Evaluation` edition disambiguation. `oobeSystem` pass: creates `qa`/`qa` admin account, auto-logon, `FirstLogonCommands` install OpenSSH.Server capability, set sshd auto-start, open firewall TCP/22, disable firewall profiles, set DefaultShell to PowerShell. |
| `packer/http/meta-data` | cloud-init NoCloud datasource meta-data file (empty). Required presence for `ds=nocloud-net` boot; meta-data populated by instance-id/local-hostname if needed. |
| `packer/http/user-data` | Ubuntu cloud-init autoinstall config (`#cloud-config`). Sets hostname `qa-ubuntu`, user `qa` (SHA-512 crypt password "qa"), installs openssh-server/curl/ca-certificates, LVM storage layout, authorized-keys (`REPLACE_WITH_YOUR_PUBLIC_KEY` placeholder), password-less sudo via `/target/etc/sudoers.d/qa`. Served via `http_directory = "http"` + `autoinstall ds=nocloud-net` boot command. |
| `packer/macos-arm.pkr.hcl` | Packer template for macOS ARM (M1). `vmware-vmx` builder (`source.vmware-vmx.macos-arm`) starts from existing `var.source_vmx` VM (no ISO unattended install). Vars: `source_vmx`, `vm_name=macos-14-arm`, `ssh_username=qa`, `ssh_password=qa`, `cpus=4`, `memory=8192`, `ssh_timeout=15m`. Provisions via `provision-common.sh` + `provision-macos.sh`. Output `../output/${var.vm_name}`. |
| `packer/macos-x86.pkr.hcl` | Packer template for macOS x86. `vmware-vmx` builder (`source.vmware-vmx.macos`) from existing `var.source_vmx`. Vars: `source_vmx`, `vm_name=macos-14-x86`, `ssh_username=qa`, `ssh_password=qa`, `cpus=4`, `memory=8192`, headless. Provisions `provision-common.sh` + `provision-macos.sh`. Output `../output/${var.vm_name}`. |
| `packer/scripts/provision-common.sh` | Shared provisioner: installs nvm v0.40.1 (`curl raw.githubusercontent.com/nvm-sh/nvm/v0.40.1/install.sh`), Node.js LTS (`nvm install --lts`, `nvm alias default lts/*`), appends NVM_DIR loader to `~/.bashrc`. Called by `provision-linux.sh` and `provision-macos.sh`. |
| `packer/scripts/provision-linux.sh` | Ubuntu provisioner. `apt update/upgrade`, installs `build-essential curl git openssh-server python3 ca-certificates xvfb libnss3 libgtk-3-0 libgbm1 libasound2` (xvfb + Electron shared libs for `08-electron-real-launch.sh`), enables ssh, runs `provision-common.sh`, cleans apt cache. |
| `packer/scripts/provision-macos.sh` | macOS provisioner. Installs Xcode Command Line Tools via `softwareupdate` (headless-friendly touch trick), Homebrew (`NONINTERACTIVE=1`), `brew install git`, runs `provision-common.sh`. |
| `packer/scripts/provision-windows.ps1` | Windows provisioner. Installs Chocolatey, git, nvm-windows, Node.js LTS, VS Build Tools 2022 (`VCTools` workload for node-pty native compile), OpenSSH.Server capability + sshd auto-start + DefaultShell=PowerShell. `Update-Environment` helper re-imports Machine+User env block into Process scope (Chocolatey PATH staleness workaround). |
| `packer/ubuntu-arm.pkr.hcl` | Packer template for Ubuntu ARM. `vmware-iso` builder (`source.vmware-iso.ubuntu-arm`), `guest_os=arm-ubuntu-64`, `cpus=2`, `memory=4096`, `disk_size=40960`, `http_directory=http`, `autoinstall ds=nocloud-net` boot command. Provisions `provision-common.sh` + `provision-linux.sh`. Output `../output/${var.vm_name}`. |
| `packer/ubuntu-x86.pkr.hcl` | Packer template for Ubuntu x86. `vmware-iso` builder (`source.vmware-iso.ubuntu`), `guest_os=ubuntu-64`, `cpus=2`, `memory=4096`, `disk_size=40960`, `http_directory=http`, `autoinstall ds=nocloud-net` boot command. Provisions `provision-common.sh` + `provision-linux.sh`. Output `../output/${var.vm_name}`. |
| `packer/vars/macos-14-arm.pkrvars.hcl` | Vars for `macos-arm.pkr.hcl`. `source_vmx=REPLACE_WITH_PATH_TO_MACOS_ARM_VM.vmx` (placeholder, fails preflight until filled), `vm_name=macos-14-arm`, `ssh_username=qa`, `ssh_password=qa`, `cpus=4`, `memory=8192`. |
| `packer/vars/macos-14.pkrvars.hcl` | Vars for `macos-x86.pkr.hcl`. `source_vmx=REPLACE_WITH_PATH_TO_MACOS_VM.vmx` (placeholder, fails preflight until filled), `vm_name=macos-14-x86`, `ssh_username=qa`, `ssh_password=qa`, `cpus=4`, `memory=8192`. |
| `packer/vars/ubuntu-24-arm.pkrvars.hcl` | Vars for `ubuntu-arm.pkr.hcl`. `iso_url=https://cdimage.ubuntu.com/releases/24.04/release/ubuntu-24.04.2-live-server-arm64.iso`, `iso_checksum=sha256:REPLACE_WITH_ACTUAL_ARM64_CHECKSUM` (placeholder, fails preflight), `vm_name=ubuntu-24-arm`, `guest_os=arm-ubuntu-64`, `cpus=2`, `memory=4096`, `disk_size=40960`. |
| `packer/vars/ubuntu-24.pkrvars.hcl` | Vars for `ubuntu-x86.pkr.hcl`. `iso_url=https://releases.ubuntu.com/24.04/ubuntu-24.04.2-live-server-amd64.iso`, `iso_checksum=sha256:d6fea1f11b4d23b481a48f4f195464a770c004a1b1f0edee4b2f180b149d3a3e`, `vm_name=ubuntu-24-x86`, `guest_os=ubuntu-64`, `cpus=2`, `memory=4096`, `disk_size=40960`. |
| `packer/vars/win-11.pkrvars.hcl` | Win 11 Enterprise Eval x86_64 Packer vars. Sets `iso_url`, `iso_checksum` (placeholders), `vm_name=windows-11-x86`, `guest_os=windows9-64`, `cpus=2`, `memory=4096`, `disk_size=65536`. 90-day eval window. |
| `packer/windows.pkr.hcl` | Packer vmware-iso template builds Windows 11 base image. vmx_data enforces UEFI + Secure Boot + vTPM 2.0 (`firmware=efi`, `tpm.present=TRUE`), `disk_adapter_type=lsisas1068`, `network_adapter_type=e1000e`, `vhv.enable=TRUE`. Communicator ssh (`qa`/`qa`, 45m timeout). Floppy loads `http/autounattend.xml`. Provisions via `scripts/provision-windows.ps1` + `windows-restart`, then verifies node/npm/git. |
| `README.md` | Full QA setup + usage docs. Prereqs (Packer, VMware Fusion, `ssh-keygen -t ed25519 -f ~/.ssh/qa_vm_key`), ISO download table, hardware split (Intel Mac x86 builder, M1 Mac ARM builder), quick start (`make build-/test-/manual-`), manual macOS VM install steps (`vmware-vmx` builder, no unattended install), test coverage list (install, server start, WS, terminal, git ops, Electron zip V2 bootstrap Windows, NSIS Setup.exe smoke Windows, Electron real-launch Linux `xvfb-run`), directory structure map. |
| `scripts/run-test.sh` | QA test orchestrator. Clone → wait SSH → upload tests → run → destroy. Args: `<platform> <base-image-dir> <ssh-user> <ssh-key> [ssh-timeout]`. Windows path uploads Electron ZIP (`QA_ELECTRON_ZIP` env, default `packages/electron/out/.../PI-Dashboard-win32-x64.zip`) to `C:/qa-artifacts`. Runs `run-all.ps1` (Windows) / `run-all.sh` (Unix). trap cleanup destroys clone on exit. |
| `scripts/vm-clone.sh` | Linked-clone a VMware base image via `vmrun clone`. Args: `<base-image-dir> <clone-name> [gui]`. Locates `.vmx`, starts clone `nogui` (default) or `gui`. Emits clone `.vmx` path to stdout for command substitution. |
| `scripts/vm-destroy.sh` | Destroy cloned VMs via `vmrun stop hard` + `deleteVM`. Args: `<clone-name>` or `--all`. Operates under `output/clones/`. `--all` removes every clone dir. |
| `scripts/vm-wait-ssh.sh` | Poll VMware VM SSH readiness. Args: `<vmx-path> <ssh-user> <ssh-key> [timeout=120]`. Loops `vmrun getGuestIPAddress`, then `ssh -i <key> -o BatchMode=yes` until succeed. Emits VM IP to stdout. |
| `tests/01-install.ps1` | Windows install smoke. `npm install -g @blackbelt-technology/pi-dashboard`; verify `pi-dashboard --version` non-empty. Exit 1 on empty version. |
| `tests/01-install.sh` | Unix install smoke. Sources nvm, `npm install -g @blackbelt-technology/pi-dashboard`, verify version + global node_modules dir present (`npm root -g`). Catches node-pty compile absence. |
| `tests/02-server-start.ps1` | Windows server-start smoke. `Start-Process pi-dashboard start`, poll `http://localhost:8000/api/health` for 200 (15s). finally block calls `pi-dashboard stop` + force-kills proc. |
| `tests/02-server-start.sh` | Unix server-start smoke. `pi-dashboard start &`, poll `/api/health` for HTTP 200 (15s). Verifies `~/.pi/dashboard/server.log` exists + non-empty (catches v0.4.6 `spawnDetached stdio[1]='ignore'` 0-byte regression). trap cleanup stops server. |
| `tests/03-websocket.ps1` | Windows WebSocket smoke. Probe `/api/health`, then inline Node script `net.connect` pi gateway :9999 + browser WS :8000 with 3s timeouts. Exit 1 if either fails. |
| `tests/03-websocket.sh` | Unix WebSocket smoke. Probe pi gateway `ws://localhost:9999` + browser `ws://localhost:8000/ws` via `ws` module (5s timeout). Falls back to curl port-connectivity when `ws` unavailable. Requires server running. |
| `tests/04-terminal.ps1` | Windows PTY smoke. POST `http://localhost:8000/api/terminals` (`{cwd: $env:TEMP}`); fallback inline node-pty check spawns `powershell.exe echo hello-pty` via ConPTY, asserts `hello-pty` received. |
| `tests/04-terminal.sh` | Unix PTY smoke. POST `/api/terminals` `{"cwd":"/tmp"}`, parse `id`. Fallback verifies `require('node-pty')` loads. Requires running server. |
| `tests/05-git-ops.ps1` | Windows git smoke. `git init` temp repo + commit, query `http://localhost:8000/api/git/branches?dir=<base64url>`. Fallback `git branch --list`. Cleans temp dir in finally. |
| `tests/05-git-ops.sh` | Unix git smoke. `git init` temp repo + commit, base64url-encode dir, GET `/api/git/branches?dir=<enc>`, grep `main|master`. Fallback local `git branch --list`. Cleans temp dir. |
| `tests/06-electron-offline-bundle.sh` | Validate offline-packages bundle inside packaged Electron app Resources. Arg `<app-resources-dir>`. Checks `offline-packages/{manifest.json,npm-cache.tar.gz}` exist; node parses manifest (`sha256`, `packages[]`, `targetPlatform`); `shasum -a 256`/`sha256sum` verifies tarball hash against manifest. Exit 0 (warn-skip) if no checksum utility. |
| `tests/07-electron-bootstrap-v2.ps1` | Electron V2 LaunchSource bootstrap e2e on Windows ZIP. Extract `C:\qa-artifacts\PI-Dashboard-win32-x64.zip` → launch `pi-dashboard.exe` → wait `/api/health` (180s, ports 8000 + 8112). Asserts `starter==Electron`, `managedDir/.version`, `pi-coding-agent` + cliPath installed under `~/.pi-dashboard`, `~/.pi/dashboard/server.log` non-empty (catches spawnDetached stdio regression). Skips when ZIP absent. |
| `tests/08-electron-real-launch.sh` | Linux Electron AppImage launch smoke under `xvfb-run`. Polls `/api/health` within 90 s, asserts `starter == "Electron"`, `~/.pi/dashboard/server.log` non-empty, no `FATAL` in stdout. Skips (exit 0) when AppImage absent; fails when `xvfb-run` missing. Pre-launch seeds a `.bin/openspec`→`bin/openspec.js` shebang symlink + `~/.config/openspec/config.json` (profile core); post-health asserts `GET /api/openspec/config` returns 200 + profile core (pre-fix stripped-PATH shebang dies 127→502), SKIP if seed uncreatable. See change: expand-electron-qa-coverage, fix-openspec-config-read-bundled-node. |
| `tests/09-electron-mac-launch.sh` | macOS in-CI Electron launch smoke. Resolves `.app` from `packages/electron/out/PI-Dashboard-darwin-*/` (forge package output), falls back to copying out of mounted DMG (`xattr -dr com.apple.quarantine` on copy). Direct-execs inner Mach-O `Contents/MacOS/pi-dashboard` (NOT `open`); no `--no-sandbox`. Wipes `~/.pi/dashboard/server.log` pre-launch. Seeds `~/.pi/dashboard/first-run-done` pre-launch to skip blocking welcome-wizard window (else server spawn waits on wizard close, no CI user). Launches `--disable-gpu` (no usable runner GPU; macOS analog of xvfb). Polls `/api/health` ≤120s; asserts HTTP 200, `launchSource==electron`, server.log size>0, no `FATAL` in Electron stdout. `SKIP:`+exit 0 when `.app` absent. Boot-proof not floor-proof (runner macOS 14/15 above 10.15 floor; static `otool minos` check in `_electron-build.yml` is label-only floor guard). Invoked from macOS legs of `.github/workflows/_electron-build.yml` after floor check. Pre-launch seeds a `.bin/openspec`→`bin/openspec.js` shebang symlink + `~/.config/openspec/config.json` (profile core); post-health asserts `GET /api/openspec/config` returns 200 + profile core (pre-fix stripped-PATH shebang dies 127→502), SKIP if seed uncreatable. See change: add-macos-electron-launch-smoke, fix-openspec-config-read-bundled-node. |
| `tests/09-image-fit-extension.ps1` | Windows port of pi-image-fit install + dep-tree sanity. Verifies `@blackbelt-technology/pi-image-fit` installs, `src/extension.ts` present, jimp 1.x in tree, no `sharp` / `@napi-rs/image` / `@napi-rs/canvas`. Falls back to local workspace `packages/image-fit-extension` on registry miss. |
| `tests/09-image-fit-extension.sh` | Bash pi-image-fit install + dep-tree sanity. `npm install @blackbelt-technology/pi-image-fit` in temp dir, asserts `src/extension.ts` shipped, jimp 1.x present, no native-image deps. Workspace fallback on pre-publish registry miss. |
| `tests/10-bundled-git.ps1` | Verifies bundled dugite-native git + sh on Windows. Forces `windowsGitSource=bundled` via `/api/config`, asserts `/api/health.gitSource.source == "bundled"`, `gitPath` under `resources/git`, bundled `git status` + `sh --version` run. Restores original setting in finally. See change: embed-git-bash-on-windows. |
| `tests/10-faux-model.sh` | VM smoke: faux prompt round-trip on clean box. Node driver connects `/ws`, snapshots pre-existing sessions, spawns `pi --mode rpc -e <fixture> --model faux/faux-1` (FAUX_SCRIPT=plain-text), drives prompt via REST, asserts scripted text on `/ws`. `SKIP:`+exit 0 when pi absent. Registered in `qa/tests/run-all.sh`. See change: add-faux-model-integration-tests. |
| `tests/run-all.ps1` | Windows QA suite runner. Runs ordered test list (`01-install` … `10-bundled-git`), tallies PASS/FAIL/SKIP, runs `pi-dashboard stop` cleanup, exits 1 on any FAIL or SKIP. |
| `tests/run-all.sh` | Bash QA suite runner. Sources nvm, runs ordered test list (`01-install` … `10-faux-model`), detects SKIP via first-line `SKIP:` prefix, tallies PASS/FAIL, prints summary box, exits 1 on any FAIL. |
| `tests/windows-nsis-branding.ps1` | NSIS installer branding check. Asserts HKCU Add/Remove `Publisher == "BlackBelt Technology"`, optionally extracts uninstaller icon for SHA compare against built asset. Params: `-Setup`, `-UninstallerIco`. |
| `tests/windows-nsis-install-custom-dir.ps1` | NSIS silent install to user-chosen dir (design D3 regression guard). Runs `Setup.exe /S /D=<Dir>` via ProcessStartInfo (raw arg, no re-quoting), polls for `pi-dashboard.exe`, asserts HKCU `InstallLocation` matches. Param: `-Setup`, `-Dir` (default `D:\TestApps\PI Dashboard`). |
| `tests/windows-nsis-install.ps1` | NSIS per-user default-path silent install (restore-windows-nsis-installer guard). Downloads URL or uses local `Setup.exe`, runs `/S /D=$LOCALAPPDATA\Programs\PI Dashboard`, polls for exe + Start Menu shortcut, asserts HKCU Add/Remove entry with `Publisher == "BlackBelt Technology"`. Param: `-Setup`. |
| `tests/windows-nsis-launch.ps1` | Launches installed `pi-dashboard.exe`, polls `/api/health` for 200 within `TimeoutSec` (default 60). On failure dumps Electron stdout/stderr + `~/.pi/dashboard/server.log` tail. Params: `-Dir`, `-Port`, `-TimeoutSec`. |
| `tests/windows-nsis-no-permachine.ps1` | Asserts NSIS installer is per-user only (design D2). No HKLM Add/Remove entry (32- or 64-bit view), install dir not under Program Files. Assumes `windows-nsis-install.ps1` already ran. |
| `tests/windows-nsis-uninstall.ps1` | NSIS uninstall preserves user data (design D4). Seeds `~/.pi/qa-preserve-marker.txt`, runs uninstaller `/S` (resolved from registry `UninstallString` or `Uninstall*.exe` glob), polls install dir gone, HKCU entry gone, `~/.pi` + `~/.pi-dashboard` intact. Param: `-Dir`. |
