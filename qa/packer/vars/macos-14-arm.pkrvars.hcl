# macOS 14 Sonoma aarch64 (ARM)
# For building on M1 Mac
# Requires a manually-installed macOS ARM VM (see README.md)

source_vmx   = "REPLACE_WITH_PATH_TO_MACOS_ARM_VM.vmx"
vm_name      = "macos-14-arm"
ssh_username = "qa"
ssh_password = "qa"
cpus         = 4
memory       = 8192
