# 09-image-fit-extension.sh — index

Bash pi-image-fit install + dep-tree sanity. `npm install @blackbelt-technology/pi-image-fit` in temp dir, asserts `src/extension.ts` shipped, jimp 1.x present, no native-image deps. Workspace fallback on pre-publish registry miss.
