# 15-omit-dev-build.sh — index

X1: a fresh checkout builds the client under `--omit=dev` (#357). Three arms over a pristine `git clone` each — (1) `npm install --omit=dev --engine-strict=false` isolates the BUILD failure (`Cannot find module 'vite/package.json'`); (2) `npm install --omit=dev` with root `.npmrc engine-strict=true` in force = the ONLY full-fidelity repro of the real pi codepath, SKIPs loudly below Node 26 (vacuous otherwise); (3) `pnpm install --prod --frozen-lockfile` for locked-tree reproducibility. Resolves pnpm's omit-dev flag up front (`--prod` vs `--omit=dev`) so arm 3 cannot pass on a silently-accepted no-op. Asserts exit 0 + `packages/client/dist/index.html`; a pass/fail split across arms signals npm↔pnpm resolution drift. Arm 1's `--engine-strict=false` is a test-only bypass, NOT a user workaround. See change: fix-pi-install-node26-and-omit-dev-build.
