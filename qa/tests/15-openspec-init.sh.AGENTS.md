# 15-openspec-init.sh — index

X8/X12: POST `/api/openspec/init` with a squatted `openspec` 0.0.0 stub earlier on PATH — stub never invoked (tool-registry wins), the harness project's `openspec-explore` skill + `opsx-*` prompt files written under its .pi tree (`--tools pi` survived), update-status `up-to-date` after init (signature recorded). See change: add-openspec-init-affordances.
