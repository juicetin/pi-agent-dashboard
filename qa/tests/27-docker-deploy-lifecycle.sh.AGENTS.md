# 27-docker-deploy-lifecycle.sh — index

OPT-IN L2 (`PI_QA_DOCKER=1`), not in `run-all.sh`. Runs the DOCUMENTED `docker compose up -d` — no test entrypoint, no `user: root`, fresh volumes — and asserts it boots, then holds `PI_QA_DOCKER_SETTLE` (120s) and requires `RestartCount == 0`, because `restart: unless-stopped` makes a cycling container look up at any single instant. Then the task-8.7 half: the gateway socket exists on the VOLUME mount owned by `pi`, a `ws+unix://` client inside the container registers through it, and the session comes back with NO `originDeviceId`. Written red: it found the VOLUME root-ownership EACCES loop, the daemonizing PID 1, a 401 on socket upgrades once TCP is enabled, and socket peers being attributed REMOTE. Spawn-based registration was rejected as the probe — a real pi needs credentials the deployment does not ship. See change: add-pi-gateway-transport-identity.
