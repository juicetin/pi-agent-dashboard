# 28-gateway-windows.ps1 — index

Windows gateway transport + identity (tasks 5.1/5.5/5.7/12.53). Four sections in falling order of certainty: `/api/health` advertises a NUMERIC loopback port with no `gateway-*.sock` artifact anywhere and a real listener on it; a bridge connects, is `terminate()`d like a sleeping laptop, and reconnects as exactly ONE session; the live `instanceId` is readable for identity checks; and a second STANDARD OS user (`New-LocalUser`, asserted NOT in Administrators) attempts to read `~/.pi/dashboard/local/token` in the REAL profile — inheritance is the mechanism under test, so a temp HOME would measure the wrong ACL. Reports ACL inspection and empirical read SEPARATELY and says which of "denied" vs "infeasible" it hit; a broad-principal DACL with no empirical read is a hard fail. A successful read is task 5.6's trigger (pre-existing, own change). Run by `.github/workflows/ci-gateway-platform.yml`.
