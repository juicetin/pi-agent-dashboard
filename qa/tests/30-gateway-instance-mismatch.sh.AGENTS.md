# 30-gateway-instance-mismatch.sh — index

A record naming one instance while another answers (task 5.4b, D14). Drives a REAL hand-run pi through the rendezvous-record path — a dashboard-spawned session gets `PI_DASHBOARD_SOCKET`, which outranks the record, so it never exercises this. Control arm: a truthful record draws no refusal. Mismatch arm: `identity` tampered (ports untouched, so the endpoint stays reachable) must produce `instance verification refused` naming BOTH ids and `disconnecting`, and must NOT fall back to a discovered substitute. Loads the bridge from this checkout via a hermetic `settings.json`; SKIPs (exit 0) with no pi binary. Teeth: making the caller ignore a correct verdict — the failure a pure-function unit test structurally cannot see — turns it red. Ports 18870/19870.
