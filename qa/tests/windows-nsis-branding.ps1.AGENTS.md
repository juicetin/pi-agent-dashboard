# windows-nsis-branding.ps1 — index

NSIS installer branding check. Asserts HKCU Add/Remove `Publisher == "BlackBelt Technology"`, optionally extracts uninstaller icon for SHA compare against built asset. Params: `-Setup`, `-UninstallerIco`.
