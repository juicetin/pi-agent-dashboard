// Fixture (b): jiti's transform preserves comments, so a textual check would
// false-positive here. `import.meta.resolve` is mentioned in this doc-comment
// only. MUST NOT be reported.
export const value = 1;
