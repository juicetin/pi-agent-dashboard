# commit-windows-parity.sh — index

Stages and commits fix-windows-server-parity + consolidate-platform-handlers work in 5 logical chunks (fix:, test:, docs: x3). Modes: all | dry | 1..5. commit_stage helper resets index, stages per-stage filesets, commits with imperative conventional messages.
