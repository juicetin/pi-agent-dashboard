# Proposal — Add keyboard shortcut help

## Why

Shortcuts exist but are undiscoverable; there is no overlay listing them.

## What Changes

- Add a `?` overlay.
- Generate it from the keymap so it cannot drift.

## Discipline Skills

None apply: this change adds UI surface with no new external call, auth path or latency budget.
