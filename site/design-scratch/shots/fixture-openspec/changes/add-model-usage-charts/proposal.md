# Proposal — Add model usage charts

## Why

Token and cost history is recorded but never shown, so nobody can see which model is eating the budget.

## What Changes

- Chart tokens and cost over time.
- Break down by provider and model.
- Export the window as CSV.

## Discipline Skills

None apply: this change adds UI surface with no new external call, auth path or latency budget.
