# session-cost-rollup

## ADDED Requirements

### Requirement: Session cost rollup

The dashboard SHALL sum cost per folder and per workspace.

#### Scenario: session-cost-rollup applies

- **WHEN** a user opens the affected surface
- **THEN** the new behaviour is visible without a reload
