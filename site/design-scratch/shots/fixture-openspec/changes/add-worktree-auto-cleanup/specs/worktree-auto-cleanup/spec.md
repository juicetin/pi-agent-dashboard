# worktree-auto-cleanup

## ADDED Requirements

### Requirement: Worktree auto cleanup

The dashboard SHALL detect merged branches with no live session.

#### Scenario: worktree-auto-cleanup applies

- **WHEN** a user opens the affected surface
- **THEN** the new behaviour is visible without a reload
