# openspec-change-grouping

## ADDED Requirements

### Requirement: Openspec change grouping

The dashboard SHALL land the change behind its spec delta.

#### Scenario: openspec-change-grouping applies

- **WHEN** a user opens the affected surface
- **THEN** the new behaviour is visible without a reload
