# plugin-registry

## ADDED Requirements

### Requirement: Plugin registry

The dashboard SHALL collapse to a single manifest.

#### Scenario: plugin-registry applies

- **WHEN** a user opens the affected surface
- **THEN** the new behaviour is visible without a reload
