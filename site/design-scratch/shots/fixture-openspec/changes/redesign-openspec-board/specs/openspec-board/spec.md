# openspec-board

## ADDED Requirements

### Requirement: Openspec board

The dashboard SHALL add per-repo groups with colour and order.

#### Scenario: openspec-board applies

- **WHEN** a user opens the affected surface
- **THEN** the new behaviour is visible without a reload
