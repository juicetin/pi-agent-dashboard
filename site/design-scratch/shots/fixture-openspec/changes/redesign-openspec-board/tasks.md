# Tasks — Redesign openspec board

## 1. Implementation

- [x] 1.1 Add per-repo groups with colour and order.
- [x] 1.2 Persist manual change ordering per column.
- [x] 1.3 Join `groupId` server-side so clients never recompute it.
- [x] 1.4 Add per-repo groups with colour and order.
- [x] 1.5 Persist manual change ordering per column.
- [x] 1.6 Join `groupId` server-side so clients never recompute it.
- [x] 1.7 Add per-repo groups with colour and order.
