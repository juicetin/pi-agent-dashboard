# remote-access

## Purpose

The dashboard SHALL expose a tunnel with a scannable QR code.

## Requirements

### Requirement: Access from anywhere

The dashboard SHALL expose a tunnel with a scannable QR code.

#### Scenario: access from anywhere

- **WHEN** a tunnel is enabled
- **THEN** a QR code encoding the public URL is shown
