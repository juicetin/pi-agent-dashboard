# session-monitoring

## Purpose

The dashboard SHALL show every known session with its state, model and elapsed time.

## Requirements

### Requirement: Session state is visible at a glance

The dashboard SHALL show every known session with its state, model and elapsed time.

#### Scenario: session state is visible at a glance

- **WHEN** a session changes state
- **THEN** its card reflects the new state within one broadcast
