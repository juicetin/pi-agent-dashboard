{
  "name": "sample-git",
  "version": "0.3.1",
  "private": true,
  "type": "module",
  "description": "Session store and formatting helpers",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "test": "vitest run",
    "lint": "biome check ."
  },
  "devDependencies": {
    "typescript": "^5.7.2",
    "vitest": "^2.1.8"
  }
}
