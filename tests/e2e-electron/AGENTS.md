# DOX — tests/e2e-electron

Files in this directory. One row per file. Non-source area. Electron-E2E suite: launches the REAL packaged Electron app via Playwright `_electron` (no Docker). Distinct from `tests/e2e/` (web client, Docker :18000). Config: root `playwright.electron.config.ts`. Run: `npm run test:e2e:electron` (pretest packages the app via `electron-forge package`; skips if `packages/electron/out` exists or `PW_ELECTRON_BINARY` set). CI: `.github/workflows/ci-e2e-electron.yml` (workflow_dispatch; matrix ubuntu-latest under xvfb + windows-latest direct — windows exercises the win32 `decideIsZombie` branch in the real app). See change: electron-attach-ownership-fixes.

| File | Purpose |
|------|---------|
| `electron-lifecycle.ts` | Harness helpers. `startFakeHealthServer(health, {port?,healthDelayMs?})` — stub `/api/health` (crafted payload, ~400ms delay so dialog stub installs first), minimal HTML for other GETs (BrowserWindow loadURL), records POST `/api/shutdown`. `makeThrowawayHome(port)` — temp HOME with `~/.pi/dashboard/config.json` (port pinned) + first-run marker (skips wizard → attach arm). `resolvePackagedBinary()` — finds `packages/electron/out/*/` binary per-OS (`PW_ELECTRON_BINARY` override). `launchElectron({home, zombiePrompt?})` — `_electron.launch` (clears DASHBOARD_STARTER; `--no-zombie-prompt` when false). `stubDialog(app, idx)` / `readDialogCalls(app)` — monkeypatch `dialog.showMessageBox` in main via `app.evaluate`, record calls on `globalThis.__zombieDialogCalls` (native modals can't be clicked). `captureMenuTemplates(app)` / `readMenuTemplates(app)` record built `Menu.buildFromTemplate` templates (plain {label,enabled,type}) on `globalThis.__menuTemplates` while returning a real Menu (native tray menu can't be read back). `isPortInUse(port)` — local-safety skip guard. `FAKE_PORT` = 8000 (Doctor version-skew fetcher hard-codes `:8000`). See change: electron-attach-ownership-fixes. |
| `zombie-adoption.electron.spec.ts` | Tasks 4.4/7.3. Attaches app to zombie-shaped fake `/api/health` (launchSourceEffective electron, bootParentAlive false, ppid≠bootParentPid, no stored pid). Asserts: modal shown with 3 buttons + zombie PID in detail; `--no-zombie-prompt` suppresses; "Take ownership" (idx 0) → app quit POSTs `/api/shutdown` (decideShutdownOnQuit after setSpawnedPid). Skips if :8000 in use. See change: electron-attach-ownership-fixes. |
| `doctor-version-skew.electron.spec.ts` | Task 7.5. Attaches app to fake `/api/health` with version≠app version + launchSource standalone; clicks loading-page `#doctor-btn` → doctor window; asserts "Attached server version" row = WARN pill + suggestion contains `npm i -g @blackbelt-technology/pi-dashboard@`. Skips if :8000 in use. See change: electron-attach-ownership-fixes. |
