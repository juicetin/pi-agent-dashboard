# anthropic-bridge-activation.spec.ts — index

L3 spec (change: add-flow-plugin-e2e-tests). Asserts the flows-anthropic-bridge regression via `/api/health`.plugins[] (uses `ensureGitSession` to avoid piling heavy pi-flows sessions). `both` (managed default): FIRM `bridgeLoadedFrom: "packages[]"` + loaded + enabled (the bridge-invisible-to-pi / "no sessions reporting" regression guard); live `lastProbe.status` is BEST-EFFORT only — the bridge's peer-probe status is emitted on the pi-session bus and NOT reliably forwarded to the server store in this build (documented v1 gap), so active/waiting_peers is not firmly observable via the API. Env-gated variants (opt-in, separately-booted): `no-am` -> `waiting_peers` (needs the status-forward path); `bad-registration` -> `bridgeLoadedFrom !== packages[]` (observable, server-computed). Needs PI_E2E_SEED=1.
