# blackhole-settings.spec.ts — index

L3 spec (change: add-blackhole-plugin). Covers test-plan X3 + F1-F9 for `/settings/plugins/blackhole`. F6 (extension absent) runs against the REAL unrouted harness state — `pi-blackhole` is deliberately never installed, so the requirement probe reports it missing and the plugin's OWN not-installed state renders. Every other row `page.route`s `/api/plugins` (rewrites `missingRequirements` to `[]`, keeping all other rows authentic) and `/api/plugins/blackhole/config` (config fixture or 409 parse-error), then asserts rendered DOM + accessibility tree: keyboard-only reorder convergence, boundary controls disabled-not-absent, accessible names naming the model, `data-excluded` tail vs `sessionFallback`, no-restart copy, host save-bar dirty/discard, and absence of per-session content. The dirty-overlay dismissal matrix is Escape + backdrop only: a route-backed overlay is a flush `Dialog` and renders no built-in ✕; its ABSENCE is asserted inside each row so the shrunk gesture list is not silent coverage loss. See change: fix-flush-dialog-scroll-and-close-collision.
