# custom-entry-fallback.spec.ts — index

L3 (change: render-inline-reasoning-and-custom-entries, E11). `[[faux:custom-entries]]` drives real pi.sendMessage/appendEntry; toggles `customEntryFallback` via the View popover — rows flip without reload. → see `custom-entry-fallback.spec.ts.AGENTS.md`
