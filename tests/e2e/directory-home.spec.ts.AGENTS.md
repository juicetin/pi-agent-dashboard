# directory-home.spec.ts — index

L3 for the `/folder/:encodedCwd` directory home page (change: add-directory-home-page). F1: open affordance → type → send → converges on a new `/session/:id` with the prompt in the transcript. Mobile: back from the home page pops to the depth-0 card list. Mirrors `encodeFolderPath` locally (the web package does not export internals). See change: add-folder-actions-menu — the dedicated `folder-open-home-<cwd>` icon is DELETED, so every case drives the header row `folder-home-row-<cwd>`; adds a `folder actions menu` describe covering F1 (opening keeps route + expanded state via `folder-body-<cwd>`), F2/F3 (row click navigates, stays expanded), F4 (zero `folder-open-home-*` nodes) and F5/F6/F7 — a table-driven viewport sweep asserting `data-menu-form` is `sheet` at 375x900 and 1200x560 (the mobile predicate is compound: `<768w OR <600h`) and `popover` at 1200x900, plus a bounding-box check for full-width/no-overflow vs floating. Needs `PI_E2E_SEED=1`.
