# error-lifecycle.spec.ts — index

Playwright spec. Single-card error-lifecycle surface end-to-end via faux model (no LLM). 4 tests. Test 1: `[[faux:model-error]]` → ONE settled `error-banner` + `error-banner-dismiss`; `error-banner-retry` count 0 (manual retry removed), `error-banner-stop` count 0 (Stop shows only while retrying), `retry-banner` count 0 (settled). Test 2 (clear-only ✕, task 7.2): `[[faux:model-error]]` → click `error-banner-dismiss` → card hidden + session STILL alive (`[[faux:plain-text]]` round-trips in same session, proving ✕ did NOT abort). Test 3: `[[faux:model-error]]` then `[[faux:ask-select]]` → error card STILL visible while ask_user paused (deferred clear; not cleared on agent_start / toolUse stop). Test 4: `[[faux:model-error]]` then `[[faux:plain-text]]` → card hidden after PLAIN_TEXT_MARKER (clears on confirmed-good `stop`). Needs `PI_E2E_SEED=1`. The retrying sub-line (`retry-banner` while pi re-attempts) + "Stop (ends the session)" abort are NOT covered here — driving a real pi retry loop through the faux harness is a separate spike (task 7.1). See change: simplify-error-retry-single-card.
