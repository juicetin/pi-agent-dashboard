# gateway-origin-surfaces.spec.ts — index

L3 for the transport/identity CLIENT surfaces (test-plan #F1–#F5 → tasks 12.47–12.51). Produces a REAL remote bridge by pairing + minting a ticket from the host (docker-bridge address ⇒ `originDeviceId` derived), and a LOCAL control from inside the container over loopback. Asserts: origin chip names the device; `session_moved` renders the moved badge live AND across a reload; ended REMOTE offers no resume/fork while ended LOCAL does. Two traps encoded: ended cards are collapsed behind `folder-ended-toggle-<cwd>` (a session search auto-expands them — without it absence assertions pass vacuously), and a closed socket does NOT end a session for up to 2×180 s `HEARTBEAT_TIMEOUT`, so both arms end via `session_moved`. F2 covers only the `tcp` transport arm; the UDS `address()`-returns-a-path arm is L1-only. All five proven fails-on-revert. See change: add-pi-gateway-transport-identity. F7 adds the server-side half of the resume gate (task 13.6): it sends `resume_session` over the BUS — the path drag-to-resume and every automation use — and asserts the refusal, since F3's hidden button is a sign, not a door. Asserted on the bus rather than the page because `resume_result` goes to the socket that ASKED; a browser that did not initiate is correctly told nothing. Proven fails-on-revert against a mutated image, where F3 still passed while F7 caught the open hole.
