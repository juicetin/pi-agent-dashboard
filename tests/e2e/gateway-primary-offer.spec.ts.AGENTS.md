# gateway-primary-offer.spec.ts — index

F7/F8 of add-zrok-custom-reserved-name — confirm-gated primary switch and the registration offer. Asserts the NEGATIVE across a real click: the consequence is on screen while `/api/config` is still untouched. Mutates `tunnel.provider` + `gateways` on the shared harness; `afterAll` restores both. See change: add-zrok-custom-reserved-name.
