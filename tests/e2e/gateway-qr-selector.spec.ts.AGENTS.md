# gateway-qr-selector.spec.ts — index

Browser E2E for the Gateway single-QR network selector (change: add-gateway-qr-network-selector; automates most of manual task 5.3). Stubs `/api/tunnel/endpoints` + `/api/pair/payload` via `page.route` (no live tunnel in the container) with a public-TLS + local + LAN mix. Navigates Settings → Gateway nav rail. Test 1: exactly one `gateway-qr-canvas`, default `aria-checked` on the public/pairing row, `data-qr-text` starts `pi:pair:v1.`, decodes the real canvas bitmap with `jsQR` (== `data-qr-text`); selecting the LAN row swaps panel (copystring/confirm gone, `gateway-link-note` shown), `data-qr-text` == bare URL, jsQR decode == URL. Test 2: `ArrowDown` on the checked radio moves selection to localhost (`aria-checked` shifts + QR swaps). `jsqr` devDep. Uses `gotoDashboard`, `byTestId`.
