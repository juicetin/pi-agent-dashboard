# gateway-url-action.spec.ts — index

L3 spec (change: config-override-oauth-redirect-base, D12/D13/D15). Drives the shared `gateway-url-manager` on `/settings/gateway`. F5: add an https gateway → `publicBaseUrls` + `cors.allowedOrigins` + `gateways[]` written in ONE `PUT /api/config`, row reports `data-status=ok`. F8: delete the cors entry behind the gateway's back → row flips to `incomplete`, `Fix` restores exactly that value and does not duplicate the untouched list. F9 (the payoff): `OPTIONS /api/health` with `Origin: <gateway>` returns `access-control-allow-origin` with NO restart — pins the D15 live CORS read. F7: remove → confirmation names each reverted field, values disappear from config. F6: an `http://` URL disables the OAuth + QR checkboxes with a stated reason, demands a trusted network, and prefills the exact `/32` host. F11: dialog contrast ≥3.0 in dark + light (same relative gate as `severity-contrast.spec.ts`). F12: the first-run setup guide renders the same manager. `afterAll` restores the captured `publicBaseUrls`/`cors`/`trustedNetworks`/`gateways` so a throw cannot leak a widened CORS list into later specs. 7/7 green against the docker harness; F5/F12 caught a real double-render (the page embeds `GatewaySetupGuide`, which also mounted the manager — fixed via `showGatewayUrls={false}`).
