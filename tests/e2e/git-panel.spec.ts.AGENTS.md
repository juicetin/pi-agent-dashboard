# git-panel.spec.ts — index

Scenario 5.2 spec. Calls `ensureGitSession`. Asserts page-level `git-branch-btn` (title "Switch branch") visible. Proves bridge reported `session.gitBranch` from git status read of sample-git fixture. Re-scoped from worktree-only composer-git-group. See change: add-e2e-spawn-scenarios.
