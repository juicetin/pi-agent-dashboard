# global-setup.ts — index

Playwright globalSetup. `PW_E2E_USE_RUNNING=1` → only verify `/api/health` (30s). Else spawn `docker/test-up.sh -d --build` (--build MANDATORY: server+client run from baked image) detached from throwaway tmp workspace (keeps overlay off repo), STRIPS `DASHBOARD_PORT`/`PI_GATEWAY_PORT`/`PW_E2E_PORT`/`PW_GATEWAY_PORT` from spawn env so test-up.sh hash-derives ports in-window, sets `PI_E2E_SEED=1` in spawn env, blanks `ANTHROPIC_API_KEY`/`OPENAI_API_KEY`/`GEMINI_API_KEY` so host keys never leak into disposable container, write `test-results/.e2e-managed` marker `{workspace,pid,logPath}`, `bootHealthyPorts` re-reads `.pi-test-harness.json` each iteration via `resolvePortsFromStateFile` + polls derived `/api/health` until 200 (180s), then locks `process.env.PW_E2E_PORT`+`PW_GATEWAY_PORT` for workers. Preflight `assertBrowserInstalled()` runs FIRST (before USE_RUNNING branch + before container boot). Resolves `chromium.executablePath()` via `@playwright/test` module (not `.bin/playwright` shim), `fs.existsSync` gate, try/catch backstop. Missing browser → throw fail-fast naming `npx playwright install chromium`, no container boot. `assertBrowserInstalled()` skips bundled-Chromium preflight when `PW_CHANNEL` set (system-browser path). Managed spawn env also sets `PI_TEST_PEERS` (default `both`) so the flow-plugin L3 specs get the pi-flows engine + anthropic-messages peer wired by test-entrypoint.sh. See change: add-playwright-e2e. See change: parallelize-test-harness. See change: add-e2e-spawn-scenarios. See change: self-heal-host-playwright-browser. See change: optimistic-prompt-progress. See change: add-flow-plugin-e2e-tests.
