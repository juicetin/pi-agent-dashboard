# headless-reload-dispatch.spec.ts — index

L3 for `fix-out-of-band-reload` (#F1–#F3). Bare `/reload` on a headless harness session: one terminal `/reload` pill, a NEW pid after the respawn, status converging off `ended`, accumulated tokens surviving the re-register.
