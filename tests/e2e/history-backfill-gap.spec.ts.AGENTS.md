# history-backfill-gap.spec.ts — index

L3 for `fix-lazy-history-backfill-ux` (F1–F6, F8–F11, X3, X5 — twelve scenarios). SERIAL; owns the `maxReplayEvents` mutation + `/api/restart` and restores both, and ends its own `beforeAll` session (a `beforeAll` spawn is never in the auto-reap delta). Budget `MIN_REPLAY_WINDOW` 100 over one `[[faux:long-transcript]]`, not 2000 over ~20k — the path is parametric and L1 pins the shipped default. THREE harness traps, each of which first presented as a product bug: the divider is VIRTUALIZED off-screen (scroll up before asserting); `locator.click()` auto-scrolls + focuses (real click moved scrollTop 0→280 BEFORE the splice, dispatched click 0→0 with divider y 589→589 while scrollHeight grew 4309→40883), so scroll tests use `dispatchEvent("click")`; and the divider's `y` carries the virtualizer's own estimate convergence (~21px/3s with NO backfill), so `scrollTop` is the asserted invariant. F8 SKIPS when no slice orphans a tool call — that is snapping working, and E24 gates the stamp at L1.
