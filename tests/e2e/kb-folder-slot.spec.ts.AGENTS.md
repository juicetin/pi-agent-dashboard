# kb-folder-slot.spec.ts — index

Playwright spec. KB folder slot end-to-end in Docker harness. Pins `/fixtures/kb-sample` (fixture carries knowledge_base.json origin=project sources:["."] + 2 md). Scopes KB row via xpath ancestor of `folder-urgency-sort-<cwd>` anchor (folder group div has no testid; sidebar hosts seeded sample-git too). Asserts folder-kb-section renders, folder-kb-open-settings reachable in every state, not-indexed → folder-kb-index-now click → POST /api/kb/reindex (session-less server reindex) → data-state populated + folder-kb-count contains chunks. Label click → /folder/:encodedCwd/kb overlay; asserts kb-settings-page, kb-config-origin project, one kb-source-row, kb-dbpath index.db. pinFixture helper settles initial listing + fills + asserts textbox value + Select (dodges PathPicker fill-reset race). Idempotent: skips pin when anchor present. Settles out of transient loading data-state before branching. Needs PI_E2E_SEED=1. Second test: pins /fixtures/kb-parent + nested worktree /fixtures/kb-parent/worktrees/kb-wt (no config); opens worktree KB settings; asserts kb-copy-parent + kb-create-config affordances; clicks kb-copy-parent → origin flips project + one kb-source-row + kb-config-count shows files (indexed); reloads → sidebar worktree row populated (v1 row refetches on mount/expand, not on settings-page index). prepareShell handles onboarding + dashboard modes. kbRowFor(page,cwd) helper scopes rows. See change: add-kb-folder-slot.
