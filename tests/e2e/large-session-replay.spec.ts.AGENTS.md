# large-session-replay.spec.ts — index

L3 gate for `compact-warm-replay-stream` (#399). Wire-level (same 2nd-browser-context technique as `replay-truncate.spec.ts`: empty IndexedDB → `lastSeq:0` → FULL replay). Sends `[[faux:thinking-text]]` twice, then captures `event_replay` frames until `isLast`. **P1** (reformulated from the ≤2×-cold-load ratio, which is not observable from the browser — that stays gated at L1 by P2): ZERO superseded `message_update` survives, bounded by `tool_execution_start` flush seeds, thinking updates exempt. Seq union strictly increasing + duplicate-free. **F5** seq gaps do not misfire the client reset rule (both turns still render). **F6** reasoning still renders — asserts the COLLAPSED `reasoning-block` (replay path ⇒ `streamedLive` falsy), needs `PATCH /api/preferences/display {reasoning:true}`. F3 deliberately not asserted here (racing a live token stream is flaky at L3); gated at L1 by E9 + the `streamingTail` fixture. See change: compact-warm-replay-stream.
