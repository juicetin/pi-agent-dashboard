# list-models-registry-ready.spec.ts — index

Playwright spec (L3). Live proof of the `list_models` registry-readiness discriminator. Spawns a fresh git session, sends `[[faux:tool-list-models]]` → the REAL bridge `list_models` tool executes against the faux-populated session registry (`faux/faux-1` registered via `pi.registerProvider` → `getRegistry()` truthy, `getAvailable()` non-empty). Step 2 echoes `registryReady=<b> count=<N> hasFaux=<b>` (via `summarizeListModelsResult`); asserts `registryReady=true` + `hasFaux=true` as a plain transcript marker (no tool-card collapse/virtualization dependence). Imports `LIST_MODELS_MARKER_PREFIX` from `qa/fixtures/faux-scenarios.js`. Absent-registry `registryReady:false` (race) stays unit-proven. See change: fix-list-models-empty-on-unhydrated-registry.
