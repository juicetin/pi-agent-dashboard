# openspec-board-drop-contrast.spec.ts — index

L3 spec (change: fix-openspec-board-drop-targeting). Marker + active-rail contrast on the LIVE drag affordances across 9 themes × {light,dark}; WCAG 2.1 SC 1.4.11 non-text floor 3:1 against the composited column surface. Covers test-plan A1/A2/A3 in one sweep (`--accent-primary` is static, so blue-accented themes are the labelled risk subset).
