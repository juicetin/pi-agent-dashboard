# openspec-locality-gate.spec.ts — index

L3 spec (change: scope-openspec-auto-attach-to-session-cwd). Faux `openspec-foreign-cd` proves a `cd`-into-another-repo openspec command never attaches or renames the card (F2). F1 stays L1 — the client never hydrates `notifyLog`.
