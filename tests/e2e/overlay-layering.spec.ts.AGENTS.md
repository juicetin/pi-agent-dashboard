# overlay-layering.spec.ts — index

Browser E2E gate for `add-overlay-layering-system` (test-plan #F3/#F4): folder-actions menu paints ABOVE the `isolate` session cards via `elementFromPoint` (no underlap); portaled `fixed` panel stays anchored (`panel.top−trigger.bottom≈4px`) after scrolling `session-list-scroll`. See change: add-overlay-layering-system.
