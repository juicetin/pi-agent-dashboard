# package-queue-visible.spec.ts — index

Browser E2E gate for `unify-pi-core-into-package-queue` (D9 rewritten — visible queue). Stubs `/api/pi-core/versions` (2 core pkgs `updateAvailable:true`), `/api/packages/installed`, `/api/packages/check-updates`, and gates each `/api/pi-core/update` POST behind its own releasable promise — harness container is already at latest so no Update affordance renders otherwise; only server responses are faked, browser+bundle+`packageQueue`+clicks are real. Proves: mid-flight click on another core row AND an extension row → both `-queued` pills + "Queued" labels, zero 409 text, no premature POST; Move disabled (kebab) while queueable Update stays enabled; **client-side** Back→settings-btn nav preserves running+queued state (NOT `page.goto` — hard reload kills the module singleton by design); FIFO drain `PI`→`DASH`→extension. Writes `test-results/visible-queue-mid-flight.png`. Attach: `PW_E2E_USE_RUNNING=1 PW_E2E_PORT=$(jq -r .dashboardPort .pi-test-harness.json)`.
