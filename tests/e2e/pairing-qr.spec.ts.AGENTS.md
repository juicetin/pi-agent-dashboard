# pairing-qr.spec.ts — index

Browser E2E for the camera-scannable pairing QR (change: make-pairing-qr-camera-scannable). Physical camera scan is manual (7.2); this drives everything it triggers. Test 1: `request.get('/api/pair/payload')` mints a REAL payload (urls=[`http://localhost:<port>`], exposed only under PI_E2E_SEED via server `isTestLoopbackOrigin`); `page.goto('/pair#'+encodePayloadString(payload))` → real Ed25519 challenge + redeem → 8-digit `pair-landing-confirm-code` in DOM → operator `POST /api/pair/approve {code,confirmCode}` (trusted-net auth) → phone poll stores bearer (localStorage `pi-dashboard:device-bearer`) + `waitForURL('/')` → real `/api/paired-devices` registry non-empty. Test 2: `/pair` with no fragment → `pair-landing-error` + `pair-landing-restart`. Node-side `encodePayloadString` = `pi:pair:v1.`+Buffer base64url. Needs PI_E2E_SEED=1.
