# plugin-settings-pages.spec.ts — index

L3 spec (test-plan rows F1-F14, X1-X5, X7; change: plugin-settings-pages). Per-plugin settings pages at `/settings/plugins/<id>`: nav child vs parent active state, deep link surviving reload, live rail drop on disable, chrome-only disabled page + re-enable, load-error and missing-requirement banners, index never rendering settings inline, General clean of plugin sections, no `position: fixed` plugin footer, Save Bar naming every dirty page (`Plugins › <name>`) + single-Save fan-out, and the rail guard firing only for a dirty PLUGIN page. Fixture states seeded by `docker/test-entrypoint.sh` under `PI_E2E_SEED`: `e2e-broken` (server entry throws → `status.error`), `e2e-needs-req` (absent pi extension → `missingRequirements`). Dirties `hermes-memory` via `hermes-input-memoryCharLimit` and built-ins via the Developer `Dev Build on Reload` toggle (both derive the edit from the CURRENT value/state so an earlier Save cannot make a later edit a silent no-op). Multi-page dirty specs navigate through the rail, never `page.goto` (a reload discards the draft under test).
