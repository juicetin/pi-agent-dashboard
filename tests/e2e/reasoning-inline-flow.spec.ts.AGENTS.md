# reasoning-inline-flow.spec.ts — index

L3 (change: render-inline-reasoning-and-custom-entries, E12). `[[faux:thinking-long]]` streams a 30-step block; toggling `reasoningInlineFlow` via the View popover removes the 400px cap; collapse still works. → see `reasoning-inline-flow.spec.ts.AGENTS.md`
