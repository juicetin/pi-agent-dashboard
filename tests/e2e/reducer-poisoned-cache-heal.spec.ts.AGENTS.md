# reducer-poisoned-cache-heal.spec.ts — index

Playwright E2E for `fix-reducer-crash-undefined-toolname` (manual tasks 5.1+5.2). 2 tests, faux model. Spawns session, `[[faux:plain-text]]` round-trip, lets debounced writer flush. Test 1 (5.1): `page.evaluate` poisons IndexedDB `pi-dashboard-replay-cache`/`sessions` entry — appends a `tool_execution_start` with `toolName` ABSENT (schemaVersion preserved → stays a HIT), reloads, asserts `header-app-bar` mounts (no root-unmount black screen) + `PLAIN_TEXT_MARKER` repaints + zero `/toLowerCase/` pageerror (the old above-error-boundary rehydrate crash). Test 2 (5.2): clears `sessions` store (`store.clear()`, not `deleteDatabase` which the app's open conn blocks), reloads, captures WS `subscribe` frame → asserts `lastSeq:0` full replay + transcript repaints (clean-path no-regression). Needs `PI_E2E_SEED=1`. See change: fix-reducer-crash-undefined-toolname.
