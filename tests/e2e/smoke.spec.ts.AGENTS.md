# smoke.spec.ts — index

Smoke spec, wiring proof only. Asserts shell renders (title `PI Dashboard` + `header-app-bar`), no `role=alert` disconnect banner within 5s hold (option A negative-hold WS proof). Real WS round-trip deferred to scenario backlog. See change: add-playwright-e2e.
