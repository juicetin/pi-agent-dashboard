# subagent-thin-tick-liveness.spec.ts — index

L3 for the subagent push/pull split: terminal fidelity after refresh, `/api/health` `storeTrim.subagentTicks`/`subagentFatTicks` prove the strip engages on real producer output (fat must be a minority), and the P5 inspector-open share is read from `__piSubagentInspectorTelemetry()` and checked against the C4 50 % abort threshold. Documents the observable narrowing: F1/F2/F4 have NO L3 observable here (harness subagents finish in ~600 ms, no mid-run window) and are owned at L1. See change: reduce-subagent-details-payload.
