# subagent-tick-throttle.spec.ts — index

L3 cadence rows for the subagent-tick throttle (change: reduce-bridge-tick-bandwidth): F1/P1 (floor ≥5 in 10 s + rate ≤2.2 fps), P2 (OFF ≥4× ON), F2 (no tick after `tool_execution_end`), F5 (no floor asserted across a >2 s producer-quiet gap), P3 (delivered-tick gap p95 ≤1.5W/max ≤3W, measured on the LIVE wire not stored/replay — the parent collapse change trims stored updates). Driven by the SYNTHETIC `faux-agent-ticks.ext.ts` producer (`[[faux:synthetic-agent-ticks]]`), NOT a nested faux subagent (which cannot sustain ≥10 s; measurement.md Bug 2). GATED: self-skips unless the Playwright env carries `PI_SYNTH_AGENT_TICKS=1`; runs on its own harness arm (`PI_E2E_SEED=1 PI_TEST_PEERS=both PI_SYNTH_AGENT_TICKS=1 ./docker/test-up.sh -d` then `PW_E2E_USE_RUNNING=1 PW_E2E_PORT=$PORT PI_SYNTH_AGENT_TICKS=1 npx playwright test subagent-tick-throttle`).
