# table-copy.spec.ts — index

Playwright spec (change: fix-table-copy-empty-clipboard, automates manual task 5.1). `[[faux:copy-surfaces]]` streams a GFM table + fenced `js` block; MarkdownContent is `React.memo` (single render). `context.grantPermissions(["clipboard-read","clipboard-write"])` then reads REAL clipboard via `navigator.clipboard.readText()`. Scopes the table's "Copy as Markdown" via its unique "Copy as TSV" sibling (`xpath=..`) since MessageBubble footer also has a md button. Asserts: table md copies `\| Name \| Age \|`+cells, TSV copies `Name\tAge`+rows, `Copy code` copies `const x = 1`, message `Copy as plain text` copies rendered innerText (contains cells, NOT the `\| --- \|` separator = degradation fix). Uses `spawnFreshGitSession`, `sendPrompt`. Needs `PI_E2E_SEED=1`.
