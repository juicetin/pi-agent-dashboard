# tail-only-scroll-perf.spec.ts — index

ADVISORY opt-in scroll-smoothness probe for `tail-only` (test-plan #P2, task 7.1). SKIPPED unless `PW_PERF=1`, following `chat-render-perf.spec.ts` — absolute frame budgets in a shared container are machine-dependent and flake as CI gates. Builds ONE session and measures it TWICE with `replayWindowMode` flipped underneath it, so the arms differ only in replay shape (two sessions would let transcript content drift into the measurement). Parks at the BOTTOM first: `openWindowedSession` ends pinned at the top, and the first draft measured an idle list (`distancePx: 0`) and passed vacuously. Now asserts distance travelled, `reachedTop`, and rendered-row GROWTH (13 -> 20/21) proving a splice landed inside the sampled window. Compares a DISTANCE-NORMALISED long-frame rate, since `tail-only` travels ~5x further while loading. Built at ~1.8k events, not #P2's original 20k — no session-seeding path exists in `tests/e2e/`; manifest amended. See change: add-tail-only-replay-window.
