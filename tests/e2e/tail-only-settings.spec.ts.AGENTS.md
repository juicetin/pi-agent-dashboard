# tail-only-settings.spec.ts — index

L3 for the `replayWindowMode` control (F14, F15) plus X8. F14: at `maxReplayEvents: 0` the select is DISABLED and states the dependency (disabled-with-reason, not hidden — hiding makes the dependency invisible). F15: changing only the mode writes only the mode (asserted on the request body AND on persisted siblings). X8 is NOT reachable as specified — retention BELOW the window makes the retained stream smaller than the window, so `computeReplayWindow` short-circuits and announces NO gap (first draft failed on `scrollDividerIntoDom`); above the window `gapCount` is store-read and servable. `unservable` is a RACE, not a config. The row pins the reachable property: degrades to no gap, not to a divider offering unloadable events. See change: add-tail-only-replay-window.
