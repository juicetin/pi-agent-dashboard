# terminal.spec.ts — index

Scenario 5.4 spec. `ensureGitSession`, clicks session card to select, clicks `open-inline-terminal-button`, asserts xterm "Terminal input" textarea visible (terminal WS live). See change: add-e2e-spawn-scenarios.
