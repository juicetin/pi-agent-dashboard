# uncommitted-indicator-commit.spec.ts — index

Playwright E2E for the uncommitted-indicator + commit-from-card feature (change: add-session-uncommitted-indicator-and-commit; QA scenarios V.1–V.4 + V.2b). Setup-only backdoor: git state dirtied/cleaned via the dashboard's OWN same-origin REST (`dirtyMarkdown`/`cleanupCommit` helpers) — pill, dialog, picker, commit, AI-draft driven through the real UI. V.1 dirty README.md → reload → `git-dirty-count`=1. V.2 dirty README+notes → uncheck README in dialog → commit → only README.md dirty. V.2b two same-cwd sessions → `group-commit-btn` count=1 + card-scoped `git-dirty-pill` count=0 → folder commit clears count. V.3 AI-draft resolves (subject fills OR `commit-draft-unavailable`) + session-card count stable (no visible pollution). V.4 no `git-ahead`/`git-behind` chips when clean+no-upstream. Each spec self-cleans in afterEach (shared fixture repo). Needs `PI_E2E_SEED=1`. See change: add-session-uncommitted-indicator-and-commit.
